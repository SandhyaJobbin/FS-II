---
phase: 02-candidate-entry-test-assembly
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - tests/assembly/test_assembly.ts
  - tests/assembly/fixtures/questions_fixture.json
  - assessment-app/package.json (if vitest not already installed)
autonomous: true
requirements: [ENTRY-01, ENTRY-02, ENTRY-03, ASSM-01, ASSM-02, ASSM-03]

must_haves:
  truths:
    - "All 6 test groups pass: normalizeEmail, duplicate detection, quota compliance, case atomicity, answer-key sanitization, difficulty_tier passthrough"
    - "Quota compliance test asserts EXACT counts: grammar=8, sentence_correction=5, macro=2, reading=5, closure=5, attention_l1=20, attention_l2=20, critical=40, total=105"
    - "Case atomicity test verifies every Attention and Critical question in the output belongs to a case where ALL 4 sibling questions are also present"
    - "Answer-key test verifies zero 'is_correct' fields in the sanitized client payload (recursive search)"
    - "Tests run offline — no network calls, no GAS runtime, no live spreadsheet access"
  artifacts:
    - tests/assembly/test_assembly.ts
    - tests/assembly/fixtures/questions_fixture.json
  prohibitions:
    - "Do NOT import from 'backend/Code.gs' directly — extract the pure logic functions as inline JS in the test file"
    - "Do NOT mock Math.random() globally — instead verify quota compliance deterministically by running the sampler 50 times and checking counts"
---

<objective>
Build the Phase 2 test gate: a vitest suite that proves all 6 ENTRY/ASSM requirements against a fixture extract of the real question bank. Tests run offline, are deterministic, and cover the exact success criteria.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create fixture question bank (representative slice)</name>
  <files>tests/assembly/fixtures/questions_fixture.json</files>
  <action>
    Create a minimal but representative JSON fixture containing:
    - 12 Grammar questions (need to draw 8)
    - 8 Sentence Correction questions (need 5)
    - 4 Macro questions (need 2)
    - 2 Reading passages (5 questions each = 10 total, need 1 passage = 5)
    - 6 Closure questions (need 5)
    - 8 Attention L1 cases (4 Qs each = 32 total, need 5 cases = 20)
    - 8 Attention L2 cases (4 Qs each = 32 total, need 5 cases = 20)
    - 15 Critical Thinking cases (4 Qs each = 60 total, need 10 cases = 40)

    Each question object MUST include `is_correct` on options (simulating the full QUESTIONS array), plus `difficulty_tier`. This is needed to test sanitization stripping.

    Schema per question:
    {
      "id": "eng-grammar-q01",
      "bank": "english",
      "section": "grammar",
      "level": null,
      "case_id": null,
      "case_title": null,
      "tabs": null,
      "tables": null,
      "difficulty_tier": "straightforward",
      "response_type": "mcq_single",
      "stem": "Test question stem.",
      "options": [
        { "letter": "A", "text": "Option A", "is_correct": false },
        { "letter": "B", "text": "Option B", "is_correct": true },
        { "letter": "C", "text": "Option C", "is_correct": false },
        { "letter": "D", "text": "Option D", "is_correct": false }
      ]
    }
  </action>
</task>

<task type="auto">
  <name>Task 2: Write vitest test suite</name>
  <files>tests/assembly/test_assembly.ts</files>
  <read_first>
    - tests/assembly/fixtures/questions_fixture.json
    - backend/Code.gs lines 140–585 (normalizeEmail, assembleQuestionSet, sampleRandom, groupBy, handleStartAttempt client payload)
    - .planning/phases/02-candidate-entry-test-assembly/02-PATTERNS.md (all 7 patterns)
  </read_first>
  <action>
    Create `tests/assembly/test_assembly.ts` with:

    // --- Extracted GAS logic (inline, no imports from Code.gs) ---
    Copy normalizeEmail(), sampleRandom(), groupBy() verbatim from Code.gs.
    Implement assembleQuestionSet(questions, quotas) as a pure function (takes questions array + quotas object, returns array of IDs).
    Implement sanitizeQuestion(q) implementing Pattern 4.

    // --- QUOTAS fixture ---
    const QUOTAS = { grammar: {count:8}, sentence_correction:{count:5}, macro:{count:2},
      reading:{count:1}, closure:{count:5}, attention_l1:{count:5}, attention_l2:{count:5}, critical:{count:10} };

    // --- Test groups ---

    describe("ENTRY-02: normalizeEmail", () => {
      test("Gmail dot-removal", ...)
      test("Gmail plus-addressing strip", ...)
      test("Gmail combined variants", ...)
      test("Case-fold all emails", ...)
      test("Non-Gmail: no dot removal", ...)
      test("Empty/null input", ...)
    })

    describe("ENTRY-03: Duplicate detection", () => {
      // Simulate the Attempts sheet lookup logic:
      // call startAttempt logic with first email → success
      // call again with dot-variant email → isDuplicate: true
      test("Same email blocked", ...)
      test("Gmail dot variant blocked", ...)
      test("Gmail plus variant blocked", ...)
      test("Different email allowed", ...)
    })

    describe("ASSM-01: Quota compliance", () => {
      // Run assembleQuestionSet 10 times, each time assert:
      test("grammar count = 8", ...)
      test("sentence_correction count = 5", ...)
      test("macro count = 2", ...)
      test("reading (1 passage = 5 questions) count = 5", ...)
      test("closure count = 5", ...)
      test("attention_l1 count = 20 (5 cases × 4)", ...)
      test("attention_l2 count = 20 (5 cases × 4)", ...)
      test("critical count = 40 (10 cases × 4)", ...)
      test("total = 105", ...)
    })

    describe("ASSM-03: Case atomicity", () => {
      test("All Attention L1 questions in output belong to complete 4-Q cases", ...)
      test("All Attention L2 questions in output belong to complete 4-Q cases", ...)
      test("All Critical questions in output belong to complete 4-Q cases", ...)
      test("No partial cases (case with 1-3 questions) appear in output", ...)
    })

    describe("ASSM-02: Frozen set", () => {
      // Simulate the frozen-set invariant: same assembled ID list used for
      // both question serving and grading (stateless — test that assembleQuestionSet
      // always returns exactly quota-count IDs from the available pool)
      test("All IDs in assembled set exist in source question bank", ...)
      test("No duplicate IDs in assembled set", ...)
    })

    describe("GRADE-05 guard: Answer-key sanitization", () => {
      test("sanitizeQuestion strips is_correct from options", ...)
      test("sanitizeQuestion preserves difficulty_tier", ...)
      test("No is_correct field anywhere in sanitized payload (recursive)", ...)
      test("Sanitized payload has letter and text on each option", ...)
    })
  </action>
</task>

<task type="auto">
  <name>Task 3: Install vitest in assessment-app (if not present) and run tests</name>
  <files>assessment-app/package.json, tests/assembly/</files>
  <read_first>
    - assessment-app/package.json (check for vitest)
  </read_first>
  <action>
    Check if vitest is installed. If not, add it to devDependencies.
    Create a minimal `vitest.config.ts` at workspace root if not present.
    Run: `npx vitest run tests/assembly/test_assembly.ts`
    All tests must pass. Fix any failures — document fixes in 02-SUMMARY.md.
  </action>
</task>

</tasks>
