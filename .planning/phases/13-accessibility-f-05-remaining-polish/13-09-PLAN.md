---
phase: 13-accessibility-f-05-remaining-polish
plan: 09
type: execute
wave: 3
depends_on:
  - 13-03
  - 13-04
  - 13-05
  - 13-07
  - 13-08
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/13-accessibility-f-05-remaining-polish/13-09-SUMMARY.md
autonomous: false
requirements:
  - A11Y-01
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05
  - UX-06
  - REPORT-03
  - EMAIL-01
  - BUG-04
user_setup:
  - service: google-apps-script
    why: "Manual paste + Save + Deploy of backend/AsyncGrading.gs + new backend/Emails.gs into the existing Apps Script project. Same manual-deploy shape as Phases 9, 10, 11 (no clasp configured — verified from Phase 10 7:05p observation)."
    dashboard_config:
      - task: "Open the Apps Script project bound to the FS Attempts spreadsheet"
        location: "Google Drive → open the sheet → Extensions → Apps Script"
      - task: "Paste backend/AsyncGrading.gs contents into the AsyncGrading.gs file in the Apps Script editor"
        location: "Apps Script editor → AsyncGrading.gs"
      - task: "Create a new script file named Emails and paste backend/Emails.gs contents"
        location: "Apps Script editor → + button → Script → name it Emails"
      - task: "Save both files (Ctrl+S / Cmd+S)"
        location: "Apps Script editor"
      - task: "Deploy → Manage deployments → edit the existing Web App deployment → New version → Deploy"
        location: "Apps Script editor → Deploy button"
      - task: "Copy the resulting Web App URL and re-enter it in the recruiter admin panel if it differs from localStorage.fs_gas_url (per Phase 9 stale-cache lesson)"
        location: "Apps Script editor → Deploy dialog"
  - service: vercel-or-hosting
    why: "Push assessment-app changes (light theme + ReportScreen breakdown + TestScreen refactor + AxeDevReporter dev dep) to the hosting used for the Next.js app so candidates and recruiters see the new UI."
    dashboard_config:
      - task: "Push the merged branch to trigger the existing Next.js deploy pipeline"
        location: "Repo host"

must_haves:
  truths:
    - All 5 pre-deploy plans (13-03, 13-04, 13-05, 13-07, 13-08) have shipped SUMMARY.md files and green verify blocks before this plan fires.
    - Live Apps Script Web App deployment succeeds; AsyncGrading.gs + Emails.gs versions match the repo tree at commit time.
    - Live Next.js deployment succeeds; ReportScreen + TestScreen light-theme + Answer Detail section + zone-numbering + email-block reading item + CT case-always all verified in a fresh incognito browser session.
    - Live end-to-end test attempt produces a candidate email + recruiter email with the new 640px HTML template AND a non-empty plain-text fallback; email is legible in Gmail web AND at least one secondary client (Outlook web / Apple Mail).
    - REQUIREMENTS.md v1.1 section adds new IDs (A11Y-01, UX-01..UX-06, REPORT-03, EMAIL-01) with traceability rows pointing to Phase 13; BUG-04 status updated to Complete.
    - ROADMAP.md Phase 13 entry rewritten (currently STALE per prior observation 957) — Goal, Requirements, Success Criteria, and Plans block match the 9-plan reality.
    - Backlog items 999.1, 999.2, and 999.3 are marked shipped (or superseded by Phase 13) in the Backlog section, with the specific plan number that closed each.
  artifacts:
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-09-SUMMARY.md — live-verify results with the 12-item checklist filled in.
    - Updated .planning/REQUIREMENTS.md — 10 traceability rows (BUG-04 marked Complete plus 9 new IDs).
    - Updated .planning/ROADMAP.md — Phase 13 block rewritten, Backlog 999.1/999.2/999.3 marked closed.
  key_links:
    - Follows the exact close-out pattern used by 11-04-PLAN.md Task 4 per 13-PATTERNS.md § Workstream 5.
    - Depends on all of 13-03/04/05/07/08 landing green.
    - Phase 10 A6 recruiter-only rationale contract preserved by 13-06 + 13-07 sync-check assertion.
---

<objective>
Wave 3 close-out: paste-deploy Apps Script (AsyncGrading + new Emails) and Next.js (all frontend changes), run a live end-to-end verification, capture live email output in Gmail + Outlook/Apple Mail, then finalize documentation (REQUIREMENTS.md traceability + ROADMAP.md Phase 13 rewrite + backlog closure). Mirrors the successful close-out pattern from plan 11-04.

Purpose: Without this plan, Phase 13 exists in the repo but not in the browser or in inboxes. This is the "phase actually shipped" gate.
Output: 1 new SUMMARY, 2 planning-doc updates. No code changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-accessibility-f-05-remaining-polish/CONTEXT.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-RESEARCH.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-PATTERNS.md
@.planning/phases/11-recruiter-analytics-dashboard/11-04-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Deploy backend AsyncGrading.gs + new Emails.gs to Apps Script</name>
  <files>backend/AsyncGrading.gs, backend/Emails.gs</files>
  <action>Human-only deployment. Clasp is not configured (per 7:05p 2026-07-31 observation); paste-and-deploy in the Apps Script editor is the only path. Follow the 7-step how-to-verify block exactly. Task 3 must not fire until the operator confirms `deployed`.</action>
  <verify>
    <human-check>Human confirms the new Web App deployment succeeded and a smoke run of buildCandidateEmail returns a populated payload.</human-check>
  </verify>
  <done>Resume-signal received (`deployed` or `blocked: <error>`).</done>
  <what-built>Live deployment of Phase 13 backend changes: extended buildReportFromAttemptsRow (13-06 transcript read + strip) and extracted Emails.gs (13-08 HTML email templates). No clasp configured (per 7:05p 2026-07-31 observation), so paste-and-deploy in the Apps Script editor is the only path — same shape as Phases 9-06, 10-07, and 11-04.</what-built>
  <how-to-verify>
    1. Open the Apps Script project bound to the FS Attempts spreadsheet (Google Drive → open the sheet → Extensions → Apps Script).
    2. Select AsyncGrading.gs in the editor sidebar. Paste the full contents of the repo's `backend/AsyncGrading.gs` over the existing file. Save (Ctrl+S / Cmd+S).
    3. Create a new script file: sidebar + button → Script → name it exactly `Emails` (Apps Script appends `.gs` automatically). Paste the full contents of the repo's `backend/Emails.gs`. Save.
    4. Verify Code.gs is UNCHANGED (this phase does not modify it — Phase 10 recruiter transcript endpoint is preserved).
    5. Deploy → Manage deployments → edit the existing Web App deployment → New version → Description: `Phase 13 — email templates + candidate transcripts`. Click Deploy.
    6. Copy the new Web App URL. If it differs from the cached `localStorage.fs_gas_url` in the recruiter browser, plan to re-enter it in Task 3 (Phase 9 lesson: stale cached URLs are the #1 live-verify failure mode).
    7. In the Apps Script editor, run one test invocation of `buildCandidateEmail(<fixture>)` in the debugger to confirm Emails.gs functions are callable from AsyncGrading.gs (they share global scope).
  </how-to-verify>
  <resume-signal>Type "deployed" once the new deployment is live and a smoke run of buildCandidateEmail returned a payload with plainBody + htmlBody, or "blocked: &lt;error&gt;" to surface a deploy problem.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Deploy assessment-app frontend to hosting</name>
  <files>assessment-app/src/app/globals.css, assessment-app/src/components/ReportScreen.tsx, assessment-app/src/components/TestScreen.tsx, assessment-app/src/components/AxeDevReporter.tsx</files>
  <action>Human-only deployment. Push the merged Phase 13 branch to trigger the existing Next.js hosting pipeline. Confirm the deployed URL loads clean in incognito with the light theme active. Task 3 must not fire until the operator confirms `deployed`.</action>
  <verify>
    <human-check>Human confirms the site loads clean in incognito with no console errors and axe-core NOT shipped in prod.</human-check>
  </verify>
  <done>Resume-signal received (`deployed <commit-sha> <deploy-url>` or `blocked: <error>`).</done>
  <what-built>Live deployment of Phase 13 frontend changes: light-theme migration (13-04), TestScreen extract-first refactor (13-01) + UX updates (13-05), ReportScreen ARIA landmarks (13-03), ReportScreen Answer Detail section (13-07), AxeDevReporter dev-only tooling (13-02).</what-built>
  <how-to-verify>
    1. Merge the Phase 13 branch into the deploy branch used by the existing Next.js hosting pipeline.
    2. Push to trigger the deploy.
    3. Watch the deploy log until it reports success. Capture the deploy URL and commit SHA for the SUMMARY.
    4. In a fresh incognito browser session, open the deployed URL. Confirm:
       - The landing page renders on the light theme (near-white background, dark text).
       - No console errors on load.
       - AxeDevReporter warnings do NOT appear in production (dev-only guard from 13-02 must strip axe-core; a smoke-check of DevTools Network for `axe.min.js` should return zero hits).
  </how-to-verify>
  <resume-signal>Type "deployed &lt;commit-sha&gt; &lt;deploy-url&gt;" once the site loads clean in incognito, or "blocked: &lt;error&gt;".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Live end-to-end verification — 12-item checklist + write 13-09-SUMMARY.md</name>
  <files>.planning/phases/13-accessibility-f-05-remaining-polish/13-09-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09-async-grading-report-delivery-pipeline/09-06-SUMMARY.md — the live-verify SUMMARY format to copy.
    - .planning/phases/11-recruiter-analytics-dashboard/11-04-PLAN.md — the same close-out pattern applied to Phase 11.
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-*-SUMMARY.md (all 8 prior plan SUMMARYs) — reference their green verify outputs.
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-03-postfix-axe-report.md — reference for baseline "zero critical/serious" claim.
  </read_first>
  <action>
    Perform the 12-item live-verify checklist below in a fresh incognito browser session against the live deploy URLs from Tasks 1 and 2:

    1. Load the landing page → light theme applied, layout wider than the pre-Phase-13 single centered card.
    2. Start a full attempt with a fresh email. Reach an English Grammar item → question counter shows "Question X of {englishZoneCount}" (zone-relative, not global).
    3. Reach an English Proficiency Part 6 (reading) item → scenario renders as From/To/Subject email-letterhead block (serif body); "Re-review the email" button visible and functional (scrolls-and-focuses).
    4. Reach an mcq_multi item (in any zone) → cyan "Select all that apply" pill visible above the options.
    5. Reach a Critical Thinking case with tabs → CaseFilePanel renders on the right column via CaseDashboard.
    6. Complete the attempt → ThankYouScreen shows.
    7. Within 5 minutes, candidate email arrives. Open in Gmail web:
       - Renders as the new 640px HTML template with tier badge in the tier color.
       - Three-column trait cards visible.
       - Narrative insight rendered with left border in tier color.
       - No "Violations: N" / "Integrity" line (candidate divergence preserved from D-02).
       - Plain-text fallback body is non-empty when the message is viewed in "show original" mode.
    8. Recruiter email arrives at the shared recruiter inbox. Open in Gmail web:
       - Same 640px template PLUS a violation/integrity summary row.
       - Subject line contains the candidate name and tier.
    9. Open the recruiter email in a secondary client (Outlook web OR Apple Mail):
       - Layout does NOT collapse (no giant single column, no missing table cells) — table-based layout survives per 13-RESEARCH.md Pitfall 4.
    10. Open the candidate ReportScreen (via a direct URL to the attempt if that path exists, else via a dev-only preview route):
        - Answer Detail section renders IF the fixture attempt has GradingTranscripts rows (for rubric-graded open-text answers).
        - Each entry shows verdict pill + criteriaMet checklist (✓/✗ with sr-only Met/Not met text).
        - `rationale`, `overrideVerdict`, and `overrideAt` MUST NOT appear anywhere in the rendered output — inspect DOM to confirm.
    11. On ReportScreen with AxeDevReporter running (dev build), run axe on the page → 0 critical + 0 serious violations reported (matches the plan 13-03 post-fix baseline).
    12. On the deployed production URL, confirm `axe.run` is NOT present in the network tab or shipped bundles (dev gate from 13-02 verified in prod).

    Write `.planning/phases/13-accessibility-f-05-remaining-polish/13-09-SUMMARY.md` following the standard SUMMARY template. MUST include:
      - Task 1 + Task 2 resume-signal outputs (backend Apps Script version, frontend commit SHA + deploy URL).
      - The 12-item checklist with observed outcome per item.
      - Screenshots or paste-links for at least: (a) Gmail-rendered candidate email, (b) Gmail-rendered recruiter email, (c) secondary-client render (Outlook or Apple Mail), (d) ReportScreen Answer Detail section rendered.
      - Any deviations from the plan.
      - Verification that the D-01 client-strip contract holds live (item 10 explicit DOM inspection outcome).
  </action>
  <verify>
    <automated>test -f .planning/phases/13-accessibility-f-05-remaining-polish/13-09-SUMMARY.md && grep -c "12-item" .planning/phases/13-accessibility-f-05-remaining-polish/13-09-SUMMARY.md ; echo "---"; grep -c "D-01" .planning/phases/13-accessibility-f-05-remaining-polish/13-09-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY.md exists at the expected path.
    - SUMMARY contains a 12-item checklist section with an observed outcome per item.
    - SUMMARY records the backend Apps Script version + frontend commit SHA + deploy URL.
    - SUMMARY confirms the D-01 client-strip contract via DOM inspection (item 10).
    - SUMMARY includes screenshots or paste-links for the 4 required artifacts.
  </acceptance_criteria>
  <done>Live verification complete; evidence captured for future forensics.</done>
</task>

<task type="auto">
  <name>Task 4: Finalize REQUIREMENTS.md traceability + rewrite ROADMAP.md Phase 13 block + close backlog 999.1/999.2/999.3</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md v1.1 section (currently BUG-04 unchecked at line 107; UI-06 unmapped at line 112; 10 new IDs need to be added — A11Y-01, UX-01..UX-06, REPORT-03, EMAIL-01).
    - .planning/REQUIREMENTS.md traceability table (lines 148-213) — the section that gets one row per requirement ID.
    - .planning/ROADMAP.md Phase 13 block (lines 269-279 — currently STALE per prior observation 957, says "small independent a11y remediation" and lists Requirements: TBD, Plans: TBD).
    - .planning/ROADMAP.md Backlog section (lines 310-343, entries 999.1 / 999.2 / 999.3) — needs closure marker linking to the closing Phase 13 plan.
    - .planning/phases/11-recruiter-analytics-dashboard/11-04-PLAN.md Task 4 (lines 175-231) — the exact scoped-Edit close-out pattern.
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-09-SUMMARY.md — for observed live-verify outcomes.
  </read_first>
  <action>
    Use `Edit` scoped replacements throughout — never `Write` a whole file for these two.

    Step A — REQUIREMENTS.md updates (v1.1 requirements section AND traceability table):

    A1. BUG-04 line (approximately line 107): update from `- [ ] **BUG-04**: ARIA landmarks added to \`ReportScreen.tsx\` (F-05)` to `- [x] **BUG-04**: ARIA landmarks added to \`ReportScreen.tsx\` (F-05) — closed by Phase 13 (2026-08-01)`.

    A2. Add the following 10 new requirement lines to the v1.1 Requirements section, inserted grouped logically (Accessibility group before UI Quality, UX group after Accessibility, Reporting group inside Grading, Email group inside Async Reporting):

    ```
    ### Accessibility

    - [x] **A11Y-01**: ReportScreen.tsx passes axe-core baseline with zero critical + zero serious violations (Phase 13-03)

    ### UI/UX

    - [x] **UX-01**: Candidate-facing surfaces use a single light theme (no dark mode, no toggle) (Phase 13-04)
    - [x] **UX-02**: Layout widened beyond the previous max-w-580 single centered card; comfortable multi-column margins on desktop (Phase 13-04)
    - [x] **UX-03**: EP Part 6 (reading) scenario renders as an email-letterhead block with a "Re-review the email" button (Phase 13-05)
    - [x] **UX-04**: Critical Thinking case file panel is always visible for CT questions (fallback message when tabs empty) (Phase 13-05)
    - [x] **UX-05**: Zone-relative question numbering ("Question 3 of 12") replaces global "Q1 of 105" during the test (Phase 13-05)
    - [x] **UX-06**: mcq_multi questions carry a visible "Select all that apply" pill; AoD collapsible cards verified (Phase 13-05)
    ```

    Add to the Reporting group:
    ```
    - [x] **REPORT-03**: Candidate ReportScreen surfaces per-question criteriaMet breakdown from GradingTranscripts (rationale withheld — recruiter-only per Phase 10 A6) (Phase 13-06 backend + 13-07 frontend)
    ```

    Add to the Async Reporting group:
    ```
    - [x] **EMAIL-01**: Candidate and recruiter emails use a 640px table-based HTML template with tier badge, per-trait cards, and non-empty plain-text fallback (Phase 13-08)
    ```

    A3. In the Traceability table, insert 10 rows (BUG-04 status flip + 9 new IDs) pointing to `Phase 13: Accessibility (F-05) & Remaining Polish` with `Complete` status if Task 3 verified all live items green, otherwise `Pending`.

    Step B — ROADMAP.md Phase 13 block rewrite (scoped Edits only). Replace the current block (lines 269-279) with:

    ```
    ### Phase 13: Accessibility (F-05) & Remaining Polish

    **Goal**: Ship the F-05 ARIA remediation on ReportScreen (BUG-04) plus the four bundled workstreams: light-theme migration + layout widening (999.1), richer per-question strength/weakness breakdown on the candidate report (999.2), and 640px HTML email templates for candidate + recruiter (999.3).
    **Depends on**: Nothing (parallelizable with Phase 10-12)
    **Requirements**: BUG-04, A11Y-01, UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, REPORT-03, EMAIL-01
    **Success Criteria** (what must be TRUE):

      1. ReportScreen.tsx has ARIA landmarks + roles + progressbar semantics; passes axe-core baseline (zero critical + zero serious violations)
      2. Candidate-facing surfaces render on a single light theme; layout widened; prefers-reduced-motion honored
      3. Reading items show an email-letterhead scenario with a re-review button; CT items always show a case-file panel; question counter is zone-relative; mcq_multi carries a visible cue
      4. Candidate ReportScreen shows per-question criteriaMet breakdown (rationale withheld — recruiter-only per Phase 10 A6, enforced by server strip + client type + CI grep gate)
      5. Candidate + recruiter emails use a 640px table-based HTML template with tier badge, per-trait cards, XSS-escaped user strings, and non-empty plain-text fallback

    **Plans**: 9 plans

    Plans:
    - [x] 13-01-PLAN.md — Wave 0 foundation: extract-first refactor of TestScreen.tsx (zero behavior change)
    - [x] 13-02-PLAN.md — Wave 0 foundation: axe-core dev tooling + baseline axe report
    - [x] 13-03-PLAN.md — Wave 1: ARIA landmarks + roles on ReportScreen (BUG-04 / A11Y-01)
    - [x] 13-04-PLAN.md — Wave 1: light theme migration + layout widening (UX-01, UX-02)
    - [x] 13-05-PLAN.md — Wave 1: EP Part 6 + CT case-always + zone numbering + mcq_multi cue (UX-03..UX-06)
    - [x] 13-06-PLAN.md — Wave 2: backend transcripts read + strip + defensive fallback (REPORT-03 backend)
    - [x] 13-07-PLAN.md — Wave 2: ReportScreen Answer Detail section + client-strip sync-check gate (REPORT-03 frontend)
    - [x] 13-08-PLAN.md — Wave 3: extract Emails.gs + rewrite buildReportHtml as 640px template + plain-text fallback + escape helper (EMAIL-01)
    - [x] 13-09-PLAN.md — Wave 3: deploy + live verify + REQUIREMENTS/ROADMAP finalization (this plan)
    ```

    Set individual `[x]` markers based on observed completion state at commit time (leave `[ ]` for any plan whose SUMMARY is not final).

    Step C — ROADMAP.md Backlog closure (scoped Edits, one per backlog entry):

    C1. On the Phase 999.1 header (line ~310), append ` — CLOSED BY PHASE 13`. Add a "Closed:" line under the Goal referencing the specific plans that shipped each of the 7 items (items 1/5/6/7 via 13-04/13-05; items 2/3/4 were already done pre-13).

    C2. On the Phase 999.2 header (line ~327), append ` — CLOSED BY PHASE 13 (13-06 backend + 13-07 frontend)`.

    C3. On the Phase 999.3 header (line ~336), append ` — CLOSED BY PHASE 13 (13-08)`.

    Step D — ROADMAP.md Progress table row for Phase 13 (line ~301): update `Plans Complete` from `0/0` to `9/9` (or actual observed), `Status` from `Not started` to `Complete`, and set today's date under `Completed`.
  </action>
  <verify>
    <automated>grep -c "A11Y-01" .planning/REQUIREMENTS.md ; echo "---"; grep -c "UX-01" .planning/REQUIREMENTS.md ; echo "---"; grep -c "REPORT-03" .planning/REQUIREMENTS.md ; echo "---"; grep -c "EMAIL-01" .planning/REQUIREMENTS.md ; echo "---"; grep -c "\[x\] \*\*BUG-04\*\*" .planning/REQUIREMENTS.md ; echo "---ROADMAP---"; grep -c "13-01-PLAN.md" .planning/ROADMAP.md ; echo "---"; grep -c "CLOSED BY PHASE 13" .planning/ROADMAP.md ; echo "---"; grep -c "9 plans" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md v1.1 section adds all 9 new IDs (A11Y-01, UX-01..UX-06, REPORT-03, EMAIL-01).
    - REQUIREMENTS.md BUG-04 is now `[x]` with a "closed by Phase 13" annotation.
    - REQUIREMENTS.md traceability table has 10 rows pointing to Phase 13.
    - ROADMAP.md Phase 13 block rewritten — Requirements line lists the 10 concrete IDs (no more "TBD"), Plans line reflects 9-plan count with the enumerated checklist.
    - ROADMAP.md Backlog entries 999.1 / 999.2 / 999.3 headers marked CLOSED BY PHASE 13 with the specific plans cited.
    - ROADMAP.md Progress table row for Phase 13 updated (not 0/0, not Not started).
    - No other section of REQUIREMENTS.md or ROADMAP.md is disturbed (scoped Edits only).
  </acceptance_criteria>
  <done>Documentation reflects reality: 10 requirements traced to Phase 13, backlog closed, ROADMAP Phase 13 block finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → live Apps Script Web App | Manual paste-deploy; risk of missing a file or forgetting to click "New version" (Phase 9-06 learned lesson). |
| Local repo → live Next.js hosting | Standard CI deploy trigger. |
| Recruiter/candidate browser + email client → live production | First real exposure of the new HTML template + candidate transcript section — mail-client rendering variance is the primary quality gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-24 | Tampering | Stale cached `localStorage.fs_gas_url` producing "404" false alarms | high | mitigate | Task 1 how-to-verify step 6 explicitly calls out re-entering the URL — the exact fix Phase 9-06 learned. |
| T-13-25 | Information Disclosure | Rationale text leaks into the candidate ReportScreen render in production | critical | mitigate | Task 3 item 10 requires explicit DOM inspection confirming absence of rationale/overrideVerdict/overrideAt. Three-layer defense (server strip + type exclusion + CI grep) already in place from 13-06/13-07. |
| T-13-26 | Information Disclosure | axe-core ships in production bundle, warning users about a11y violations at runtime | medium | mitigate | Task 3 item 12 confirms `axe.run` NOT present in prod network tab; 13-02 build-time strip already proven. |
| T-13-27 | Denial of Service | Outlook Word engine collapses the new email template layout | high | mitigate | Task 3 item 9 requires opening the recruiter email in a secondary client and verifying no layout collapse. If it collapses, roll back Emails.gs to the previous template. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies in this plan. Wave 3 does not modify package.json. |
</threat_model>

<verification>
- Task 1 checkpoint resolved with `deployed`.
- Task 2 checkpoint resolved with `deployed <commit-sha> <deploy-url>`.
- Task 3 automated: SUMMARY.md exists with 12-item checklist and D-01 verification.
- Task 4 automated: grep confirms new IDs in REQUIREMENTS.md, Phase 13 block rewritten in ROADMAP.md, backlog CLOSED markers present.
</verification>

<success_criteria>
- All 10 Phase 13 requirement IDs (BUG-04, A11Y-01, UX-01..UX-06, REPORT-03, EMAIL-01) are traceable to shipped, live-verified plans in REQUIREMENTS.md.
- ROADMAP.md Phase 13 block is no longer STALE — reflects the 4-workstream + 9-plan reality.
- Backlog 999.1 / 999.2 / 999.3 explicitly marked CLOSED BY PHASE 13 with cited plans.
- Live evidence captured for future forensics (Gmail + secondary-client email screenshots, ReportScreen Answer Detail render, axe baseline confirmed).
- D-01 (rationale withhold) contract verified live via DOM inspection.
</success_criteria>

<output>
Create `.planning/phases/13-accessibility-f-05-remaining-polish/13-09-SUMMARY.md` when done. Follow the standard SUMMARY template; include the 12-item live-verify checklist outcomes and links to the captured evidence artifacts.
</output>

## Artifacts this phase produces

- **New:** `.planning/phases/13-accessibility-f-05-remaining-polish/13-09-SUMMARY.md` — live-verify results, 12-item checklist, screenshots/paste-links, D-01 verification outcome.
- **Modified:** `.planning/REQUIREMENTS.md` — 10 traceability rows added/updated (BUG-04 status + 9 new IDs), v1.1 section extended with Accessibility/UI-UX/Reporting/Async-Reporting entries.
- **Modified:** `.planning/ROADMAP.md` — Phase 13 block rewritten (Goal, Requirements, Success Criteria, Plans), Backlog 999.1/999.2/999.3 marked CLOSED BY PHASE 13, Progress table row updated.
- **No code changes.** **No new dependencies.** **No sheet schema changes.**