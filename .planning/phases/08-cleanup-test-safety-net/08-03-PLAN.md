---
phase: 08-cleanup-test-safety-net
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app.js
  - frontend/index.html
  - frontend/style.css
  - .planning/debug/case-dashboard-object-child.md
autonomous: true
requirements:
  - BUG-05

must_haves:
  truths:
    - "frontend/ directory no longer exists in repo"
    - "No broken references to frontend/ in project docs or configs"
  artifacts: []
  key_links:
    - ".planning/debug/case-dashboard-object-child.md references legacy frontend/app.js — update to note deletion"
---

<objective>
Delete the legacy frontend/ directory (3 files: app.js, index.html, style.css) and clean all stale references in project documentation.

Purpose: frontend/ is dead code — pre-React vanilla JS superseded by assessment-app/ (Next.js). Confirmed zero references from production code (ARCHITECTURE.md line 25). Dead-but-present code causes confusion about which implementation is authoritative.
Output: frontend/ deleted; docs updated.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cleanup-test-safety-net/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Delete frontend/ directory</name>
  <files>frontend/app.js, frontend/index.html, frontend/style.css</files>
  <action>
Delete all 3 files: `frontend/app.js` (889 lines), `frontend/index.html`, `frontend/style.css`. Remove the now-empty `frontend/` directory. Per D-04 (F-06 decision): confirmed dead code, safe to remove. ARCHITECTURE.md line 25 confirms zero references from production code. deploy.yml only builds assessment-app/.
  </action>
  <verify>
    <automated>Test-Path frontend 2>$null; if ($?) { echo "FAIL: frontend/ still exists"; exit 1 } else { echo "PASS" }</automated>
  </verify>
  <done>
    frontend/ directory completely removed from repo.
  </done>
</task>

<task type="auto">
  <name>Clean stale frontend/ references in docs</name>
  <files>.planning/debug/case-dashboard-object-child.md</files>
  <action>
Grep entire repo for `frontend/` (excluding `.planning/` planning artifacts, `.claude/` skills, `node_modules/`). Only actionable reference found: `.planning/debug/case-dashboard-object-child.md` lines 60, 78-79 mention `legacy frontend/app.js`. Update these to note the file has been deleted (Phase 8 F-06). Do NOT modify ROADMAP.md, REQUIREMENTS.md, PROJECT.md, CONTEXT.md, RESEARCH.md, PITFALLS.md, ARCHITECTURE.md — these are planning artifacts that document the decision to remove frontend/ and must retain historical context. Per D-04: search and update references in docs and configs.
  </action>
  <verify>
    <automated>rg "frontend/" --glob "!node_modules" --glob "!.planning" --glob "!.claude" --glob "!.agents" --glob "!assessment-app/.claude" -l | Measure-Object -Line | Select-Object -ExpandProperty Lines</automated>
  </verify>
  <done>
    No stale frontend/ references in production code or configs. Planning docs retain historical context.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Deletion-only task; no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Denial of Service | accidental deletion of live code | low | mitigate | ARCHITECTURE.md confirms zero production references. deploy.yml only builds assessment-app/. Pre-deletion grep validates no imports. |
| T-08-SC | Tampering | npm installs | high | mitigate | No packages touched. |
</threat_model>

<verification>
Test-Path frontend  # must return false
rg "frontend/" --glob "!node_modules" --glob "!.planning" --glob "!.claude" --glob "!.agents" --glob "!assessment-app/.claude" -l  # must return empty
</verification>

<success_criteria>
frontend/ deleted. No stale references in production code. Planning docs retain decision history.
</success_criteria>

<output>
Create `.planning/phases/08-cleanup-test-safety-net/08-03-SUMMARY.md` when done
</output>
