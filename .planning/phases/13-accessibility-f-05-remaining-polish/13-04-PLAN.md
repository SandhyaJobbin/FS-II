---
phase: 13-accessibility-f-05-remaining-polish
plan: 04
type: execute
wave: 1
depends_on:
  - 13-01
files_modified:
  - assessment-app/src/app/globals.css
  - assessment-app/src/components/ReportScreen.tsx
  - assessment-app/src/components/TestScreen.tsx
  - assessment-app/src/components/CaseDashboard.tsx
  - assessment-app/src/components/case-dashboard/DashboardCards.tsx
  - assessment-app/src/components/test-screen/QuestionHeader.tsx
  - assessment-app/src/components/test-screen/ScenarioBlock.tsx
  - assessment-app/src/components/test-screen/OptionsList.tsx
  - assessment-app/src/components/test-screen/ZoneOverlays.tsx
  - assessment-app/src/components/test-screen/CaseFilePanel.tsx
autonomous: false
requirements:
  - UX-01
  - UX-02
tags:
  - theme
  - light-mode
  - layout

must_haves:
  truths:
    - `assessment-app/src/app/globals.css` `:root` CSS variables have LIGHT values (background #f8fafc-ish, foreground #0f172a-ish, card-bg white-ish, text-secondary AA-contrast on white per 13-RESEARCH.md Pattern 4).
    - CSS variable NAMES are preserved (only VALUES change) per 13-RESEARCH.md Pitfall 3 — this prevents a 40+ component search-replace churn.
    - Every hardcoded dark Tailwind class (approx 70 occurrences per prior observation) on the affected components is replaced with a light-theme equivalent (slate-100 for card-bg, slate-700 for secondary text, slate-900 for primary text, slate-200 for borders).
    - Per D-01 layout goal: the outer max-w-*-based single-card constraint on ReportScreen and TestScreen is removed or widened so the content uses comfortable multi-column margins on desktop while remaining readable on mobile.
    - Body text scale bumped where readability requires — target `text-sm` → `text-base` where source Tailwind was `text-xs` or `text-sm` on core content (not badge labels or footer text).
    - `prefers-reduced-motion` media query added to globals.css disabling non-essential CSS animations, per 13-RESEARCH.md Pitfall 8.
    - Post-migration re-run of axe from plan 13-02 AxeDevReporter on ReportScreen still reports zero critical + zero serious violations — the theme swap must not regress a11y (13-03 gains).
  artifacts:
    - Rewritten `assessment-app/src/app/globals.css` with light-mode `:root` values, unchanged variable names, and a `prefers-reduced-motion` media query.
    - Component-by-component migration of dark-theme Tailwind classes to light equivalents across ReportScreen, TestScreen, CaseDashboard, DashboardCards, and the 5 test-screen sub-components.
    - Human sign-off checkpoint on the light-theme visual outcome.
  key_links:
    - 13-RESEARCH.md § Pattern 4 (Light Theme via CSS Variables) — the exact hex palette and per-class swap rules.
    - Prior observation 951 (2026-08-01 1:41p): 70 hardcoded dark Tailwind classes across 3 components.
    - Depends on 13-01 having reduced TestScreen.tsx to < 500 lines so migration edits do not blow the cap further.
---

<objective>
Wave 1: single light theme migration (D-01, UX-01) + layout widening (UX-02). Replace dark `:root` CSS variable VALUES with light equivalents in globals.css, then per-component swap the ~70 hardcoded dark Tailwind classes (bg-slate-9*, text-white, border-slate-8*, etc.) to their light-mode equivalents. Preserve variable NAMES to avoid a repo-wide search-replace risk. Add `prefers-reduced-motion` to globals.css. Add a blocking human checkpoint at the end for visual sign-off before advancing.

Purpose: A2 / D-01 locks a SINGLE light theme (no toggle, no dark mode). Layout must expand from the current cramped `max-w-[580px]` centered card to a comfortable multi-column desktop layout with larger body text. This is a design-quality lift, not a functional change — but it touches many files, so a human visual approval gate is included.
Output: 10 modified files, 0 new dependencies. Human sign-off on the visual outcome.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-accessibility-f-05-remaining-polish/CONTEXT.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-RESEARCH.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-PATTERNS.md
@assessment-app/src/app/globals.css
@assessment-app/src/components/ReportScreen.tsx
@assessment-app/src/components/CaseDashboard.tsx
@assessment-app/src/components/case-dashboard/DashboardCards.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Swap globals.css :root values to light-theme palette + add prefers-reduced-motion</name>
  <files>assessment-app/src/app/globals.css</files>
  <read_first>
    - assessment-app/src/app/globals.css in full (already in context).
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-RESEARCH.md § Pattern 4 (exact hex palette recommendations).
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-RESEARCH.md § Pitfall 3 (rename variables discouraged; only swap values).
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-RESEARCH.md § Pitfall 8 (prefers-reduced-motion requirement).
  </read_first>
  <action>
    Edit `assessment-app/src/app/globals.css`:

    1. In the `:root` block, replace VALUES only — keep every variable NAME. Use the palette from 13-RESEARCH.md Pattern 4:
       - `--background: #f8fafc;` (was #070a13)
       - `--foreground: #0f172a;` (was #f8fafc)
       - `--bg-primary: #f8fafc;`
       - `--card-bg: rgba(255, 255, 255, 0.9);`
       - `--card-border: rgba(15, 23, 42, 0.08);`
       - `--accent: #0891b2;` (darkened cyan for AA contrast on white)
       - `--accent-glow: rgba(8, 145, 178, 0.15);`
       - `--text-primary: #0f172a;`
       - `--text-secondary: #475569;` (AA 4.5:1 on white)
       - `--text-muted: #64748b;`
       - `--success: #059669;` (darkened for AA)
       - `--error: #dc2626;`
       - `--error-bg: rgba(220, 38, 38, 0.06);`
       - Keep `--border-radius` and `--transition-speed` unchanged.

    2. Keep the `@theme inline` alias block unchanged (it references the variable names, which are unchanged).

    3. Update the `.glass-glow-bg` gradient to use `rgba(248, 250, 252, 0)` (light bg fadeout) instead of the dark `rgba(7, 10, 19, 0)`, so the ambient glow visually blends with the new light background. The `radial-gradient` inner color continues to use `var(--accent-glow)`.

    4. Update the scrollbar tweaks: `::-webkit-scrollbar-track` background to `rgba(0, 0, 0, 0.03)` and `::-webkit-scrollbar-thumb` background to `rgba(0, 0, 0, 0.12)` — light-theme neutral tones.

    5. Add a `@media (prefers-reduced-motion: reduce)` block AT THE END of globals.css that sets `.animate-fade-in { animation: none; }` and includes a `* { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; }` universal disable — this covers CSS animations. framer-motion respects the same media query natively, so no JS changes are needed.

    Do NOT rename any variable. Do NOT add new variables in this task.
  </action>
  <verify>
    <automated>cd assessment-app && grep -c "prefers-reduced-motion" src/app/globals.css ; echo "---"; grep -E "^\s*--background:" src/app/globals.css ; echo "---"; grep -E "^\s*--card-bg:" src/app/globals.css ; echo "---"; grep -E "^\s*--text-secondary:" src/app/globals.css ; echo "---BUILD---"; npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `--background` VALUE starts with `#f8` (light-mode hex).
    - `--card-bg` VALUE starts with `rgba(255` (white-ish).
    - `--text-secondary` VALUE is a slate-500-ish hex (AA-contrast on white — 4.5:1 minimum).
    - `prefers-reduced-motion` media query present in globals.css.
    - No variable renames — all previously-defined variable NAMES still present in `:root`.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>globals.css is the single light-mode palette source, reduced-motion honored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Per-component dark-Tailwind → light-Tailwind swap across ReportScreen, TestScreen, CaseDashboard, DashboardCards, and 5 test-screen sub-components</name>
  <files>assessment-app/src/components/ReportScreen.tsx, assessment-app/src/components/TestScreen.tsx, assessment-app/src/components/CaseDashboard.tsx, assessment-app/src/components/case-dashboard/DashboardCards.tsx, assessment-app/src/components/test-screen/QuestionHeader.tsx, assessment-app/src/components/test-screen/ScenarioBlock.tsx, assessment-app/src/components/test-screen/OptionsList.tsx, assessment-app/src/components/test-screen/ZoneOverlays.tsx, assessment-app/src/components/test-screen/CaseFilePanel.tsx</files>
  <read_first>
    - Each of the 9 target files (verify they exist; 5 test-screen sub-components come from plan 13-01).
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-RESEARCH.md § Pattern 4 last block — the recommended per-class swap table (bg-slate-900/70 → bg-slate-100, text-slate-300 → text-slate-700, etc.).
    - Prior observation 951 (2026-08-01 1:41p): 70 hardcoded dark Tailwind classes across 3 components. Grep each target file to enumerate ALL occurrences before editing.
  </read_first>
  <action>
    Per-file, systematic swap. Use these mappings (mechanical replace per file; do not attempt a single find-replace across the whole project):
      - `bg-slate-900/70` / `bg-slate-900/60` / `bg-slate-900/50` → `bg-slate-100`
      - `bg-slate-950/40` / `bg-slate-950/30` / `bg-slate-950/20` → `bg-white` (for content wells) or `bg-slate-50` (for subtle wells)
      - `text-white` → `text-slate-900`
      - `text-slate-300` → `text-slate-700`
      - `text-slate-500` → `text-slate-600` (secondary label)
      - `text-slate-200` → `text-slate-800`
      - `border-slate-800/60` → `border-slate-200`
      - `border-slate-800` → `border-slate-200`
      - `hover:bg-slate-800/80` → `hover:bg-slate-200/80`
      - Keep the existing `bg-card`, `text-[var(--text-secondary)]`, and `border-[var(--card-border)]` tokens as-is (they auto-pick up the new palette from Task 1).

    Layout widening on ReportScreen (UX-02):
      - Change outer wrapper className from `w-full max-w-[580px] mx-auto animate-fade-in` to `w-full max-w-[900px] mx-auto animate-fade-in px-6 md:px-8` — widens the card while preserving centered layout.
      - Bump the inner `<div className="px-8 py-6 flex flex-col gap-5">` gap to `gap-6` and inner section text sizes: on body text where the current source is `text-xs`, upgrade to `text-sm` in content-carrying areas (score labels remain small).

    Layout widening on TestScreen (UX-02):
      - Wherever an outer wrapper uses `max-w-*` to constrain content to a single-card centered column, widen the constraint or remove it, so the two-column grid (scenario left, question right) uses the full available viewport minus comfortable page-gutter padding (`px-6 md:px-10`). Do not change the grid definition itself.

    Do NOT change any ARIA attribute added by plan 13-03. Do NOT change any JSX structure, handler, or state. Diffs should be class-attribute-only.
  </action>
  <verify>
    <automated>cd assessment-app && npx tsc --noEmit 2>&1 | tail -10 ; echo "---REMAINING-DARK-CLASSES---"; grep -rEn "text-white|bg-slate-9|bg-slate-950|border-slate-8|text-slate-300" src/components/ReportScreen.tsx src/components/TestScreen.tsx src/components/CaseDashboard.tsx src/components/case-dashboard/DashboardCards.tsx src/components/test-screen/ 2>/dev/null | head -30 ; echo "---BUILD---"; npm run build 2>&1 | tail -10 ; echo "---AXE-SANITY---"; test -f .planning/phases/13-accessibility-f-05-remaining-polish/13-03-postfix-axe-report.md && echo "post-fix-axe-baseline-still-usable"</automated>
  </verify>
  <acceptance_criteria>
    - grep for the dark-class list in the 9 target files returns zero remaining occurrences OR only occurrences that are inside `sr-only` or comment blocks (justify each).
    - ReportScreen outer wrapper max-width increased (`max-w-[900px]` or wider) with new horizontal padding.
    - TestScreen outer wrapper widened for scenario+question two-column layout.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
    - Diff is class-attribute-only across all 9 files — no JSX restructure, no handler change, no ARIA regression.
  </acceptance_criteria>
  <done>All target components render on the light palette; layout widened; a11y attributes untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human visual sign-off on light theme + widened layout</name>
  <files>assessment-app/src/app/globals.css, assessment-app/src/components/ReportScreen.tsx, assessment-app/src/components/TestScreen.tsx</files>
  <action>Human visual verification of the completed light-theme migration + layout widening across candidate-facing surfaces. See how-to-verify for the 7-step walk-through. Do not advance to Wave 2 until this checkpoint resolves.</action>
  <verify>
    <human-check>Human walks the 7-step how-to-verify flow in a running dev server and types the resume-signal.</human-check>
  </verify>
  <done>Resume-signal received (`approved` or a describe-issues re-work note).</done>
  <what-built>Complete light-theme migration across candidate-facing surfaces (ReportScreen, TestScreen with its 5 sub-components, CaseDashboard, DashboardCards) plus layout widening on ReportScreen and TestScreen. Palette values live in globals.css :root; per-component dark classes replaced with light equivalents.</what-built>
  <how-to-verify>
    1. Start the dev server: `cd assessment-app && npm run dev`.
    2. Open http://localhost:3000/ in a fresh browser window.
    3. Complete a WelcomeScreen + AssemblyScreen flow into the test.
    4. Walk one item from each zone: English Grammar, Attention to Detail (with a case scenario), Critical Thinking (with a case dashboard). Confirm:
       - Backgrounds are light (near-white), text is dark and readable.
       - No stray dark panels or "black card" appearances anywhere.
       - Layout uses more horizontal space than the previous cramped centered card.
       - Zone transition overlay renders on the light palette.
    5. Trigger a completed report path (either via a full submit or the dev fixture route from plan 13-02) and confirm ReportScreen:
       - Renders on the light palette.
       - Uses the widened max-width (approx 900px+).
       - TraitBars are readable, tier badge is visible, ungraded notice (if fixture triggers it) still legible.
    6. In DevTools, verify the AxeDevReporter's `[axe]` warnings are unchanged from plan 13-03 post-fix state (no new critical/serious violations introduced by the theme swap).
    7. Test with `prefers-reduced-motion: reduce` toggled on in DevTools Rendering panel — CSS fade animations should stop; framer-motion animations should snap to end state.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 items check out, or describe issues (e.g., "stray dark border on TestScreen options list") for a re-work.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme migration blast radius → downstream visual QA | Migration touches 10 files across all candidate-facing components; human sign-off gate before Wave 2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-08 | Information Disclosure | Insufficient AA color contrast in the new palette causes text unreadable for low-vision users | high | mitigate | Palette values chosen from 13-RESEARCH.md Pattern 4 with documented AA-contrast rationale. Task 3 sign-off includes reading TestScreen + ReportScreen text visually. AxeDevReporter re-run catches any color-contrast regression. |
| T-13-09 | Denial of Service | Screen-reader users lose ARIA landmarks from plan 13-03 if theme migration accidentally removes attributes | high | mitigate | Task 2 acceptance criteria requires class-attribute-only diffs; ARIA verification is part of Task 3 sign-off (item 6). |
| T-13-10 | Tampering | Vestibular disorder impact from unrelenting framer motion transitions | medium | mitigate | Task 1 adds `prefers-reduced-motion` media query in globals.css; framer-motion honors the same OS preference natively. |
</threat_model>

<verification>
- Task 1 automated: variable values are light, prefers-reduced-motion present, build clean.
- Task 2 automated: dark-class grep returns zero remaining, tsc clean, build clean.
- Task 3 human: developer walks 3 zones + ReportScreen + reduced-motion in browser, approves or requests re-work.
</verification>

<success_criteria>
- UX-01 satisfied: single light theme across all candidate-facing components (no toggle, no dark mode).
- UX-02 satisfied: layout widened to use more horizontal space; body text larger where readability calls for it.
- A11Y-01 preserved: 13-03 post-fix axe report still applies (no new critical/serious violations introduced by the theme swap).
- Reduced-motion honored per WCAG 2.3.3.
</success_criteria>

<output>
Create `.planning/phases/13-accessibility-f-05-remaining-polish/13-04-SUMMARY.md` when done. Include screenshots (or paths thereto) of at least: WelcomeScreen, TestScreen mid-item, CT case dashboard, ReportScreen. Reference the Task 3 sign-off resume-signal.
</output>

## Artifacts this phase produces

- **Modified:** `assessment-app/src/app/globals.css` — light-mode `:root` values, prefers-reduced-motion media query.
- **Modified:** `assessment-app/src/components/ReportScreen.tsx` — dark→light Tailwind class swaps + widened wrapper.
- **Modified:** `assessment-app/src/components/TestScreen.tsx` — dark→light Tailwind class swaps + widened wrapper.
- **Modified:** `assessment-app/src/components/CaseDashboard.tsx` — dark→light Tailwind class swaps.
- **Modified:** `assessment-app/src/components/case-dashboard/DashboardCards.tsx` — dark→light Tailwind class swaps.
- **Modified:** the 5 test-screen sub-components — dark→light Tailwind class swaps.
- **No behavior change. No new dependencies. No ARIA regressions.**
