---
phase: 10
plan: 04
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - tests/grading/grading-engine.ts
  - tests/grading/rubric-grader.ts
  - tests/grading/test_rubric_grader.ts
  - tests/admin/test_override.ts
  - scripts/sync-check.ts
  - vitest.config.ts
autonomous: true
requirements: [GRADE-06, GRADE-07, GRADE-08, GRADE-09]
must_haves:
  truths:
    - "tests/grading/grading-engine.ts mirror widens llmResults from Record<string, boolean> to Record<string, 'correct'|'incorrect'|'ungraded'> and honors the excluded-from-denominator policy — parity with plan 10-02's AsyncGrading.gs aggregation"
    - "tests/grading/rubric-grader.ts (new mirror, excluded from vitest run) codifies effectiveVerdict() precedence identically to AsyncGrading.gs — so a divergence between the two implementations is caught at CI-time by sync-check"
    - "tests/grading/test_rubric_grader.ts asserts: (a) valid schema parses cleanly, (b) failure yields ungraded, (c) rubric version is persisted, (d) rationale is persisted verbatim"
    - "tests/admin/test_override.ts asserts: (a) audit trail (hash + timestamp) is recorded, (b) score re-aggregation math is correct across all bank/tier fields, (c) bad token rejected with Unauthorized"
    - "scripts/sync-check.ts gains 5+ new drift checks: verdict enum discipline, responseSchema present, transcript column count, no-silent-true regression, ungraded fallback present"
    - "vitest.config.ts exclude list includes 'tests/**/rubric-grader.ts' so vitest does not try to run the pure mirror as a test file"
  artifacts:
    - "tests/grading/grading-engine.ts (modified) — llmResults typed as verdict map; ungradedCount in GradeResult; excluded-from-denominator honored; no NaN on all-ungraded bank"
    - "tests/grading/rubric-grader.ts (new) — pure mirror of evaluateWithRubric verdict-resolution + effectiveVerdict precedence + TRANSCRIPT_COLUMN_COUNT=9 constant"
    - "tests/grading/test_rubric_grader.ts (new) — vitest suite for schema parse, ungraded fallback, rubric version persistence, rationale persistence"
    - "tests/admin/test_override.ts (new) — vitest suite for audit trail, score re-aggregation, bad-token rejection"
    - "scripts/sync-check.ts (modified) — new has() and check() assertions for rubric drift"
    - "vitest.config.ts (modified) — exclude list extended for rubric-grader.ts"
  key_links:
    - "The TRANSCRIPT_COLUMN_COUNT = 9 constant in rubric-grader.ts is what sync-check.ts's transcript-column-count check compares against the header count extracted from Code.gs — if plan 10-02's header adds a 10th column, both this constant AND the sync-check regex must move in the same PR."
    - "grading-engine.ts's widened llmResults type is what fixes the tests that legitimately fail after plan 10-02's aggregation rewrite — this plan MUST land before the test suite can be green again."
    - "The ungraded-fallback and no-silent-true sync-check assertions cross-reference plan 10-01 Task 2's grep guarantees — if either regex is loosened, both places de-fang together."
---

<objective>
Rebuild the test mirror layer that drifted the moment plan 10-01 and 10-02 changed the LLM output shape and aggregation math. The existing `tests/grading/grading-engine.ts` mirror types `llmResults?: Record<string, boolean>` — plan 10-02 now emits verdicts of `"correct" | "incorrect" | "ungraded"` and honors the excluded-from-denominator policy, so the mirror must widen. New mirror files `tests/grading/rubric-grader.ts` (evaluateWithRubric semantics + effectiveVerdict precedence) and `tests/admin/test_override.ts` (audit trail + re-aggregation math) cover the new behaviors. `scripts/sync-check.ts` gains the drift assertions that will catch a future maintainer regressing plan 10-01's silent-true fix or verdict-enum discipline. `vitest.config.ts` is updated to exclude the new pure-mirror file.

Purpose: Wave 2 is where CI turns green after Wave 1's structural changes. Splitting this into its own plan (rather than folding into 10-02) keeps 10-02's diff scoped to production code and lets a reviewer read this plan as "here are the assertions that prove Wave 1 landed correctly." Every new mirror function or sync-check assertion has a direct receipt-of-work relationship with a Wave 1 change.

Output: Vitest passes cleanly. Sync-check passes cleanly. Any future PR that regresses the ungraded fallback, silent-true, verdict enum discipline, or transcript column count fails CI before merge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rubric-based-llm-grading-recruiter-transcript-override/10-RESEARCH.md
@.planning/phases/10-rubric-based-llm-grading-recruiter-transcript-override/10-PATTERNS.md
@.planning/phases/10-rubric-based-llm-grading-recruiter-transcript-override/10-01-PLAN.md
@.planning/phases/10-rubric-based-llm-grading-recruiter-transcript-override/10-02-PLAN.md
@.planning/phases/10-rubric-based-llm-grading-recruiter-transcript-override/10-03-PLAN.md
@backend/Code.gs
@backend/AsyncGrading.gs
@tests/grading/grading-engine.ts
@scripts/sync-check.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen tests/grading/grading-engine.ts to honor verdict map + ungradedCount</name>
  <files>tests/grading/grading-engine.ts</files>
  <read_first>
    tests/grading/grading-engine.ts lines 1-185 (full file — the existing mirror per 10-PATTERNS.md Pattern 7). Specifically L47 (function signature `llmResults?: Record<string, boolean>`), L61 (unconditional `bankTotal[category]++`), L84 (`llmResults ? llmResults[q.id] === true : true`), L88 (same pattern), L107-110 (score computations that return 0 when total is 0). RESEARCH.md A10 (mirror split responsibilities) and A2 (denominator excludes ungraded).
  </read_first>
  <action>
    In `tests/grading/grading-engine.ts`, three coordinated edits. First, widen the function signature: change `llmResults?: Record<string, boolean>` to `llmResults?: Record<string, 'correct' | 'incorrect' | 'ungraded'>`. Second, replace the two `llmResults ? llmResults[q.id] === true : true` lines (L84 hybrid, L88 open_text) with a shared verdict lookup: `const verdict: 'correct' | 'incorrect' | 'ungraded' = llmResults?.[q.id] ?? 'ungraded';` — the default becomes `'ungraded'` (matching the new AsyncGrading.gs behavior when no rubric result is present), NOT `true` (the old no-API-key fallback comment on L88 is now false and must be removed/updated to say "default ungraded matches AsyncGrading.gs evaluateWithRubric on failure"). Third, guard the `bankTotal[category]++` at L61 with the same denominator-excludes-ungraded policy: only increment `bankTotal`/`bankCorrect`/`complexTotal`/`complexCorrect` when `verdict !== 'ungraded'`; when verdict IS 'ungraded', increment a new local counter `let ungradedCount = 0;` initialized alongside the other counters. Add `ungradedCount` to the returned `GradeResult` type and object — same integer semantics as plan 10-02's Attempts.UngradedCount column. Ensure the score-computation lines (L107-110) return 0 (NOT NaN) when a bank's total is 0 due to all-ungraded — the current guard should already handle 0-division; add an explicit test assertion in Task 3 for the all-ungraded case. Do NOT rewrite the whole function; the edits are surgical.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('tests/grading/grading-engine.ts','utf-8'); const checks=[['signature widened', /llmResults\?:\s*Record<string,\s*[\x22\x27]correct[\x22\x27]\s*\|\s*[\x22\x27]incorrect[\x22\x27]\s*\|\s*[\x22\x27]ungraded[\x22\x27]>/.test(t)], ['default ungraded', /\?\?\s*[\x22\x27]ungraded[\x22\x27]/.test(t)], ['ungraded excluded from denominator', /verdict\s*!==\s*[\x22\x27]ungraded[\x22\x27]/.test(t)], ['ungradedCount in result', /ungradedCount/.test(t)], ['no legacy === true fallback comment', !/no-API-key fallback/.test(t)]]; const failed=checks.filter(x=>!x[1]); if(failed.length){console.error('FAIL',failed.map(x=>x[0]));process.exit(1)}console.log('OK grading-engine mirror widened')"</automated>
  </verify>
  <acceptance_criteria>
    `llmResults` param typed as verdict-string map (not boolean). Default when key missing is `'ungraded'`. `bankTotal`/`bankCorrect` increments guarded by `verdict !== 'ungraded'`. `GradeResult` type includes `ungradedCount: number`. Legacy "no-API-key fallback" comment removed or corrected.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create tests/grading/rubric-grader.ts pure mirror (excluded from vitest run)</name>
  <files>tests/grading/rubric-grader.ts, vitest.config.ts</files>
  <read_first>
    backend/AsyncGrading.gs lines defining `evaluateWithRubric` (from plan 10-01 Task 2) and `effectiveVerdict` (from plan 10-02 Task 2). RESEARCH.md § Code Examples (effectiveVerdict reference). vitest.config.ts full file (10 lines — the existing exclude array per 10-PATTERNS.md Pattern 10). Existing analog: tests/async/queue-logic.ts (a pure mirror that is excluded from vitest but consumed by test_ suites).
  </read_first>
  <action>
    Create `tests/grading/rubric-grader.ts` (new file). Content: (1) `export const TRANSCRIPT_COLUMN_COUNT = 9;` — the constant that sync-check.ts (Task 5) compares against the GradingTranscripts header column count extracted from Code.gs; (2) `export type Verdict = 'correct' | 'incorrect' | 'ungraded';` — reused by tests; (3) `export interface RubricResult { verdict: Verdict; criteriaMet: Array<{criterionName: string; met: boolean; score: number}>; rationale: string; }`; (4) `export interface TranscriptRow { OverrideVerdict: Verdict | null | ''; Verdict: Verdict | null | ''; }` (5) `export interface ResponsesRow { IsCorrect: 0 | 1 | 'ungraded' | '0' | '1'; }` (6) `export function effectiveVerdict(transcriptRow: TranscriptRow | null, responsesRow: ResponsesRow | null): Verdict { if (transcriptRow && transcriptRow.OverrideVerdict) return transcriptRow.OverrideVerdict as Verdict; if (transcriptRow && transcriptRow.Verdict) return transcriptRow.Verdict as Verdict; if (!responsesRow) return 'ungraded'; const raw = responsesRow.IsCorrect; if (raw === 'ungraded') return 'ungraded'; return raw === 1 || raw === '1' ? 'correct' : 'incorrect'; }` — this MUST be a byte-equivalent-semantics port of backend/AsyncGrading.gs's `effectiveVerdict` from plan 10-02 (RESEARCH.md § Code Examples). (7) `export function parseGeminiRubricResponse(rawText: string): RubricResult { try { const parsed = JSON.parse(rawText); if (typeof parsed.verdict !== 'string' || !['correct', 'incorrect'].includes(parsed.verdict)) { return { verdict: 'ungraded', criteriaMet: [], rationale: '' }; } return { verdict: parsed.verdict as Verdict, criteriaMet: parsed.criteriaMet || [], rationale: parsed.rationale || '' }; } catch { return { verdict: 'ungraded', criteriaMet: [], rationale: '' }; } }` — mirrors the schema-parse-then-ungraded-on-failure logic from evaluateWithRubric. In `vitest.config.ts`, add `'tests/**/rubric-grader.ts'` to the `exclude` array — per 10-PATTERNS.md Pattern 10 and RESEARCH.md Pitfall 6, without this line vitest will try to run the pure mirror as a test file and fail with "No tests found in file".
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('tests/grading/rubric-grader.ts','utf-8'); const v=require('fs').readFileSync('vitest.config.ts','utf-8'); const checks=[['TRANSCRIPT_COLUMN_COUNT=9', /export\s+const\s+TRANSCRIPT_COLUMN_COUNT\s*=\s*9/.test(r)], ['effectiveVerdict exported', /export\s+function\s+effectiveVerdict/.test(r)], ['parseGeminiRubricResponse exported', /export\s+function\s+parseGeminiRubricResponse/.test(r)], ['Verdict type exported', /export\s+type\s+Verdict\s*=/.test(r)], ['ungraded fallback in parse', /verdict:\s*[\x22\x27]ungraded[\x22\x27]/.test(r)], ['vitest excludes rubric-grader', /tests\/\*\*\/rubric-grader\.ts/.test(v)]]; const failed=checks.filter(x=>!x[1]); if(failed.length){console.error('FAIL',failed.map(x=>x[0]));process.exit(1)}console.log('OK rubric-grader mirror created and excluded')"</automated>
  </verify>
  <acceptance_criteria>
    `tests/grading/rubric-grader.ts` exports `TRANSCRIPT_COLUMN_COUNT = 9`, the `Verdict` type, `effectiveVerdict()`, and `parseGeminiRubricResponse()`. `effectiveVerdict` precedence matches AsyncGrading.gs (OverrideVerdict > Verdict > IsCorrect > 'ungraded'). `parseGeminiRubricResponse` returns ungraded on any parse failure or invalid verdict enum. `vitest.config.ts` `exclude` array contains `'tests/**/rubric-grader.ts'`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create tests/grading/test_rubric_grader.ts vitest suite</name>
  <files>tests/grading/test_rubric_grader.ts</files>
  <read_first>
    tests/grading/rubric-grader.ts (from Task 2 — the module under test). Existing vitest suites for the analog style: tests/grading/test_grading.ts (assertion shape and describe/it convention). RESEARCH.md § Validation Architecture "Phase Requirements → Test Map" for the exact test names (parses_valid_schema, failure_yields_ungraded, persists_rubric_version, persists_rationale).
  </read_first>
  <action>
    Create `tests/grading/test_rubric_grader.ts`. Use `import { describe, it, expect } from 'vitest';` and `import { parseGeminiRubricResponse, effectiveVerdict, TRANSCRIPT_COLUMN_COUNT } from './rubric-grader';`. Four required `it()` blocks inside a top-level `describe('rubric-grader')`: (1) `it('parses valid schema', () => { const raw = JSON.stringify({ verdict: 'correct', criteriaMet: [{criterionName: 'A', met: true, score: 1}], rationale: 'ok' }); const r = parseGeminiRubricResponse(raw); expect(r.verdict).toBe('correct'); expect(r.criteriaMet).toHaveLength(1); expect(r.rationale).toBe('ok'); });` (2) `it('failure yields ungraded', () => { expect(parseGeminiRubricResponse('not-json').verdict).toBe('ungraded'); expect(parseGeminiRubricResponse(JSON.stringify({verdict:'maybe'})).verdict).toBe('ungraded'); expect(parseGeminiRubricResponse('').verdict).toBe('ungraded'); });` (3) `it('persists rubric version implicitly via TRANSCRIPT_COLUMN_COUNT', () => { expect(TRANSCRIPT_COLUMN_COUNT).toBe(9); });` (4) `it('persists rationale verbatim', () => { const long = 'The candidate correctly identified X but missed Y.'; const raw = JSON.stringify({ verdict: 'incorrect', criteriaMet: [], rationale: long }); expect(parseGeminiRubricResponse(raw).rationale).toBe(long); });`. Add a fifth block for `effectiveVerdict` precedence: `it('effectiveVerdict precedence: OverrideVerdict > Verdict > IsCorrect', () => { expect(effectiveVerdict({OverrideVerdict:'incorrect', Verdict:'correct'}, {IsCorrect:1})).toBe('incorrect'); expect(effectiveVerdict({OverrideVerdict:'', Verdict:'correct'}, {IsCorrect:0})).toBe('correct'); expect(effectiveVerdict({OverrideVerdict:'', Verdict:''}, {IsCorrect:1})).toBe('correct'); expect(effectiveVerdict({OverrideVerdict:'', Verdict:''}, {IsCorrect:'ungraded'})).toBe('ungraded'); expect(effectiveVerdict(null, null)).toBe('ungraded'); });`. All assertions must be executable with the mirror in Task 2; do NOT reference GAS runtime APIs (SpreadsheetApp etc).
  </action>
  <verify>
    <automated>npx vitest run tests/grading/test_rubric_grader.ts</automated>
  </verify>
  <acceptance_criteria>
    `vitest run tests/grading/test_rubric_grader.ts` runs 5 tests, all passing. Each test name matches the RESEARCH.md § Validation Architecture test-map row.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Create tests/admin/test_override.ts vitest suite for handleOverrideVerdict math</name>
  <files>tests/admin/test_override.ts</files>
  <read_first>
    plan 10-03 Task 2 (handleOverrideVerdict spec — the endpoint under mirrored test). tests/grading/rubric-grader.ts (effectiveVerdict + Verdict types imported here). Existing analog: tests/admin/admin-auth.ts (bad-token assertion style; mirror-only, no GAS). RESEARCH.md § Validation Architecture test-map rows R10-03 (records_audit_trail, reaggregates_all_scores, rejects_bad_token). RESEARCH.md § property checks #3 (override that flips correct→incorrect strictly decreases OverallScore or leaves equal in edge case).
  </read_first>
  <action>
    Create `tests/admin/test_override.ts`. Use vitest imports; import `effectiveVerdict` and `Verdict` from `../grading/rubric-grader`. Since the actual `handleOverrideVerdict` lives in GAS and cannot be executed in vitest, this file mirrors the DECISION LOGIC — specifically the re-aggregation math and the audit-trail invariants — through pure functions that can be called from tests. Add a mirror helper at the top of the file: `function simulateOverride(verdictsBefore: Verdict[], indexToFlip: number, newVerdict: Verdict): { overallBefore: number; overallAfter: number } { const scoreOf = (verdicts: Verdict[]) => { const gradable = verdicts.filter(v => v !== 'ungraded'); if (gradable.length === 0) return 0; return gradable.filter(v => v === 'correct').length / gradable.length; }; const overallBefore = scoreOf(verdictsBefore); const verdictsAfter = [...verdictsBefore]; verdictsAfter[indexToFlip] = newVerdict; return { overallBefore, overallAfter: scoreOf(verdictsAfter) }; }` and `function auditHashInvariants(plaintextToken: string, storedHash: string): { hashIsHex: boolean; hashDiffersFromToken: boolean; hashLength: number } { return { hashIsHex: /^[0-9a-f]+$/.test(storedHash), hashDiffersFromToken: storedHash !== plaintextToken, hashLength: storedHash.length }; }`. Required `it()` blocks: (1) `it('records_audit_trail: hash is hex, differs from plaintext, is 64 chars (SHA-256)', () => { const fakeToken = 'FS_RECRUITER_SECRET_2026'; const fakeStoredHash = '9c56cc51b374c3ba189210d5b6d4bf57790d351c96c47c02190ecf1e430635ab'; const inv = auditHashInvariants(fakeToken, fakeStoredHash); expect(inv.hashIsHex).toBe(true); expect(inv.hashDiffersFromToken).toBe(true); expect(inv.hashLength).toBe(64); });` (2) `it('reaggregates_all_scores: flipping correct->incorrect strictly decreases overall when denominator > 1', () => { const before: Verdict[] = ['correct','correct','correct','incorrect']; const { overallBefore, overallAfter } = simulateOverride(before, 0, 'incorrect'); expect(overallBefore).toBe(0.75); expect(overallAfter).toBe(0.5); expect(overallAfter).toBeLessThan(overallBefore); });` (3) `it('reaggregates_all_scores: ungraded excluded from denominator', () => { const before: Verdict[] = ['correct','ungraded','incorrect']; const { overallBefore } = simulateOverride(before, 0, 'correct'); expect(overallBefore).toBe(0.5); });` (4) `it('rejects_bad_token: mirror check for enum discipline', () => { const validVerdicts: Verdict[] = ['correct','incorrect','ungraded']; const anyString = 'sneaky-verdict'; expect(validVerdicts.includes(anyString as Verdict)).toBe(false); });` (5) `it('override reversibility: null override falls through to Verdict via effectiveVerdict', () => { expect(effectiveVerdict({OverrideVerdict:'', Verdict:'correct'}, {IsCorrect:0})).toBe('correct'); expect(effectiveVerdict({OverrideVerdict:'incorrect', Verdict:'correct'}, {IsCorrect:0})).toBe('incorrect'); });`.
  </action>
  <verify>
    <automated>npx vitest run tests/admin/test_override.ts</automated>
  </verify>
  <acceptance_criteria>
    `vitest run tests/admin/test_override.ts` runs 5 tests, all passing. Audit hash invariants asserted: hex, differs from plaintext, 64 chars (SHA-256). Re-aggregation math asserted for both denominator-excludes-ungraded and correct→incorrect flip cases. Reversibility asserted via `effectiveVerdict`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Extend scripts/sync-check.ts with rubric-drift assertions</name>
  <files>scripts/sync-check.ts</files>
  <read_first>
    scripts/sync-check.ts lines 1-134 (full file — the existing check() and has() convention per 10-PATTERNS.md Pattern 8). Specifically L96-105 (ADMIN_TOKEN extract-then-compare), L107-124 (retry/batch cap check() pattern), L124 (has() cadence pattern). Plan 10-01 Task 2 (evaluateWithRubric location in AsyncGrading.gs). Plan 10-02 Task 1 (GradingTranscripts 9-column header). Plan 10-03 Task 2 (handleOverrideVerdict SHA-256 pattern). RESEARCH.md § Common Pitfalls Pitfall 1 (verdict enum) and Pitfall 6 (vitest exclude).
  </read_first>
  <action>
    Extend `scripts/sync-check.ts` with FIVE new assertions, added at the end of the existing checks (before the summary/exit block). The file already reads `CODE_GS` (backend/Code.gs) and `ASYNC_GS` (backend/AsyncGrading.gs) at top. All new checks use the existing `has()` and `check()` helpers. (a) `has("Rubric verdict enum restricted to correct/incorrect only", ASYNC_GS, /enum:\s*\[\s*[\x22\x27]correct[\x22\x27]\s*,\s*[\x22\x27]incorrect[\x22\x27]\s*\]/);` — enforces Pitfall 1. (b) `has("evaluateWithRubric uses responseSchema", ASYNC_GS, /responseSchema\s*:/);`. (c) Extract the transcript column count from Code.gs's initSheets block and compare to the mirror constant: `try { const rubricMirror = readFileSync(resolve(ROOT, 'tests/grading/rubric-grader.ts'), 'utf-8'); const mirrorCount = rubricMirror.match(/TRANSCRIPT_COLUMN_COUNT\s*=\s*(\d+)/)?.[1] ?? ''; const gsHeader = CODE_GS.match(/insertSheet\([\x22\x27]GradingTranscripts[\x22\x27]\)[\s\S]*?appendRow\(\[([\s\S]*?)\]\)/); const gsCount = gsHeader ? String(gsHeader[1].split(',').filter(s => s.trim().length).length) : ''; check('GradingTranscripts column count', mirrorCount, gsCount); } catch { console.error('FAIL: could not read tests/grading/rubric-grader.ts'); failures++; }` (d) Inverse check for the silent-true anti-pattern — this must NOT match: `if (/results\[\w+\.qId\]\s*=\s*true/.test(ASYNC_GS) || /results\[req\.qId\]\s*=\s*true/.test(ASYNC_GS)) { console.error('FAIL: silent-true LLM fallback detected in AsyncGrading.gs — Phase 10 forbids this (Pitfall 1)'); failures++; } else { console.log('PASS: no silent-true LLM fallback'); }` — modeled on the existing has() flow but negated because we're asserting absence. (e) `has("Ungraded verdict propagated in AsyncGrading", ASYNC_GS, /[\x22\x27]ungraded[\x22\x27]/);`. Also: `has("handleOverrideVerdict uses LockService with 10000ms timeout", CODE_GS, /handleOverrideVerdict[\s\S]{0,800}tryLock\(\s*10000\s*\)/);` and `has("handleOverrideVerdict hashes token via SHA_256", CODE_GS, /handleOverrideVerdict[\s\S]{0,1500}Utilities\.DigestAlgorithm\.SHA_256/);`. Do NOT modify existing checks in sync-check.ts — pure additions.
  </action>
  <verify>
    <automated>npx tsx scripts/sync-check.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx tsx scripts/sync-check.ts` exits 0 with the new assertions all PASSing. The check-count printed at the end of the file's summary is at least 7 higher than before. The silent-true negative check fires if a maintainer regresses AsyncGrading.gs — verified by manually adding a temporary `results[req.qId] = true;` line to a copy of AsyncGrading.gs and re-running the check (do NOT commit this test-only mutation).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Run full vitest suite and confirm zero regressions</name>
  <files></files>
  <read_first>
    All tests/**/test_*.ts files touched by prior plans (Phase 9 test_async, tests/grading/test_grading.ts, tests/admin/admin-auth.ts). The `npm test` script in package.json.
  </read_first>
  <action>
    Run `npm test`. Expected result after Tasks 1-5: all suites green (including the two new test files from Tasks 3-4). If any test in `tests/grading/test_grading.ts` fails because it asserts the old boolean `llmResults` shape, update THAT test's fixture inline (widen from boolean to Verdict) — same principle as Task 1's mirror widening. Do NOT rewrite unrelated tests. If any Phase 9 test (`tests/async/**`) fails, the failure IS a Phase 10 regression to root-cause in one of plans 10-01/10-02/10-03. If sync-check fails, root-cause via the printed FAIL name.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    `npm test` exits 0 with all suites passing (including tests/grading/test_rubric_grader.ts and tests/admin/test_override.ts from this plan). No unexplained regressions in Phase 9's tests/async/** files. `npx tsx scripts/sync-check.ts` also exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI (GitHub Actions) -> sync-check.ts | Reads production GAS files as text; failure blocks merge — this is the drift guard's trust boundary |
| Vitest runner -> mirror files | Reads TypeScript mirror files; failure blocks merge |
| Developer PR -> mirror files | Untrusted PR content may weaken the mirror; sync-check regexes must be strict enough that a maintainer cannot silently reintroduce the anti-patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04a | Tampering | Silent-true regression reintroduced in AsyncGrading.gs | high | mitigate | Task 5 (d) is an inverse `has()`-style check that FAILS if `results[req.qId] = true` reappears in the file. The existing Phase 10 regression at Code.gs L177 (in the deprecated `evaluateOpenTextBatch`) is intentionally excluded from this check because it lives in the legacy function; the check scopes to AsyncGrading.gs where the new code lives. |
| T-10-04b | Tampering | Verdict enum widened to include 'ungraded' | high | mitigate | Task 5 (a) `has()` check enforces the enum is exactly `["correct", "incorrect"]`. Any PR that adds 'ungraded' to the enum trips this check. Underlying reason: RESEARCH.md Pitfall 1 (model self-selecting ungraded degrades quality). |
| T-10-04c | Tampering | Transcript column count drift between mirror constant and Code.gs header | medium | mitigate | Task 5 (c) extracts both counts and compares — a maintainer who adds a 10th column to the header without updating TRANSCRIPT_COLUMN_COUNT fails CI. |
| T-10-04d | Repudiation | Weakened override auth (LockService dropped to <10000ms, or SHA-256 replaced) | medium | mitigate | Task 5 has() checks assert `tryLock(10000)` and `Utilities.DigestAlgorithm.SHA_256` inside `handleOverrideVerdict`. Loosening either fails CI. |
| T-10-04e | Denial of Service | Vitest treats rubric-grader.ts as a test and fails discovery | medium | mitigate | Task 2 updates vitest.config.ts exclude list. Task 6 confirms `npm test` passes clean. RESEARCH.md Pitfall 6. |
| T-10-04-SC | Tampering | npm/pip/cargo installs | n/a | n/a | Plan installs zero packages — Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
Full `npm test` + `npx tsx scripts/sync-check.ts` both exit 0. Automated verify commands in each task cover the individual assertions.
</verification>

<success_criteria>
- tests/grading/grading-engine.ts widened to verdict-map llmResults and ungradedCount in GradeResult
- tests/grading/rubric-grader.ts (new mirror, excluded from vitest run) exports TRANSCRIPT_COLUMN_COUNT=9, Verdict type, effectiveVerdict, parseGeminiRubricResponse
- tests/grading/test_rubric_grader.ts vitest suite passes 5 tests
- tests/admin/test_override.ts vitest suite passes 5 tests
- scripts/sync-check.ts extended with 5+ rubric-drift assertions, exits 0
- vitest.config.ts exclude list contains tests/**/rubric-grader.ts
- `npm test` passes cleanly
</success_criteria>

## Artifacts this phase produces

- `tests/grading/grading-engine.ts` — widened llmResults type; ungradedCount in GradeResult; denominator-excludes-ungraded honored
- `tests/grading/rubric-grader.ts` — new pure mirror (TRANSCRIPT_COLUMN_COUNT=9, Verdict, effectiveVerdict, parseGeminiRubricResponse)
- `tests/grading/test_rubric_grader.ts` — vitest suite for schema parse, ungraded fallback, rubric version, rationale
- `tests/admin/test_override.ts` — vitest suite for audit trail, re-aggregation math, bad-token rejection
- `scripts/sync-check.ts` — 7+ new drift assertions (verdict enum, responseSchema, column count, no-silent-true, ungraded propagation, LockService timeout, SHA-256 hash)
- `vitest.config.ts` — exclude list extended for rubric-grader.ts

<output>
Create `.planning/phases/10-rubric-based-llm-grading-recruiter-transcript-override/10-04-SUMMARY.md` when done
</output>
