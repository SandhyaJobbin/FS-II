---
phase: 01-content-ingestion-question-bank
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - docker-compose.yml
  - .env
  - pytest.ini
  - vitest.config.ts
  - tests/__init__.py
  - tests/conftest.py
  - tests/ingestion/__init__.py
  - tests/ingestion/test_smoke.py
  - tests/node/smoke.test.ts
autonomous: true
requirements: [INGEST-01, INGEST-03]
user_setup:
  - service: docker
    why: "Local Postgres 16 container — no Postgres instance exists (RESEARCH.md A4)"
    dashboard_config:
      - task: "Install and start Docker Desktop"
        location: "https://www.docker.com/products/docker-desktop/ — alternative: any local Postgres 16 on port 5432"

must_haves:
  truths:
    - "Python 3.13 has pydantic and pytest importable; python-docx 1.2.0 and openpyxl 3.1.5 confirmed present (pre-verified in 01-RESEARCH.md)"
    - "Node deps installed at pinned versions: zod 4.4.3, prisma 7.9.1, @prisma/client 7.9.1, plus typescript/tsx/vitest dev deps"
    - "Postgres 16 reachable on localhost:5432 with database fs_assessment and credentials matching .env DATABASE_URL"
    - "pytest tests/ingestion -x -q exits 0 and npx vitest run exits 0 (smoke tests green)"
  artifacts:
    - docker-compose.yml (postgres:16 service)
    - .env (DATABASE_URL) — gitignored
    - package.json + tsconfig.json
    - pytest.ini + vitest.config.ts
    - tests/conftest.py (docx fixture builders for all 4 marker conventions + malformed corpus)
  key_links:
    - "DATABASE_URL credentials match docker-compose POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB"
    - ".env listed in .gitignore (DB password must not be committed)"
---

<objective>
Provision the greenfield execution environment: pinned Python + Node dependencies, a Dockerized Postgres 16 content store, and the pytest/vitest test skeleton that every later plan's verification depends on.

Purpose: INGEST-01 demands a queryable content store (Postgres — none exists yet) and INGEST-03 demands fail-loud validation proven by tests (no test infra exists yet). This is the Wave 0 foundation.
Output: working toolchain, running Postgres, green smoke tests, docx fixture builders for all four answer-marker conventions.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md
@.planning/phases/01-content-ingestion-question-bank/01-PATTERNS.md
@.planning/phases/01-content-ingestion-question-bank/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pinned Python + Node toolchain</name>
  <files>package.json, tsconfig.json, .gitignore</files>
  <read_first>
    - .planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md (Standard Stack + Package Legitimacy Audit — all packages already approved, none ASSUMED/SUS)
    - .planning/phases/01-content-ingestion-question-bank/01-PATTERNS.md (Dependency Pinning convention)
    - CLAUDE.md (project rules: files under 500 lines, nothing saved to root outside /src /tests /docs /config /scripts — package.json/tsconfig/.gitignore are standard root config, allowed)
  </read_first>
  <action>
    Python: run `pip install pydantic pytest` and `pip show python-docx openpyxl` to confirm 1.2.0 / 3.1.5 already installed (research verified them working against the real files — do NOT reinstall or upgrade).
    Node: create `package.json` at repo root — name `fs-assessment-content`, `"private": true`, `"type": "module"`. Then `npm install zod@4.4.3 @prisma/client@7.9.1` and `npm install -D prisma@7.9.1 typescript tsx vitest @types/node`. Client and CLI versions MUST stay in lockstep at 7.9.1 (STACK.md compatibility table).
    Create `tsconfig.json`: strict true, target ES2022, module NodeNext, moduleResolution NodeNext, esModuleInterop, skipLibCheck, include prisma + tests/node.
    Create `.gitignore` with exactly: `node_modules/`, `.env`, `__pycache__/`, `.pytest_cache/`, `ingestion/.venv/`, `*.pyc`.
    Do NOT run create-next-app or scaffold any web app — that is a later-phase concern (PATTERNS.md convention 8).
  </action>
  <verify>
    <automated>pip show pydantic pytest; npm ls zod prisma @prisma/client; npx prisma --version</automated>
  </verify>
  <acceptance_criteria>
    - `pip show pydantic` and `pip show pytest` both print Version lines (exit 0)
    - `npm ls zod` prints zod@4.4.3; `npm ls prisma @prisma/client` prints 7.9.1 for both
    - `npx prisma --version` prints 7.9.x (exit 0)
    - package.json exists at repo root with `"type": "module"`; tsconfig.json exists with `"strict": true`
    - .gitignore contains `.env` and `node_modules/`
  </acceptance_criteria>
  <done>Pinned toolchain installed; prisma CLI and client at 7.9.1; zod 4.4.3; pytest + pydantic importable.</done>
</task>

<task type="auto">
  <name>Task 2: Provision Postgres 16 via docker compose</name>
  <files>docker-compose.yml, .env</files>
  <read_first>
    - .gitignore (created in Task 1 — confirm .env entry before writing secrets)
    - .planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md (Environment Availability: Postgres missing, Docker postgres:16 fallback)
  </read_first>
  <precondition>Docker Desktop installed and running (user_setup), OR a local Postgres 16 already listening on 5432 — if the latter, skip compose and write .env to point at it.</precondition>
  <reversibility rating="reversible">Local dev container; `docker compose down -v` destroys it with zero external effect.</reversibility>
  <action>
    Create `docker-compose.yml` at repo root: service `db`, image `postgres:16`, container_name `fs-postgres`, environment POSTGRES_DB=`fs_assessment`, POSTGRES_USER=`postgres`, POSTGRES_PASSWORD=`postgres-dev`, ports `"127.0.0.1:5432:5432"` (localhost bind only — not exposed to LAN), named volume `pgdata` on /var/lib/postgresql/data.
    Run `docker compose up -d`, then poll readiness (up to ~30s).
    Create `.env` at repo root with one line: `DATABASE_URL="postgresql://postgres:postgres-dev@localhost:5432/fs_assessment"`.
    Confirm `.env` is NOT staged by git (`git check-ignore .env` exits 0).
  </action>
  <verify>
    <automated>docker exec fs-postgres pg_isready -U postgres; docker exec fs-postgres psql -U postgres -d fs_assessment -tc "SELECT version();"</automated>
  </verify>
  <acceptance_criteria>
    - `docker exec fs-postgres pg_isready -U postgres` prints `accepting connections` (exit 0)
    - `SELECT version()` output contains `PostgreSQL 16`
    - .env exists with DATABASE_URL matching compose credentials and db name
    - `git check-ignore .env` exits 0
  </acceptance_criteria>
  <done>Postgres 16 container running, fs_assessment database reachable via DATABASE_URL, .env gitignored.</done>
</task>

<task type="auto">
  <name>Task 3: Test skeleton + docx fixture builders (Wave 0 per 01-VALIDATION.md)</name>
  <files>pytest.ini, vitest.config.ts, tests/__init__.py, tests/conftest.py, tests/ingestion/__init__.py, tests/ingestion/test_smoke.py, tests/node/smoke.test.ts</files>
  <read_first>
    - .planning/phases/01-content-ingestion-question-bank/01-VALIDATION.md (Wave 0 Requirements: tests/ingestion/, tests/conftest.py fixtures per marker convention + malformed corpus)
    - .planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md (four marker conventions table; Code Examples for docx shapes)
  </read_first>
  <action>
    Create `pytest.ini`: `[pytest]` with `testpaths = tests` and `addopts = -q` (no watch-mode flags).
    Create `tests/__init__.py` and `tests/ingestion/__init__.py` (empty).
    Create `tests/conftest.py` with pytest fixtures that build REAL in-memory/temp docx files via python-docx (not mocks):
      - `docx_trailing_check` — a question paragraph + option lines where correct option text ends with `✅`
      - `docx_leading_check` — option lines starting with `✅` (one single-answer, one select-all with 2 ✅)
      - `docx_checkbox_pair` — leading `☑` (correct) and `☐` (incorrect) lines
      - `docx_bold_run` — option paragraphs where exactly one option's run has `bold=True`
      - `malformed_missing_option` — question with a stem but zero option lines
      - `malformed_no_marker` — options present, no correct marker of any convention
      - `malformed_two_bold` — two options with bold runs
      Each fixture returns the docx file path (tmp_path). All file writes use `encoding="utf-8"` explicitly.
    Create `tests/ingestion/test_smoke.py`: one test per builder asserting the fixture file exists and python-docx can open it and find the expected glyphs/bold runs.
    Create `vitest.config.ts`: include `tests/node/**/*.test.ts` and `prisma/**/*.test.ts`, environment node.
    Create `tests/node/smoke.test.ts`: single trivial `expect(1+1).toBe(2)` so vitest exits 0.
  </action>
  <verify>
    <automated>pytest tests/ingestion -x -q; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ingestion -x -q` exits 0 with ≥7 passed (one per fixture)
    - `npx vitest run` exits 0 with ≥1 passed
    - conftest.py exposes all 7 named fixtures; each writes docx with explicit utf-8 encoding
  </acceptance_criteria>
  <done>pytest and vitest both green on smoke tests; fixture builders cover all 4 marker conventions + 3 malformed shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip/npm registries → dev machine | Dependency install (supply chain) |
| .env → python/node processes | DB credentials consumed from disk |
| Docker container → host LAN | Postgres port exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Info Disclosure | .env (DATABASE_URL with password) | medium | mitigate | .env in .gitignore from Task 1; `git check-ignore .env` asserted in Task 2 acceptance |
| T-01-02 | Info Disclosure | docker-compose port mapping | low | mitigate | Bind `127.0.0.1:5432` only — DB unreachable from LAN |
| T-01-03 | Tampering | Unpinned transitive dependency drift | medium | mitigate | Exact pins in install commands (zod@4.4.3, prisma/@prisma/client@7.9.1, python-docx 1.2.0 / openpyxl 3.1.5 untouched) |
| T-01-SC | Tampering | npm/pip installs | high | mitigate | All packages pre-approved in 01-RESEARCH.md Package Legitimacy Audit (zero [ASSUMED]/[SUS]/[SLOP]); no blocking human checkpoint required |
</threat_model>

<verification>
- `pytest tests/ingestion -x -q` green
- `npx vitest run` green
- `docker exec fs-postgres psql -U postgres -d fs_assessment -tc "SELECT version();"` contains PostgreSQL 16
- `npx prisma --version` 7.9.x
</verification>

<success_criteria>
Toolchain pinned, Postgres 16 reachable via DATABASE_URL, both test runners green on smoke tests, 7 docx fixture builders available for later plans.
</success_criteria>

## Artifacts this phase produces (this plan)

- Files: `package.json`, `tsconfig.json`, `.gitignore`, `docker-compose.yml`, `.env`, `pytest.ini`, `vitest.config.ts`, `tests/__init__.py`, `tests/conftest.py`, `tests/ingestion/__init__.py`, `tests/ingestion/test_smoke.py`, `tests/node/smoke.test.ts`
- pytest fixtures (tests/conftest.py): `docx_trailing_check`, `docx_leading_check`, `docx_checkbox_pair`, `docx_bold_run`, `malformed_missing_option`, `malformed_no_marker`, `malformed_two_bold`
- Env var: `DATABASE_URL`
- Docker container: `fs-postgres` (postgres:16), database `fs_assessment`

<output>
Create `.planning/phases/01-content-ingestion-question-bank/01-01-SUMMARY.md` when done
</output>
