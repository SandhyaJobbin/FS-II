---
phase: 11-recruiter-analytics-dashboard
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/analytics/analytics-reducers.ts
  - tests/analytics/test_analytics.ts
  - tests/analytics/fixtures/attempts_20.json
  - vitest.config.ts
  - scripts/sync-check.ts
autonomous: true
requirements:
  - ADMIN-06
  - ADMIN-07
  - ADMIN-08
  - ADMIN-09
  - ADMIN-11
must_haves:
  truths:
    - Pure-JS analytics reducer mirror exists so backend logic can be unit-tested off the GAS runtime.
    - Parity test proves `buildQuestionStats` (analytics reducer) produces the same per-question pass-rate on a fixture attempt as `computeAggregatesForAttempt` (per-attempt reducer) — Pitfall 2 gate.
    - Analytics mirror re-exports `effectiveVerdict` (never re-implements it) so drift is structurally impossible.
    - sync-check drift assertions block PRs that let the analytics backend diverge from the mirror or drop the auth/instrumentation gates.
  artifacts:
    - tests/analytics/analytics-reducers.ts (pure mirror, no vitest imports)
    - tests/analytics/test_analytics.ts (score-trend + question-stats parity + correlation + discrimination + ungraded-caveat + auth)
    - tests/analytics/fixtures/attempts_20.json (mixed statuses, ungraded, overrides, low-N and healthy-N slices)
    - vitest.config.ts (excludes analytics-reducers.ts from spec runs)
    - scripts/sync-check.ts (sections 16-19 for the analytics endpoint drift gates)
  key_links:
    - tests/analytics/analytics-reducers.ts re-exports `effectiveVerdict` from tests/grading/rubric-grader.ts (single-source resolver).
    - scripts/sync-check.ts sections 16-19 read backend/Code.gs + backend/Analytics.gs to enforce endpoint action name, auth gate, effectiveVerdict reuse, meta.aggregationMs instrumentation.
---

<objective>
Wave 0 test-infra + parity guard. Ship the analytics reducer mirror, its vitest spec, the shared fixture, the vitest exclude update, and the sync-check drift assertions BEFORE any backend or frontend code exists. This protects Phase 10's A2 denominator policy (per ADMIN-06/07/08/09/11) from being silently violated by new analytics code in Waves 1-3.

Purpose: Every downstream reducer is testable on the Node runtime, and CI blocks merges where analytics drifts from the per-attempt grading resolver. Prevents Pitfall 2 (report says 85%, dashboard says 71% on the same candidate) by construction, not by convention.
Output: 5 files (2 new tests + 1 new fixture + 2 modified configs). Zero backend files touched. Zero frontend files touched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md
@.planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md
@tests/grading/rubric-grader.ts
@tests/grading/test_rubric_grader.ts
@vitest.config.ts
@scripts/sync-check.ts
@backend/AsyncGrading.gs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create analytics reducer mirror + shared fixture</name>
  <files>tests/analytics/analytics-reducers.ts, tests/analytics/fixtures/attempts_20.json</files>
  <read_first>
    - tests/grading/rubric-grader.ts (mirror shape to copy — 43 lines, pure functions, re-exports effectiveVerdict, no vitest imports)
    - backend/AsyncGrading.gs lines 38-45 (effectiveVerdict signature) and 92-174 (computeAggregatesForAttempt — the per-attempt reducer this parity-tests against)
    - .planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md § Aggregation Recipes (score trend, question stats, violation correlation, discrimination index, bias signals) and § Data-Shape Reference (Attempts 15 cols, Responses 5 cols, GradingTranscripts 9 cols)
    - .planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md § 9 (test mirror pattern) and § 5 (A2 denominator constraint block)
  </read_first>
  <behavior>
    - Mirror exports named pure functions: `buildScoreTrend(attemptsByStatus)`, `buildQuestionStats(responses, transcripts)`, `buildViolationCorrelation(attemptsByStatus)`, `buildDiscriminationIndex(attemptsByStatus, responses, transcripts, MIN)`, `buildBiasSignals(attemptsByStatus)`.
    - Mirror re-exports `effectiveVerdict` from tests/grading/rubric-grader.ts (import + re-export) — MUST NOT re-implement the resolver.
    - Mirror inputs are ROW ARRAYS matching sheet layout (Attempts idx 0..14, Responses idx 0..4, GradingTranscripts idx 0..8) so parity with GAS reducers is trivial.
    - Fixture attempts_20.json contains: 20 attempts across ≥4 calendar days, mix of statuses (submitted/graded/emailed/pending_grading/grading_failed), 5 attempts with UngradedCount > 0, 3 attempts with OverrideVerdict on at least one transcript row, at least one questionId with <5 samples (below MIN low-N threshold) and one with ≥5 samples, at least one attempt with ViolationCount > 0 and one with ViolationCount = 0.
    - Fixture must include one attempt where `computeAggregatesForAttempt` (referenced via tests/grading/grading-engine.ts existing mirror) and `buildQuestionStats` should produce identical per-question pass-rates — this is the Pitfall 2 parity gate.
  </behavior>
  <action>
    Create tests/analytics/analytics-reducers.ts as a pure TypeScript mirror modeled on tests/grading/rubric-grader.ts (per D-implicit / research Pattern 4). Structure:
    (a) Type declarations: `AttemptRow = [string, string, string, string, string, string, string, number|string, number, number, number, number, string, string, number|string]` (15 cols), `ResponseRow = [string, string, string, boolean|string, string]` (5 cols), `TranscriptRow = [string, string, string, string, string, string, string, string, string]` (9 cols), and a `Verdict = 'correct' | 'incorrect' | 'ungraded'` union.
    (b) Constants: `READY_STATUSES = ['submitted', 'graded', 'emailed'] as const` (mirror Code.gs:55), `MIN_QUESTION_SAMPLES = 5`, `MIN_TREND_ATTEMPTS = 5`, `MIN_TREND_DAYS = 3`, `MIN_CORR_SAMPLES = 10`, `MIN_BIAS_SAMPLES = 15`, `MIN_DISCRIM_QUARTILE_N = 3`.
    (c) `import { effectiveVerdict } from '../grading/rubric-grader';` then `export { effectiveVerdict };` — single-source resolver (per hard constraint in 11-PATTERNS.md § 4).
    (d) Five pure reducers matching the shapes in 11-RESEARCH.md § Aggregation Recipes. Each reducer returns `{ enough_data: boolean, threshold: number, samples: number, ...payload }`. Every rate is emitted alongside `sampleSize` (never a naked percentage — 11-PATTERNS.md § 3). Denominator for pass-rate uses `graded = correct + incorrect` (excludes ungraded — Pitfall 2 / A2 policy).
    (e) Reducers must be O(N): build hash maps keyed by attemptId / questionId in ONE pass, then join by key. NO nested loops over two sheets (Pitfall 1 / 6-minute cap invariant).
    (f) Create tests/analytics/fixtures/attempts_20.json with three top-level keys `attempts`, `responses`, `transcripts` — each an array of row-arrays matching the sheet indexing above. Include the mix described in the behavior block. Header rows are NOT included in the fixture (already stripped, matching what handleAdminAnalytics will pass after `.slice(1)`).
    Keep the mirror file under 250 lines total. Do NOT add any dependency on vitest, no `import { describe, it, expect }` — this file must be safely importable from anywhere.
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment" && node -e "const m = require('./tests/analytics/analytics-reducers.ts'.replace('.ts','')); console.log(typeof m)" || npx tsc --noEmit tests/analytics/analytics-reducers.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/analytics/analytics-reducers.ts compiles under `npx tsc --noEmit tests/analytics/analytics-reducers.ts` with zero errors.
    - The file contains exactly one `import { effectiveVerdict } from '../grading/rubric-grader'` and re-exports it.
    - The file does NOT contain a local `function effectiveVerdict` definition (grep must return 0 matches for `^function effectiveVerdict|^export function effectiveVerdict`).
    - tests/analytics/fixtures/attempts_20.json parses as valid JSON with `attempts.length === 20`, `responses.length >= 100`, `transcripts.length >= 20`.
    - File is under 250 lines (project rule — keep under 500).
  </acceptance_criteria>
  <done>Mirror + fixture exist, mirror re-exports effectiveVerdict verbatim, fixture spans the low-N and healthy-N cases and the ungraded/override mix, and the mirror is importable without vitest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create vitest spec exercising all five reducers + Pitfall-2 parity assertion</name>
  <files>tests/analytics/test_analytics.ts, vitest.config.ts</files>
  <read_first>
    - tests/analytics/analytics-reducers.ts (created in Task 1 — the mirror under test)
    - tests/analytics/fixtures/attempts_20.json (fixture from Task 1)
    - tests/grading/test_rubric_grader.ts (describe/it structure to copy)
    - tests/grading/grading-engine.ts (existing per-attempt mirror — parity test imports `gradeAttempt` or equivalent from here)
    - vitest.config.ts (current exclude list — Task must add `'tests/**/analytics-reducers.ts'`)
    - .planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md § Phase Requirements → Test Map (7 test rows) and § Wave 0 Gaps
  </read_first>
  <behavior>
    - `buildScoreTrend` bucket + threshold cases (ADMIN-06):
      - Returns day-bucketed points sorted by date, avgScore rounded, per-day n, when fixture has ≥5 attempts across ≥3 days.
      - Returns `{ enough_data: false }` when given only 4 attempts across 2 days.
      - Ignores attempts with empty EndTime (active / pending_grading).
      - Coerces non-finite OverallScore to skipped (belt-and-braces per Pitfall 7).
    - `buildQuestionStats` A2 parity case (ADMIN-07, Pitfall 2):
      - For a chosen attempt in the fixture, running `buildQuestionStats(responses.filter(attemptId==X), transcripts.filter(attemptId==X))` produces per-question `passRate` values whose weighted average equals the `overallPercentage` produced by the existing per-attempt reducer on the same attempt (within ±1 for rounding).
      - `passRate === null` and `enough_data === false` for a questionId with < MIN_QUESTION_SAMPLES (5).
      - Ungraded answers are counted in `.ungraded` but excluded from `sampleSize` denominator.
    - `buildViolationCorrelation` (ADMIN-08):
      - Pearson r on a hand-computed 12-point subset of the fixture is within 0.001 of a reference value provided inline in the test (do NOT install simple-statistics).
      - Returns `{ enough_data: false, r: null }` when samples < 10.
      - Interpretation label matches thresholds `weak < 0.2 ≤ moderate < 0.5 ≤ strong`.
    - `buildDiscriminationIndex` (ADMIN-09):
      - Fixture includes a curated question where top-quartile scorers passed 20% and bottom-quartile scorers passed 80% (anti-discriminating); assertion flags that qId in the bottom-N list.
      - Skips questions with < MIN_QUESTION_SAMPLES.
    - Ungraded caveat surfacing:
      - `buildQuestionStats(...).rows.some(r => r.ungraded > 0) === true` for the fixture (data-quality invariant — ungraded count MUST NOT be zero-defaulted).
    - Auth invariant (endpoint-level — placeholder here, real check lives in Wave 1):
      - This spec DOES include a stub `test.todo("handleAdminAnalytics rejects missing/wrong token — verified in Wave 1 test_analytics_auth.ts")` so the test map is discoverable.
  </behavior>
  <action>
    Create tests/analytics/test_analytics.ts modeled on tests/grading/test_rubric_grader.ts. Structure:
    (a) `import { describe, it, expect } from 'vitest';`
    (b) `import { buildScoreTrend, buildQuestionStats, buildViolationCorrelation, buildDiscriminationIndex, buildBiasSignals, effectiveVerdict } from './analytics-reducers';`
    (c) `import fixture from './fixtures/attempts_20.json' assert { type: 'json' };` (or the equivalent Node ESM idiom used by the existing rubric-grader spec — check that file first).
    (d) `import { gradeAttempt /* or existing helper name */ } from '../grading/grading-engine';` for the Pitfall-2 parity assertion.
    (e) Five `describe` blocks — one per reducer — covering the behavior cases above. Use `describe.each` where a table of thresholds cleans up assertions.
    (f) One dedicated `describe('Pitfall 2: analytics denominator parity with per-attempt reducer', ...)` block with the weighted-average assertion.
    Update vitest.config.ts: add `'tests/**/analytics-reducers.ts'` to the `exclude` array (position: alphabetically after `'tests/**/admin-auth.ts'` or at end of list — match existing formatting). Do NOT modify any other config field.
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment" && npx vitest run tests/analytics/test_analytics.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/analytics/test_analytics.ts` exits 0 with all spec blocks passing.
    - `npx vitest run` (full suite) does NOT attempt to run analytics-reducers.ts as a spec (proves the exclude landed).
    - The Pitfall-2 parity assertion is present (grep `Pitfall 2` returns 1+ match in test_analytics.ts).
    - The correlation test uses inline reference numbers, NOT an import of `simple-statistics` (grep `simple-statistics` in tests/analytics/ returns 0 matches).
    - Test file is under 400 lines.
  </acceptance_criteria>
  <done>Full analytics spec passes, parity gate is in place, and vitest correctly excludes the mirror from spec collection.</done>
</task>

<task type="auto">
  <name>Task 3: Extend scripts/sync-check.ts with sections 16-19 (analytics drift gates)</name>
  <files>scripts/sync-check.ts</files>
  <read_first>
    - scripts/sync-check.ts (existing sections 1-15 — copy the numbered-section convention verbatim; note section 15 is the current tail before the Summary block)
    - .planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md § 8 (sync-check pattern) — includes the exact regex sketches for sections 16-19
    - .planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md § Common Pitfalls (Pitfall 1 → meta.aggregationMs instrumentation; Pitfall 2 → effectiveVerdict reuse)
    - backend/AsyncGrading.gs (top of file — confirm no `handleAdminAnalytics` yet; assertions must be future-looking against backend/Analytics.gs which Wave 1 will create)
  </read_first>
  <action>
    Add four new sections to scripts/sync-check.ts, immediately before the `─── Summary ───` block. Match the exact `// ─── N. label ───...` comment convention (see sections 1-15).
    (a) Add a new file load near line 21: `const ANALYTICS_GS_PATH = resolve(ROOT, "backend/Analytics.gs");` and load defensively: `let ANALYTICS_GS = ""; try { ANALYTICS_GS = readFileSync(ANALYTICS_GS_PATH, "utf-8"); } catch { /* Wave 1 has not created this file yet — sections 18-19 will FAIL loudly until then, which is intended */ }`. Do NOT crash sync-check if the file is absent — a graceful FAIL is the signal Wave 1 must land.
    (b) Section 16: `has("adminAnalytics action registered in doGet", CODE_GS, /action === "adminAnalytics"/);`
    (c) Section 17: `has("handleAdminAnalytics first-lines checkAdminAuth", CODE_GS, /function handleAdminAnalytics[\s\S]{0,300}checkAdminAuth\(token\)/);` — the `{0,300}` window forces the auth call to be within the first ~10 lines of the handler body (matches all other admin handlers).
    (d) Section 18: `has("Analytics reducers reuse effectiveVerdict (not re-implemented)", ANALYTICS_GS, /buildQuestionStats[\s\S]{0,2000}effectiveVerdict\(/);`
    (e) Section 19: `has("handleAdminAnalytics instruments meta.aggregationMs (Pitfall 1)", CODE_GS, /meta[\s\S]{0,100}aggregationMs/);`
    Preserve the failing-on-first-error semantics (increment `failures`, do NOT throw). File must stay well under 500 lines.
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment" && npx tsx scripts/sync-check.ts ; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/sync-check.ts` runs to completion without throwing (exit code may be 1 — sections 16-19 are EXPECTED to FAIL until Wave 1 lands; that failure is the enforcement signal, not a bug).
    - Sections 1-15 output remains unchanged (all still PASS).
    - Section 16-19 output shows exactly 4 new labeled lines (either PASS or FAIL — never absent/silent).
    - File length remains under 300 lines.
  </acceptance_criteria>
  <done>Sync-check has 4 new sections wired to backend/Code.gs and backend/Analytics.gs; the "Analytics.gs not yet created" case degrades gracefully into a clear FAIL, and no existing section regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline → repo | sync-check.ts is the drift gate; if it silently passes on missing files, drift lands undetected |
| test runtime → mirror module | Vitest imports the mirror; if the mirror ever imports vitest, spec-loading side effects contaminate other tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | Analytics reducer resolver | high | mitigate | Mirror re-exports `effectiveVerdict` from tests/grading/rubric-grader.ts; sync-check § 18 blocks any re-implementation (per D-implicit hard constraint from 11-PATTERNS § 4). |
| T-11-02 | Information Disclosure | Fixture data | low | accept | Fixture uses synthetic names/emails only — do not copy real Attempts rows into fixtures. |
| T-11-03 | Denial of Service | Slow reducer under large N | medium | mitigate | Reducers enforce O(N) hash-map joins (per D-implicit / Pitfall 1). Wave 3 verify measures live `meta.aggregationMs`. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies introduced in Wave 0. Fixture is pure JSON; parity uses hand-computed reference numbers instead of installing simple-statistics. |
</threat_model>

<verification>
- `npx vitest run tests/analytics/test_analytics.ts` — all reducer + parity tests pass.
- `npx vitest run` — full suite green; analytics-reducers.ts NOT collected as spec.
- `npx tsx scripts/sync-check.ts` — runs cleanly; sections 16-19 emit labeled FAIL lines pointing at the missing backend/Analytics.gs and missing handleAdminAnalytics (this is the enforcement signal for Wave 1, not a Wave 0 defect).
- `wc -l tests/analytics/*.ts scripts/sync-check.ts` — every file under 500.
</verification>

<success_criteria>
- Analytics mirror is testable on Node runtime with zero GAS dependencies.
- The Pitfall-2 A2 denominator policy is provable — any drift between per-attempt and per-question reducers fails a test.
- sync-check has 4 new drift gates ready to enforce Wave 1's contract.
- Zero backend or frontend code exists yet — Wave 1 starts with tests in place.
</success_criteria>

<output>
Create `.planning/phases/11-recruiter-analytics-dashboard/11-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **New file:** `tests/analytics/analytics-reducers.ts` — pure JS mirror of the analytics reducers landing in Wave 1; re-exports `effectiveVerdict` from `tests/grading/rubric-grader.ts`.
- **New file:** `tests/analytics/test_analytics.ts` — vitest spec covering all 5 reducers plus the Pitfall-2 A2 parity gate against the per-attempt reducer.
- **New file:** `tests/analytics/fixtures/attempts_20.json` — shared fixture (20 attempts × ~50 responses each, mixed statuses, ungraded/override mix, low-N & healthy-N slices).
- **Modified:** `vitest.config.ts` — one line added to `exclude` array.
- **Modified:** `scripts/sync-check.ts` — 4 new numbered sections (16-19) enforcing endpoint action registration, auth gate, effectiveVerdict reuse, and `meta.aggregationMs` instrumentation.
