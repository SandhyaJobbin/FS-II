---
phase: 10
plan: 07
type: execute
wave: 5
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06"]
files_modified: []
autonomous: false
requirements: [GRADE-06, GRADE-07, GRADE-08, GRADE-09, GRADE-10]
must_haves:
  truths:
    - "A live candidate submission with at least one open_text answer produces a GradingTranscripts row with a real Gemini rationale and a schema-conformant verdict — confirms GRADE-06 in production, not just unit-tested"
    - "A deliberately triggered Gemini failure (invalid key or network cut) produces verdict='ungraded' in GradingTranscripts and increments ungradedCount on the Attempts row — confirms GRADE-07 live"
    - "Recruiter opens the admin modal for that attempt and sees the full transcript panel (rationale + criteriaMet) and a working Override button — confirms GRADE-08 live"
    - "Recruiter flips a verdict via Override, the report score recomputes visibly within seconds, and OverrideVerdict/OverrideAt/OverrideTokenHash are populated on the transcript row — confirms GRADE-09 live"
    - "Candidate's emailed report + ThankYouScreen copy reflect the new human-review disclosure and (when ungradedCount > 0) the pending-review notice — confirms GRADE-10 live"
  artifacts:
    - "Live Apps Script deployment with all Phase 10 backend changes (rubric, evaluateWithRubric, GradingTranscripts sheet, override endpoints) pushed and serving"
    - "Redeployed Next.js frontend with the transcript panel + ungraded notice + updated ThankYouScreen copy"
    - "A single test candidate attempt walked through end-to-end with the recorded observations logged in the SUMMARY"
  key_links:
    - "This plan is the ONLY place all six prior plans are exercised together against real Gemini API + real Google Sheets + real MailApp — none of that can be run locally. It is the final gate before Phase 10 closes."
    - "Depends on plan 10-06's human-approved rubrics — shipping this plan against plan 10-01's generic skeletons would give live grading results that are technically correct but strategically weak."
---

<objective>
Deploy the code produced by plans 10-01 through 10-05 (and the content from 10-06) to the live Apps Script project + live Next.js deployment, then walk a full recruiter workflow end-to-end against the real backends to confirm every one of Phase 10's five Success Criteria (GRADE-06 through GRADE-10) holds in production, not just in unit tests.

Purpose: Apps Script has no local execution path (Phase 9's 09-06 established this: no `.clasp.json`, no clasp devDependency, no CLI-based deploy). Every backend change from plans 10-01/02/03 must be copy-pasted into the web editor, and every UI change from 10-05 must be redeployed to the Next.js host. This plan is the only place the full rubric→transcript→override→re-aggregation loop actually runs. It also gathers the wall-clock measurement RESEARCH.md Pitfall 3 called out as a Wave 3 deferred: how long does a rubric-graded batch actually take against Gemini in practice?

Output: A live, working rubric-graded Phase 10 pipeline. Phase 10 closed out.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rubric-based-llm-grading-recruiter-transcript-override/10-RESEARCH.md
@.planning/phases/09-async-grading-report-delivery-pipeline/09-06-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy backend + frontend and stage a test-cleanup snapshot</name>
  <context>
    Same no-CLI/no-API-deploy gap that Phase 9's 09-06 accepted: no `.clasp.json` exists, no `clasp` script in `package.json`. Pushing Code.gs + AsyncGrading.gs changes, forcing OAuth re-consent for any new scopes (none new in Phase 10 — same MailApp/UrlFetchApp/SpreadsheetApp scopes as Phase 9), and creating a new web app deployment version can only be done through the Apps Script web editor. Next.js frontend deploy path is whatever the project already uses (verify via `cd assessment-app && git log --oneline -5 -- vercel.json 2>/dev/null` or ask the operator).
  </context>
  <action>
    Human operator deploys the full Phase 10 code — Code.gs updates, AsyncGrading.gs updates, and the Next.js frontend rebuild — to the live environments, and stages a rollback plan.
  </action>
  <manual-steps>
    1. Open the Apps Script web editor for this deployment. Copy the CURRENT (post-plan-10-06) `backend/Code.gs` content over the existing Code.gs file in the editor. Copy the CURRENT `backend/AsyncGrading.gs` content over the existing AsyncGrading.gs file. Save both. Take a screenshot of the top of each file showing the file name and the first 30 lines — this is your rollback snapshot in case a live issue forces a revert.
    2. In the editor, run the built-in `initSheets()` function once from the function dropdown. This creates the new `GradingTranscripts` sheet lazily (plan 10-02 Task 1) and extends the `Attempts` header with the `UngradedCount` column. Verify in Google Sheets: open the spreadsheet, confirm a new `GradingTranscripts` tab exists with the 9-column header, and confirm the `Attempts` sheet's row 1 now has 15 columns ending in `UngradedCount`. Legacy Attempts rows will show blank in column O — this is expected (RESEARCH.md Pitfall 5 no-backfill policy; the `Number(row[14]) || 0` coalesce in plan 10-02 Task 3 handles it at read time).
    3. From the function dropdown, run `evaluateWithRubric` once manually with a small test input — or, more practically, submit a real test attempt in Task 2 and confirm the first `processGradingQueue` trigger fire completes without throwing (any missing OAuth scope prompt would appear here; approve if it does).
    4. Create a new web app deployment version (Deploy → Manage deployments → Edit → New version) so `doPost`/`doGet` serve the updated routers for `overrideVerdict` and `getAttemptTranscript`.
    5. Deploy the Next.js frontend using whatever deploy pipeline the project already uses (if unclear, ask the operator; do not invent a new deploy path). Confirm the redeployed URL serves the updated `admin/page.tsx`, `ReportScreen.tsx`, and `ThankYouScreen.tsx` — a quick manual test is to open the admin page and confirm the TranscriptPanel scaffolding appears in DevTools even before any transcript loads (search DOM for the string "Grading Transcript (recruiter-only)").
    6. Stage the rollback plan: if any step in Task 2 catches a live issue, revert by (a) pasting the pre-Phase-10 Code.gs and AsyncGrading.gs back into the editor from the screenshots above, (b) redeploying the previous Next.js version, (c) leaving the `GradingTranscripts` sheet in place (harmless if unused) and leaving the `UngradedCount` column in Attempts (harmless — reads default to 0).
  </manual-steps>
  <resume-signal>Type "deployed" once backend + frontend are live and initSheets has been run — or describe the failure (e.g. "OAuth scope prompt for a scope I didn't recognize" or "Next.js build failed")</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live end-to-end verification of the rubric-graded pipeline + override loop</name>
  <action>
    Human operator submits a real candidate attempt against the freshly deployed live environment and walks the 11-step checklist in `<how-to-verify>` below, confirming rubric grading, transcript persistence, ungraded fallback (deliberate GEMINI_API_KEY corruption test), recruiter transcript panel, verdict override with SHA-256 audit, override reversibility, and updated candidate copy — all before Phase 10 is considered done.
  </action>
  <what-built>
    The full Phase 10 pipeline: rubric-based Gemini grading with structured responseSchema output, per-answer transcript persistence in `GradingTranscripts`, an ungraded-verdict path that never silently rolls up as correct, a recruiter transcript panel + override control inside the admin modal with SHA-256-audited timestamps, and updated candidate copy on ThankYouScreen + a conditional amber notice on ReportScreen.
  </what-built>
  <how-to-verify>
    1. Open the live candidate URL and start a test attempt using a throwaway email. Answer all questions, deliberately writing THOUGHTFUL open-text answers for at least the first 3 open_text questions you encounter (so plan 10-06's rubrics have real content to grade). Submit.
    2. Confirm `ThankYouScreen` renders immediately AND now contains the updated copy: "Automated scoring runs first; open-text answers may be reviewed and adjusted by a recruiter" (plan 10-05 Task 4, GRADE-10). Copy is present — no polling, no score visible.
    3. Wait up to 5 minutes for `processGradingQueue` to fire (Phase 9's cadence). Open the Google Sheets `GradingTranscripts` tab and find the row(s) for your test attemptId. Confirm each rubric-graded question has: (a) `Verdict` = correct or incorrect (NOT ungraded — Gemini should have worked), (b) `CriteriaMetJSON` is a JSON array of `{criterionName, met, score}` objects, (c) `Rationale` is a non-empty English sentence explaining the verdict. This confirms GRADE-06 live (schema-conformant Gemini output + persisted rationale).
    4. Open the `Attempts` sheet, find your test row, confirm `UngradedCount` column O = 0 for this successful run. Confirm columns H:K (score percentages), M (tier), N (narrative) all populated as normal — Phase 9's flow is unbroken.
    5. Open the admin panel URL, log in with the admin token, find your test attempt in the list, click to open the modal. Confirm: (a) `<ReportScreen>` renders as before with score+tier+narrative, (b) BELOW ReportScreen (still inside the modal), a "Grading Transcript (recruiter-only)" heading appears, (c) each rubric-graded question renders with verdict badge + criteria list + rationale text + Mark correct / Mark incorrect override buttons — GRADE-08 live.
    6. Click "Mark incorrect" on a question that currently shows verdict="correct" (or vice versa). Within a few seconds, confirm: (a) the report score at the top of the modal recomputes visibly (Overall score drops or rises by ~1/N where N is the graded-question count), (b) the transcript row's verdict badge updates to show the overridden state (e.g. a purple "Overridden: incorrect" badge), (c) the Google Sheets `GradingTranscripts` row now has `OverrideVerdict = "incorrect"`, a real ISO 8601 timestamp in `OverrideAt`, and a 64-char lowercase-hex string in `OverrideTokenHash` (SHA-256). Confirms GRADE-09 live including audit trail.
    7. Reversibility check: click "Revert to LLM verdict" on the just-overridden question. Confirm the report score recomputes back to its original value, and the `GradingTranscripts` row now shows `OverrideVerdict` as empty/blank (but `OverrideAt`/`OverrideTokenHash` retain the most-recent reversion's audit data — RESEARCH.md A4).
    8. Deliberate ungraded test: this is optional but STRONGLY recommended for GRADE-07 confidence. Temporarily corrupt the `GEMINI_API_KEY` Script Property (add "-broken" to the end), submit ANOTHER test attempt with open_text answers, wait for the trigger, and confirm: (a) the `GradingTranscripts` rows for this new attempt all have `Verdict = "ungraded"`, (b) the `Attempts.UngradedCount` for this attempt equals the number of rubric-graded questions, (c) the emailed report (if it lands) does NOT lie about scores — the ungraded questions are excluded from the denominator per plan 10-02's A2 policy. RESTORE the correct GEMINI_API_KEY immediately after this step. Confirms GRADE-07 live.
    9. Candidate-copy notice check: view the same test attempt's report — either through the admin modal or by fetching the candidate email — and confirm that when `ungradedCount > 0` (from step 8 above), the amber "Pending Review" notice renders on the candidate-facing ReportScreen with the copy "Note: N open-text answer(s) are pending review. Your score may be updated once a recruiter completes review." (plan 10-05 Task 3, GRADE-10). Rationale text MUST NOT appear on the candidate ReportScreen even now — recruiter-only per A6.
    10. Sync-check gate: run `npx tsx scripts/sync-check.ts` locally against the committed backend files — exits 0 with all Phase 10 drift checks passing. Run `npm test` locally — exits 0 with all Phase 9 + Phase 10 suites green.
    11. Optional wall-clock instrumentation for RESEARCH.md Pitfall 3: check the `PendingGrading.LastError` column for your test attempts — if empty, batch grading fit within the 6-minute cap. If a "Exceeded maximum execution time" error appears, plan a follow-up to lower the batch cap from 5 to 3 in AsyncGrading.gs (defer to backlog).
  </how-to-verify>
  <resume-signal>Type "approved" once all checks pass, or describe exactly which step failed and what was observed (e.g. "step 6: report score did not update after override — Overall stayed at 68% for 60+ seconds" or "step 8: ungraded rows had Verdict='' rather than 'ungraded'")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator -> Apps Script web editor (Code.gs, AsyncGrading.gs paste) | Same manual-paste boundary Phase 9 accepted; screenshots are the rollback material |
| Human operator -> GEMINI_API_KEY temporary corruption (Task 2 step 8) | Deliberate GRADE-07 test; operator MUST restore the correct key immediately after — a forgotten step here silently breaks grading for all future real submissions |
| Live web app deployment -> real candidate/recruiter emails | Same MailApp trust boundary Phase 9 accepted; unchanged in Phase 10 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07a | Elevation of Privilege | GEMINI_API_KEY left corrupted after Task 2 step 8 | high | mitigate | Task 2 step 8's `<how-to-verify>` explicitly says "RESTORE the correct GEMINI_API_KEY immediately after this step" and step 10 confirms sync-check passes (which would incidentally not catch the key issue, but the operator's own verification loop across steps 3-9 would surface persistent ungraded rows). Consider setting a 15-minute reminder before starting step 8. |
| T-10-07b | Tampering | Frontend + backend deploy out of sync (backend has new endpoints, frontend still on old build, or vice versa) | high | mitigate | Task 1's manual steps deploy backend BEFORE frontend, and Task 2 step 5 explicitly checks the redeployed frontend serves the TranscriptPanel by searching DOM for the "Grading Transcript (recruiter-only)" string — divergence surfaces immediately, not after the first real recruiter tries to override. |
| T-10-07c | Repudiation | No paper trail of who deployed when | medium | accept | Same trust model Phase 9's 09-06 accepted — the operator signs off in the SUMMARY, and git commits provide code provenance. Per-deploy audit logging is a separate backlog item (999.6 if it arises). |
| T-10-07d | Denial of Service | New rubric prompts blow the 6-min Apps Script execution cap | medium | mitigate | Task 2 step 11 checks PendingGrading.LastError for the "Exceeded maximum execution time" signal. Batch cap stays at 5 attempts per drain (RESEARCH.md Pitfall 3, unchanged in Phase 10). If measured cap-exceedance appears, backlog-defer a batch cap reduction to 3. |
| T-10-07e | Information Disclosure | Rationale text visible to the wrong role | high | mitigate | Task 2 step 9 explicitly confirms the amber notice on the candidate ReportScreen contains NO rationale — only the count. Task 2 step 5 confirms rationale appears inside the token-gated admin modal (recruiter-only, A6). Divergence would surface immediately as visible text in a candidate email. |
| T-10-07-SC | Tampering | npm/pip/cargo installs | n/a | n/a | Plan installs zero packages — Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
This plan is entirely manual/checkpoint-driven — Apps Script has no local execution path. All prior plans' automated suites (`npm test`, `npx tsx scripts/sync-check.ts`) must already be green before reaching this plan (enforced by Wave 1/2 completion). Task 2 step 10 re-runs both suites as a final sanity check. The plan's own verification is the live 11-step click-through in Task 2.
</verification>

<success_criteria>
- Backend code deployed to Apps Script; frontend redeployed
- initSheets() has created GradingTranscripts and extended Attempts header
- A live candidate submission produces a GradingTranscripts row with real Gemini rationale and schema-conformant verdict (GRADE-06 live)
- A deliberately corrupted GEMINI_API_KEY produces verdict="ungraded" and correct UngradedCount (GRADE-07 live)
- Recruiter modal shows transcript panel with rationale + override controls (GRADE-08 live)
- Override flips verdict, recomputes score visibly, populates SHA-256 audit fields; revert restores (GRADE-09 live)
- ThankYouScreen + conditional ungraded notice serve updated copy (GRADE-10 live)
- npm test + sync-check both exit 0 after deployment
</success_criteria>

## Artifacts this phase produces

- Live Apps Script deployment (Code.gs + AsyncGrading.gs) with Phase 10 changes serving
- Live Next.js frontend with transcript panel + ungraded notice + updated copy serving
- One end-to-end test candidate attempt walked through with all 11 verification steps recorded in SUMMARY

<output>
Create `.planning/phases/10-rubric-based-llm-grading-recruiter-transcript-override/10-07-SUMMARY.md` when done
</output>
