---
phase: 08-cleanup-test-safety-net
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Code.gs
  - tests/admin/test_admin.ts
autonomous: true
requirements:
  - BUG-02

must_haves:
  truths:
    - "checkAdminAuth(token) exists as named function in Code.gs, replacing 3 inline checks"
    - "test_admin.ts imports and tests checkAdminAuth from a shared module, not a local reimplementation"
    - "Valid token returns truthy; invalid/empty/missing token returns falsy"
  artifacts:
    - backend/Code.gs
    - tests/admin/test_admin.ts
    - tests/admin/admin-auth.ts
  key_links:
    - "checkAdminAuth must reference ADMIN_TOKEN constant (line 48), not hardcode value"
    - "test_admin.ts must import from tests/admin/admin-auth.ts, not Code.gs directly (GAS incompatible with Node)"
---

<objective>
Extract checkAdminAuth(token) from 3 inline auth checks in Code.gs into a single named function. Create a Node-importable mirror in tests/admin/admin-auth.ts. Rewrite test_admin.ts to test the real extracted logic.

Purpose: test_admin.ts currently reimplements auth locally (line 14-16) — tests pass even if Code.gs auth logic changes. Extracting + mirroring makes tests catch real auth regressions.
Output: Extracted checkAdminAuth in Code.gs + mirror module + rewritten test suite.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cleanup-test-safety-net/CONTEXT.md
@backend/Code.gs
@tests/admin/test_admin.ts
</context>

<tasks>

<task type="tracer">
  <name>Extract checkAdminAuth in Code.gs + create testable mirror</name>
  <files>backend/Code.gs, tests/admin/admin-auth.ts</files>
  <action>
In Code.gs: Add `function checkAdminAuth(token) { return token === ADMIN_TOKEN; }` near line 50 (after ADMIN_TOKEN declaration at line 48). Replace 3 inline checks:
- Line 624: `if (token !== ADMIN_TOKEN)` → `if (!checkAdminAuth(token))`
- Line 660: `if (token !== ADMIN_TOKEN)` → `if (!checkAdminAuth(token))`
- Line 686: `if (token !== ADMIN_TOKEN)` → `if (!checkAdminAuth(token))`

Create `tests/admin/admin-auth.ts`: export `checkAdminAuth(token: string): boolean` mirroring Code.gs logic exactly (`return token === ADMIN_TOKEN`). Export `ADMIN_TOKEN` constant with same value `"FS_RECRUITER_SECRET_2026"`. This mirror pattern required because Code.gs is Google Apps Script (not Node-importable). Per D-02 (F-04 decision): extract function, replace 3 inline checks, make testable.
  </action>
  <verify>
    <automated>grep -c "checkAdminAuth" backend/Code.gs | grep -q "[4-9]" && grep -v '^#' backend/Code.gs | grep -c 'token !== ADMIN_TOKEN' | grep -q "^0$"</automated>
  </verify>
  <done>
    checkAdminAuth defined once in Code.gs, called at 3 sites. Zero remaining `token !== ADMIN_TOKEN` inline checks. tests/admin/admin-auth.ts exports mirror function + constant.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Rewrite test_admin.ts to test real checkAdminAuth mirror</name>
  <files>tests/admin/test_admin.ts</files>
  <behavior>
    - Test 1: checkAdminAuth("FS_RECRUITER_SECRET_2026") returns true
    - Test 2: checkAdminAuth("WRONG_SECRET") returns false
    - Test 3: checkAdminAuth("") returns false
    - Test 4: checkAdminAuth with undefined/null returns false
    - Test 5: hasViolationWarning(3) returns true, hasViolationWarning(2) returns false
    - Test 6: filterList matches by name and email, returns empty for no match
  </behavior>
  <action>
Delete local `validatePasscode` function (lines 14-16) and local `ADMIN_TOKEN` constant (line 12). Import `checkAdminAuth` and `ADMIN_TOKEN` from `./admin-auth`. Replace all `validatePasscode(...)` calls with `checkAdminAuth(...)`. Keep existing `hasViolationWarning` and `filterList` tests intact (they test real UI logic, not auth). Add null/undefined edge cases for checkAdminAuth. Per D-02: test real extracted function, not local reimplementation.
  </action>
  <verify>
    <automated>npx vitest run tests/admin/test_admin.ts</automated>
  </verify>
  <done>
    All tests pass. No local validatePasscode or ADMIN_TOKEN definition in test file. checkAdminAuth imported from admin-auth module.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API → auth check | Token comparison gates all 3 admin endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Spoofing | checkAdminAuth | medium | accept | Constant-comparison auth is existing design; token stored in GAS script (not env). Phase 8 scope is extraction, not auth redesign. Future phase should add timing-safe compare. |
| T-08-SC | Tampering | npm installs | high | mitigate | No new packages in this plan; vitest already in devDependencies. |
</threat_model>

<verification>
npx vitest run tests/admin/test_admin.ts
grep -c "checkAdminAuth" backend/Code.gs  # expect 4 (1 def + 3 calls)
</verification>

<success_criteria>
checkAdminAuth extracted in Code.gs with 3 call sites. test_admin.ts imports from admin-auth.ts mirror, all tests pass, no local auth reimplementation.
</success_criteria>

<output>
Create `.planning/phases/08-cleanup-test-safety-net/08-01-SUMMARY.md` when done
</output>
