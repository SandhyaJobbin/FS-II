---
phase: 01-content-ingestion-question-bank
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - ingestion/manifest.py
  - ingestion/parsers/__init__.py
  - ingestion/parsers/blocks.py
  - ingestion/parsers/markers.py
  - ingestion/parsers/english.py
  - ingestion/normalize.py
  - ingestion/validate.py
  - ingestion/run.py
  - prisma/schema.prisma
  - prisma/content-schema.ts
  - prisma/seed.ts
  - prisma.config.ts
  - content/questions.json
  - content/ingestion-report.json
  - tests/ingestion/test_manifest.py
  - tests/ingestion/test_extractors.py
  - tests/ingestion/test_validation_gate.py
  - tests/node/seed.test.ts
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03]

must_haves:
  truths:
    - "One full path works end-to-end: English Part 1 Grammar (30 bold-run MCQ) flows manifest → parse → normalize → validate → content/questions.json → zod re-check → single-transaction seed → 30 rows queryable in Postgres"
    - "Question slug IDs are unique across all parsed items; any collision fails the pipeline loudly with both item identities (INGEST-01 adjacency edge)"
    - "A section parsing to zero items fails manifest reconciliation against expected counts (grammar 30) — empty output never imports (INGEST-01 empty edge)"
    - "questions.json items are emitted in deterministic source order (bank → section → question position); identical reruns produce byte-identical questions.json (INGEST-01 ordering edge)"
    - "Pipeline interruption or validation failure leaves no partial artifacts — artifacts written via temp-file + atomic os.replace only on full pass (INGEST-01 concurrency edge)"
    - "Single-answer items with ≠1 marked option (0 or 2+) fail validation naming the item (INGEST-03 adjacency edge)"
    - "An MCQ item with zero parsed options fails loudly naming item identity: file, section, question number (INGEST-03 empty edge)"
    - "Validation errors are reported with item identity and the run exits non-zero — no warnings-only path for structural failures (INGEST-03 ordering edge)"
    - "English Part 1 Q1 parses with correct option = b (golden anchor verified in research)"
    - "Option.isCorrect lives only on Option rows — no denormalized key blob on Question (GRADE-05 boundary starts at schema)"
  artifacts:
    - ingestion/ pipeline (manifest, blocks, markers, english parser, normalize, validate, run)
    - prisma/schema.prisma with Bank/Section/Case/CaseTab/CaseTable/Question/Option/QuotaConfig/IngestionRun
    - content/questions.json + content/ingestion-report.json (grammar slice)
    - tests: test_manifest.py, test_extractors.py, test_validation_gate.py, tests/node/seed.test.ts
  key_links:
    - "run.py --banks english.grammar exits 0 and emits artifacts; npx prisma db seed loads them; psql counts 30 Questions"
    - "Marker extractors are selected per (doc, section) context — never a global checkmark hunt"
  prohibitions:
    - "Never use the stale 375 figure as expected total — reconcile against MANIFEST (105/160/120 ≈ 385)"
    - "Never parse *_dump.txt, file.xps, FS QB Pattern(1).xlsx, Sheet1, or the empty duplicate folder as sources — only the three V2 docx + FS QB Pattern.xlsx Sheet2"
    - "Never silently strip ✅/☑/☐ glyphs or mojibake unicode during parse or emit — stored display text preserves curly quotes and marker glyphs; normalization only for matching"
  assumptions:
    - "A-OQ4 (answer-key artifact custody): content/questions.json with embedded keys is committed to this private repo — same exposure class as the already-committed keyed docx (01-RESEARCH.md OQ-4 recommendation). FLAG for user confirmation at /gsd-verify-work."
---

<objective>
TRACER: prove the entire ingestion architecture on the thinnest real slice — English Part 1 Grammar (30 bold-run MCQ) — wired end-to-end from source docx through the Python pipeline, validated JSON artifacts, Prisma schema push, zod re-check, single-transaction seed, and a SQL query. Plus the pytest/vitest coverage that makes the slice's fail-loud behavior provable.

Purpose: every architectural risk of the phase (4 marker conventions, manifest reconciliation, all-or-nothing emit, answer-key-separated schema, atomic seed) is exercised by this one slice before any expansion. Written for keeps — expansion plans add parser modules, never rewrite this skeleton.
Output: working pipeline + seeded DB for the grammar slice; generic normalize/validate/schema/seed that expansion plans reuse unchanged.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md
@.planning/phases/01-content-ingestion-question-bank/01-PATTERNS.md
@.planning/phases/01-content-ingestion-question-bank/01-VALIDATION.md
@.planning/phases/01-content-ingestion-question-bank/01-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "ingest one section of one doc" — English Grammar through manifest → parse → validate → artifact → schema → push → seed → query</name>
  <files>ingestion/manifest.py, ingestion/parsers/__init__.py, ingestion/parsers/blocks.py, ingestion/parsers/markers.py, ingestion/parsers/english.py, ingestion/normalize.py, ingestion/validate.py, ingestion/run.py, prisma/schema.prisma, prisma/content-schema.ts, prisma/seed.ts, prisma.config.ts, content/questions.json, content/ingestion-report.json</files>
  <read_first>
    - .planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md (Source Document Anatomy — English Part 1; Code Examples: iter_block_items, bold_correct_index, parse_options; manifest values)
    - .planning/phases/01-content-ingestion-question-bank/01-PATTERNS.md (ALL reference implementations — copy as implementation seeds; Shared Patterns: fail-loud, UTF-8, answer-key custody, source whitelisting)
    - tests/conftest.py (fixture conventions from 01-01)
    - Fraud support/FS Question Bank_English Proficiency V2.docx (the source file itself — parser targets it)
  </read_first>
  <precondition>01-01 complete: Postgres reachable via DATABASE_URL in .env; prisma 7.9.1 + zod 4.4.3 + tsx installed.</precondition>
  <reversibility rating="reversible">Greenfield DB — `npx prisma db push` on an empty dev database creates tables only; droppable at zero cost. A-OQ4 artifact commit: keys already exist in repo as keyed docx, so JSON adds no new exposure class.</reversibility>
  <action>
    `ingestion/manifest.py`: MANIFEST dict with the exact research-verified values — english: file `FS Question Bank_English Proficiency V2.docx`, parts {grammar:30, sentence_correction:30, macro:10, reading_passages:5, reading_questions:25, closure:10}, total 105, tables 0; attention: file `FS Question Bank_Attention to Detail V2.docx`, levels {L1:20, L2:20}, cases_total 40, questions_total 160, tables 5, markers {✅:125, ☑:111, ☐:49}; critical: file `FS Question Bank_Critical Thinking V2.docx`, cases 30, questions_total 120, markers {✅:180}. Also SOURCE_DIR = `Fraud support` and ALLOWED_FILES whitelist (3 docx + `FS QB Pattern.xlsx`).
    `ingestion/parsers/blocks.py`: `iter_block_items(doc)` generator yielding Paragraph/Table in document order via `doc.element.body.iterchildren()` (research reference impl — `doc.paragraphs` alone misses the 5 dashboard tables).
    `ingestion/parsers/markers.py`: one extractor per convention, each returning `correct_option_indices: list[int]` and raising `MalformedItem(file, section, question_ref, reason)` on violation — `bold_correct_index(option_paragraphs)` (exactly 1 non-empty bold run else raise), `trailing_check(option_lines)` (exactly 1 line ending with ✅ else raise), `leading_check(option_lines)` (≥1 line starting with ✅), `checkbox_pair(option_lines)` (leading ☑ = correct, ☐ = incorrect, require ≥1 of each else raise). Plus `split_lines(paragraph_text)` splitting on `\n` before regex, and option regexes for lowercase `a) x` and uppercase `A. x` forms. Selection by (doc, section) context — never a global marker hunt.
    `ingestion/parsers/english.py`: exposes `BANK_ID = "english"` and `parse(source_dir)` — this task implements ONLY the Part 1 grammar walker: locate heading line matching `Part 1 – Grammar MCQs`, iterate `Qn.`-prefixed stems, collect `a)`–`d)` option paragraphs (split_lines first), apply `bold_correct_index`, emit raw item dicts with section `grammar`, position n, source {file, section, number}. Sub-topic header lines (Subject-Verb Agreement etc.) captured as optional `subcategory` string, never parsed as questions. Later plans extend this same file with Parts 2/3/6/7.
    `ingestion/normalize.py`: item model — Option(letter, text, is_correct), Item(id, bank, section, level, case_id, case_title, tabs, tables, response_type, stem, options, model_answer, position, source). response_type enum: `mcq_single | mcq_multi | open_text | hybrid` (grammar slice yields mcq_single only, but the model MUST support all four now — 50 open-ended items arrive in 01-03). Slug IDs: `{bank}-{section}-q{position:02d}` (e.g. `eng-grammar-q01`). Deterministic ordering: bank → section → position. Grammar items get level=None, case_id=None, section grouping `grammar`.
    `ingestion/validate.py`: pydantic models mirroring Item/Option + `validate_items(items, manifest, sections_run)`: (1) every mcq_single has exactly 1 correct option, mcq_multi ≥1; (2) slug uniqueness — collision raises with both identities; (3) per-section parsed counts reconcile against MANIFEST for the sections actually run (grammar must be 30); (4) zero-option MCQ raises naming file/section/number. All errors carry item identity; aggregated then raised — never warnings-only for structural failures.
    `ingestion/run.py`: CLI — auto-discovers parser modules in `ingestion/parsers/` exposing BANK_ID + parse() (so 01-03/04/05 add modules without editing run.py); `--banks` flag filters (e.g. `english.grammar`); orchestrates parse → normalize → validate → emit. Emit: `content/questions.json` + `content/ingestion-report.json` (counts per section, marker census, sha256 of each parsed source file, warnings, zero-error status) written via temp-file + `os.replace` ONLY on full validation pass; any failure → exit 1, nothing written. All I/O `encoding="utf-8"`.
    `prisma/schema.prisma`: datasource postgresql env("DATABASE_URL"); models Bank, Section, Case, CaseTab, CaseTable, Question, Option, QuotaConfig, IngestionRun. Question: id (slug PK), bankId FK, sectionId FK, caseId FK nullable, responseType enum (mcq_single/mcq_multi/open_text/hybrid), stem, modelAnswer String? (server-only), difficultyTier String? (populated in 01-06), subcategory String?, position Int, sourceRef String. Option: id, questionId FK, letter, text, isCorrect Boolean — is_correct physically separated on Option rows, never a key blob on Question. CaseTab: name verbatim + content text + position. CaseTable: caption/context + rows Json + position. QuotaConfig: section, category, volume, volumeUnit, quota Int, unit String. IngestionRun: id, ranAt, sourceHashes Json, counts Json, report Json.
    [BLOCKING — schema push gate] Run `npx prisma db push` against the Docker Postgres BEFORE the seed step; verify `npx prisma db execute`-style introspection or `\dt` shows all 9 tables. Any task verifying against live DB is blocked until this succeeds.
    `prisma/content-schema.ts`: zod schema mirroring the item model (second validation gate at the seed boundary).
    `prisma/seed.ts`: read `content/questions.json` (utf-8), zod-validate, then ONE `prisma.$transaction`: upsert Bank/Section rows, create Questions + Options (+ IngestionRun record from ingestion-report.json). Any error → rollback, exit non-zero. No hand-written SQL.
    `prisma.config.ts`: `migrations.seed` = `tsx prisma/seed.ts` (Prisma 7 config format).
    Execute the slice: `$env:PYTHONIOENCODING="utf-8"; python ingestion/run.py --banks english.grammar` then `npx prisma db seed`.
  </action>
  <verify>
    <automated>$env:PYTHONIOENCODING="utf-8"; python ingestion/run.py --banks english.grammar; npx prisma db push; npx prisma db seed; docker exec fs-postgres psql -U postgres -d fs_assessment -tc "SELECT count(*) FROM \"Question\";"</automated>
  </verify>
  <acceptance_criteria>
    - run.py exits 0; content/questions.json contains exactly 30 items, all `bank: "english"`, `section: "grammar"`, `response_type: "mcq_single"`
    - Item `eng-grammar-q01` has exactly one option with `is_correct: true` and that option's letter is `b`
    - `npx prisma db push` succeeds; `\dt` (or prisma introspect) lists Bank, Section, Case, CaseTab, CaseTable, Question, Option, QuotaConfig, IngestionRun
    - psql `SELECT count(*) FROM "Question"` returns 30; `SELECT count(*) FROM "Option" WHERE "isCorrect"` returns 30
    - ingestion-report.json contains sha256 source hash + grammar count 30 + zero errors
    - No question row contains any correct-answer field (keys only on Option.isCorrect)
  </acceptance_criteria>
  <done>The grammar slice is queryable in Postgres (30 questions, 30 correct options, Q1 key = b) after passing through every layer the phase touches; all layers are generic enough that expansion plans only add parser modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tracer-slice tests — manifest, extractors, validation gate, seed transaction</name>
  <files>tests/ingestion/test_manifest.py, tests/ingestion/test_extractors.py, tests/ingestion/test_validation_gate.py, tests/node/seed.test.ts</files>
  <read_first>
    - ingestion/manifest.py, ingestion/parsers/markers.py, ingestion/validate.py, ingestion/run.py, prisma/seed.ts (files under test)
    - tests/conftest.py (existing fixture builders)
    - .planning/phases/01-content-ingestion-question-bank/01-VALIDATION.md (Per-Task Verification Map: test_manifest, test_extractors, test_validation_gate)
  </read_first>
  <behavior>
    - test_manifest: MANIFEST english grammar == 30, totals 105/160/120; reconciliation of a parsed grammar slice (30) passes; reconciliation of 29 or 0 raises (empty/adjacency edges)
    - test_extractors: bold_correct_index returns the single bold index on the `docx_bold_run` fixture and raises on `malformed_two_bold` and `malformed_no_marker`; trailing_check / leading_check / checkbox_pair each extract correct indices from their conftest fixtures; split_lines turns a two-option-one-paragraph text into two option lines
    - test_extractors golden anchor: parsing real English Part 1 Q1–Q5 yields Q1 correct index [1] (option b)
    - test_validation_gate: `malformed_missing_option` fixture through validate → raise naming file/section/question; run.py on a malformed corpus exits 1 and writes NO artifacts (temp file absent, questions.json not created/modified)
    - test_validation_gate unicode round-trip: ✅/☑/☐ + curly quotes survive JSON emit and reload byte-identical (marker census unchanged)
    - seed.test.ts (vitest): seeding an artifact that violates the zod schema rejects AND the Question count in Postgres is unchanged afterwards (single-transaction rollback); seeding the valid grammar artifact is idempotent (second run leaves Question count at 30)
  </behavior>
  <action>
    Write the four test files against the Task 1 modules. Use conftest docx builders for unit-level extractor tests; use the REAL `Fraud support/FS Question Bank_English Proficiency V2.docx` for the golden anchor (path from manifest SOURCE_DIR). For run.py failure tests, invoke via `subprocess.run` with `--banks` pointing at a malformed-fixture bank directory and assert exit code 1 + no artifact writes. seed.test.ts assumes Docker Postgres is up (integration); point Prisma at DATABASE_URL from .env. All Python file I/O in tests uses encoding="utf-8".
  </action>
  <verify>
    <automated>pytest tests/ingestion -x -q; npx vitest run tests/node/seed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ingestion -x -q` exits 0 (smoke + new tests all green)
    - Golden anchor test asserts Q1 correct == option b and passes
    - Malformed-run test asserts exit code 1 AND absence of artifact writes
    - `npx vitest run tests/node/seed.test.ts` exits 0; rollback test proves Question count unchanged after invalid seed
  </acceptance_criteria>
  <done>Every fail-loud claim of the tracer slice has a green automated test: manifest counts, all four extractors, malformed naming + exit-1 + no-partial-write, unicode round-trip, seed transaction rollback + idempotency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docx source files → python parser | Untrusted office-format input (zip container) crossing into the pipeline |
| Python artifacts → Node seed | JSON crossing runtime boundary (zod re-check gate) |
| seed → Postgres | Write boundary; answer keys become sensitive-at-rest |
| pipeline → logs/CI | Key material must never appear in output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | DoS | Malformed/weaponized docx crashing or hanging the parser | medium | mitigate | Offline CLI only (never request path); python-docx is the standard audited parser; all-or-nothing failure; subprocess-invoked in tests with exit-code assertion |
| T-01-05 | Tampering | Silent bad-data import (missing/wrong keys, count drift) | high | mitigate | 5-layer validation: manifest reconciliation, item invariants, marker census, slug uniqueness, atomic emit + single-transaction seed; malformed-fixture tests prove exit-1 with item identity |
| T-01-06 | Info Disclosure | Answer keys leaking into logs, error output, or denormalized question rows | high | mitigate | isCorrect only on Option rows; errors name item identity (file/section/number) but never option contents or keys; no key material printed by run.py |
| T-01-07 | Tampering | Reading from a non-whitelisted source file (stale dump, superseded draft, empty husk) | high | mitigate | ALLOWED_FILES whitelist in manifest.py; parser rejects anything outside the three V2 docx names |
| T-01-SC | Tampering | npm/pip installs | high | mitigate | All packages pre-approved in 01-RESEARCH.md Package Legitimacy Audit (zero [ASSUMED]/[SUS]); installs happened in 01-01 under that audit |
</threat_model>

<verification>
- Tracer slice: 30 grammar questions + 30 correct options in Postgres; Q1 key = b
- `pytest tests/ingestion -x -q` green; `npx vitest run tests/node/seed.test.ts` green
- `npx prisma db push` completed (schema gate) before any DB-dependent verification
- content/ingestion-report.json: zero errors, grammar count 30, sha256 present
</verification>

<success_criteria>
End-to-end architecture proven on one real slice with fail-loud behavior under test; schema physically separates answer keys; seed is atomic and idempotent.
</success_criteria>

## Artifacts this phase produces (this plan)

- Python modules: `ingestion/manifest.py` (MANIFEST, SOURCE_DIR, ALLOWED_FILES), `ingestion/parsers/blocks.py` (`iter_block_items`), `ingestion/parsers/markers.py` (`MalformedItem`, `bold_correct_index`, `trailing_check`, `leading_check`, `checkbox_pair`, `split_lines`), `ingestion/parsers/english.py` (`BANK_ID`, `parse`), `ingestion/normalize.py` (`Option`, `Item`, response_type enum, slug-ID scheme), `ingestion/validate.py` (`validate_items`), `ingestion/run.py` (CLI with `--banks` filter, parser auto-discovery)
- Prisma: `prisma/schema.prisma` (Bank, Section, Case, CaseTab, CaseTable, Question, Option, QuotaConfig, IngestionRun models; responseType enum), `prisma/content-schema.ts` (zod), `prisma/seed.ts`, `prisma.config.ts` (`migrations.seed`)
- Artifacts: `content/questions.json`, `content/ingestion-report.json`
- Tests: `tests/ingestion/test_manifest.py`, `tests/ingestion/test_extractors.py`, `tests/ingestion/test_validation_gate.py`, `tests/node/seed.test.ts`
- DB state: 9 tables pushed; 30 Question + 120 Option rows (grammar slice)

<output>
Create `.planning/phases/01-content-ingestion-question-bank/01-02-SUMMARY.md` when done
</output>
