---
phase: 02-candidate-entry-test-assembly
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/Code.gs
  - assessment-app/src/types/index.ts
  - assessment-app/src/components/WelcomeScreen.tsx
  - assessment-app/.env.example
autonomous: true
requirements: [ENTRY-01, ENTRY-02, ENTRY-03, ASSM-01, ASSM-02, ASSM-03]

must_haves:
  truths:
    - "assembleQuestionSet() guards loudly if QUESTIONS.length === 0 instead of silently producing an empty attempt"
    - "Client payload from handleStartAttempt includes difficulty_tier on each question object (not answer-key material; safe and needed by Phase 4)"
    - "Reading quota comment clearly documents that count=1 passages expands to 5 questions in the assembled set"
    - "WelcomeScreen.tsx reads NEXT_PUBLIC_GAS_URL from env at startup and pre-populates the GAS URL field (falls back to empty if not set)"
    - "TypeScript Question type in types/index.ts includes difficulty_tier field"
    - ".env.example documents NEXT_PUBLIC_GAS_URL"
  prohibitions:
    - "Do NOT change quota numbers — FS QB Pattern.xlsx Sheet2 values are authoritative"
    - "Do NOT change the screen state machine in page.tsx"
    - "Do NOT add difficulty-tier-based stratified sampling — that is a v2 enhancement"
    - "Do NOT break the existing normalizeEmail or sampleRandom logic — only add guards/docs"
---

<objective>
Fix the three identified gaps in the existing backend and frontend:
1. Empty bank guard in assembleQuestionSet
2. difficulty_tier included explicitly in client payload
3. GAS URL env var fallback in WelcomeScreen
Also update TypeScript types to include difficulty_tier.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Code.gs — empty bank guard + difficulty_tier + reading comment</name>
  <files>backend/Code.gs</files>
  <read_first>
    - backend/Code.gs lines 509–585 (assembleQuestionSet + helpers)
    - backend/Code.gs lines 205–224 (client payload construction)
    - .planning/phases/02-candidate-entry-test-assembly/02-PATTERNS.md (Pattern 4)
  </read_first>
  <action>
    Edit 1 — assembleQuestionSet (line ~511): Add guard at the top:
      `if (!QUESTIONS || QUESTIONS.length === 0) { throw new Error("QUESTIONS bank is empty — re-deploy with embedded question data"); }`

    Edit 2 — client payload (line ~210): Add `difficulty_tier: q.difficulty_tier || null` to the returned object shape (after `response_type`, before `stem`). Add comment: `// NOT answer key — safe to expose; needed for Phase 4 level progress UI`

    Edit 3 — QUOTAS.reading comment (line ~25): Change the reading quota block to:
      `"reading": { "count": 1, "unit": "passages" /* 1 passage = 5 questions in assembled set */ }`

    Edit 4 — assembleQuestionSet reading comment (line ~527): Update comment from just "1 passage = 5 questions" to:
      `// 4. Sample English Reading Passage (quota: 1 passage → expands to 5 questions, all from chosen passage)`
  </action>
</task>

<task type="auto">
  <name>Task 2: types/index.ts — add difficulty_tier to Question type</name>
  <files>assessment-app/src/types/index.ts</files>
  <read_first>
    - assessment-app/src/types/index.ts (full file — 37 lines)
  </read_first>
  <action>
    Add `difficulty_tier?: 'straightforward' | 'moderate' | 'complex' | null;` field to the Question interface, after `response_type`.
    This matches what Code.gs now returns in the client payload.
  </action>
</task>

<task type="auto">
  <name>Task 3: WelcomeScreen.tsx — env var fallback for GAS URL</name>
  <files>assessment-app/src/components/WelcomeScreen.tsx</files>
  <read_first>
    - assessment-app/src/components/WelcomeScreen.tsx (full file — 121 lines)
    - .planning/phases/02-candidate-entry-test-assembly/02-PATTERNS.md (Pattern 7)
  </read_first>
  <action>
    In the `useState('')` for `gasUrl`, change the initial value to:
      `useState(process.env.NEXT_PUBLIC_GAS_URL || '')`
    This pre-populates the field in production while keeping it editable for development.
    Add a comment above: `// Pre-populated from NEXT_PUBLIC_GAS_URL env var in production`
  </action>
</task>

<task type="auto">
  <name>Task 4: Create .env.example</name>
  <files>assessment-app/.env.example</files>
  <action>
    Create the file with:
    ```
    # Google Apps Script Web App URL (deployed with "Anyone" access)
    # Set this to pre-populate the GAS URL field for candidates in production
    NEXT_PUBLIC_GAS_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
    ```
  </action>
</task>

</tasks>
