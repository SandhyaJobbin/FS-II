---
phase: 11-recruiter-analytics-dashboard
plan: 02
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - backend/Analytics.gs
  - backend/Code.gs
autonomous: true
requirements:
  - ADMIN-06
  - ADMIN-07
  - ADMIN-08
  - ADMIN-09
  - ADMIN-11
must_haves:
  truths:
    - GET action=adminAnalytics returns a single flat JSON envelope containing scoreTrend, questionStats, violationCorrelation, discriminationIndex, biasSignals, and meta.
    - Every "pass rate" number uses the A2 denominator policy — ungraded is counted separately and never merged into the denominator.
    - Endpoint requires a valid ADMIN_TOKEN; unauthenticated calls return `{ success:false, error:"Unauthorized" }`.
    - Response payload never contains Name, Email, SubmittedAnswer, options.is_correct, model_answer, or rubric content (PII + answer-key invariants).
    - `meta.aggregationMs` is present on every response so Pitfall 1 (6-min cap) is monitorable from day one.
  artifacts:
    - backend/Analytics.gs (5 pure reducers + handleAdminAnalytics orchestrator)
    - backend/Code.gs updated with one new doGet branch mapping action=adminAnalytics → handleAdminAnalytics
  key_links:
    - handleAdminAnalytics calls checkAdminAuth(token) as its first executable line (Code.gs:50 pattern).
    - Reducers call effectiveVerdict (AsyncGrading.gs:38) — GAS global scope makes it callable across .gs files with no import ceremony.
    - Wave 0 sync-check sections 16-19 now PASS against this file set.
---

<objective>
Wave 1 backend. Add `handleAdminAnalytics(token)` and the five pure reducers to a new `backend/Analytics.gs` (kept separate from `AsyncGrading.gs` because that file is already 760 lines and CLAUDE.md caps files at 500). Wire the endpoint into `doGet` as the 5th action branch. Instrument `meta.aggregationMs` from the first commit. Delivers ADMIN-06, ADMIN-07, ADMIN-08, ADMIN-09, and the low-N portion of ADMIN-11.

Purpose: The full server-side aggregation contract for the analytics dashboard. Wave 2 renders it; Wave 3 verifies it live. This is the single most trust-critical wave because every pass-rate the recruiter sees is defined here.
Output: 2 files (1 new backend module + 1 modified router). No sheet changes. No new triggers. No new script properties.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md
@.planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md
@.planning/phases/11-recruiter-analytics-dashboard/11-01-SUMMARY.md
@backend/Code.gs
@backend/AsyncGrading.gs
@tests/analytics/analytics-reducers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend/Analytics.gs with reducers + handleAdminAnalytics</name>
  <files>backend/Analytics.gs</files>
  <read_first>
    - backend/AsyncGrading.gs lines 1-45 (file header + effectiveVerdict signature) and 92-174 (computeAggregatesForAttempt — the shape to adapt from per-attempt to cross-attempt aggregation)
    - backend/Code.gs lines 40-55 (ADMIN_TOKEN, checkAdminAuth, READY_STATUSES), lines 189-196 (jsonResponse wrapper), lines 200-242 (initSheets column headers — Attempts is 15 cols, Responses is 5 cols, GradingTranscripts is 9 cols), lines 476-500 (handleGetAttemptTranscript — the analog handler)
    - tests/analytics/analytics-reducers.ts (Wave 0 mirror — the file this backend MUST match logically for the Pitfall-2 parity gate)
    - .planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md § Aggregation Recipes (full JS reducer sketches) and § Data-Shape Reference (exact column indices)
    - .planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md § 1, 3, 4, 5 (handler shape, reducer shape, effectiveVerdict hard constraint, A2 denominator hard constraint)
  </read_first>
  <action>
    Create backend/Analytics.gs as a new sibling to AsyncGrading.gs (GAS project global scope — no import statements). Structure the file in this order (mirrors AsyncGrading.gs conventions):
    (a) File header comment block explaining: this file adds cross-attempt aggregation reducers + the handleAdminAnalytics doGet handler; shares GAS global scope with Code.gs and AsyncGrading.gs; reuses `effectiveVerdict` / `READY_STATUSES` / `checkAdminAuth` / `QUESTIONS` from those files; per D-implicit / CLAUDE.md file-size rule it lives in its own file rather than growing AsyncGrading.gs past 500 lines.
    (b) Constants: `const ANALYTICS_MIN_QUESTION_SAMPLES = 5; const ANALYTICS_MIN_TREND_ATTEMPTS = 5; const ANALYTICS_MIN_TREND_DAYS = 3; const ANALYTICS_MIN_CORR_SAMPLES = 10; const ANALYTICS_MIN_BIAS_SAMPLES = 15; const ANALYTICS_MIN_DISCRIM_QUARTILE_N = 3;` — MUST match the same numeric values used in tests/analytics/analytics-reducers.ts so parity holds.
    (c) Five reducer functions matching the mirror signatures and shapes in 11-RESEARCH.md § Aggregation Recipes:
      - `function buildScoreTrend(attemptsByStatus)` — bucket by `endTime.slice(0, 10)`; skip attempts with empty EndTime or non-finite OverallScore (Pitfall 6, 7); return `{ enough_data, threshold_note, points: [{date, avgScore, n}] }`.
      - `function buildQuestionStats(responsesData, transcriptsData)` — build `txByKey = {}` keyed on `attemptId + '|' + qId`, then fold responses using `effectiveVerdict(txByKey[key] || null, {IsCorrect: r[3]})`. Per-qId counters `{correct, incorrect, ungraded}`. Projection: `sampleSize = correct + incorrect` (A2 policy — HARD CONSTRAINT per 11-PATTERNS § 5), `passRate = sampleSize >= ANALYTICS_MIN_QUESTION_SAMPLES ? Math.round(100*correct/sampleSize) : null`, `enough_data = sampleSize >= ANALYTICS_MIN_QUESTION_SAMPLES`. Also emit `ungraded` count per row so the frontend can render the caveat pill.
      - `function buildViolationCorrelation(attemptsByStatus)` — Pearson r on `(ViolationCount col 11, OverallScore col 7)` tuples; guard `n < ANALYTICS_MIN_CORR_SAMPLES` returns `{enough_data:false, r:null}`; guard zero-variance denominator returns `r: 0`. Return `points` array for the scatter chart and `interpretation` label (`weak`/`moderate`/`strong` by |r| thresholds 0.2 / 0.5).
      - `function buildDiscriminationIndex(attemptsByStatus, responsesData, transcriptsData)` — top-quartile vs bottom-quartile pass-rate per question. Skip questions with < ANALYTICS_MIN_QUESTION_SAMPLES total. For each qualifying question compute `passTop, passBottom` (each requiring ≥ ANALYTICS_MIN_DISCRIM_QUARTILE_N samples in that quartile). Return `{ enough_data, threshold, rows: [{qId, passTop, passBottom, delta, sampleTop, sampleBottom}] }` sorted by ascending `delta` (most anti-discriminating first). This is the ADMIN-09 signal per research recommendation.
      - `function buildBiasSignals(attemptsByStatus)` — recommendation-tier distribution over `last20` and `allTime` windows; return `{ enough_data, tierDistribution: [{tier, pct, n, window}] }`. Threshold `ANALYTICS_MIN_BIAS_SAMPLES = 15` (assumption A2 per research; label in UI copy is "Distribution Monitor", NOT "adverse-impact analysis" — Pitfall 3).
    (d) `function handleAdminAnalytics(token)` orchestrator — analog of handleGetAttemptTranscript (Code.gs:476-500). Body order:
      1. `if (!checkAdminAuth(token)) return { success: false, error: "Unauthorized" };` (FIRST executable line — Pitfall 5 / security invariant).
      2. `const t0 = Date.now();` (Pitfall 1 instrumentation).
      3. Read three sheets in one `getDataRange().getValues()` call each: Attempts, Responses, GradingTranscripts. Guard each `if (!sheet) return { success: true, ...empty payload with enough_data:false }` — defensive against a fresh spreadsheet where initSheets hasn't run.
      4. `.slice(1)` each to strip headers.
      5. `const attemptsByStatus = attempts.filter(function(r) { return READY_STATUSES.indexOf(r[5]) !== -1; });` (uses the constant from Code.gs:55).
      6. `const ungradedTotal = attemptsByStatus.reduce(function(a, r) { return a + (Number(r[14]) || 0); }, 0);` — Pitfall 7 coercion; A4 defensive fallback for legacy attempts with empty col 14.
      7. Call the five reducers.
      8. Return `{ success: true, generatedAt: new Date().toISOString(), meta: { totalAttempts: attemptsByStatus.length, ungradedTotal: ungradedTotal, aggregationMs: Date.now() - t0 }, scoreTrend, questionStats, violationCorrelation, discriminationIndex, biasSignals }`.
    Never include row[1] (Name) or row[2] (Email) in any returned payload — PII invariant (11-RESEARCH.md § Security Domain).
    Keep the entire file under 400 lines. Use `function` declarations (not arrow functions) throughout — Apps Script V8 supports arrow functions but the project convention across Code.gs and AsyncGrading.gs is `function` declarations for top-level exports.
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment" && grep -c "^function " backend/Analytics.gs | grep -E "^[6-9]$|^1[0-9]$" && grep -q "checkAdminAuth(token)" backend/Analytics.gs && grep -q "meta[^}]*aggregationMs" backend/Analytics.gs && grep -q "effectiveVerdict(" backend/Analytics.gs && ! grep -qE "^function effectiveVerdict" backend/Analytics.gs && ! grep -qE "row\[1\]|row\[2\]|\[1\]:|\[2\]:" backend/Analytics.gs</automated>
  </verify>
  <acceptance_criteria>
    - backend/Analytics.gs exists with ≥6 `function` declarations (5 reducers + 1 handler).
    - handleAdminAnalytics contains the literal `checkAdminAuth(token)` call as one of its first two statements (position enforced by sync-check § 17 regex window `{0,300}`).
    - The response envelope contains `meta.aggregationMs` (grep verifies both tokens present).
    - Reducers call `effectiveVerdict(` at least once (per 11-PATTERNS § 4 hard constraint).
    - No local re-implementation of `effectiveVerdict` in Analytics.gs.
    - No PII column reads (Name = col 1, Email = col 2) appear in any reducer or handler body.
    - File length under 400 lines.
  </acceptance_criteria>
  <done>backend/Analytics.gs contains all 5 reducers + orchestrator; auth is the first executable line; aggregationMs is instrumented; PII columns are never read; effectiveVerdict is reused not re-derived.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the doGet router — add 5th action branch adminAnalytics</name>
  <files>backend/Code.gs</files>
  <read_first>
    - backend/Code.gs lines 134-153 (existing doGet router — exact `else if` chain to extend)
    - backend/Code.gs lines 145-147 (the `getAttemptTranscript` branch — the pattern to append immediately after)
    - .planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md § 2 (doGet wiring pattern with the exact insertion point)
  </read_first>
  <action>
    Locate the existing router branch chain in `doGet` (approximately Code.gs:139-147). Immediately after the `getAttemptTranscript` branch (`} else if (action === "getAttemptTranscript") { return jsonResponse(handleGetAttemptTranscript(params.attemptId, params.token)); }`) and before the trailing `return jsonResponse({ error: "Invalid action or method" }, 400);` fallback, add a new branch:
    ```
    } else if (action === "adminAnalytics") {
      return jsonResponse(handleAdminAnalytics(params.token));
    ```
    Do NOT modify doPost (analytics is read-only per 11-RESEARCH.md § Architecture). Do NOT modify the `try/catch` wrapper at Code.gs:138,150-152 — the new branch inherits its error handling automatically. Do NOT touch any other Code.gs region (Phase 10 handlers at Code.gs:433-712 are read-only for Phase 11, per 11-PATTERNS.md § "Files planner should NOT touch").
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment" && grep -c 'action === "adminAnalytics"' backend/Code.gs | grep -q '^1$' && grep -c "handleAdminAnalytics(params.token)" backend/Code.gs | grep -q '^1$' && npx tsx scripts/sync-check.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exactly ONE occurrence of `action === "adminAnalytics"` exists in Code.gs (grep count == 1).
    - Exactly ONE occurrence of `handleAdminAnalytics(params.token)` exists in Code.gs.
    - `npx tsx scripts/sync-check.ts` exits 0 — all 19 sections now PASS (Wave 0 sections 16-19 were designed to fail until this wave lands; they must all be green now).
    - The doGet function still parses (no unbalanced braces) — verifiable by grepping for the existing `} catch (err) {` in doGet still being reachable from the router chain.
  </acceptance_criteria>
  <done>doGet has exactly 5 action branches; sync-check is fully green; no other Code.gs region is modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public GAS Web App URL → handleAdminAnalytics | Any HTTP client can hit this endpoint; only a valid ADMIN_TOKEN gates access |
| Sheet reads → JSON response | Server owns the projection; must never emit PII columns or answer-key fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Information Disclosure | handleAdminAnalytics without auth gate | critical | mitigate | `checkAdminAuth(token)` is the first executable line; sync-check § 17 blocks any refactor that pushes it deeper. |
| T-11-05 | Information Disclosure | PII (Name/Email) leaking into aggregation payload | high | mitigate | Reducers key on `attemptId` / `qId` only; row[1] (Name) and row[2] (Email) columns are never referenced. Verification greps confirm absence. |
| T-11-06 | Information Disclosure | Answer-key or rubric content in payload | high | mitigate | Reducers project `{qId, passRate, sampleSize, ungraded}` only. `options.is_correct`, `model_answer`, and `Rationale` are never returned. |
| T-11-07 | Denial of Service | 6-minute Apps Script cap under high N | medium | mitigate | Reducers are O(N) hash-map joins. `meta.aggregationMs` instrumented; Wave 3 verifies live number against ANALYTICS_MIN thresholds. CacheService upgrade path documented but explicitly out for v1. |
| T-11-08 | Tampering | Token-in-URL logging (existing pattern) | medium | accept | Reused for consistency with all 4 existing admin GETs (Code.gs router); broader `Authorization: Bearer` refactor is out of Phase 11 scope per 11-RESEARCH.md Pitfall 5. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | Wave 1 introduces zero new dependencies (backend is pure GAS built-ins). No package-manager changes. |
</threat_model>

<verification>
- `npx tsx scripts/sync-check.ts` exits 0 — all sections (1-19) PASS.
- Manual grep: `grep -n "checkAdminAuth(token)" backend/Analytics.gs | head -1` shows the call within the first ~10 lines of `handleAdminAnalytics`.
- Manual grep: `grep -c "row\[1\]\|row\[2\]" backend/Analytics.gs` returns 0.
- Manual grep: `grep -c 'action === "adminAnalytics"' backend/Code.gs` returns 1.
- File line counts: `wc -l backend/Analytics.gs backend/Code.gs` — Analytics.gs under 400, Code.gs delta is only ~3 lines (one new else-if branch).
</verification>

<success_criteria>
- ADMIN-06 (score trend), ADMIN-07 (question pass-rate), ADMIN-08 (violation-vs-score correlation), ADMIN-09 (bottom-N discriminating questions), and ADMIN-11 (low-N fallback per-signal) are all satisfied by handleAdminAnalytics's return payload.
- A2 denominator policy is preserved — every rate uses `graded = correct + incorrect` as the denominator; ungraded counts are surfaced, never merged in.
- Security invariants hold: auth first, no PII, no answer keys, no rubric content in the response.
- Pitfall 1 monitoring is live from the first commit via `meta.aggregationMs`.
</success_criteria>

<output>
Create `.planning/phases/11-recruiter-analytics-dashboard/11-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **New file:** `backend/Analytics.gs` — 5 pure reducers (`buildScoreTrend`, `buildQuestionStats`, `buildViolationCorrelation`, `buildDiscriminationIndex`, `buildBiasSignals`) + `handleAdminAnalytics(token)` orchestrator. Reuses `effectiveVerdict`, `READY_STATUSES`, `checkAdminAuth`, and `QUESTIONS` from sibling `.gs` files via GAS global scope.
- **Modified:** `backend/Code.gs` — one new `else if (action === "adminAnalytics")` branch in `doGet` (approx 3 lines added at the existing router chain).
- **No sheet schema changes.** **No new triggers.** **No new script properties.** **Zero new dependencies.**
