---
phase: 13-accessibility-f-05-remaining-polish
plan: 03
type: execute
wave: 1
depends_on:
  - 13-02
files_modified:
  - assessment-app/src/components/ReportScreen.tsx
  - .planning/phases/13-accessibility-f-05-remaining-polish/13-03-postfix-axe-report.md
autonomous: true
requirements:
  - A11Y-01
  - BUG-04
tags:
  - a11y
  - aria
  - reportscreen

must_haves:
  truths:
    - ReportScreen.tsx has a single `<main role="main" aria-labelledby="report-heading">` landmark wrapping the entire report body.
    - Every section motion.div in the report body carries `role="region"` + `aria-labelledby="<slug>-label"` pointing at the existing uppercase-tracked label span.
    - TraitBar renders `role="progressbar"` with `aria-valuenow`, `aria-valuemin=0`, `aria-valuemax=100`, `aria-valuetext` including a semantic band (Strong/Moderate/Developing) so screen readers convey meaning without relying on color.
    - Every decorative SVG on ReportScreen has `aria-hidden="true"` + `focusable="false"`; meaningful icons (warning/checkmark that convey status without a text label) carry `role="img"` + `aria-label`.
    - Zero critical and zero serious axe-core violations remain on ReportScreen render, verified via the same AxeDevReporter route used to capture the 13-02 baseline.
  artifacts:
    - Modified assessment-app/src/components/ReportScreen.tsx with ARIA landmarks, roles, and semantic replacements per 13-RESEARCH.md § Patterns 1-3.
    - Post-fix axe report at .planning/phases/13-accessibility-f-05-remaining-polish/13-03-postfix-axe-report.md showing 0 critical + 0 serious violations.
  key_links:
    - 13-02-baseline.md — the list of violations this plan must eliminate.
    - assessment-app/src/components/ReportScreen.tsx line 21 (TraitBar) — needs progressbar role additions.
    - assessment-app/src/components/ReportScreen.tsx line 68 (outer wrapper) — becomes the `<main>` landmark.
---

<objective>
Wave 1: eliminate every critical/serious axe-core violation captured in the 13-02 baseline for ReportScreen. Add ARIA landmarks (main + regions), progress-bar semantics to TraitBar, aria-hidden to decorative SVGs, and a sr-only band-label to defeat the color-only-indicator violation on trait colors. This is the F-05 / BUG-04 requirement finally landing.

Purpose: F-05 is the only accessibility requirement explicitly enumerated in REQUIREMENTS.md (line 107). This plan closes it, plus recommends the new A11Y-01 (zero critical/serious violations) as the measurable acceptance for future audits.
Output: 1 modified component, 1 post-fix axe report, 0 new dependencies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-accessibility-f-05-remaining-polish/CONTEXT.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-RESEARCH.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-PATTERNS.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-02-baseline.md
@assessment-app/src/components/ReportScreen.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add ARIA landmarks, region roles, progressbar semantics, and SVG aria-hidden to ReportScreen.tsx</name>
  <files>assessment-app/src/components/ReportScreen.tsx</files>
  <read_first>
    - assessment-app/src/components/ReportScreen.tsx in full — locate the 8 anchor points listed in 13-RESEARCH.md § Workstream 1 (outer wrapper line 68, header motion.div line 74, `<h2>` line 90, the 6 section motion.div blocks at lines 99/127/143/162/183/207, and the 3 decorative SVGs at lines 85/190/215).
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-02-baseline.md — the exact list of critical + serious violations that must be eliminated.
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-RESEARCH.md § Patterns 1, 2, 3 — landmark, progressbar, and SVG icon strategies with the exact ARIA attribute lists.
  </read_first>
  <action>
    Apply the following ARIA additions to ReportScreen.tsx. All additions are non-visual — no className changes in this task except for one screen-reader-only span. Do not restructure the JSX; only add attributes and the sr-only span.

    1. Outer wrapper (currently `<div className="w-full max-w-[580px] mx-auto animate-fade-in">` at line 68): change the outer `<div>` to `<main role="main" aria-labelledby="report-heading">` — keep className.
    2. Header `<h2>` at line 90: add `id="report-heading"` so it anchors the main landmark's aria-labelledby.
    3. For each of the 6 section motion.div blocks (Candidate Info / Trait Scores / Insight / Recommendation / Ungraded / Integrity), add:
       - `role="region"`
       - `aria-labelledby="<slug>-label"` where `<slug>` is one of: `candidate`, `traits`, `insight`, `recommendation`, `ungraded`, `integrity`.
       - Add `id="<slug>-label"` to the existing `<span className="text-[10px] font-bold uppercase tracking-widest ...">` label span inside each block. For Candidate Info (which has no label span), add a visually-hidden `<h2 id="candidate-label" className="sr-only">Candidate Information</h2>` immediately inside the motion.div.
    4. TraitBar component (lines 21-39): rewrite per 13-RESEARCH.md Pattern 2 — the inner track div gets `role="progressbar"`, `aria-valuenow={value}`, `aria-valuemin={0}`, `aria-valuemax={100}`, `aria-labelledby={labelId}`, and `aria-valuetext={\`${value} percent, ${bandLabel}\`}` where `bandLabel = value >= 70 ? 'strong' : value >= 50 ? 'moderate' : 'developing'`. Also add an `<span className="sr-only">` inside the label row that says ` — ${bandLabel}` so screen readers hear the band even when reading the label alone. The label `<span>` gets `id={labelId}` where `labelId = \`trait-${label.replace(/\\s+/g, '-').toLowerCase()}\``.
    5. SVG icons: to the decorative SVG at line 85 (checkmark next to "Assessment Complete") and the decorative SVG at line 215 (shield next to "Integrity Events Logged"), add `aria-hidden="true"` and `focusable="false"`. The SVG at line 190 (warning in the Ungraded Notice) carries meaning (conveys "warning") and gets `role="img"` + `aria-label="Warning"` instead.
    6. Verify existing Tailwind class `sr-only` is available (it is a built-in Tailwind utility; no CSS additions needed).

    Do NOT change any dark-theme classes in this plan (that is plan 13-04's job). Do NOT add per-question breakdown (that is plan 13-07's job).
  </action>
  <verify>
    <automated>cd assessment-app && npx tsc --noEmit 2>&1 | tail -10 ; echo "---"; grep -c 'role="main"' src/components/ReportScreen.tsx ; echo "---"; grep -c 'role="region"' src/components/ReportScreen.tsx ; echo "---"; grep -c 'role="progressbar"' src/components/ReportScreen.tsx ; echo "---"; grep -c 'aria-hidden="true"' src/components/ReportScreen.tsx ; echo "---"; grep -c 'id="report-heading"' src/components/ReportScreen.tsx ; echo "---BUILD---"; npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `role="main"` appears exactly once in ReportScreen.tsx.
    - `role="region"` appears at least 6 times (one per section block).
    - `role="progressbar"` appears at least once inside TraitBar.
    - `aria-hidden="true"` appears at least 2 times (the two decorative SVGs).
    - `id="report-heading"` appears exactly once and is on the h2 that says "Assessment Complete".
    - `npx tsc --noEmit` produces zero new errors.
    - `npm run build` succeeds.
    - No dark-theme class swaps in the diff (that is 13-04's scope).
    - No new imports beyond what ARIA additions require (React already imported).
  </acceptance_criteria>
  <done>ReportScreen has ARIA landmarks, section regions, progressbar semantics, aria-hidden on decorative SVGs, and sr-only band labels for color-independent status conveyance.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Capture post-fix axe report and confirm zero critical + zero serious violations</name>
  <files>.planning/phases/13-accessibility-f-05-remaining-polish/13-03-postfix-axe-report.md</files>
  <read_first>
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-02-baseline.md — for line-by-line comparison of fixed violations.
    - .planning/phases/13-accessibility-f-05-remaining-polish/13-02-SUMMARY.md — to remember which route/fixture was used for the baseline (must use the same one for apples-to-apples comparison).
  </read_first>
  <action>
    Repeat the Task 3 capture procedure from plan 13-02, using the same route and fixture. Save the output to `.planning/phases/13-accessibility-f-05-remaining-polish/13-03-postfix-axe-report.md` with the same structure (Critical / Serious / Moderate / Minor / Incomplete). Add a "Diff vs 13-02 baseline" section listing:
      - Violations eliminated (baseline had them, post-fix does not)
      - Violations remaining (still present) — MUST be zero in Critical + zero in Serious
      - New violations introduced (should be zero — flag as regression if any)

    If any critical or serious violations remain, iterate Task 1 (add missing attributes) and re-capture — do NOT lower the acceptance threshold. If a violation is a false positive (e.g., color-contrast against a gradient background, per 13-RESEARCH.md Pitfall 7), document the exception with the observed rule id, why it is a false positive, and the manual verification you performed.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-accessibility-f-05-remaining-polish/13-03-postfix-axe-report.md ; echo "---"; grep -E "^### Critical" .planning/phases/13-accessibility-f-05-remaining-polish/13-03-postfix-axe-report.md ; echo "---"; grep -E "^### Serious" .planning/phases/13-accessibility-f-05-remaining-polish/13-03-postfix-axe-report.md ; echo "---"; grep -c "Diff vs 13-02 baseline" .planning/phases/13-accessibility-f-05-remaining-polish/13-03-postfix-axe-report.md</automated>
  </verify>
  <acceptance_criteria>
    - Post-fix axe report file exists at the expected path.
    - Report contains a "Diff vs 13-02 baseline" section.
    - The Critical section is empty OR contains only documented false positives with manual verification.
    - The Serious section is empty OR contains only documented false positives with manual verification.
    - Route and fixture used are identical to 13-02 baseline capture.
  </acceptance_criteria>
  <done>Zero critical + zero serious violations on ReportScreen; A11Y-01 measurably satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component render → screen reader | Assistive-tech users are the trust-relying party. Bad ARIA is worse than no ARIA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Repudiation | Ungrounded a11y claim ("passes axe") without a captured report | high | mitigate | Task 2 requires a captured post-fix report on disk, with a diff-vs-baseline section — no verbal claim of compliance. |
| T-13-06 | Tampering | ARIA additions accidentally break existing framer motion animations or click handlers | medium | mitigate | Task 1 forbids className changes and JSX restructuring; the diff should show only new attributes + one sr-only span. Build + tsc must both pass. |
| T-13-07 | Denial of Service | Screen reader disorientation from wrong role hierarchy (e.g., nested main) | high | mitigate | Only one `role="main"` per document — Task 1 verify greps for count == 1. |
</threat_model>

<verification>
- Task 1 automated: grep counts match, tsc clean, build clean.
- Task 2 automated: post-fix report exists with diff section, and Critical/Serious sections are empty.
- Manual: developer walks the fixture route with VoiceOver (macOS) or NVDA (Windows) and confirms the report reads in landmark order: main → candidate → traits → insight → recommendation → [ungraded] → integrity.
</verification>

<success_criteria>
- BUG-04 / F-05 satisfied: ARIA landmarks + roles on ReportScreen.tsx.
- A11Y-01 measurably satisfied: zero critical + zero serious axe-core violations on ReportScreen render.
- Progress bars announce their value AND their band (Strong/Moderate/Developing) to screen readers — color-only violation eliminated.
- No behavior change to any interactive element; no changes to theme classes (13-04's scope).
</success_criteria>

<output>
Create `.planning/phases/13-accessibility-f-05-remaining-polish/13-03-SUMMARY.md` when done. Include the eliminated-violations count and a link to the post-fix axe report.
</output>

## Artifacts this phase produces

- **Modified:** `assessment-app/src/components/ReportScreen.tsx` — adds landmark (main), 6 region roles, progressbar semantics on TraitBar, aria-hidden on 2 decorative SVGs, role=img+aria-label on 1 meaningful SVG, and sr-only band label for trait colors.
- **New:** `.planning/phases/13-accessibility-f-05-remaining-polish/13-03-postfix-axe-report.md` — post-fix axe report with diff-vs-baseline section.
- **No behavior change. No new dependencies. No theme class changes (13-04's scope).**
