---
phase: 01-content-ingestion-question-bank
plan: 06
type: execute
wave: 4
depends_on: ["01-03", "01-04", "01-05"]
files_modified:
  - ingestion/difficulty_tags.py
  - ingestion/normalize.py
  - ingestion/validate.py
  - content/difficulty-tags.json
  - content/questions.json
  - content/ingestion-report.json
  - tests/ingestion/test_ingest_e2e.py
  - tests/ingestion/test_tags.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-04]

must_haves:
  truths:
    - "Full bank ingests: questions.json holds exactly 385 items (English 105 + Attention 160 + Critical Thinking 120) with zero validation errors"
    - "Every one of the 385 items carries a difficulty_tier from the closed enum {straightforward, moderate, complex} — no null, no fourth value (INGEST-02 adjacency edge)"
    - "Tag assignment is fully deterministic — identical sources always yield identical category/level/case/tier tags and byte-identical questions.json across reruns (INGEST-02 ordering edge)"
    - "Re-running the pipeline on unchanged sources produces byte-identical questions.json/quotas.json; re-running the seed leaves DB row counts unchanged (INGEST-01 idempotency edge)"
    - "Partial DB state is impossible: the full 385-item load commits in ONE Prisma transaction or rolls back entirely (INGEST-01 concurrency edge, DB leg)"
    - "DB is queryable: 385 Questions, 70 Cases (40 attention + 30 CT + reading/closure groups as designed), 8 QuotaConfig rows with verbatim values + unit annotations, 0 Questions with null difficultyTier"
    - "ingestion-report.json shows counts 105/160/120, censuses (38+125 ✅, 111 ☑, 49 ☐, 180 ✅), sha256 per source, zero unresolved errors"
  artifacts:
    - ingestion/difficulty_tags.py + content/difficulty-tags.json (385 entries)
    - content/questions.json (385 items, final), content/ingestion-report.json (final)
    - tests/ingestion/test_ingest_e2e.py, tests/ingestion/test_tags.py
  key_links:
    - "difficulty-tags.json keyed by question slug ID → normalize applies tier → validate enforces 100% coverage (missing tier = loud failure)"
    - "seed.ts (unchanged since 01-02) loads the full artifact + quotas into QuotaConfig in the same transaction"
  prohibitions:
    - "Never silently default all difficulty tiers to 'moderate' (or any single value) — GRADE-03's out-of-the-box insight would have zero signal; tiers come only from the recorded heuristic below, pending user confirmation"
    - "Never import partial data: no warnings-only path for structural failures; all-or-nothing emit and single-transaction load are the only two outcomes"
  assumptions:
    - "A-OQ1 (difficulty tier source): tiers are supplied via content/difficulty-tags.json (research OQ-1 option-a format — auditable data file keyed by question ID), with initial values generated by the deterministic heuristic in Task 1 (option-b rules) and marked provisional via the file's _meta block. SME review/replacement of that file is the confirmed end state. FLAG for user confirmation at /gsd-verify-work."
---

<objective>
Closeout: difficulty-tier assignment per recorded assumption A-OQ1, the full 385-item pipeline run, the full atomic DB load (questions + quotas), and the final e2e/tags test gates that prove every INGEST success criterion.

Purpose: INGEST-02's tier tag lands as auditable data without silently defaulting; INGEST-01's queryable store reaches full volume; idempotency and atomicity proven at full scale.
Output: final committed artifacts + fully seeded Postgres + complete green test suite.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md
@.planning/phases/01-content-ingestion-question-bank/01-PATTERNS.md
@.planning/phases/01-content-ingestion-question-bank/01-03-SUMMARY.md
@.planning/phases/01-content-ingestion-question-bank/01-04-SUMMARY.md
@.planning/phases/01-content-ingestion-question-bank/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ingestion/difficulty_tags.py + content/difficulty-tags.json (heuristic-seeded, provisional per A-OQ1)</name>
  <files>ingestion/difficulty_tags.py, content/difficulty-tags.json, ingestion/normalize.py, ingestion/validate.py</files>
  <read_first>
    - ingestion/normalize.py, ingestion/validate.py, ingestion/run.py (extension points — check existing hooks before editing)
    - .planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md (OQ-1: no authored tiers; options a/b/c; 'do NOT silently default everything to moderate'; English per-part mark values 1/1/2/5/2.5 as coarse proxy)
  </read_first>
  <action>
    `ingestion/difficulty_tags.py`: CLI that runs the full parse (all banks) and emits `content/difficulty-tags.json` — `{question_slug_id: tier}` for all 385 IDs. Heuristic rules (deterministic, applied in order, first match wins):
      1. response_type == mcq_multi → "complex"
      2. response_type == hybrid → "moderate" (Case Closure)
      3. english section in {macro (2 marks), reading (5 marks), closure (2.5 marks)} → "moderate"
      4. english section in {grammar (1 mark), sentence_correction (1 mark)} → "straightforward"
      5. attention/critical mcq_single → "straightforward"
    The JSON file carries a `_meta` block: {provenance: "provisional heuristic per A-OQ1 — pending SME review", rules: the five rules above as strings, generated_by: "ingestion/difficulty_tags.py"}. Regeneration must be byte-identical for unchanged sources (sorted keys).
    `ingestion/normalize.py` (minimal edit): when `content/difficulty-tags.json` exists, load it and set Item.difficulty_tier from the slug map; when absent (slice runs in earlier waves), tier stays None — this is the ONLY change.
    `ingestion/validate.py` (minimal edit): when the tags file is present, every item MUST have tier ∈ {straightforward, moderate, complex} and the tags file MUST cover 100% of parsed IDs — any miss raises naming the ID. When absent, tier validation is skipped (slice runs).
  </action>
  <verify>
    <automated>$env:PYTHONIOENCODING="utf-8"; python ingestion/difficulty_tags.py; python -c "import json; d=json.load(open('content/difficulty-tags.json',encoding='utf-8')); tags={k:v for k,v in d.items() if not k.startswith('_')}; print(len(tags), set(tags.values()))"</automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0; printed: 385 entries, value set ⊆ {straightforward, moderate, complex} (all three values present — NOT a single-value default)
    - `_meta.provenance` string contains `A-OQ1`
    - Second run produces byte-identical file (deterministic)
    - normalize.py/validate.py diffs are limited to the tag-load and tier-coverage hooks
  </acceptance_criteria>
  <done>385 tier assignments exist as an auditable, regenerable, provisional data file; pipeline enforces 100% tier coverage whenever the file is present.</done>
</task>

<task type="auto">
  <name>Task 2: Full run + full atomic DB load (questions + quotas) [schema already pushed in 01-02]</name>
  <files>content/questions.json, content/ingestion-report.json, prisma/seed.ts</files>
  <read_first>
    - prisma/seed.ts (current loader — extend for QuotaConfig only; check first, do not rewrite)
    - prisma/schema.prisma (QuotaConfig + IngestionRun models)
    - .planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md (Pattern 3: all-or-nothing; quota_config verbatim + unit)
  </read_first>
  <precondition>01-03/04/05 merged (all parser modules present); 01-02's `npx prisma db push` already applied — this task re-seeds, never re-pushes schema.</precondition>
  <reversibility rating="reversible">Dev-content reload on a greenfield database; re-runnable from committed artifacts at any time.</reversibility>
  <action>
    Minimal seed.ts extension: after Questions/Options, load `content/quotas.json` inside the SAME `prisma.$transaction` — create 8 QuotaConfig rows (section, category, volume, volumeUnit, quota, unit verbatim from the artifact) and write the IngestionRun record (sourceHashes, counts, full report JSON). No second transaction; no partial states.
    Execute: `$env:PYTHONIOENCODING="utf-8"; python ingestion/run.py` (no --banks filter → all banks + quotas + tier application) then `npx prisma db seed`.
    Idempotency proof: run the seed a second time; assert counts unchanged (seed must upsert/clear-and-reload deterministically within its transaction — pick clear-and-reload of bank content per run, preserving IngestionRun history).
  </action>
  <verify>
    <automated>docker exec fs-postgres psql -U postgres -d fs_assessment -tc "SELECT (SELECT count(*) FROM \"Question\"), (SELECT count(*) FROM \"Option\"), (SELECT count(*) FROM \"Option\" WHERE \"isCorrect\"), (SELECT count(*) FROM \"Case\"), (SELECT count(*) FROM \"QuotaConfig\"), (SELECT count(*) FROM \"Question\" WHERE \"difficultyTier\" IS NULL);"</automated>
  </verify>
  <acceptance_criteria>
    - psql returns: Question 385; Option count == sum of MCQ options (grammar 120 + reading 100 + closure 30 + attention + CT option totals as parsed); isCorrect count == 385-item MCQ key total from report census; Case 70+ (40 attn + 30 CT + reading passages + closures); QuotaConfig 8; null difficultyTier 0
    - ingestion-report.json: counts 105/160/120, zero errors, sha256 present for all 4 source files
    - Second `npx prisma db seed` leaves all counts identical; IngestionRun count increments by exactly 1
    - QuotaConfig rows match quotas.json verbatim (spot: CT row quota=10 unit=cases)
  </acceptance_criteria>
  <done>Full 385-item bank + 8 quotas live in Postgres, loaded atomically, idempotently re-seedable, with a full audit record per run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Final gates — full-bank e2e + tags tests</name>
  <files>tests/ingestion/test_ingest_e2e.py, tests/ingestion/test_tags.py</files>
  <read_first>
    - ingestion/run.py, ingestion/validate.py, ingestion/difficulty_tags.py (code under test)
    - .planning/phases/01-content-ingestion-question-bank/01-VALIDATION.md (phase gate: full suite green + report zero errors)
  </read_first>
  <behavior>
    - test_ingest_e2e: full run → 385 items; per-bank counts 105/160/120; manifest reconciliation passes for all three docs; marker censuses match (english reading 38 ✅; attention 125/111/49; critical 180 ✅)
    - test_ingest_e2e determinism: two consecutive full runs produce byte-identical questions.json AND quotas.json (report excluded — timestamps)
    - test_ingest_e2e atomicity: perturbed fixture (one broken item injected into a copy of one bank) → run exits ≠ 0 and previously emitted artifacts remain untouched (mtime/bytes unchanged)
    - test_ingest_e2e unicode round-trip: marker census computed AFTER json dump+reload equals census from parse
    - test_tags: every item has bank + section; case-based items (attention, critical, reading, closure) have case_id; english grammar/sentence_correction/macro items have section grouping (no item with BOTH level/case null AND section null); every item tier ∈ enum with 100% coverage of tags file ↔ parsed IDs
  </behavior>
  <action>
    Create both test files. Full-run tests may reuse the already-emitted committed artifacts for shape assertions but MUST execute at least one real `python ingestion/run.py` subprocess for the exit-code/determinism checks (tmp working copies for the perturbation case). All I/O encoding="utf-8". Then run the complete phase suite.
  </action>
  <verify>
    <automated>pytest tests/ingestion -q; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ingestion -q` exits 0 — entire phase suite green (smoke, manifest, extractors, validation_gate, english/attention/critical e2e, quotas, ingest_e2e, tags)
    - `npx vitest run` exits 0 (seed transaction + idempotency tests from 01-02 still green at full volume)
    - Determinism test proves byte-identical artifacts across two runs
    - Atomicity test proves perturbed input leaves prior artifacts byte-untouched
  </acceptance_criteria>
  <done>Phase gate green: full suite passes, 385 items validated + tagged + queryable, artifacts deterministic, load atomic, zero unresolved report errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full artifacts → Postgres | 385-item + 8-quota atomic write (largest single failure surface of the phase) |
| difficulty-tags.json → grading (Phase 3) | Provisional tier data flowing toward GRADE-03 signal |
| CI/logs ← full run | Key material volume at maximum during full emit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Tampering | Partial bank in Postgres (interrupted seed) silently breaking Phase 2 quota sampling | high | mitigate | Single $transaction for questions+options+quotas; atomicity test proves perturbed input leaves prior state untouched; idempotent re-seed |
| T-01-18 | Tampering | Silent tier fabrication (single-value default) draining GRADE-03 signal | medium | mitigate | Tier assignment only via the recorded A-OQ1 heuristic with all three values present; _meta.provenance marker; 100% coverage enforced by validate.py; flagged for user confirmation at verify |
| T-01-19 | Info Disclosure | Answer keys/model answers printed during full-run logging or committed beyond existing exposure | medium | mitigate | run.py prints counts/censuses only, never option text/keys; artifacts committed per A-OQ4 (same exposure class as keyed docx already in repo) |
| T-01-SC | Tampering | npm/pip installs | high | mitigate | No new installs in this plan; covered by 01-RESEARCH.md audit |
</threat_model>

<verification>
- `python ingestion/run.py` exits 0 → 385 items; `npx prisma db seed` exits 0
- psql: 385 Questions, 0 null difficultyTier, 8 QuotaConfig, 70+ Cases
- `pytest tests/ingestion -q` + `npx vitest run` both fully green
- ingestion-report.json: zero unresolved errors
</verification>

<success_criteria>
All four INGEST requirements satisfied at full volume: 385 validated, tagged, answer-keyed items + 8 verbatim quotas queryable in Postgres, with deterministic artifacts, atomic load, and the complete test suite green.
</success_criteria>

## Artifacts this phase produces (this plan)

- New module: `ingestion/difficulty_tags.py` (heuristic generator; 5 ordered rules)
- New artifact: `content/difficulty-tags.json` (385 slug→tier entries + `_meta` provenance block)
- Final artifacts: `content/questions.json` (385 items), `content/quotas.json` (8 rows), `content/ingestion-report.json` (counts/censuses/sha256/zero-errors)
- normalize.py hook: difficulty_tier application from tags file; validate.py hook: 100% tier-coverage enforcement
- seed.ts extension: QuotaConfig + IngestionRun inside the same transaction
- DB state: 385 Questions, 70+ Cases, 8 QuotaConfig, full Option key set, IngestionRun audit rows
- Test files: `tests/ingestion/test_ingest_e2e.py`, `tests/ingestion/test_tags.py`

<output>
Create `.planning/phases/01-content-ingestion-question-bank/01-06-SUMMARY.md` when done
</output>
