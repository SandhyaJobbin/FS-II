---
phase: 13-accessibility-f-05-remaining-polish
plan: 06
type: execute
wave: 2
depends_on:
  - 13-03
files_modified:
  - backend/AsyncGrading.gs
  - tests/backend/test_build_report_from_attempts_row.ts
autonomous: true
requirements:
  - REPORT-03
tags:
  - backend
  - transcripts
  - defensive-fallback

must_haves:
  truths:
    - `buildReportFromAttemptsRow(attemptId)` in backend/AsyncGrading.gs now attempts to read the `GradingTranscripts` sheet after loading the Attempts row and returns a `transcripts: TranscriptCandidatePayload[]` field on the report object, per D-03 (defensive fallback).
    - Transcript read failure (missing sheet OR read exception OR JSON.parse throw on criteriaMet cell) is caught, logged via `Logger.log`, and the function returns `transcripts: []` rather than throwing — the candidate report path never crashes on a legacy attempt with no transcript rows.
    - The candidate-facing transcript entries stripped server-side to include only: `qId`, `questionStem`, `verdict`, `criteriaMet` (parsed JSON), `response_type`. `rationale`, `overrideVerdict`, `overrideAt`, and any override-token audit fields MUST NOT appear on the candidate path, per D-01 (defense-in-depth vs frontend-only stripping).
    - The RECRUITER path (existing `handleGetAttemptTranscript` in backend/Code.gs) is UNCHANGED — recruiters still see the full transcript with rationale + override fields.
    - A Node/vitest unit test in tests/backend/test_build_report_from_attempts_row.ts covers three cases: (1) sheet present with matching rows returns stripped payload, (2) sheet present with zero matching rows returns transcripts: [], (3) sheet missing / read throws returns transcripts: [] and does not propagate the error.
    - No changes to Phase 9 scoring or tier logic, no changes to evaluateWithRubric, no changes to GradingTranscripts write path.
  artifacts:
    - Modified backend/AsyncGrading.gs — extended `buildReportFromAttemptsRow` returning stripped `transcripts`.
    - New tests/backend/test_build_report_from_attempts_row.ts — three-case vitest suite with a mock SpreadsheetApp shim.
  key_links:
    - backend/Code.gs `handleGetAttemptTranscript` (lines 476-500) — the read pattern to mirror per 13-PATTERNS.md § Sheet-scan.
    - Phase 10 A6 recruiter-only rationale contract — preserved by this plan's stripping.
    - Plan 13-07 (candidate ReportScreen breakdown) consumes the stripped `transcripts` field returned by this plan.
---

<objective>
Wave 2 backend: extend `buildReportFromAttemptsRow` to fetch matching GradingTranscripts rows, strip the recruiter-only fields (`rationale`, `overrideVerdict`, `overrideAt`), and attach them as `transcripts: []` on the candidate report payload — with a defensive try/catch fallback that returns `transcripts: []` on any read failure (D-03). This is defense-in-depth: even if plan 13-07's frontend does not filter, the server has already stripped.

Purpose: Phase 10 backend already writes GradingTranscripts. This plan is a pure READ extension for the candidate report path — no write path changes, no scoring changes, no evaluateWithRubric touch. The defensive fallback unblocks Wave 3 deploy from Phase 10-07's verify-status ambiguity per D-03.
Output: 1 modified backend file, 1 new vitest, no new dependencies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-accessibility-f-05-remaining-polish/CONTEXT.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-RESEARCH.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-PATTERNS.md
@backend/AsyncGrading.gs
@backend/Code.gs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing vitest that expects buildReportFromAttemptsRow to return stripped transcripts + fall back to [] on read failure</name>
  <files>tests/backend/test_build_report_from_attempts_row.ts</files>
  <read_first>
    - backend/AsyncGrading.gs lines 394-421 (current `buildReportFromAttemptsRow`).
    - backend/Code.gs lines 476-500 (`handleGetAttemptTranscript` — the read pattern that will be adapted).
    - tests/grading/test_rubric_grader.ts if it exists, OR tests/grading/test_grading.ts — for the local vitest + mock-SpreadsheetApp convention used elsewhere in this repo.
  </read_first>
  <behavior>
    - Test 1: sheet present with 2 matching rows → returns transcripts of length 2 where each entry has ONLY `qId`, `questionStem`, `verdict`, `criteriaMet` (array), `response_type`; entries do NOT have `rationale`, `overrideVerdict`, `overrideAt`, or any override-token hash field.
    - Test 2: sheet present with 0 matching rows → returns transcripts: [].
    - Test 3: sheet missing (getSheetByName returns null) → returns transcripts: [] and does NOT throw.
    - Test 4: sheet present but a criteriaMet cell contains malformed JSON → returns transcripts: [] for that row (or skips it) and does NOT throw for the whole report.
    - Test 5: all four assertions must still return the existing report fields (attemptId, name, email, overallScore, traitScores, recommendationTier, narrativeInsight, violationCount, ungradedCount) unchanged.
  </behavior>
  <action>
    Create `tests/backend/test_build_report_from_attempts_row.ts` using vitest. Introduce a minimal SpreadsheetApp shim that lets tests control (a) whether Attempts / GradingTranscripts sheets exist, (b) their row data, and (c) whether a getSheetByName call returns null. Because `buildReportFromAttemptsRow` lives in .gs and vitest cannot import .gs directly, mirror the function into a TS test-helper at tests/backend/build-report-from-attempts-row.ts (matching the existing tests/grading/grading-engine.ts mirror pattern). Include this mirror path in scripts/sync-check.ts drift assertions (append the pair to the existing sync-check config so future drift is caught in CI — do NOT skip this; the mirror-drift pattern is documented and enforced per prior observation 918).

    Run the vitest and confirm it FAILS in the RED step (function does not yet return `transcripts`). Commit as `test(13-06): add failing test for buildReportFromAttemptsRow transcripts stripping`.
  </action>
  <verify>
    <automated>test -f tests/backend/test_build_report_from_attempts_row.ts && test -f tests/backend/build-report-from-attempts-row.ts && npm test -- --run tests/backend/test_build_report_from_attempts_row.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Both the test file and mirror module exist.
    - `npm test -- --run tests/backend/test_build_report_from_attempts_row.ts` FAILS at this step (RED).
    - sync-check pair added for backend/AsyncGrading.gs `buildReportFromAttemptsRow` ↔ tests/backend/build-report-from-attempts-row.ts.
  </acceptance_criteria>
  <done>Failing test committed; specification is now executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transcripts read + strip + defensive fallback in backend/AsyncGrading.gs (GREEN)</name>
  <files>backend/AsyncGrading.gs, tests/backend/build-report-from-attempts-row.ts</files>
  <read_first>
    - backend/Code.gs lines 476-500 (the sheet-scan pattern to mirror).
    - backend/AsyncGrading.gs current `buildReportFromAttemptsRow` (lines 394-421).
    - 13-PATTERNS.md § "Sheet-scan + defensive missing-sheet guard".
  </read_first>
  <action>
    In `backend/AsyncGrading.gs`, extend `buildReportFromAttemptsRow(attemptId)`:

    1. Before the final `return` object, wrap a new block in try/catch:
       ```
       var transcripts = [];
       try {
         var transcriptsSheet = ss.getSheetByName("GradingTranscripts");
         if (transcriptsSheet) {
           var tData = transcriptsSheet.getDataRange().getValues();
           // Column indices: 0=attemptId, 1=qId, 2=rubricVersion, 3=verdict, 4=criteriaMet(JSON), 5=rationale, 6=overrideVerdict, 7=overrideAt, ...
           for (var t = 1; t < tData.length; t++) {
             if (tData[t][0] === attemptId) {
               var parsedCriteria = [];
               try {
                 parsedCriteria = JSON.parse(tData[t][4] || "[]");
               } catch (_) {
                 // Defensive: skip this row's criteria if malformed; do NOT crash the whole report.
                 parsedCriteria = [];
               }
               transcripts.push({
                 qId: tData[t][1],
                 questionStem: lookupQuestionStem(tData[t][1]),  // helper — reuse existing question lookup if present, otherwise pass through
                 verdict: tData[t][3],
                 criteriaMet: parsedCriteria,
                 response_type: lookupResponseType(tData[t][1])   // helper — same as above
               });
             }
           }
         }
       } catch (e) {
         Logger.log("[13-06] transcripts read failed for attemptId=" + attemptId + ": " + e);
         transcripts = [];
       }
       ```

    2. If `lookupQuestionStem` / `lookupResponseType` do not already exist in AsyncGrading.gs, either (a) use the existing question-lookup helper (search AsyncGrading.gs + Code.gs for `getQuestionById` or similar), or (b) return the qId as questionStem placeholder if no lookup exists — the frontend can fall back to displaying the qId. Do NOT invent new sheet reads; the goal is minimum blast radius.

    3. Add `transcripts: transcripts` to the returned object.

    4. Explicitly do NOT include `rationale`, `overrideVerdict`, `overrideAt`, or any override-token hash on the returned entries — this is the D-01 defense-in-depth strip. A comment above the transcripts.push block must read: `// D-01: candidate payload excludes rationale/override* per Phase 10 A6 recruiter-only contract.`

    5. Update the TS mirror at tests/backend/build-report-from-attempts-row.ts with the equivalent logic. Re-run the vitest — it must now PASS (GREEN).

    Commit as `feat(13-06): buildReportFromAttemptsRow includes stripped transcripts with defensive fallback`.
  </action>
  <verify>
    <automated>cd . && grep -c "transcripts" backend/AsyncGrading.gs ; echo "---"; grep -c "D-01" backend/AsyncGrading.gs ; echo "---"; grep -Ec "rationale|overrideVerdict|overrideAt" backend/AsyncGrading.gs | head -1 ; echo "---SYNC-CHECK---"; npx tsx scripts/sync-check.ts 2>&1 | tail -20 ; echo "---VITEST---"; npm test -- --run tests/backend/test_build_report_from_attempts_row.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `buildReportFromAttemptsRow` in backend/AsyncGrading.gs returns a `transcripts` array on the report object.
    - Each transcript entry contains exactly the 5 candidate-safe fields (qId, questionStem, verdict, criteriaMet, response_type) — no rationale, no overrideVerdict, no overrideAt, no override-token fields.
    - Defensive fallback returns `transcripts: []` on missing sheet, missing rows, JSON parse failure of a single row, or any read exception.
    - Comment citing D-01 present near the transcripts.push block.
    - The vitest passes (GREEN).
    - `npx tsx scripts/sync-check.ts` reports zero drift for the new mirror pair.
    - Existing Phase 9 report fields (attemptId, name, email, overallScore, traitScores, recommendationTier, narrativeInsight, violationCount, ungradedCount) unchanged in shape and value.
    - backend/AsyncGrading.gs line count stays under 900 (currently 760); if the read block pushes it materially closer to 1000, plan 13-08 will extract email builders to backend/Emails.gs which brings it well under.
  </acceptance_criteria>
  <done>Backend returns candidate-safe stripped transcripts with defensive fallback; tests pass; sync-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GradingTranscripts sheet → candidate browser payload | Server MUST strip recruiter-only fields (rationale, override*) before responding to the candidate report endpoint. |
| Candidate report endpoint → third-party mail forwards | Any content in the report response may end up quoted in email or shared externally; strip contract is the primary defense. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-14 | Information Disclosure | Recruiter-only rationale text leaks into candidate report payload | critical | mitigate | Task 2 explicitly excludes rationale/overrideVerdict/overrideAt from the transcript push; Task 1 test asserts these fields are absent; sync-check drift assertion locks the mirror. |
| T-13-15 | Denial of Service | GradingTranscripts read exception crashes the candidate report path for legacy attempts | high | mitigate | Task 2 wraps the read in try/catch returning transcripts: []; JSON.parse of criteriaMet also wrapped defensively per row. |
| T-13-16 | Tampering | Malformed criteriaMet JSON in one row poisons the entire report response | medium | mitigate | Per-row try/catch on JSON.parse in the read loop — one bad row is dropped, not the whole report. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies in this plan. |
</threat_model>

<verification>
- Task 1 automated: test file + mirror file exist; vitest FAILS at RED.
- Task 2 automated: vitest PASSES at GREEN; sync-check drift clean; grep confirms transcripts + D-01 comment present.
</verification>

<success_criteria>
- REPORT-03 unblocked on the backend side.
- D-01 defense-in-depth: server-side strip of recruiter-only fields verified by vitest.
- D-03 defensive fallback: transcripts read failure never crashes the candidate report path.
- Phase 10 recruiter transcript contract (handleGetAttemptTranscript in backend/Code.gs) unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-accessibility-f-05-remaining-polish/13-06-SUMMARY.md` when done. Reference the vitest run output and the sync-check status.
</output>

## Artifacts this phase produces

- **Modified:** `backend/AsyncGrading.gs` — extended `buildReportFromAttemptsRow` returns `transcripts: []` with candidate-safe stripped entries + defensive fallback.
- **New:** `tests/backend/test_build_report_from_attempts_row.ts` — three-case vitest suite (present/empty/missing/malformed JSON).
- **New:** `tests/backend/build-report-from-attempts-row.ts` — TS mirror of the .gs logic for vitest.
- **Modified:** `scripts/sync-check.ts` — new drift-check pair for the mirror above.
- **No changes to write path, scoring, evaluateWithRubric, or handleGetAttemptTranscript. No new dependencies.**
