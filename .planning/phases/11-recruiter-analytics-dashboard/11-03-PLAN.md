---
phase: 11-recruiter-analytics-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 11-02
files_modified:
  - assessment-app/package.json
  - assessment-app/package-lock.json
  - assessment-app/src/types/index.ts
  - assessment-app/src/components/analytics/LowNFallback.tsx
  - assessment-app/src/components/analytics/UngradedCaveat.tsx
  - assessment-app/src/components/analytics/ScoreTrendChart.tsx
  - assessment-app/src/components/analytics/QuestionStatsTable.tsx
  - assessment-app/src/components/analytics/ViolationScatter.tsx
  - assessment-app/src/components/analytics/BiasSignalsPanel.tsx
  - assessment-app/src/app/admin/analytics/page.tsx
  - assessment-app/src/app/admin/page.tsx
autonomous: false
requirements:
  - ADMIN-06
  - ADMIN-07
  - ADMIN-08
  - ADMIN-09
  - ADMIN-11
must_haves:
  truths:
    - A recruiter authenticated in `/admin` can navigate to `/admin/analytics` and see 4 charts rendered from the live `handleAdminAnalytics` payload.
    - Each analytics card shows either its chart or a `LowNFallback` component when its signal returned `enough_data: false` — cards are never hidden or blank.
    - Every card whose signal used the A2 denominator (excludes ungraded) renders an `UngradedCaveat` pill showing `{ungradedCount} answer(s) pending recruiter review — excluded from pass-rate.`
    - The Refresh button is debounced client-side to a minimum 3-second interval to protect UrlFetch quota (per Open Q5).
    - The "Distribution Monitor" panel is explicitly labeled as such (not "bias-direction" or "adverse-impact") and carries a persistent tooltip clarifying it does NOT measure demographic bias (per Pitfall 3).
  artifacts:
    - assessment-app/package.json + package-lock.json with recharts@3.10.1
    - AnalyticsPayload TypeScript interface in src/types/index.ts
    - 2 reusable notice components (LowNFallback, UngradedCaveat)
    - 4 chart components (ScoreTrendChart, QuestionStatsTable, ViolationScatter, BiasSignalsPanel)
    - New client route src/app/admin/analytics/page.tsx
    - Navigation entry from src/app/admin/page.tsx to /admin/analytics
  key_links:
    - Analytics page reads sessionStorage `fs_admin_token` (existing pattern from admin/page.tsx:45-55) and redirects to `/admin` if absent.
    - Fetch URL format `${gasUrl}${?|&}action=adminAnalytics&token=${encodeURIComponent(token)}` matches admin/page.tsx:66 pattern exactly.
    - All chart components import recharts primitives explicitly (no barrel import) to preserve tree-shaking (Pitfall 4).
---

<objective>
Wave 2 frontend. Ship the `/admin/analytics` route, its 4 chart components, 2 reusable caveat components (LowN + Ungraded), the `AnalyticsPayload` type, and a navigation entry from the existing admin panel. Renders the ADMIN-06/07/08/09/11 payload delivered by Wave 1 as a recruiter-facing dashboard using recharts@3.10.1.

Purpose: The visible half of Phase 11 — everything the recruiter interacts with. Design discipline: 4 cards, each independently gated by `enough_data`, each visually consistent with the existing dark-theme admin panel and the amber caveat pattern already established on ReportScreen.
Output: 1 new dep, 1 type extension, 6 new components, 1 new route, 1 navigation edit in the existing admin page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md
@.planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md
@.planning/phases/11-recruiter-analytics-dashboard/11-02-SUMMARY.md
@assessment-app/AGENTS.md
@assessment-app/package.json
@assessment-app/src/app/admin/page.tsx
@assessment-app/src/components/ReportScreen.tsx
@assessment-app/src/types/index.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — recharts@3.10.1</name>
  <files>assessment-app/package.json</files>
  <action>Human-only verification checkpoint. Do not run `npm install` in this task — Task 2 runs the install after this gate resolves. See how-to-verify for the required manual audit steps.</action>
  <what-built>Pre-install verification checkpoint (per RESEARCH.md § Package Legitimacy Audit + threat register T-11-SC).</what-built>
  <how-to-verify>
    Before running `npm install recharts@3.10.1`, verify at https://www.npmjs.com/package/recharts that:
    1. The package is published by the `recharts` maintainer team (github.com/recharts/recharts) — NOT a typosquat like `recharts-3`, `recharts-x`, or `re-charts`.
    2. Version 3.10.1 exists and its "Published" date matches roughly 2026-07-25 (per RESEARCH.md § Standard Stack VERIFIED note).
    3. Weekly downloads are in the millions and the repo has 20k+ stars on GitHub.
    4. React 19 is listed as a compatible peer.
    RESEARCH.md verdict: "OK — Approved" (audit table has ONE row, verdict `OK`). This checkpoint is a belt-and-braces confirmation before the install command runs, matching the Phase 10 pattern for external-service dependencies.
  </how-to-verify>
  <verify>
    <human-check>Recruiter/operator confirms all 4 npmjs.com audit points in the how-to-verify block.</human-check>
  </verify>
  <done>Human types "approved" (or "abort") — Task 2 blocks until this signal is received.</done>
  <resume-signal>Type "approved" to proceed with `npm install recharts@3.10.1`, or "abort" to stop this task and re-open the package audit.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install recharts + declare AnalyticsPayload types</name>
  <files>assessment-app/package.json, assessment-app/package-lock.json, assessment-app/src/types/index.ts</files>
  <read_first>
    - assessment-app/package.json (confirm React 19.2.4 and Next.js 16.2.12 already installed; no existing chart lib)
    - assessment-app/src/types/index.ts (existing Report, TranscriptEntry, TraitScores shapes to match style)
    - assessment-app/AGENTS.md ("This is NOT the Next.js you know" — read node_modules/next/dist/docs/ if App Router conventions are unclear)
    - .planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md § 10 (suggested AnalyticsPayload TypeScript shape)
    - .planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md § Aggregation Recipes (return shapes per reducer — must match server exactly)
  </read_first>
  <action>
    Step A — install:
    ```
    cd assessment-app && npm install recharts@3.10.1
    ```
    Commit package.json and package-lock.json together.

    Step B — types:
    Append the following interface to assessment-app/src/types/index.ts (match existing formatting: flat interfaces, `?:` for optional fields, string unions for enums, matching the Report + TranscriptEntry style already in the file). Interface content must be the exact contract Wave 1's `handleAdminAnalytics` returns:

    - `AnalyticsSignalMeta` — reusable `{ enough_data: boolean; threshold: number; samples: number }` subset that every card consumes.
    - `AnalyticsPayload` — top-level `{ success: boolean; generatedAt: string; meta: { totalAttempts: number; ungradedTotal: number; aggregationMs: number }; scoreTrend: ...; questionStats: ...; violationCorrelation: ...; discriminationIndex: ...; biasSignals: ... }`.
    - Nested shapes:
      - `scoreTrend: { enough_data: boolean; threshold_note: string; points: Array<{ date: string; avgScore: number; n: number }> }`
      - `questionStats: { threshold: number; rows: Array<{ qId: string; correct: number; incorrect: number; ungraded: number; sampleSize: number; passRate: number | null; enough_data: boolean }> }`
      - `violationCorrelation: { enough_data: boolean; samples: number; points: Array<{ v: number; s: number }>; r: number | null; interpretation?: 'weak' | 'moderate' | 'strong' }`
      - `discriminationIndex: { enough_data: boolean; threshold: number; rows: Array<{ qId: string; passTop: number; passBottom: number; delta: number; sampleTop: number; sampleBottom: number }> }`
      - `biasSignals: { enough_data: boolean; tierDistribution: Array<{ tier: 'Strong Fit' | 'Consider' | 'Not Recommended'; pct: number; n: number; window: 'last20' | 'allTime' }> }`
    Do NOT modify any existing interface — append only. Do NOT rename `AnalyticsPayload` — the frontend fetch return type keys off this name.
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment/assessment-app" && node -e "const p=require('./package.json'); if (p.dependencies.recharts !== '3.10.1') { console.error('recharts version mismatch:', p.dependencies.recharts); process.exit(1); }" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `assessment-app/package.json` `dependencies.recharts === "3.10.1"` (exact pin, no caret).
    - `assessment-app/package-lock.json` is regenerated and contains the recharts entry.
    - `npx tsc --noEmit` (inside assessment-app) passes with zero new errors — proves AnalyticsPayload compiles and no existing type broke.
    - `AnalyticsPayload` interface exists in src/types/index.ts and exports all six top-level fields (`success`, `generatedAt`, `meta`, `scoreTrend`, `questionStats`, `violationCorrelation`, `discriminationIndex`, `biasSignals`).
    - No changes to existing `Report`, `TranscriptEntry`, `TraitScores`, `Question` interfaces (grep confirms).
  </acceptance_criteria>
  <done>recharts is pinned and installed, types compile, and the new AnalyticsPayload contract mirrors Wave 1's return envelope 1:1.</done>
</task>

<task type="auto">
  <name>Task 3: Build reusable caveat components — LowNFallback + UngradedCaveat</name>
  <files>assessment-app/src/components/analytics/LowNFallback.tsx, assessment-app/src/components/analytics/UngradedCaveat.tsx</files>
  <read_first>
    - assessment-app/src/components/ReportScreen.tsx lines 181-200 (amber-notice pattern to copy — border-amber-400/30 bg-amber-400/10, framer-motion `fadeUp` variant, exact SVG icon + label + body layout)
    - .planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md § 7 (exact block-to-copy from ReportScreen + prop shape differences)
  </read_first>
  <action>
    Both components live under `assessment-app/src/components/analytics/` (new directory). Both use the `'use client'` directive at the top (rechars pattern requires client components; keep the analytics tree consistently client-side).

    `LowNFallback.tsx`:
    - Props: `{ threshold: number; samples: number; label?: string }` where `label` defaults to `'Not Enough Data Yet'`.
    - Copies the ReportScreen.tsx:184-200 block palette + framer-motion fadeUp variant verbatim.
    - Header pill text: `NOT ENOUGH DATA YET`.
    - Body text: `Need ≥{threshold} samples — currently {samples}.`
    - Icon: same info-circle SVG as ReportScreen (12/8/16 line/circle SVG).

    `UngradedCaveat.tsx`:
    - Props: `{ ungradedCount: number }`.
    - Same palette/motion pattern.
    - Header pill text: `DATA QUALITY NOTE`.
    - Body text: `{ungradedCount} answer(s) pending recruiter review — excluded from pass-rate.`
    - When `ungradedCount === 0`, component returns `null` (do not render an empty pill).

    Both files should be under 60 lines each. Keep the fadeUp variant local (do not extract to shared unless the analytics/ tree already has a variants file — it does not).
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment/assessment-app" && npx tsc --noEmit && grep -q "border-amber-400/30 bg-amber-400/10" src/components/analytics/LowNFallback.tsx && grep -q "border-amber-400/30 bg-amber-400/10" src/components/analytics/UngradedCaveat.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist, both start with `'use client';`, both compile under `npx tsc --noEmit`.
    - Both use the exact amber palette strings from ReportScreen.tsx:184-200 (border-amber-400/30 bg-amber-400/10 palette — grep verifies).
    - `UngradedCaveat` returns null when `ungradedCount === 0` (visible in the component body).
    - Both files under 60 lines.
  </acceptance_criteria>
  <done>Two reusable amber notice components exist, both matching the ReportScreen visual pattern for cross-app consistency.</done>
</task>

<task type="auto">
  <name>Task 4: Build 4 chart components + analytics route + admin nav entry</name>
  <files>assessment-app/src/components/analytics/ScoreTrendChart.tsx, assessment-app/src/components/analytics/QuestionStatsTable.tsx, assessment-app/src/components/analytics/ViolationScatter.tsx, assessment-app/src/components/analytics/BiasSignalsPanel.tsx, assessment-app/src/app/admin/analytics/page.tsx, assessment-app/src/app/admin/page.tsx</files>
  <read_first>
    - assessment-app/src/app/admin/page.tsx lines 1-120 (auth pattern, sessionStorage read, gasUrl handling, fetch + error state — the route pattern to mirror)
    - assessment-app/src/app/admin/page.tsx lines 358-444 (header layout, tier color map — reuse for BiasSignalsPanel)
    - assessment-app/AGENTS.md → node_modules/next/dist/docs/ (App Router conventions for Next.js 16.2.12; consult before authoring page.tsx)
    - .planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md § Common Pitfalls #4 (recharts import discipline — named primitives only, no barrel)
    - .planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md § 6 (analytics route pattern — auth + fetch + debounced refresh)
    - assessment-app/src/types/index.ts (AnalyticsPayload contract from Task 2)
  </read_first>
  <action>
    Each of the four chart components starts with `'use client';` and imports ONLY the primitives it needs from `recharts` (Pitfall 4 tree-shake discipline). No `import * as Recharts` and no barrel-style `import { LineChart, BarChart, ScatterChart, Cell, ... }` unless every symbol is actually used in that file.

    `ScoreTrendChart.tsx` (ADMIN-06):
    - Props: `{ signal: AnalyticsPayload['scoreTrend']; ungradedTotal: number }`.
    - If `!signal.enough_data`, render `<LowNFallback threshold={5} samples={signal.points.length} label="Not Enough Data Yet" />` and return.
    - Otherwise: `<ResponsiveContainer width="100%" height={280}>` wrapping `<LineChart data={signal.points}>` with `<XAxis dataKey="date" />`, `<YAxis domain={[0, 100]} />`, `<Tooltip />`, `<Line type="monotone" dataKey="avgScore" />`. Dark-theme axis colors matching existing admin panel palette.
    - Card footer: `<UngradedCaveat ungradedCount={ungradedTotal} />` (renders null if 0).
    - Imports: `import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts';`.

    `QuestionStatsTable.tsx` (ADMIN-07):
    - Props: `{ signal: AnalyticsPayload['questionStats']; ungradedTotal: number }`.
    - Renders a sortable table: columns `qId | sampleSize | passRate | ungraded`.
    - Rows with `enough_data === false` show `passRate` as a muted "—" with `sampleSize < threshold` next to it (inline "insufficient data" — do NOT hide the row).
    - Small horizontal bar next to each `passRate` cell using `<BarChart>` with a single bar per row (or a simple CSS bar — recruiter cue, not a full chart).
    - Card footer: `<UngradedCaveat ungradedCount={ungradedTotal} />`.

    `ViolationScatter.tsx` (ADMIN-08):
    - Props: `{ signal: AnalyticsPayload['violationCorrelation'] }`.
    - If `!signal.enough_data`, render `<LowNFallback threshold={10} samples={signal.samples} />` and return.
    - Otherwise: `<ScatterChart>` with `<XAxis dataKey="v" name="Violations" />`, `<YAxis dataKey="s" name="Score" domain={[0,100]} />`, `<Tooltip cursor={{ strokeDasharray: '3 3' }} />`, `<Scatter data={signal.points} />`.
    - Prominently display the Pearson r value AND the sample count together (never r without n — Pitfall / anti-pattern).
    - Interpretation label ("weak"/"moderate"/"strong") next to r.
    - Imports: `import { ScatterChart, Scatter, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts';`.

    `BiasSignalsPanel.tsx` (Distribution Monitor label — per Pitfall 3):
    - Props: `{ signal: AnalyticsPayload['biasSignals']; discrimination: AnalyticsPayload['discriminationIndex'] }`.
    - Header text MUST be `Distribution Monitor` (not "Bias-Direction Indicator" — see 11-RESEARCH.md § Pitfall 3).
    - Persistent tooltip / info button on the panel header displaying: `This chart shows how the mix of Strong Fit / Consider / Not Recommended has shifted between the last 20 attempts and all-time. It does NOT measure bias against any protected class — this system does not collect demographic data. See v2 ADMIN2-04 for adverse-impact monitoring.`
    - Renders the tier distribution as two stacked bars (last20 vs allTime), using the exact `tierColorMap` from admin/page.tsx:440-444 (Strong Fit = emerald, Consider = amber, Not Recommended = red).
    - Below the distribution: a discrimination-index sub-section showing the bottom-N (default 5) `discrimination.rows` with the smallest `delta` — this is ADMIN-09 living in the same visual card.
    - Both sub-sections independently gated by their own `enough_data`; render `LowNFallback` per-subsection when false.
    - Imports: `import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, Cell } from 'recharts';`.

    `src/app/admin/analytics/page.tsx` (route entry):
    - `'use client';` at the top.
    - Copy the auth+gasUrl pattern from admin/page.tsx:45-55 verbatim (sessionStorage `fs_admin_token` + localStorage `fs_gas_url` + env fallback).
    - If `!isAuthenticated` on mount, use Next.js 16 App Router client navigation (`useRouter().replace('/admin')`) — consult node_modules/next/dist/docs/ for the correct 16.2.12 idiom before writing this line.
    - Fetch: `${gasUrl}${gasUrl.includes('?') ? '&' : '?'}action=adminAnalytics&token=${encodeURIComponent(token)}` — exact string shape of admin/page.tsx:66.
    - State: `payload: AnalyticsPayload | null`, `loading`, `error`, `lastRefreshAt: number`.
    - Refresh button handler: check `Date.now() - lastRefreshAt < 3000` and short-circuit with a small "wait a moment" hint (Pitfall Open Q5 — 3s debounce). No polling / auto-refresh.
    - Layout: 4 cards in a 2×2 grid on desktop, single column on mobile. Card order: ScoreTrend, QuestionStatsTable, ViolationScatter, BiasSignalsPanel.
    - Header shows `Analytics generated at {payload.generatedAt} · {payload.meta.totalAttempts} attempts · {payload.meta.aggregationMs}ms` — surfaces Pitfall 1 monitoring to the recruiter.
    - Error/loading state: reuse the exact "Connection failed. Verify..." copy from admin/page.tsx:77.

    `src/app/admin/page.tsx` (nav entry — minimal edit):
    - Locate the header section (approximately lines 358-381 per PATTERNS.md § 6).
    - Add ONE navigation `<Link href="/admin/analytics">Analytics</Link>` (or the Next.js 16 equivalent from node_modules/next/dist/docs/) next to the existing logout/config controls, styled consistently.
    - Do NOT modify any other admin/page.tsx region (transcript panel, override controls, candidate list — all Phase 10 work is read-only per 11-PATTERNS.md § "Files planner should NOT touch").

    All chart components must keep to < 200 lines each. The route file should stay < 300 lines. If any component exceeds 300, extract sub-components before shipping.
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment/assessment-app" && npx tsc --noEmit && npx next build 2>&1 | tee /tmp/next_build.log && grep -q "Compiled successfully" /tmp/next_build.log && ! grep -qE "Distribution Monitor.*bias-direction|adverse.impact" src/components/analytics/BiasSignalsPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` inside assessment-app passes.
    - `npx next build` inside assessment-app succeeds (`Compiled successfully` in output).
    - All 4 chart components import ONLY specific primitives from recharts (no barrel import; grep `from 'recharts'` on each file shows a named-imports list, not `import * as`).
    - `BiasSignalsPanel.tsx` header text is `Distribution Monitor` (grep confirms). It does NOT contain the strings `bias-direction indicator` or `adverse-impact analysis` in visible copy.
    - `src/app/admin/analytics/page.tsx` uses `encodeURIComponent(token)` when building the fetch URL (grep confirms).
    - `src/app/admin/analytics/page.tsx` implements the 3-second Refresh debounce (grep for a 3000 or 3s literal near a Refresh handler).
    - Exactly ONE navigation link to `/admin/analytics` exists in `src/app/admin/page.tsx` (grep count).
    - Bundle inspection: the `/admin/analytics` chunk emitted by `next build` is under 200 KB gzipped (per Pitfall 4). If build output shows > 200 KB, split with dynamic imports before this task closes.
  </acceptance_criteria>
  <done>All 4 charts, both caveat components, the new route, and the nav entry are shipped; the route reads real payload from the Wave 1 endpoint; the recruiter can navigate `/admin → /admin/analytics` and back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GAS Web App | Client-side fetch carries the shared ADMIN_TOKEN via query string (existing pattern per Pitfall 5) |
| Recruiter → dashboard copy | UI labels shape recruiter expectations about the strength of the "bias" signal (Pitfall 3 compliance risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-09 | Repudiation / Legal | BiasSignalsPanel copy overreach | high | mitigate | Header text is fixed to "Distribution Monitor"; persistent tooltip explicitly disclaims demographic bias measurement; automated grep in Task 4 blocks "bias-direction" / "adverse-impact" strings from shipping. |
| T-11-10 | Information Disclosure | Token echoed into DOM / dev tools | low | accept | Token is stored in sessionStorage per existing project pattern; not embedded in HTML. Same risk profile as the 4 existing admin GETs. |
| T-11-11 | Denial of Service | Refresh spam burning UrlFetch quota | medium | mitigate | 3-second client-side debounce on Refresh button (Open Q5); no auto-refresh / polling; documented in Task 4 acceptance. |
| T-11-12 | Tampering | recharts supply-chain compromise | high | mitigate | Task 1 human checkpoint verifies npmjs.com listing before install; package pinned exact `3.10.1` (no caret); package-lock.json committed. |
| T-11-13 | Tampering | Non-idiomatic Next.js 16 App Router usage | medium | mitigate | AGENTS.md warning honored; Task 4 read_first requires consulting `node_modules/next/dist/docs/` before writing the route file. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | Task 1 blocking-human checkpoint gates the single `npm install recharts@3.10.1` command. |
</threat_model>

<verification>
- `cd assessment-app && npx tsc --noEmit` — zero errors.
- `cd assessment-app && npx next build` — Compiled successfully; `/admin/analytics` chunk under 200 KB gzipped.
- Manual navigation (deferred to Wave 3 live verify): recruiter logs in at `/admin`, clicks Analytics link, sees 4 cards populated from live server payload.
- Manual copy check: BiasSignalsPanel header renders `Distribution Monitor` in the browser, tooltip contains the demographic-bias disclaimer.
- `grep -c "import" src/components/analytics/*.tsx | awk -F: '{s+=$2} END {print s}'` — sanity check that recharts imports are per-file and named.
</verification>

<success_criteria>
- Recruiters can reach `/admin/analytics` from the existing admin panel and see all 4 analytics cards.
- Each card independently renders EITHER its chart OR a LowNFallback — never blank.
- Ungraded caveats appear on cards whose signals used the A2 denominator (per requirement ADMIN-11 + Phase 10 GRADE-07 continuity).
- The BiasSignalsPanel's labeling and disclaimer prevent misinterpretation as adverse-impact analysis (Pitfall 3 compliance).
- Bundle size for `/admin/analytics` stays under 200 KB gzipped.
- No unauthorized user can hit `/admin/analytics` — page redirects to `/admin` when session token absent.
</success_criteria>

<output>
Create `.planning/phases/11-recruiter-analytics-dashboard/11-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **New dep:** `recharts@3.10.1` (exact pin) — 4 chart primitives (LineChart, ScatterChart, BarChart, ResponsiveContainer + supporting) tree-shaken per Pitfall 4 discipline.
- **Type extension:** `AnalyticsPayload` interface in `src/types/index.ts` — 1:1 mirror of Wave 1's return envelope.
- **New components:**
  - `src/components/analytics/LowNFallback.tsx` — reusable "not enough data yet" amber notice.
  - `src/components/analytics/UngradedCaveat.tsx` — reusable ungraded-count amber pill (auto-hides at 0).
  - `src/components/analytics/ScoreTrendChart.tsx` — recharts LineChart (ADMIN-06).
  - `src/components/analytics/QuestionStatsTable.tsx` — sortable table with bar sparklines (ADMIN-07).
  - `src/components/analytics/ViolationScatter.tsx` — recharts ScatterChart + Pearson r + n (ADMIN-08).
  - `src/components/analytics/BiasSignalsPanel.tsx` — Distribution Monitor (tier drift) + bottom-N discriminating questions (ADMIN-09 co-located).
- **New route:** `src/app/admin/analytics/page.tsx` — auth-guarded client component; 4-card grid; 3s debounced Refresh; surfaces `generatedAt` + `aggregationMs` in the header.
- **Modified:** `src/app/admin/page.tsx` — single nav link added to `/admin/analytics`. No other region touched.
