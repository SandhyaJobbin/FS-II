---
phase: 08-cleanup-test-safety-net
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/grading/grading-engine.ts
  - tests/grading/test_grading.ts
autonomous: true
requirements:
  - BUG-01

must_haves:
  truths:
    - "gradeAttempt accepts optional llmResults param; open_text uses LLM result when provided"
    - "hybrid questions require BOTH MCQ correct AND LLM text correct when llmResults provided"
    - "Existing tests (no llmResults param) still pass — backward compatible"
    - "Divergence test catches mirror-vs-Code.gs drift on open_text/hybrid scoring"
  artifacts:
    - tests/grading/grading-engine.ts
    - tests/grading/test_grading.ts
  key_links:
    - "gradeAttempt signature change must not break test_assembly.ts or test_integrity.ts imports"
    - "LLM mock fixtures must match evaluateOpenTextBatch return shape: Record<qId, boolean>"
---

<objective>
Fix grading-engine.ts mirror to handle open_text and hybrid questions using LLM results (matching Code.gs lines 450-515). Add divergence-test fixtures that verify LLM-scored paths produce correct integrated scores.

Purpose: Current mirror treats open_text as always-correct (line 86) and hybrid only grades MCQ portion (lines 74-83). Code.gs calls evaluateOpenTextBatch and uses results for both types. Mirror must test LLM integration path.
Output: Fixed grading-engine.ts with optional llmResults param + divergence tests proving LLM score integration.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cleanup-test-safety-net/CONTEXT.md
@tests/grading/grading-engine.ts
@tests/grading/test_grading.ts
@backend/Code.gs
</context>

<tasks>

<task type="tracer">
  <name>Fix gradeAttempt to accept llmResults and handle open_text/hybrid correctly</name>
  <files>tests/grading/grading-engine.ts</files>
  <action>
Add optional 3rd parameter to gradeAttempt: `llmResults?: Record&lt;string, boolean&gt;`.

Fix open_text branch (currently lines 84-87, `isCorrect = true`): when `llmResults` provided, use `isCorrect = llmResults[q.id] === true`. When absent, keep `isCorrect = true` (matches Code.gs no-API-key fallback at line 71).

Fix hybrid branch (currently lines 74-83): when `llmResults` provided, require BOTH MCQ selection correct AND `llmResults[q.id] === true` (`isCorrect = mcqCorrect && textCorrect`). When absent, grade MCQ portion only (current behavior, matches Code.gs no-key fallback).

This matches Code.gs handleSubmitAnswers lines 499-515 exactly: hybrid = `mcqCorrect && textCorrect`, open_text = `llmResults[qId] === true`. Per D-01 (F-03 decision): mock LLM with fixture responses, verify score integration.

CRITICAL: Do not change function signature for existing callers. `llmResults` is optional. test_assembly.ts and test_integrity.ts import gradeAttempt with 2 args — must still work.
  </action>
  <verify>
    <automated>npx vitest run tests/grading/test_grading.ts tests/integrity/test_integrity.ts tests/assembly/test_assembly.ts</automated>
  </verify>
  <done>
    gradeAttempt accepts optional llmResults. open_text uses llmResults[q.id] when provided. hybrid requires MCQ + LLM when provided. All existing tests pass unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Add LLM-path divergence tests to test_grading.ts</name>
  <files>tests/grading/test_grading.ts</files>
  <behavior>
    - Test 1: open_text with llmResults={ot1: true} → counted correct
    - Test 2: open_text with llmResults={ot1: false} → counted incorrect
    - Test 3: open_text with no llmResults → defaults to correct (backward compat)
    - Test 4: hybrid with MCQ correct + llmResults={h1: true} → correct
    - Test 5: hybrid with MCQ correct + llmResults={h1: false} → incorrect (text fails)
    - Test 6: hybrid with MCQ wrong + llmResults={h1: true} → incorrect (MCQ fails)
    - Test 7: LLM scores integrate into overallScore correctly (weighted by question count)
    - Test 8: LLM scores affect trait scores for correct bank
    - Test 9: Divergence guard — if Code.gs scoring weight changes, fixture catches it
  </behavior>
  <action>
Add new describe block `DIVERGENCE — LLM-scored paths (F-03)` to test_grading.ts. Create fixture builders: `makeOpenTextQ(id, bank, difficulty)` and `makeHybridQ(id, bank, correctLetter, difficulty)`. Write tests covering all 9 behaviors above. Each test calls gradeAttempt with explicit llmResults map (e.g., `{ ot1: true, ot2: false }`). Verify overallScore, traitScores, and perQuestion[].isCorrect match expected integration. Per D-01: fixture responses like score 0.8 mapped to boolean, verify integration into final grade.
  </action>
  <verify>
    <automated>npx vitest run tests/grading/test_grading.ts</automated>
  </verify>
  <done>
    9+ new divergence tests pass. LLM-scored open_text and hybrid paths verified end-to-end. Fixtures catch scoring integration bugs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM API → grading logic | External API returns scores that affect candidate grades |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Tampering | llmResults param | low | accept | llmResults only passed in test context; production Code.gs calls evaluateOpenTextBatch internally. Mirror param is for fixture testing only. |
| T-08-03 | Information Disclosure | test fixtures | low | accept | Fixtures contain no real candidate data; synthetic only. |
| T-08-SC | Tampering | npm installs | high | mitigate | No new packages; vitest already in devDependencies. |
</threat_model>

<verification>
npx vitest run tests/grading/test_grading.ts tests/integrity/test_integrity.ts tests/assembly/test_assembly.ts
</verification>

<success_criteria>
grading-engine.ts handles open_text/hybrid with LLM results matching Code.gs. 9+ divergence tests pass. All existing tests still pass (backward compatible).
</success_criteria>

<output>
Create `.planning/phases/08-cleanup-test-safety-net/08-02-SUMMARY.md` when done
</output>
