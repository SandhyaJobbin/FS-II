---
phase: 01-content-ingestion-question-bank
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - ingestion/parsers/attention.py
  - tests/ingestion/test_attention_e2e.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03]

must_haves:
  truths:
    - "Attention doc yields exactly 160 items across 40 cases (Level 1: 20 cases, Level 2: 20 cases), 4 questions per case"
    - "Marker census reconciles exactly: 125 ✅, 111 ☑, 49 ☐ — mismatch is a loud failure, never a warning"
    - "All 5 real docx tables land inside their enclosing case/tab (document-order binding) — zero tables orphaned"
    - "Per-case dashboard tab names stored verbatim (28 distinct names exist; typically 2–3 tabs per case) — no fixed five-tab assumption"
    - "Multi-line option paragraphs (options sharing one paragraph via embedded newlines, e.g. L2 Case 1 Q1) split into distinct options with correct keys"
    - "Multi-select items carry ≥1 ☑ (correct) AND ≥1 ☐ (incorrect) — polarity preserved, never inverted"
    - "Every Attention item carries level tag (L1/L2) + case_id grouping (INGEST-02)"
  artifacts:
    - ingestion/parsers/attention.py
    - tests/ingestion/test_attention_e2e.py
  key_links:
    - "iter_block_items (01-02) interleaves paragraphs + tables in document order — the only way tables bind to cases"
    - "markers.checkbox_pair and markers.trailing_check reused from 01-02 — no extractor duplication"
  prohibitions:
    - "Never collapse or rename the 28 distinct dashboard tab names into UI-04's assumed fixed five (Customer Report / Booking Details / Review Information / Property Listing Information / Account Information) — the store keeps per-case tab names verbatim and Phase 4 renders arbitrary tab sets"
---

<objective>
Expansion: Attention to Detail parser — the hardest document. 40 dashboard cases × 4 questions across two levels, with per-case multi-tab dashboards (28 distinct tab names), 5 embedded docx tables carrying evidence, two marker conventions (trailing ✅ single-answer; ☑/☐ multi-select), and multi-line option paragraphs.

Purpose: INGEST-01/02 for 160 items; the case/tab/table structures this parser emits are what Prisma Case/CaseTab/CaseTable models were shaped for in the tracer — Phase 4's dashboard UI reads exactly this shape.
Output: full Attention bank parsed and e2e-tested against the real docx.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md
@.planning/phases/01-content-ingestion-question-bank/01-PATTERNS.md
@.planning/phases/01-content-ingestion-question-bank/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: parsers/attention.py — levels, cases, verbatim tabs, table binding, both marker conventions</name>
  <files>ingestion/parsers/attention.py</files>
  <read_first>
    - ingestion/parsers/blocks.py (iter_block_items — mandatory for table interleaving)
    - ingestion/parsers/markers.py (trailing_check, checkbox_pair, split_lines — reuse)
    - ingestion/normalize.py (Item contract: case_id, case_title, tabs, tables fields)
    - .planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md (Attention anatomy: LEVEL 1/2 headings, CASE N: TITLE shape, Tab K: name blocks, 28-name census, 5 table headers, marker census 125/111/49, L2 Case 1 multi-line options; Pitfalls 2/3/7)
  </read_first>
  <action>
    Expose `BANK_ID = "attention"` + `parse(source_dir)` (run.py registry contract — zero run.py edits).
    Walk `iter_block_items` in document order. Level headings: regex `LEVEL 1: REVIEW & LISTING ACCURACY INVESTIGATION` / `LEVEL 2: ACCOUNT & FRAUD PATTERN INVESTIGATION` → level tag L1/L2. Case headings: regex `^CASE (\d+):\s*(.+)` → case_id `attn-l{1|2}-case-{n:02d}`, case_title verbatim.
    Dashboard tabs: `Tab K: <name>` blocks — store each tab as {name VERBATIM, position K, content lines}; arbitrary count per case (typically 2–3); never validate against a fixed name list.
    Tables: when a Table block appears, bind to the CURRENT case + tab context; store as {context: tab name or case, rows: list of row-cell-lists, position}. After parse, assert every table landed inside a case (5 total per manifest).
    Questions: after `Questions` line, `Q1.`–`Q4.` stems; option lines `A. text`–`D. text` — ALWAYS split_lines on paragraph text before the option regex (multi-line paragraphs verified at L2 Case 1 Q1). Marker selection per question: if any line leads with ☑/☐ → `checkbox_pair` (response_type mcq_multi; require ≥1 of each glyph); else `trailing_check` (response_type mcq_single; exactly 1 trailing ✅).
    Keep a running marker census (✅/☑/☐ counts); validate.py's reconciliation asserts 125/111/49 and tables==5 and cases==40 and questions==160 for this bank — extend validate.py ONLY if the census hook is missing (it was designed for censuses in 01-02; check first, do not rewrite).
    File under 500 lines (CLAUDE.md).
  </action>
  <verify>
    <automated>$env:PYTHONIOENCODING="utf-8"; python ingestion/run.py --banks attention; python -c "import json; d=json.load(open('content/questions.json',encoding='utf-8')); a=[i for i in d['items'] if i['bank']=='attention']; print(len(a), len({i['case_id'] for i in a}), sum(len(i['tables']) for i in a if i['tables']))"</automated>
  </verify>
  <acceptance_criteria>
    - run.py exits 0; printed: 160 items, 40 distinct case_ids, ≥5 table bindings
    - ingestion-report.json census for attention == {✅: 125, ☑: 111, ☐: 49} exactly
    - 20 cases tagged L1, 20 cases tagged L2; each case has exactly 4 questions
    - ≥20 distinct tab names across the doc; none renamed to a fixed five
    - Zero tables with null case binding
    - L2 Case 1 Q1 options count ≥2 (multi-line paragraph split correctly)
  </acceptance_criteria>
  <done>160 Attention items in 40 cases with verbatim tabs, 5 bound tables, exact marker census, and correct mcq_single/mcq_multi split.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attention e2e tests against the real docx</name>
  <files>tests/ingestion/test_attention_e2e.py</files>
  <read_first>
    - tests/conftest.py (fixture conventions; malformed corpus)
    - ingestion/parsers/attention.py, ingestion/parsers/markers.py, ingestion/validate.py (code under test)
    - .planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md (Pitfall 3: polarity inversion; Pitfall 7: detached tables)
  </read_first>
  <behavior>
    - Full Attention parse → 160 items / 40 cases / 20+20 levels / 4 questions per case
    - Census assertions: 125 ✅, 111 ☑, 49 ☐
    - Multi-select item: ≥1 correct AND ≥1 incorrect option (polarity: ☐ never stored as correct)
    - Single-answer item: exactly 1 correct
    - 5 tables present, each bound to a case, with non-empty rows
    - L2 Case 1 Q1 (verified multi-line paragraph) yields separate options A and B
    - Tab names verbatim: collect distinct names → ≥20; spot-assert presence of `Review Information`, `Account Profile`, `Review Activity`
    - Malformed fixture: multi-select question where ALL options are ☑ (no ☐) → validation raises naming the item (polarity invariant)
    - Malformed fixture: single-answer question with 2 trailing ✅ → raises naming item
  </behavior>
  <action>
    Create tests/ingestion/test_attention_e2e.py against the REAL `Fraud support/FS Question Bank_Attention to Detail V2.docx` (path from manifest.SOURCE_DIR). Malformed fixtures built in-test via python-docx (copy real doc, flip one ☐ run to ☑ / append a second ✅) into tmp_path, then subprocess run.py asserting exit ≠ 0 and item identity in stderr. All I/O encoding="utf-8".
  </action>
  <verify>
    <automated>pytest tests/ingestion/test_attention_e2e.py -q; pytest tests/ingestion -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ingestion/test_attention_e2e.py -q` exits 0 covering every behavior line above
    - Full suite `pytest tests/ingestion -x -q` green (no regressions)
    - Both malformed tests assert exit ≠ 0 AND item identity in stderr
  </acceptance_criteria>
  <done>Attention bank proven end-to-end: counts, census, polarity, table binding, verbatim tabs, multi-line split, and two loud-failure polarity/marker cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Attention docx → parser | Untrusted complex office file (tables, two marker systems, multi-line paragraphs) |
| case/tab/table structures → DB | Evidence content the candidate UI will render verbatim in Phase 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Tampering | Multi-select polarity inversion (☐ read as correct, or ☑/☐ stripped) mis-keys ~37 questions | high | mitigate | Explicit two-glyph mapping in checkbox_pair; invariant ≥1 ☑ AND ≥1 ☐; census reconciliation 111/49; malformed all-☑ fixture test |
| T-01-12 | Tampering | Dashboard tables detached from cases or dropped (evidence loss → unanswerable questions) | high | mitigate | Document-order iter_block_items binding; manifest asserts tables==5; every table must land inside a case; e2e row-content assertions |
| T-01-13 | Tampering | Marker misattribution when options share one paragraph (key lands on wrong option) | high | mitigate | split_lines before option regex; L2 Case 1 Q1 golden shape test; per-question correct-count invariants |
| T-01-SC | Tampering | npm/pip installs | high | mitigate | No new installs in this plan; covered by 01-RESEARCH.md audit |
</threat_model>

<verification>
- `python ingestion/run.py --banks attention` exits 0 with 160/40/5
- Census exactly 125 ✅ / 111 ☑ / 49 ☐ in ingestion-report.json
- `pytest tests/ingestion -x -q` green including test_attention_e2e.py
</verification>

<success_criteria>
Hardest document fully ingested: 160 items, 40 cases, verbatim tab names, bound tables, polarity-correct multi-select keys — all census-verified against manifest.
</success_criteria>

## Artifacts this phase produces (this plan)

- New module: `ingestion/parsers/attention.py` (`BANK_ID = "attention"`, `parse(source_dir)`; case_id scheme `attn-l{1|2}-case-{n:02d}`)
- Data shapes exercised: Case with verbatim CaseTab entries + bound CaseTable rows; mcq_multi via ☑/☐ polarity
- Test file: `tests/ingestion/test_attention_e2e.py` (census, polarity, tables, tabs, multi-line, 2 malformed fixtures)
- Emitted artifact: content/questions.json with 160 Attention items (regenerated; final full-bank emit in 01-06)

<output>
Create `.planning/phases/01-content-ingestion-question-bank/01-04-SUMMARY.md` when done
</output>
