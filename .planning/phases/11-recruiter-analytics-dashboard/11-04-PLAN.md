---
phase: 11-recruiter-analytics-dashboard
plan: 04
type: execute
wave: 3
depends_on:
  - 11-03
files_modified:
  - .planning/phases/11-recruiter-analytics-dashboard/11-04-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - ADMIN-06
  - ADMIN-07
  - ADMIN-08
  - ADMIN-09
  - ADMIN-11
user_setup:
  - service: google-apps-script
    why: "Manual paste + Save + Deploy of backend/Code.gs and new backend/Analytics.gs into the existing Apps Script project. Same manual-deploy shape as Phases 9 and 10 (no clasp configured — verified from Phase 10 7:05p observation)."
    dashboard_config:
      - task: "Open the Apps Script project bound to the FS Attempts spreadsheet"
        location: "Google Drive → Extensions → Apps Script (of the Attempts sheet)"
      - task: "Paste backend/Code.gs contents into the Code.gs file in the Apps Script editor"
        location: "Apps Script editor → Code.gs"
      - task: "Create a new Analytics.gs file in the Apps Script editor and paste backend/Analytics.gs contents"
        location: "Apps Script editor → + → Script → name it Analytics"
      - task: "Save both files (Ctrl+S / Cmd+S)"
        location: "Apps Script editor"
      - task: "Deploy → Manage deployments → Edit the existing Web App deployment → New version → Deploy"
        location: "Apps Script editor → Deploy button"
      - task: "Copy the resulting Web App URL and confirm it matches the URL cached in localStorage.fs_gas_url (recruiter's browser). If it differs, clear localStorage and re-enter the new URL — Phase 9 verification learned that stale cached URLs are the #1 live-verify failure mode."
        location: "Apps Script editor → Deploy dialog"
  - service: vercel-or-hosting
    why: "Push assessment-app changes (recharts install + new analytics route + admin nav) to the hosting used for the Next.js app so the browser can load /admin/analytics."
    dashboard_config:
      - task: "Push the merged branch to trigger the existing Next.js deploy pipeline (or run `next build && next start` for a local verify)."
        location: "Repo host (existing deploy trigger)"
must_haves:
  truths:
    - GradingTranscripts + Attempts.UngradedCount are confirmed populated for at least one live attempt post-deploy (mitigates Phase 10 A4 risk).
    - Live `handleAdminAnalytics(token)` call returns a 200 with a valid AnalyticsPayload envelope for a real recruiter session.
    - `meta.aggregationMs` on live data is captured and recorded in this plan's SUMMARY (baseline for Pitfall 1 monitoring).
    - Recruiter sees 4 populated cards OR clearly-labeled LowNFallback panels — never a blank card, never a raw error banner.
    - REQUIREMENTS.md traceability table maps ADMIN-06..09 and ADMIN-11 to Phase 11, and ROADMAP Phase 11 Requirements line no longer says "TBD".
  artifacts:
    - .planning/phases/11-recruiter-analytics-dashboard/11-04-SUMMARY.md with recorded aggregationMs + verify screenshots/notes.
    - Updated REQUIREMENTS.md traceability rows (5 rows: ADMIN-06 through ADMIN-09, ADMIN-11).
    - Updated ROADMAP.md Phase 11 block: Requirements line, ADMIN-10 backlog note, and plan checklist marked complete.
  key_links:
    - Phase 10 plan 10-07 Task 2 (deferred live-verify) is confirmed OR the defensive-fallback strategy is documented before this wave proceeds (checkpoint 1 gate).
    - Backlog entry filed for ADMIN-10 (LLM narrative digest) since it is deferred out of Phase 11 v1 per RESEARCH.md Assumption A7.
---

<objective>
Wave 3 deployment + live verify + docs cleanup. Verify Phase 10 data quality first (mitigate A4 risk), deploy backend + frontend, measure live `aggregationMs`, confirm each of the 4 analytics cards renders correctly under real recruiter session against real attempt data, and close out documentation (REQUIREMENTS traceability, ROADMAP finalization, ADMIN-10 backlog note). Delivers the "phase actually shipped" gate for ADMIN-06/07/08/09/11.

Purpose: Same shape as Phase 10-07 — the paste-and-deploy gate that Phase 9 learned is easy to skip and hard to un-skip. Without this wave, the dashboard exists in the repo but not in the browser.
Output: 1 new SUMMARY.md with recorded live measurements; 2 documentation files updated to close the traceability loop.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md
@.planning/phases/11-recruiter-analytics-dashboard/11-01-SUMMARY.md
@.planning/phases/11-recruiter-analytics-dashboard/11-02-SUMMARY.md
@.planning/phases/11-recruiter-analytics-dashboard/11-03-SUMMARY.md
@.planning/phases/10-rubric-based-llm-grading-recruiter-transcript-override/10-07-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: A4 dependency gate — Phase 10 live-verify status</name>
  <files>.planning/phases/10-rubric-based-llm-grading-recruiter-transcript-override/10-07-PLAN.md</files>
  <action>Human-only gate. Confirm Condition A (Phase 10 live-verify complete + GradingTranscripts populated) OR Condition B (defensive-fallback risk acknowledged in writing). See how-to-verify for the specific sheet checks and the exact resume-signal wording. Task 2 must not fire until this checkpoint resolves.</action>
  <verify>
    <human-check>Human confirms Condition A via sheet inspection OR types the Condition B risk acknowledgment.</human-check>
  </verify>
  <done>Resume-signal received (`condition-a-verified`, `condition-b-accepted: <note>`, or `abort-to-run-10-07-first`).</done>
  <what-built>Pre-deploy gate on the Phase 10 A4 assumption: GradingTranscripts + Attempts.UngradedCount are populated for live attempts. Analytics denominators rely on these being correct.</what-built>
  <how-to-verify>
    Manually confirm ONE of the following two conditions before continuing:

    CONDITION A — Phase 10 verify is done:
    1. Open the Attempts sheet and confirm the newest post-Phase-10-deploy attempt has a non-empty `UngradedCount` (column O).
    2. Open the GradingTranscripts sheet and confirm at least one row exists for that attempt (with a Verdict value in {correct, incorrect, ungraded}).
    3. If both are true → the analytics denominator will be correct on live data → proceed.

    CONDITION B — Phase 10 verify is still deferred:
    1. Confirm this by reading `.planning/phases/10-rubric-based-llm-grading-recruiter-transcript-override/10-07-PLAN.md` Task 2 status.
    2. If Condition A is NOT met, document the defensive-fallback plan explicitly here: the backend already coerces `Number(row[14]) || 0` for legacy attempts (Wave 1 Task 1 acceptance criteria), and `effectiveVerdict` returns `"ungraded"` when neither transcript nor response row indicates a verdict — so analytics will still render, but the `ungradedTotal` on the dashboard header will be low for legacy attempts.
    3. Accept that risk in writing (paste your acknowledgment into the resume signal) before proceeding.

    Do NOT proceed to Task 2 until one of the two conditions is confirmed.
  </how-to-verify>
  <resume-signal>Type one of: "condition-a-verified", "condition-b-accepted: <one-line risk note>", or "abort-to-run-10-07-first".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Deploy backend/Code.gs + backend/Analytics.gs to Apps Script</name>
  <files>backend/Code.gs, backend/Analytics.gs</files>
  <action>Human-only deployment. Clasp is not configured (verified 2026-07-31 observation 809); paste-and-deploy in the Apps Script editor is the only path. Follow the 8-step how-to-verify block exactly — pasting Code.gs first, then creating a new Analytics.gs file, saving both, redeploying the existing Web App as a new version, and re-syncing the browser's cached URL if it differs. Task 3 must not fire until the operator confirms `deployed`.</action>
  <verify>
    <human-check>Human confirms the Web App deployment succeeded and `/admin` loads without console errors.</human-check>
  </verify>
  <done>Resume-signal received (`deployed` or `blocked: <error>`).</done>
  <what-built>Live deployment of Wave 1's server changes. No clasp is configured (per 7:05p 2026-07-31 observation), so this is a manual paste-and-deploy — same shape as Phase 9-06 and Phase 10-07.</what-built>
  <how-to-verify>
    1. Open the Apps Script project bound to the FS Attempts spreadsheet (Google Drive → open the sheet → Extensions → Apps Script).
    2. Select Code.gs in the editor sidebar. Paste the full contents of the repo's `backend/Code.gs` over the existing file. Save (Ctrl+S / Cmd+S).
    3. Create a new script file: sidebar + button → Script → name it exactly `Analytics` (Apps Script appends `.gs` automatically). Paste the full contents of the repo's `backend/Analytics.gs`. Save.
    4. Verify AsyncGrading.gs is unchanged (this wave should NOT modify it — Wave 1 only added a sibling file).
    5. Deploy → Manage deployments → the existing Web App deployment → pencil-edit icon → Version: `New version` → Description: `Phase 11 — analytics endpoint`. Click Deploy.
    6. Copy the new Web App URL from the deploy dialog.
    7. In an incognito browser, open the app admin panel. If the Analytics link 404s or `handleAdminAnalytics is not defined` shows in the console, the paste missed a file — re-check Analytics.gs was created and saved.
    8. Paste the new Web App URL into the browser's admin panel config if it differs from the cached `localStorage.fs_gas_url` (recall: Phase 9 verification failed on the first attempt due to a stale cached URL — the fix is to re-enter the URL in an incognito window and re-authenticate).
  </how-to-verify>
  <resume-signal>Type "deployed" once the new deployment is live and the /admin panel loads without errors, or "blocked: <error>" to surface a deploy problem.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy assessment-app + capture live verification evidence</name>
  <files>.planning/phases/11-recruiter-analytics-dashboard/11-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09-async-grading-report-delivery-pipeline/09-06-SUMMARY.md (Phase 9 live-verify SUMMARY format — copy this shape)
    - .planning/phases/11-recruiter-analytics-dashboard/11-03-SUMMARY.md (front-end artifacts to point at)
    - .planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md § Common Pitfalls #1 (aggregationMs threshold at 2000ms → CacheService upgrade trigger)
  </read_first>
  <action>
    Step A — deploy assessment-app: push the merged branch to trigger the existing Next.js deploy pipeline (per user_setup declaration). Confirm the deploy succeeded and `/admin/analytics` returns HTTP 200 in a fresh incognito session.

    Step B — live verification checklist (perform manually in the browser, then capture results in the SUMMARY):
      1. Log in at `/admin` with the recruiter passcode.
      2. Click the `Analytics` nav link → confirm `/admin/analytics` renders without console errors.
      3. Confirm all 4 cards render — either populated charts OR clearly-labeled `LowNFallback` panels. Screenshot each card state (attach filenames to SUMMARY).
      4. Read the page header — record the reported `meta.aggregationMs` value and `meta.totalAttempts` count in the SUMMARY.
      5. Click Refresh once — confirm it re-fetches. Click Refresh again within 2 seconds — confirm the 3-second debounce prevents a second fetch (observable via network tab).
      6. Confirm the BiasSignalsPanel header text renders as `Distribution Monitor` and the disclaimer tooltip is present.
      7. Confirm at least one `UngradedCaveat` pill renders on either the ScoreTrend or QuestionStats card (proves ungraded surfacing works end-to-end; if `ungradedTotal === 0` for every live attempt, note it explicitly — this is a data condition, not a bug).
      8. Open the browser network tab and confirm the `adminAnalytics` request URL contains `token=` (existing project pattern per Pitfall 5 — expected, not a bug).
      9. Log out of `/admin` in one tab; in a fresh incognito tab, navigate directly to `/admin/analytics` — confirm it redirects to `/admin` (unauthorized access blocked at the client route level).

    Step C — write `.planning/phases/11-recruiter-analytics-dashboard/11-04-SUMMARY.md`. Follow the standard SUMMARY template. MUST include:
      - Deployment result (Apps Script version, Next.js commit SHA, deploy URL).
      - Live-verify checklist results (each of the 9 items above with observed outcome).
      - Recorded `meta.aggregationMs` value + `meta.totalAttempts` at time of test.
      - If `aggregationMs > 2000ms`, add a follow-up backlog line for the CacheService upgrade path (Pitfall 1).
      - Any deviations from the plan.
      - Explicit note about ADMIN-10 (LLM narrative digest) being deferred out of Phase 11 v1 per RESEARCH.md Assumption A7 — pointer to the backlog entry filed in Task 4.
  </action>
  <verify>
    <automated>test -f "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment/.planning/phases/11-recruiter-analytics-dashboard/11-04-SUMMARY.md" && grep -q "aggregationMs" "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment/.planning/phases/11-recruiter-analytics-dashboard/11-04-SUMMARY.md" && grep -q "Distribution Monitor" "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment/.planning/phases/11-recruiter-analytics-dashboard/11-04-SUMMARY.md"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY.md file exists at the expected path.
    - SUMMARY contains a numeric `aggregationMs` observation.
    - SUMMARY records the 9-item verification checklist with an observed outcome per item.
    - SUMMARY includes the ADMIN-10 deferral note pointing to the backlog entry from Task 4.
    - If `aggregationMs > 2000ms` was observed, SUMMARY includes a CacheService follow-up line (Pitfall 1).
  </acceptance_criteria>
  <done>Backend + frontend are live, all 4 cards verified in browser, aggregationMs baseline recorded, and the SUMMARY captures everything needed for future forensics.</done>
</task>

<task type="auto">
  <name>Task 4: Close the documentation loop — REQUIREMENTS + ROADMAP + backlog ADMIN-10</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 198-203 (5 rows: ADMIN-06..09 + ADMIN-11 currently marked "TBD (roadmap pending)")
    - .planning/REQUIREMENTS.md line 202 (ADMIN-10 row — must become "Phase 11.5 or backlog")
    - .planning/ROADMAP.md lines 222-235 (Phase 11 block — Requirements line, Plans line, plan checklist)
    - .planning/ROADMAP.md lines 290+ (Backlog section format — copy the 999.1/999.2/999.3 template shape)
  </read_first>
  <action>
    Step A — REQUIREMENTS.md traceability update (scoped Edits, one per row):
    For each of ADMIN-06, ADMIN-07, ADMIN-08, ADMIN-09, ADMIN-11 in the traceability table:
      - Change `| ADMIN-XX | TBD (roadmap pending) | Pending |` → `| ADMIN-XX | Phase 11: Recruiter Analytics Dashboard | Complete |` (or `| ADMIN-XX | Phase 11: Recruiter Analytics Dashboard | Pending |` if this task fires before all Wave 3 verify items are ticked — set the status to match observed reality from Task 3).
    For ADMIN-10:
      - Change `| ADMIN-10 | TBD (roadmap pending) | Pending |` → `| ADMIN-10 | Backlog 999.4 (deferred out of Phase 11 v1 per RESEARCH.md Assumption A7) | Deferred |`.

    Step B — ROADMAP.md Phase 11 block update (scoped Edits only — DO NOT rewrite the whole file):
    - Line ~226: `**Requirements**: TBD (core analytics dashboard, bias-direction indicator)` → `**Requirements**: ADMIN-06, ADMIN-07, ADMIN-08, ADMIN-09, ADMIN-11 (ADMIN-10 deferred to Backlog 999.4 — Distribution Monitor label used instead of "bias-direction indicator" per RESEARCH.md Pitfall 3)`.
    - Line ~234: `**Plans**: TBD` → `**Plans**: 4 plans`.
    - Immediately after the Plans line, add:
      ```
      Plans:
      - [x] 11-01-PLAN.md — Wave 0 test infra: analytics-reducers mirror + parity spec + sync-check drift assertions
      - [x] 11-02-PLAN.md — Wave 1 backend: handleAdminAnalytics + 5 reducers in new backend/Analytics.gs + doGet router
      - [x] 11-03-PLAN.md — Wave 2 frontend: recharts@3.10.1 + AnalyticsPayload types + 4 charts + 2 caveats + /admin/analytics route + admin nav entry
      - [x] 11-04-PLAN.md — Wave 3 deploy + live verify + docs cleanup
      ```
      (Set `[x]` per observed completion state at commit time; leave `[ ]` if a specific plan's SUMMARY isn't final yet.)
    - Line ~281 (Progress table row for Phase 11): update `Plans Complete` from `0/0` to `4/4` (or the actual current tally) and `Status` from `Not started` to `Complete`. Add today's date under `Completed`.

    Step C — Backlog entry for ADMIN-10 (append to ROADMAP.md Backlog section, after existing 999.1/999.2/999.3):
    ```
    ### Phase 999.4: LLM-generated analytics narrative digest — ADMIN-10 (BACKLOG)

    **Goal:** Captured for future planning. RESEARCH.md § Open Questions Q4 and § Assumption A7 recommended deferring ADMIN-10 (LLM-generated narrative digest summarizing the dashboard) out of Phase 11 v1 for two reasons: (1) each additional signal shipped tightens the 6-min Apps Script cap risk (Pitfall 1) — an LLM call inside handleAdminAnalytics compounds it; (2) an LLM digest introduces a new failure mode ("digest failed to generate") that needs its own fallback UI. Ship the deterministic 4-card dashboard first (Phase 11), promote this backlog item once live aggregationMs is stable and recruiter feedback confirms the digest is desired.
    **Requirements:** ADMIN-10
    **Plans:** 0 plans

    Plans:
    - [ ] TBD (promote with /gsd-review-backlog when ready)
    ```

    Use `Edit` (scoped replacements) throughout — never `Write` a whole file for ROADMAP.md or REQUIREMENTS.md.
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment" && grep -q "ADMIN-06 | Phase 11" .planning/REQUIREMENTS.md && grep -q "ADMIN-10 | Backlog 999.4" .planning/REQUIREMENTS.md && grep -q "Requirements..: ADMIN-06, ADMIN-07, ADMIN-08, ADMIN-09, ADMIN-11" .planning/ROADMAP.md && grep -q "999.4" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md traceability rows for ADMIN-06/07/08/09/11 now point to `Phase 11: Recruiter Analytics Dashboard`.
    - REQUIREMENTS.md row for ADMIN-10 now points to `Backlog 999.4` with status `Deferred`.
    - ROADMAP.md Phase 11 `**Requirements:**` line lists the 5 concrete requirement IDs (no more "TBD").
    - ROADMAP.md Phase 11 Plans line reflects the actual 4-plan count and each plan is enumerated in the checklist.
    - ROADMAP.md Progress table row for Phase 11 no longer says `0/0` / `Not started`.
    - ROADMAP.md Backlog section contains the new 999.4 entry.
    - No other section of ROADMAP.md or REQUIREMENTS.md is disturbed (scoped Edits only).
  </acceptance_criteria>
  <done>Documentation reflects reality: 5 requirements traced to Phase 11, ADMIN-10 correctly filed to backlog 999.4, ROADMAP Phase 11 block finalized, and no legacy "TBD" strings remain in the Phase 11 traceability path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → live Apps Script Web App | Manual paste-deploy; risk of missing a file or forgetting to click "New version" |
| Recruiter browser → live dashboard | First real exposure of the analytics contract to a real recruiter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-14 | Tampering | Stale cached `localStorage.fs_gas_url` producing "Analytics 404" false alarms | high | mitigate | Task 2 explicitly calls out re-entering the URL in an incognito window — the exact fix Phase 9-06 learned the hard way (STATE.md decision log). |
| T-11-15 | Information Disclosure | Deploy exposing analytics before Phase 10 data is populated (A4) | medium | mitigate | Task 1 blocking-human gate requires either Condition A verified or Condition B risk-acknowledged before Task 2 fires. |
| T-11-16 | Repudiation | Missing `aggregationMs` baseline weakening future forensics | medium | mitigate | Task 3 records observed `aggregationMs` in SUMMARY.md; if > 2000ms, appends a CacheService follow-up backlog line. |
| T-11-17 | Elevation of Privilege | Direct navigation to `/admin/analytics` bypassing auth | high | mitigate | Task 3 Step B item 9 explicitly verifies the client-route redirect. Backend still requires token — auth is layered. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies in Wave 3; recharts already committed in Wave 2. |
</threat_model>

<verification>
- Task 1 checkpoint resolved with `condition-a-verified` or `condition-b-accepted: <note>`.
- Task 2 checkpoint resolved with `deployed`.
- Task 3 SUMMARY.md exists with the 9-item live checklist filled in.
- Task 4 grep automation passes (verify block).
- Manual: recruiter loads `/admin/analytics` in incognito with fresh auth, sees populated dashboard, no console errors.
</verification>

<success_criteria>
- All 5 concrete requirement IDs (ADMIN-06/07/08/09/11) are traceable to a shipped, live-verified Phase 11 in REQUIREMENTS.md.
- ADMIN-10 is explicitly filed as Backlog 999.4 with rationale, not silently dropped.
- ROADMAP.md Phase 11 block reflects the 4-plan reality — no lingering "TBD" strings.
- `meta.aggregationMs` baseline is captured for future Pitfall 1 monitoring.
- Phase 10 A4 risk is either resolved (Condition A) or explicitly accepted in writing (Condition B).
</success_criteria>

<output>
Create `.planning/phases/11-recruiter-analytics-dashboard/11-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **New file:** `.planning/phases/11-recruiter-analytics-dashboard/11-04-SUMMARY.md` — live-verify results, observed `aggregationMs`, 9-item checklist outcomes, ADMIN-10 deferral note.
- **Modified:** `.planning/REQUIREMENTS.md` — 5 traceability rows repointed to Phase 11, ADMIN-10 row repointed to Backlog 999.4 with status `Deferred`.
- **Modified:** `.planning/ROADMAP.md` — Phase 11 block finalized (Requirements line, Plans count, plan checklist, Progress table row) + new Backlog 999.4 entry appended.
- **No code changes.** **No new dependencies.** **No sheet schema changes.**
