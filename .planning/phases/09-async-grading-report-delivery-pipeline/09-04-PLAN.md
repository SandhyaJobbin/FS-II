---
phase: 09
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - assessment-app/src/components/ThankYouScreen.tsx
  - assessment-app/src/app/page.tsx
  - assessment-app/src/app/admin/page.tsx
autonomous: true
requirements: [ASYNC-01, ASYNC-02]
must_haves:
  truths:
    - "Candidate sees a Thank You confirmation screen immediately on submit -- no instant on-screen score/report, no auto-refresh/polling loop (ASYNC-01, D-13/D-14/D-15/D-16)"
    - "Admins can still identify report-ready attempts in the admin panel after Status literals change from 'submitted' to 'pending_grading'/'graded'/'emailed' (supports ASYNC-02's server-side-continues guarantee staying observable)"
  artifacts:
    - "assessment-app/src/components/ThankYouScreen.tsx (new) -- takes no report/score-shaped prop"
    - "assessment-app/src/app/page.tsx (modified) -- 'report' screen state replaced by 'thankyou', reportData state removed"
    - "assessment-app/src/app/admin/page.tsx (modified) -- READY_STATUSES-based report-ready check matching backend/Code.gs"
  key_links:
    - "page.tsx's handleTestSubmit no longer reads response.report (plan 09-01 removed it from the API response) -- it only reads response.status"
    - "admin/page.tsx's READY_STATUSES list must stay textually identical to backend/Code.gs's READY_STATUSES (plan 09-01) -- any divergence silently hides report-ready attempts from admins"
---

<objective>
Replace the candidate's instant on-screen report with a `ThankYouScreen` confirmation (per 09-CONTEXT.md D-13 through D-16), rewire `page.tsx`'s submit flow to stop expecting an inline `report` in the API response (plan 09-01 removed it), and update the admin panel's report-readiness check so it recognizes the new post-enqueue status literals instead of only `"submitted"`.

Purpose: This is the candidate-facing half of ASYNC-01 (ROADMAP Phase 9 Success Criterion #1) and the frontend half of keeping ASYNC-02's "grading continues server-side" guarantee actually visible to admins once `Attempts.Status` starts passing through `pending_grading`/`graded` before landing on `emailed`.
Output: `ThankYouScreen.tsx`, an updated `page.tsx` submit flow, and an updated `admin/page.tsx` report-readiness check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-async-grading-report-delivery-pipeline/09-CONTEXT.md
@.planning/phases/09-async-grading-report-delivery-pipeline/09-RESEARCH.md
@.planning/phases/09-async-grading-report-delivery-pipeline/09-PATTERNS.md
@assessment-app/src/components/ReportScreen.tsx
@assessment-app/src/app/page.tsx
@assessment-app/src/app/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ThankYouScreen.tsx</name>
  <files>assessment-app/src/components/ThankYouScreen.tsx</files>
  <read_first>
    assessment-app/src/components/ReportScreen.tsx (the `fadeUp` `Variants` object and the outer shell `className` -- both reused verbatim per D-14 for visual consistency). 09-CONTEXT.md D-13 (turnaround copy says "within a few minutes"), D-14 (gamified tone, reuse ReportScreen's animation/shell), D-15 (no report/score prop of any kind), D-16 (no auto-refresh/status-polling loop). 09-PATTERNS.md's `ThankYouScreen.tsx` section for the exact import/animation-variant/shell-class excerpts to copy.
  </read_first>
  <action>
    Create `assessment-app/src/components/ThankYouScreen.tsx` as a client component (`"use client"`) that imports and reuses `ReportScreen.tsx`'s `fadeUp` `Variants` object and outer shell `className` verbatim, so the confirmation screen visually matches the rest of the gamified flow (D-14). Do not import the `Report` type or accept any prop shaped like a report/score (D-15) -- an optional `candidateName?: string` prop is acceptable for a personalized greeting since it is already available in `page.tsx`'s existing state, but nothing violation- or score-shaped. Render a confirmation message and turnaround-time copy stating the report/results will be delivered by email "within a few minutes" (D-13's exact phrasing intent), using `@phosphor-icons/react` for a checkmark-style icon and `framer-motion`'s `motion.div` with the reused `fadeUp` variant, consistent with the rest of the app's screen components. Do not add any `useEffect`, `setInterval`, `setTimeout`-based polling, or `fetch` call inside this component (D-16) -- it is a static confirmation screen with zero network activity after mount.
  </action>
  <verify>
    <automated>grep -c "setInterval\|setTimeout\|fetch(" assessment-app/src/components/ThankYouScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    `ThankYouScreen.tsx` exists, renders confirmation + "within a few minutes" turnaround copy, reuses `ReportScreen.tsx`'s `fadeUp` variant and shell class, and imports no `Report`-shaped type. The grep count for `setInterval`/`setTimeout`/`fetch(` is exactly 0 (D-16 -- no polling/auto-refresh).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace the 'report' screen with 'thankyou' in page.tsx</name>
  <files>assessment-app/src/app/page.tsx</files>
  <read_first>
    assessment-app/src/app/page.tsx (the screen-state union type, the `reportData` state declaration, `handleTestSubmit`'s current `setReportData(result.report); setScreen('report');` call, and the render branch that mounts `ReportScreen`). 09-PATTERNS.md's `page.tsx` section for the exact before/after excerpts.
  </read_first>
  <action>
    In the screen-state union type, replace the `'report'` literal with `'thankyou'`. Remove the `reportData` state declaration entirely (plan 09-01's `handleSubmitAnswers` no longer returns a `report` field, so there is nothing to store). In `handleTestSubmit`, replace the line(s) that call `setReportData(result.report)` and `setScreen('report')` with a single `setScreen('thankyou')` -- `handleTestSubmit` should now only branch on `result.status`/`result.success`, not read `result.report`. In the render branch, replace the `screen === 'report'` block (which mounted `<ReportScreen report={reportData} ... />`) with a `screen === 'thankyou'` block mounting `<ThankYouScreen candidateName={...} />` (pass whatever candidate-name state already exists in this component, e.g. the value captured at the start-attempt step -- do not introduce a new field for this). Remove the now-unused `ReportScreen` import if nothing else in this file still renders it directly, and add the `ThankYouScreen` import.
  </action>
  <verify>
    <automated>grep -c "reportData" assessment-app/src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    `page.tsx` no longer declares or references `reportData` anywhere (grep count is 0). The screen-state union uses `'thankyou'` instead of `'report'`. `handleTestSubmit` sets `screen` to `'thankyou'` on a successful submit without reading `result.report`. The render branch mounts `ThankYouScreen`, not `ReportScreen`, for the post-submit state.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Apply the READY_STATUSES allowlist to admin/page.tsx's report-readiness check</name>
  <files>assessment-app/src/app/admin/page.tsx</files>
  <read_first>
    assessment-app/src/app/admin/page.tsx around the `isSubmitted` computation (~line 330) and the status badge ternary (~lines 355-357). 09-RESEARCH.md Pitfall 4 (the admin panel silently stops showing reports if only the backend write side is updated). 09-PATTERNS.md's `admin/page.tsx` section for the exact before/after excerpt.
  </read_first>
  <action>
    Near the top of `admin/page.tsx` (module scope, alongside other constants), add `const READY_STATUSES = ['submitted', 'graded', 'emailed'];` -- this list must stay textually identical to the one added to `backend/Code.gs` in plan 09-01, since both gate the same concept (an attempt whose report can be fetched) from opposite sides of the API boundary. Replace the current `isSubmitted` computation (whatever form it takes -- likely a `status === 'submitted'` check) with `READY_STATUSES.includes(status)`. Extend the existing badge ternary (~lines 355-357) with an explicit `status === 'grading_failed'` branch rendering a distinct, visually-attention-grabbing badge (e.g. a red/error-colored badge with label text like "Grading Failed") so a permanently-failed attempt is distinguishable at a glance from a normal in-progress or complete row in the candidate list -- per D-12, permanently-failed items must be surfaced in the existing admin list/detail view, and a generic status-column string alone is too easy for an admin to miss while scanning; a dedicated color/badge is required, not optional. Do not restructure the ternary/switch beyond adding this one branch.
  </action>
  <verify>
    <automated>grep -c "READY_STATUSES" assessment-app/src/app/admin/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    `admin/page.tsx` defines `READY_STATUSES = ['submitted', 'graded', 'emailed']` and uses it to compute report-readiness instead of a bare `status === 'submitted'` check. An attempt with `status === 'pending_grading'` or `'grading_failed'` is correctly excluded from report-ready; one with `'submitted'`, `'graded'`, or `'emailed'` is correctly included. A `status === 'grading_failed'` row renders a distinct badge (visually different color/label from the other status badges) in the admin list/detail view, satisfying D-12's "surfaced" requirement without an admin needing to read the raw status column.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Candidate browser -> `ThankYouScreen` render | No new input crosses here; the component receives no report/score-shaped data at all |
| Admin browser -> `admin/page.tsx` status display | Reads attempt status from the existing admin API response; must classify the new status literals correctly to avoid over- or under-disclosing report availability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-09-09 | Information Disclosure | `ThankYouScreen.tsx` / `page.tsx` | low | mitigate | `reportData` state is removed entirely from `page.tsx` and `ThankYouScreen` accepts no report/score-shaped prop -- it is structurally impossible for violation or score data to reach the candidate's confirmation screen, not merely hidden by conditional rendering |
| T-09-10b | Spoofing / Tampering | `admin/page.tsx` report-readiness check | medium | mitigate | `READY_STATUSES` is the same explicit allowlist literal (`['submitted', 'graded', 'emailed']`) as `backend/Code.gs` (plan 09-01) -- kept as a plain array literal rather than a shared import (no shared package exists between backend and frontend), so both sides must be updated together; a mismatch fails toward under-disclosure (report hidden, not leaked) since anything outside the allowlist is treated as not-ready |
| T-09-13 | Denial of Service | `ThankYouScreen.tsx` | low | mitigate | No `setInterval`/`setTimeout`/`fetch` polling loop (D-16) -- the component makes zero requests after mount, so it cannot become an accidental load source against `doGet` |

No package-manager installs in this plan (0 new dependencies) -- Package Legitimacy Gate not applicable.
</threat_model>

<verification>
`assessment-app/` has no configured test runner (confirmed in 09-VALIDATION.md's Manual-Only Verifications table -- no `test` script, no testing library, and root `vitest.config.ts` excludes `assessment-app/**`). The automated `<verify>` commands above (grep-based) are the only automated checks available for this plan's tasks. Full behavioral confirmation (ThankYouScreen renders correctly, no polling fires, admin panel shows the right attempts) happens via the manual click-through in the plan 09-06 checkpoint, per 09-VALIDATION.md's ASYNC-01 manual-only justification.
</verification>

<success_criteria>
- `ThankYouScreen.tsx` renders a confirmation + turnaround-time message with zero report/score data and zero polling
- `page.tsx` no longer references `reportData` or the `'report'` screen state anywhere
- `admin/page.tsx` classifies `'submitted'`/`'graded'`/`'emailed'` as report-ready via the same `READY_STATUSES` list used server-side
- `admin/page.tsx` renders a visually distinct badge for `'grading_failed'` attempts, satisfying D-12's admin-visibility requirement
</success_criteria>

## Artifacts this phase produces

- `assessment-app/src/components/ThankYouScreen.tsx` (new) -- static confirmation screen, no report/score prop, no polling
- `assessment-app/src/app/page.tsx` (modified) -- `'thankyou'` screen state replaces `'report'`, `reportData` state removed
- `assessment-app/src/app/admin/page.tsx` (modified) -- `READY_STATUSES` allowlist replaces the bare `'submitted'` check

<output>
Create `.planning/phases/09-async-grading-report-delivery-pipeline/09-04-SUMMARY.md` when done
</output>