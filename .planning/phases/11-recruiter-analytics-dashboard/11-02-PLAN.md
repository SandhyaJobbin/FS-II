---
phase: 11-recruiter-analytics-dashboard
plan: 02
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - backend/Analytics.gs
  - backend/Code.gs
autonomous: true
requirements:
  - ADMIN-06
  - ADMIN-07
  - ADMIN-08
  - ADMIN-09
  - ADMIN-11
estimate:
  tokens: 46000
  raw_tokens: 31000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - GET action=adminAnalytics returns a single flat JSON envelope with scoreTrend, questionStats, violationCorrelation, discriminationIndex, biasSignals, and meta — the five reducers plus the orchestrator (D-02 pairs ADMIN-09 discrimination index and tier distribution under the same fairness signal).
    - Every "pass rate" denominator reuses Phase 10's computeAggregatesForAttempt A2 policy via effectiveVerdict (override || verdict) from AsyncGrading.gs — ungraded counted separately, never merged into the denominator (D-07 / Pitfall 2).
    - Each reducer's enough_data gate uses the LOCKED D-03 per-signal threshold — NOT a uniform 5: scoreTrend >=5 ready across >=3 days; questionStats >=5 graded per question; violationCorrelation >=10 with violations OR >=20 ready; biasSignals >=15 ready; discriminationIndex >=5 graded per question AND >=8 ready to form top/bottom quartiles.
    - Endpoint requires a valid ADMIN_TOKEN via checkAdminAuth(token) as its first executable line; unauthenticated calls return { success:false, error:"Unauthorized" } (D-08).
    - Response payload never contains Name, Email, SubmittedAnswer, options.is_correct, model_answer, or rubric content (V8 PII + answer-key invariants); Pearson r is hand-rolled in-reducer (D-11) — no simple-statistics/mathjs.
    - meta.aggregationMs is present on every response from the first commit (D-12 / Pitfall 1 instrumentation); no CacheService in v1 (success criterion 2: on-demand, no precompute).
  artifacts:
    - backend/Analytics.gs (5 pure reducers + handleAdminAnalytics orchestrator, under 400 lines)
    - backend/Code.gs with one new doGet branch mapping action=adminAnalytics -> handleAdminAnalytics (the 5th action branch)
  key_links:
    - handleAdminAnalytics calls checkAdminAuth(token) as its first executable line (Code.gs:50 pattern, D-08).
    - Reducers call effectiveVerdict (AsyncGrading.gs:38-45) via GAS global scope — no import ceremony — passing object rows shaped { Verdict: row[3], OverrideVerdict: row[6] } and { IsCorrect: row[3] } (D-07).
    - Wave-0 sync-check sections 16-19 now PASS against this file set — section 18 confirms effectiveVerdict reuse from backend/Analytics.gs.
---

<objective>
Wave 1 backend. Add handleAdminAnalytics(token) and the five pure reducers to a new backend/Analytics.gs (kept separate from AsyncGrading.gs because that file is already at 760 lines and CLAUDE.md caps files at 500). Wire the endpoint into doGet as the 5th action branch adminAnalytics. Instrument meta.aggregationMs from the first commit. Apply the D-03 per-signal low-N thresholds exactly as Wave 0's mirror does — the pre-replan plan used a uniform threshold of 5; this wave enforces 5 / 5 / 10-or-20 / 15 / 8-plus-5.

Purpose: The single most trust-critical wave — every pass-rate the recruiter sees is defined here. Wave 1 must reuse (not re-implement) effectiveVerdict to preserve the A2 denominator policy identical to Phase 10, so the report and the dashboard never disagree (D-07, Pitfall 2). The Discrimination Index pairing with the tier-distribution panel (D-02) lands as the buildDiscriminationIndex + buildBiasSignals pair.

Output: 2 files (1 new backend module under 400 lines + 1 router edit adding ~3 lines). No sheet changes. No new triggers. No new script properties. Zero new dependencies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md
@.planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md
@.planning/phases/11-recruiter-analytics-dashboard/11-CONTEXT.md
@.planning/phases/11-recruiter-analytics-dashboard/11-01-SUMMARY.md
@backend/Code.gs
@backend/AsyncGrading.gs
@tests/analytics/analytics-reducers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create backend/Analytics.gs — 5 reducers (D-03 per-signal thresholds) + handleAdminAnalytics orchestrator (D-08 auth, D-11 hand-rolled Pearson, D-12 aggregationMs)</name>
  <files>backend/Analytics.gs</files>
  <reversibility rating="costly">Thresholds here are read by the frontend fallback cards too — changing them after deploy touches both sides and the parity test fixtures (D-03). A2 parity divergence breaks recruiter trust (D-07).</reversibility>
  <read_first>
    - backend/AsyncGrading.gs lines 38-45 (effectiveVerdict signature, called via GAS global scope with no import) and 92-160 (computeAggregatesForAttempt — the A2 denominator policy this wave mirrors at the cross-attempt level; the object-row shapes { Verdict, OverrideVerdict } and { IsCorrect } the resolver expects are built at lines 112-124).
    - backend/Code.gs lines 40-55 (ADMIN_TOKEN, checkAdminAuth, READY_STATUSES), 189-196 (jsonResponse wrapper), 200-242 (initSheets column headers — Attempts is 15 cols, Responses is 5 cols, GradingTranscripts is 9 cols), 476-500 (handleGetAttemptTranscript — the analog handler whose auth-first + sheet-read shape to copy).
    - tests/analytics/analytics-reducers.ts (Wave 0 mirror — this backend MUST be logically identical; same seven D-03 threshold constants with the same numeric values, same enough_data gate per signal).
    - .planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md § Aggregation Recipes (reducer sketches) and § Data-Shape Reference (exact column indices: Attempts col 11 ViolationCount, col 7 OverallScore, col 12 RecommendationTier, col 14 UngradedCount; ResponseQuestionID col 1; GradingTranscripts Verdict col 3, OverrideVerdict col 6).
    - .planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md § 1 (handler shape), § 3 (reducer shape), § 4 (effectiveVerdict reuse hard constraint), § 5 (A2 denominator hard constraint block).
    - .planning/phases/11-recruiter-analytics-dashboard/11-CONTEXT.md D-03 (per-signal thresholds — LOCKED), D-07 (effectiveVerdict / A2 parity), D-08 (auth pattern), D-11 (hand-rolled Pearson), D-12 (aggregationMs instrumentation, no CacheService).
  </read_first>
  <behavior>
    - File is a GAS sibling of AsyncGrading.gs sharing global scope — no import statements; reuses effectiveVerdict, READY_STATUSES, checkAdminAuth, QUESTIONS from the sibling files.
    - Five reducer functions with signatures matching the Wave-0 mirror: buildScoreTrend(attemptsByStatus), buildQuestionStats(responsesData, transcriptsData), buildViolationCorrelation(attemptsByStatus), buildDiscriminationIndex(attemptsByStatus, responsesData, transcriptsData), buildBiasSignals(attemptsByStatus).
    - Each reducer's return object contains enough_data, threshold, samples, thresholdNote where applicable, aligned to D-03 per-signal thresholds (the same numeric constants Wave 0's mirror exports).
    - buildViolationCorrelation computes Pearson r with an inline two-pass reduce (sum of v, sum of s, sum of vs, sum of v-squared, sum of s-squared) — D-11 hand-rolled, zero statistics import; r is returned together with samples (the scatter tuples also returned) and an interpretation label (weak/moderate/strong by absolute r thresholds 0.2/0.5).
    - handleAdminAnalytics first executable line is checkAdminAuth(token); unauthenticated calls return { success:false, error:"Unauthorized" }; the meta block on every response (including the empty-sheet defensive branch) contains aggregationMs computed from Date.now() deltas (D-12 first commit).
    - No reducer or handler body references row[1] (Name) or row[2] (Email) (V8); no options.is_correct / model_answer / rubric Rationale is ever returned (V8 answer-key).
  </behavior>
  <action>
    Create backend/Analytics.gs with a file header explaining it adds cross-attempt aggregation reducers plus the handleAdminAnalytics doGet handler, shares GAS global scope with Code.gs and AsyncGrading.gs, reuses effectiveVerdict/READY_STATUSES/checkAdminAuth/QUESTIONS from those files, and lives in its own file to keep AsyncGrading.gs under the 500-line project rule. Use function declarations throughout (matches Code.gs/AsyncGrading.gs convention).

    Declare the seven D-03 threshold constants by name with the LOCKED values — MIN_QUESTION_SAMPLES=5, MIN_TREND_ATTEMPTS=5, MIN_TREND_DAYS=3, MIN_CORR_VIOLATIONS=10, MIN_CORR_TOTAL=20, MIN_BIAS_ATTEMPTS=15, MIN_DISCRIM_READY=8 — numerically identical to tests/analytics/analytics-reducers.ts so parity holds. Do NOT introduce the old MIN_DISCRIM_QUARTILE_N=3; the discrimination gate is the >=8 total-ready-attempts quartile threshold per D-03.

    Implement the five reducers matching the Wave-0 mirror logic and the 11-RESEARCH.md § Aggregation Recipes sketches. For each: build the lookup keyed by attemptId/questionId in one pass, then fold; invoke effectiveVerdict as the sole verdict resolver, passing transcript objects shaped { Verdict: row[3], OverrideVerdict: row[6] } and responses objects shaped { IsCorrect: row[3] } so the resolver's property names match (AsyncGrading.gs:38-45). Denominator for every pass-rate is graded = correct + incorrect (A2 policy — 11-PATTERNS § 5); ungraded is counted in its own bucket and surfaced per-row. emit sampleSize alongside every rate.

    D-03 enough_data gates per reducer, identical to the mirror: scoreTrend enough_data = (scoredReady >= MIN_TREND_ATTEMPTS) AND (distinctDays >= MIN_TREND_DAYS); questionStats per-row enough_data = (row.sampleSize >= MIN_QUESTION_SAMPLES) with a top-level enough_data = (total graded responses >= 5); violationCorrelation enough_data = (violationsCount >= MIN_CORR_VIOLATIONS) OR (readyCount >= MIN_CORR_TOTAL), threshold = 10, samples = readyCount, plus a violationsCount field; biasSignals enough_data = (readyCount >= MIN_BIAS_ATTEMPTS), threshold = 15; discriminationIndex enough_data = (readyCount >= MIN_DISCRIM_READY) threshold = 8, per-row gated on MIN_QUESTION_SAMPLES, top/bottom quartiles computed as the top and bottom 25% of ready attempts by OverallScore, returning rows with qId/passTop/passBottom/delta/sampleTop/sampleBottom sorted ascending by delta (most anti-discriminating first) — this pairs with biasSignals under the Distribution Monitor fairness panel (D-02).

    buildViolationCorrelation: implement Pearson r as an inline eight-line two-pass reducer over the (ViolationCount col 11, OverallScore col 7) tuples — no import of simple-statistics or mathjs (D-11); guard n < threshold returns r null and enough_data false; guard zero-variance denominator returns r 0. Return the scatter tuples as points so the frontend renders them with r and n together (never r without n — D-11 anti-pattern).

    handleAdminAnalytics(token) orchestrator (analog of handleGetAttemptTranscript at Code.gs:476-500): CHECKADMINAUTH FIRST EXECUTABLE LINE returning { success:false, error:"Unauthorized" } (D-08); then capture const t0 = Date.now(); read three sheets AttemptCount, Responses, GradingTranscripts in one getDataRange().getValues() call each, guarding each sheet absence with an early return of an empty payload carrying all enough_data:false and a meta block; .slice(1) each to strip headers; filter attempts to READY_STATUSES (reuse the Code.gs:55 constant); compute ungradedTotal via Number(row[14]) || 0 defensive coerce (A4 legacy fallback, Pitfall 7); call the five reducers; return { success: true, generatedAt: new Date().toISOString(), meta: { totalAttempts: attemptsByStatus.length, ungradedTotal, aggregationMs: Date.now() - t0 }, scoreTrend, questionStats, violationCorrelation, discriminationIndex, biasSignals }. aggregationMs MUST be present on every response including the defensive empty branch (D-12 first-commit commitment). Never include row[1] (Name) or row[2] (Email) in any returned payload.

    Keep the file under 400 lines. Use function declarations for all top-level exports.
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment" && lines=$(grep -c "^function " backend/Analytics.gs) && [ "$lines" -ge 6 ] && [ "$lines" -le 12 ] && grep -q "checkAdminAuth(token)" backend/Analytics.gs && grep -q "MIN_DISCRIM_READY = 8\|MIN_DISCRIM_READY=8" backend/Analytics.gs && grep -q "MIN_CORR_VIOLATIONS = 10\|MIN_CORR_VIOLATIONS=10" backend/Analytics.gs && grep -q "MIN_BIAS_ATTEMPTS = 15\|MIN_BIAS_ATTEMPTS=15" backend/Analytics.gs && grep -q "MIN_CORR_TOTAL = 20\|MIN_CORR_TOTAL=20" backend/Analytics.gs && ! grep -q "MIN_DISCRIM_QUARTILE_N" backend/Analytics.gs && grep -q "aggregationMs" backend/Analytics.gs && grep -q "effectiveVerdict(" backend/Analytics.gs && ! grep -qE "^function effectiveVerdict" backend/Analytics.gs && ! grep -q "simple-statistics\|mathjs" backend/Analytics.gs && rowPII=$(grep -cE "name: (row|rows|data)\[1\]|email: (row|rows|data)\[2\]|\.name *:.*\[1\]" backend/Analytics.gs) && [ "$rowPII" -eq 0 ] && echo "analytics.gs ok"</automated>
  </verify>
  <acceptance_criteria>
    - backend/Analytics.gs exists with between 6 and 12 function declarations (5 reducers + 1 orchestrator).
    - handleAdminAnalytics contains the literal checkAdminAuth(token) call as one of its first two statements (position enforced by sync-check section 17 regex window {0,300} when Wave 1 lands the config edit).
    - The seven D-03 threshold constants are present by name with the LOCKED numeric values: MIN_QUESTION_SAMPLES=5, MIN_TREND_ATTEMPTS=5, MIN_TREND_DAYS=3, MIN_CORR_VIOLATIONS=10, MIN_CORR_TOTAL=20, MIN_BIAS_ATTEMPTS=15, MIN_DISCRIM_READY=8.
    - The old constant MIN_DISCRIM_QUARTILE_N is ABSENT (grep returns 0) — the pre-replan uniform/threshold-3 gate is replaced by the D-03 >=8 ready quartile gate.
    - The response envelope contains meta.aggregationMs (grep verifies aggregationMs present).
    - Reducers call effectiveVerdict ( at least once; no local function effectiveVerdict definition; grep -E "^function effectiveVerdict" returns 0).
    - grep -c "simple-statistics\|mathjs" returns 0 over backend/Analytics.gs (D-11 hand-rolled Pearson).
    - No Name (col 1) or Email (col 2) PII reads leak into any returned payload (grep for the answer-key/PII column projections returns 0).
    - File length under 400 lines.
  </acceptance_criteria>
  <done>backend/Analytics.gs contains the 5 reducers + orchestrator with the D-03 per-signal thresholds (old MIN_DISCRIM_QUARTILE_N gone, >=8-ready quartile in); auth is the first executable line; aggregationMs is instrumented on every response from the first commit; PII/answer-key columns are never read; effectiveVerdict is reused not re-derived; Pearson r is hand-rolled with no statistics dep.</done>
</task>

<task type="auto">
  <name>Task 2: Wire doGet router — add 5th action branch adminAnalytics</name>
  <files>backend/Code.gs</files>
  <read_first>
    - backend/Code.gs lines 134-153 (existing doGet router — the exact else-if chain to extend; the getAttemptTranscript branch at approximately 145-147 is the immediate predecessor).
    - backend/Code.gs lines 138 and 150-152 (the try/catch wrapper the new branch inherits — do NOT touch).
    - .planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md § 2 (doGet wiring pattern with the exact insertion point and the jsonResponse wrapper at 189-196).
    - backend/Analytics.gs (Task 1 — confirms handleAdminAnalytics signature takes token only).
  </read_first>
  <action>
    Locate the doGet router chain (Code.gs around 139-147). Immediately after the existing else if (action === "getAttemptTranscript") branch and before the trailing return jsonResponse({ error: "Invalid action or method" }, 400) fallback, insert one new branch that dispatches action === "adminAnalytics" to jsonResponse(handleAdminAnalytics(params.token)) — exactly one branch, ~3 lines, matching the existing else-if formatting. Do NOT modify doPost (analytics is read-only). Do NOT modify the try/catch wrapper. Do NOT touch any other Code.gs region (Phase 10 handlers at Code.gs:433-712 are read-only per 11-PATTERNS.md § Files planner should NOT touch, and AsyncGrading.gs lines 38-174 are read-only per the same section).
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment" && grep -c 'action === "adminAnalytics"' backend/Code.gs | grep -q '^1$' && grep -c "handleAdminAnalytics(params.token)" backend/Code.gs | grep -q '^1$' && npx tsx scripts/sync-check.ts ; echo "sync_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly ONE occurrence of action === "adminAnalytics" exists in Code.gs (grep count == 1).
    - Exactly ONE occurrence of handleAdminAnalytics(params.token) exists in Code.gs.
    - The doGet function still parses (no unbalanced braces) — the existing trailing } catch (err) { is still reachable from the router chain.
    - npx tsx scripts/sync-check.ts exits 0 — all 19 sections now PASS (Wave-0 sections 16-19 were designed to FAIL until this wave lands; section 18 now confirms effectiveVerdict reuse from backend/Analytics.gs, so all four must be green).
  </acceptance_criteria>
  <done>doGet has exactly 5 action branches; sync-check is fully green; no other Code.gs region is modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public GAS Web App URL -> handleAdminAnalytics | Any HTTP client can hit this endpoint; only a valid ADMIN_TOKEN gates access |
| Sheet reads -> JSON response | Server owns the projection; must never emit PII columns or answer-key fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Information Disclosure | handleAdminAnalytics without auth gate | critical | mitigate | checkAdminAuth(token) is the first executable line (D-08); sync-check section 17 blocks any refactor that pushes it deeper. |
| T-11-05 | Information Disclosure | PII (Name/Email) leaking into aggregation payload | high | mitigate | Reducers key on attemptId / qId only; row[1] (Name) and row[2] (Email) columns are never referenced. Verification grep confirms absence (V8). |
| T-11-06 | Information Disclosure | Answer-key or rubric content in payload | high | mitigate | Reducers project { qId, passRate, sampleSize, ungraded } only. options.is_correct, model_answer, and rubric Rationale are never returned (V8 — Code.gs:310 allowlist precedent). |
| T-11-07 | Denial of Service | 6-minute Apps Script cap under high N | medium | mitigate | Reducers are O(N) hash-map joins (Pitfall 1). meta.aggregationMs instrumented from the first commit (D-12); Wave 3 verifies live number < 2000ms and files a CacheService backlog line only if exceeded. No CacheService in v1 (success criterion 2). |
| T-11-08 | Tampering | Token-in-URL logging (existing pattern) | medium | accept | Reused for consistency with all 4 existing admin GETs (D-08); the broader Authorization: Bearer refactor is a separate backlog item per 11-RESEARCH.md Pitfall 5 — intentionally NOT diverged for one endpoint. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | Wave 1 introduces zero new dependencies (backend is pure GAS built-ins; Pearson hand-rolled per D-11). No package-manager changes. |
</threat_model>

<verification>
- npx tsx scripts/sync-check.ts exits 0 — all 19 sections PASS (sections 16-19 transition from FAIL to PASS in this wave).
- grep -n "checkAdminAuth(token)" backend/Analytics.gs returns the call within the first ~10 lines of handleAdminAnalytics.
- grep -c "row\[1\]\|row\[2\]" backend/Analytics.gs returns 0 (no PII column reads; note col index references like row[11]/row[7] are allowed — only the Name/Email indices are forbidden).
- grep -c 'action === "adminAnalytics"' backend/Code.gs returns 1.
- wc -l backend/Analytics.gs backend/Code.gs — Analytics.gs under 400; Code.gs delta is ~3 lines (one new else-if branch).
</verification>

<success_criteria>
- ADMIN-06 (score trend), ADMIN-07 (question pass-rate), ADMIN-08 (violation-vs-score correlation), ADMIN-09 (bottom-N discriminating questions, paired with the tier-distribution fairness signal per D-02), and ADMIN-11 (low-N fallback per-signal) are all satisfied by handleAdminAnalytics's return payload, each gated by its D-03 per-signal threshold (NOT a uniform 5).
- A2 denominator policy is preserved — every rate uses graded = correct + incorrect as the denominator; ungraded counts are surfaced, never merged in (D-07, Pitfall 2).
- Security invariants hold: auth first (D-08), no PII (V8), no answer keys (V8), no rubric content; Pearson r hand-rolled with no statistics dep (D-11).
- Pitfall 1 monitoring is live from the first commit via meta.aggregationMs (D-12); success-criterion-2 on-demand enforcement (no CacheService) holds.
</success_criteria>

<output>
Create `.planning/phases/11-recruiter-analytics-dashboard/11-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **New file:** `backend/Analytics.gs` — 5 pure reducers (`buildScoreTrend`, `buildQuestionStats`, `buildViolationCorrelation`, `buildDiscriminationIndex`, `buildBiasSignals`) + `handleAdminAnalytics(token)` orchestrator. Exports 7 D-03 threshold constants (MIN_QUESTION_SAMPLES=5, MIN_TREND_ATTEMPTS=5, MIN_TREND_DAYS=3, MIN_CORR_VIOLATIONS=10, MIN_CORR_TOTAL=20, MIN_BIAS_ATTEMPTS=15, MIN_DISCRIM_READY=8 — old MIN_DISCRIM_QUARTILE_N gone). Reuses `effectiveVerdict`, `READY_STATUSES`, `checkAdminAuth`, `QUESTIONS` via GAS global scope; hand-rolls Pearson r (D-11); first-commits `meta.aggregationMs` (D-12).
- **Modified:** `backend/Code.gs` — one new `else if (action === "adminAnalytics")` branch in `doGet` (the 5th action branch, ~3 lines at the router chain).
- **No sheet schema changes.** **No new triggers.** **No new script properties.** **Zero new dependencies.**