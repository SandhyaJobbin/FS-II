---
phase: 01-content-ingestion-question-bank
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - ingestion/parsers/critical.py
  - ingestion/parsers/xlsx_quotas.py
  - content/quotas.json
  - tests/ingestion/test_critical_e2e.py
  - tests/ingestion/test_quotas.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-04]

must_haves:
  truths:
    - "Critical Thinking doc yields exactly 120 items across 30 cases × 4 questions, marker census exactly 180 ✅"
    - "Zero preamble lines (paragraphs 0–58, including LLM-authoring chatter at 47–58) appear in any parsed item — parsing begins at the first `CASE \\d+ –` heading"
    - "Leading-✅ convention: exactly-1 ✅ → mcq_single; multiple ✅ → mcq_multi (select-all)"
    - "content/quotas.json contains the 8 Sheet2 rows with verbatim values (8, 5, 2, 5, 5, 5, 5, 10) and a unit annotation per row (English rows=questions, Attention/CT rows=cases)"
    - "Quota loader hard-rejects Sheet1 and FS QB Pattern(1).xlsx (pre-revision sources)"
    - "Pool volumes cross-check against parsed bank volumes (Grammar 30, SC 30, Macro 10, Reading 5 passages, Closure 10, Attn L1/L2 20 cases each, CT 30 cases) — mismatch is loud"
  artifacts:
    - ingestion/parsers/critical.py
    - ingestion/parsers/xlsx_quotas.py
    - content/quotas.json
    - tests/ingestion/test_critical_e2e.py, tests/ingestion/test_quotas.py
  key_links:
    - "run.py emits quotas.json alongside questions.json via the same auto-discovery registry (BANK_ID='quotas')"
    - "Sheet2-only guard + header assertion is the single gate against pre-revision quota data"
  prohibitions:
    - "Never resolve quota unit semantics (cases vs questions) by assumption — store verbatim values WITH unit annotation only; interpretation belongs to the user / Phase 2 (01-RESEARCH.md OQ-2)"
    - "Never load Sheet1 or FS QB Pattern(1).xlsx as quota sources — both are pre-revision"
  assumptions:
    - "A-OQ2 (quota unit semantics): D-column for Attention/CT rows is stored with unit='cases' matching the Volume denomination, English rows unit='questions'; whether 5 attention cases means 5 cases (20 questions) per attempt is UNRESOLVED and deferred to user confirmation before Phase 2 assembly (01-RESEARCH.md OQ-2 recommendation). FLAG at /gsd-verify-work."
    - "INGEST-04 unclassified probe edge: the spec-less probe row for INGEST-04 could not be auto-resolved into a covered or backstop check — quota loading covers verbatim values + unit annotation + sheet guards only; any further edge semantics (e.g. quota summing to per-attempt length) remain UNRESOLVED and are flagged for manual review at /gsd-verify-work, not silently dropped."
---

<objective>
Expansion: two parsers. (1) Critical Thinking — 30 case-file scenarios × 4 questions, leading-✅ markers, guarded against the preamble that contains LLM-authoring chatter. (2) Quotas — the settled Sheet2 of FS QB Pattern.xlsx loaded verbatim with unit annotation, guarded against Sheet1 and the stale (1).xlsx copy.

Purpose: INGEST-01/02 for the last content doc (120 items) and INGEST-04 in full — quotas become a committed, queryable artifact without interpreting their unresolved unit semantics.
Output: critical.py + xlsx_quotas.py, content/quotas.json, e2e + negative-fixture tests.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md
@.planning/phases/01-content-ingestion-question-bank/01-PATTERNS.md
@.planning/phases/01-content-ingestion-question-bank/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: parsers/critical.py — preamble-guarded case walker, leading-✅ markers</name>
  <files>ingestion/parsers/critical.py</files>
  <read_first>
    - ingestion/parsers/blocks.py, ingestion/parsers/markers.py (leading_check, split_lines — reuse)
    - ingestion/normalize.py (Item contract: case_id, case scenario)
    - .planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md (CT anatomy: preamble paragraphs 0–58 with LLM chatter at 47–58; `CASE N – Title` en-dash; `Case File` scenario; `Question k` heading line + stem next line; options `A. text` leading ✅; census 180 ✅; anti-pattern: skipping the preamble guard parses phantom content)
  </read_first>
  <action>
    Expose `BANK_ID = "critical"` + `parse(source_dir)`.
    Preamble guard: iterate `iter_block_items` but DISCARD everything until the first paragraph whose text matches `^CASE (\d+) – (.+)` (en-dash). No parsing before that line — the preamble contains LLM-authoring chatter ("Understood. I'll avoid questions like: …") that would import as phantom content.
    Per case: case_id `ct-case-{n:02d}`, case_title verbatim, scenario = the `Case File` paragraph(s). Questions: a `Question k` heading line followed by the stem on the next non-empty line; option lines `A. text`–`D. text` via split_lines; marker extraction via `leading_check` — exactly 1 ✅ → mcq_single, ≥2 ✅ → mcq_multi.
    Census: running ✅ count must equal 180 across 120 questions (validate.py reconciliation); cases == 30; questions == 120.
    Difficulty: doc's verbatim note says difficulty mix is deliberate but unmarked — do NOT fabricate tiers here (01-06 owns tier assignment per A-OQ1).
    File under 500 lines.
  </action>
  <verify>
    <automated>$env:PYTHONIOENCODING="utf-8"; python ingestion/run.py --banks critical; python -c "import json; d=json.load(open('content/questions.json',encoding='utf-8')); c=[i for i in d['items'] if i['bank']=='critical']; print(len(c), len({i['case_id'] for i in c}), any('Understood' in i['stem'] for i in c))"</automated>
  </verify>
  <acceptance_criteria>
    - run.py exits 0; printed: 120 items, 30 distinct case_ids, preamble-leak check `False`
    - ingestion-report.json critical census == {✅: 180}
    - Each case has exactly 4 questions; every question has ≥1 correct option
    - No item stem or scenario contains preamble text (spot: `Understood`, `Difficulty Approach`, `objective`)
  </acceptance_criteria>
  <done>120 CT items in 30 cases with exact ✅ census and zero preamble leakage.</done>
</task>

<task type="auto">
  <name>Task 2: parsers/xlsx_quotas.py — Sheet2-only guarded loader + content/quotas.json emit</name>
  <files>ingestion/parsers/xlsx_quotas.py, content/quotas.json, ingestion/run.py</files>
  <read_first>
    - ingestion/run.py (registry contract — how a non-docx module plugs in; minimal edit: quotas emit alongside questions)
    - ingestion/manifest.py (ALLOWED_FILES whitelist; manifest volumes for cross-check)
    - .planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md (xlsx anatomy: Sheet2 verbatim table — 8 rows with values 8/5/2/5/5/5/5/10; header row assertion; merged range A2:A6; Pitfall 5: wrong sheet/file/units)
  </read_first>
  <action>
    `ingestion/parsers/xlsx_quotas.py`: expose `BANK_ID = "quotas"` + `parse(source_dir)`.
    Guards (all hard failures with clear messages): filename must be exactly `FS QB Pattern.xlsx` (reject `FS QB Pattern(1).xlsx`); `"Sheet2" in wb.sheetnames`; header row == ["Assessment Section", "Question Bank Category", "Volume", "Questions to be given"]. Load with `openpyxl.load_workbook(path, data_only=True)`.
    Read the 8 data rows VERBATIM: (English/Grammar/30 Questions/8), (English/Sentence Correction/30 Questions/5), (English/Macro Editing & Personalization/10 questions/2), (English/Reading Comprehension/5 Passages (5 each)/5), (English/Case Closure Notes/10 Questions/5), (Attention/Level 1 – Review & Listing Accuracy/20 Cases (4/case)/5), (Attention/Level 2 – Account & Fraud Pattern/20 Cases (4/case)/5), (Critical Thinking/Risk Assessment & Business Decision/30 Cases (4/case)/10).
    Annotate each row: unit = "questions" for the 5 English rows, "cases" for the 3 Attention/CT rows (matching their Volume denomination per A-OQ2). Parse the pool volume number out of the Volume string and cross-check against MANIFEST (Grammar 30, SC 30, Macro 10, Reading 5 passages, Closure 10, Attn L1 20 cases, L2 20 cases, CT 30 cases) — any mismatch raises.
    Emit `content/quotas.json`: rows with {assessment_section, category, volume, volume_unit, quota, unit} plus a top-level `"unit_semantics": "unresolved — see A-OQ2"` marker. run.py: extend emit so the quotas module writes quotas.json next to questions.json (same atomic temp+replace discipline; this is the ONLY permitted run.py edit in this plan).
  </action>
  <verify>
    <automated>$env:PYTHONIOENCODING="utf-8"; python ingestion/run.py --banks quotas; python -c "import json; d=json.load(open('content/quotas.json',encoding='utf-8')); print(len(d['rows']), [r['quota'] for r in d['rows']], [r['unit'] for r in d['rows']])"</automated>
  </verify>
  <acceptance_criteria>
    - run.py exits 0; printed: 8 rows, quotas [8, 5, 2, 5, 5, 5, 5, 10], units [questions×5, cases×3] in row order
    - quotas.json contains `unit_semantics` marker string referencing A-OQ2
    - Loader raises on a workbook missing Sheet2 and on the exact filename `FS QB Pattern(1).xlsx`
    - Pool-volume cross-check passes against MANIFEST (and provably raises when a manifest volume is perturbed — covered by Task 3 test)
  </acceptance_criteria>
  <done>8 settled quota rows committed as a guarded, unit-annotated artifact; pre-revision sources rejected; unit semantics explicitly unresolved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CT e2e + quota tests (incl. negative fixtures)</name>
  <files>tests/ingestion/test_critical_e2e.py, tests/ingestion/test_quotas.py</files>
  <read_first>
    - ingestion/parsers/critical.py, ingestion/parsers/xlsx_quotas.py (code under test)
    - tests/conftest.py (fixture conventions)
    - .planning/phases/01-content-ingestion-question-bank/01-VALIDATION.md (test_quotas named in verification map)
  </read_first>
  <behavior>
    - test_critical_e2e: 120 items / 30 cases / 4 per case; census 180 ✅; first case title non-empty; select-all item has ≥2 correct; no item contains `Understood` or other preamble strings
    - test_critical_e2e malformed: fixture docx where a question has zero leading ✅ → run exits ≠ 0 naming the item
    - test_quotas: parsed values == the 8 expected rows verbatim (section, category, volume string, quota int)
    - test_quotas: unit annotation — English rows `questions`, Attention/CT rows `cases`
    - test_quotas negative: workbook with only Sheet1 (built via openpyxl in tmp_path) → loader raises; path named `FS QB Pattern(1).xlsx` → loader raises before open
    - test_quotas negative: manifest volume perturbed (e.g. grammar 30→31) → cross-check raises
  </behavior>
  <action>
    test_critical_e2e.py against the REAL CT docx (manifest.SOURCE_DIR); malformed fixture built by copying the real doc and stripping one ✅ via python-docx. test_quotas.py builds synthetic workbooks via openpyxl for Sheet1-only and perturbed-volume cases, and asserts the real `FS QB Pattern.xlsx` Sheet2 loads to the verbatim expected table. All I/O encoding="utf-8".
  </action>
  <verify>
    <automated>pytest tests/ingestion/test_critical_e2e.py tests/ingestion/test_quotas.py -q; pytest tests/ingestion -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Both new test files exit 0 covering every behavior line above
    - Full suite `pytest tests/ingestion -x -q` green (no regressions)
    - Negative quota tests prove both rejection paths raise with clear messages
  </acceptance_criteria>
  <done>CT bank and quotas proven: exact counts/census, preamble guard, verbatim Sheet2 values, unit annotations, and all three rejection paths tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CT docx → parser | Untrusted authored content with a poison-pill preamble (LLM chatter) |
| FS QB Pattern.xlsx → quota loader | Spreadsheet with a pre-revision decoy sheet + a stale duplicate file beside it |
| quotas.json → Phase 2 | Interpretation boundary (unresolved units) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Tampering | Preamble LLM chatter imported as phantom questions | high | mitigate | Hard preamble guard (start at first `CASE \d+ –`); e2e asserts no `Understood`/`Difficulty Approach` strings in any item; count reconciliation 120/30 |
| T-01-15 | Tampering | Pre-revision quotas loaded from Sheet1 or `(1).xlsx` | high | mitigate | Filename whitelist + `Sheet2` existence assert + exact header-row assert; negative-fixture tests for both decoys |
| T-01-16 | Tampering | Quota unit semantics silently assumed (cases read as questions → wrong attempt length downstream) | medium | mitigate | Verbatim storage + unit annotation + `unit_semantics: unresolved` marker (A-OQ2); interpretation explicitly deferred, flagged at verify |
| T-01-SC | Tampering | npm/pip installs | high | mitigate | No new installs in this plan; covered by 01-RESEARCH.md audit |
</threat_model>

<verification>
- `python ingestion/run.py --banks critical` exits 0 with 120/30, census 180 ✅
- `python ingestion/run.py --banks quotas` exits 0; quotas.json == 8 verbatim rows with units
- `pytest tests/ingestion -x -q` green including both new test files
</verification>

<success_criteria>
Last content doc (120 items) ingested cleanly; INGEST-04 delivered as a guarded, verbatim, unit-annotated quotas artifact with unit semantics flagged — never silently resolved.
</success_criteria>

## Artifacts this phase produces (this plan)

- New modules: `ingestion/parsers/critical.py` (`BANK_ID = "critical"`, case_id `ct-case-{n:02d}`), `ingestion/parsers/xlsx_quotas.py` (`BANK_ID = "quotas"`, Sheet2 guards, unit annotator, pool-volume cross-check)
- New artifact: `content/quotas.json` (8 rows: assessment_section, category, volume, volume_unit, quota, unit; top-level `unit_semantics` marker)
- run.py extension: quotas emit alongside questions.json (atomic)
- Test files: `tests/ingestion/test_critical_e2e.py`, `tests/ingestion/test_quotas.py`

<output>
Create `.planning/phases/01-content-ingestion-question-bank/01-05-SUMMARY.md` when done
</output>
