---
phase: 11-recruiter-analytics-dashboard
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/analytics/analytics-reducers.ts
  - tests/analytics/fixtures/attempts_20.json
  - tests/analytics/test_score_trend.ts
  - tests/analytics/test_question_stats.ts
  - tests/analytics/test_correlation.ts
  - tests/analytics/test_discrimination.ts
  - tests/analytics/test_ungraded_caveat.ts
  - tests/analytics/test_analytics_auth.ts
  - vitest.config.ts
  - scripts/sync-check.ts
autonomous: true
requirements:
  - ADMIN-06
  - ADMIN-07
  - ADMIN-08
  - ADMIN-09
  - ADMIN-11
estimate:
  tokens: 44000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Pure-JS analytics reducer mirror with five reducers exists so backend logic is unit-testable off the GAS runtime (per D-03, D-07).
    - Each reducer returns a top-level object containing enough_data, threshold, and samples aligned to its D-03 per-signal threshold — scoreTrend (>=5 ready attempts across >=3 distinct calendar days); questionStats (>=5 graded responses per question); violationCorrelation (>=10 attempts with violationCount > 0 OR >=20 ready attempts total); biasSignals (>=15 ready attempts); discriminationIndex (>=5 graded per question AND >=8 ready attempts to form top/bottom quartiles).
    - The mirror re-exports effectiveVerdict from tests/grading/rubric-grader.ts (never re-implements it) so A2 denominator drift is structurally impossible (D-07 / Pitfall 2).
    - A parity test proves buildQuestionStats and the existing per-attempt aggregation path produce the same per-question pass-rate on a fixture attempt — the A2 denominator-excludes-ungraded policy at AsyncGrading.gs:127,144 (D-07).
    - test_correlation.ts asserts the >=10-with-violations OR >=20-total gate and test_discrimination.ts asserts the >=8 ready quartile threshold — NOT a uniform 5 (the key D-03 delta from pre-replan plans).
    - sync-check sections 16-19 block PRs that drop the adminAnalytics endpoint action, the checkAdminAuth first-line gate, the effectiveVerdict reuse gate, or the meta.aggregationMs instrumentation gate.
  artifacts:
    - tests/analytics/analytics-reducers.ts
    - tests/analytics/fixtures/attempts_20.json
    - tests/analytics/test_score_trend.ts
    - tests/analytics/test_question_stats.ts
    - tests/analytics/test_correlation.ts
    - tests/analytics/test_discrimination.ts
    - tests/analytics/test_ungraded_caveat.ts
    - tests/analytics/test_analytics_auth.ts
    - vitest.config.ts (excludes analytics-reducers.ts)
    - scripts/sync-check.ts (4 new numbered sections 16-19)
  key_links:
    - analytics-reducers.ts re-exports effectiveVerdict from ../grading/rubric-grader (single-source resolver; A2 parity anchor per D-07).
    - sync-check sections 16-19 read backend/Code.gs and backend/Analytics.gs to enforce the Wave-1 backend contract; until Wave 1 lands, sections 18-19 degrade to a clear FAIL (intended enforcement signal, not a Wave-0 defect).
---

<objective>
Wave 0 test infra + parity guard. Ship the analytics reducer mirror (five reducers plus the D-03 per-signal low-N thresholds), the shared fixture, six vitest specs asserting each reducer's enough_data behavior plus the Pitfall-2 A2 denominator parity gate, and four sync-check drift assertions — all BEFORE any backend or frontend code exists. This wave is the gate that protects Phase 10's A2 policy and CONTEXT D-03's per-signal thresholds from being silently violated by Wave 1-3 code.

Purpose: Every downstream reducer is testable on the Node runtime, and CI blocks merges where analytics drifts from the per-attempt grading resolver or from the locked per-signal thresholds. The pre-replan plans used a uniform threshold of 5; D-03 locks per-signal values (5 / 5 / 10-or-20 / 15 / 8-plus-5), so the mirror AND the correlation/discrimination tests MUST assert those exact gates — not a uniform MIN of 5.

Output: 10 files (1 mirror + 1 fixture + 6 specs + 2 config scripts). Zero backend files. Zero frontend files. Zero new dependencies (D-11: Pearson parity uses hand-computed inline reference numbers, never simple-statistics/mathjs).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md
@.planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md
@.planning/phases/11-recruiter-analytics-dashboard/11-CONTEXT.md
@.planning/phases/11-recruiter-analytics-dashboard/11-VALIDATION.md
@tests/grading/rubric-grader.ts
@tests/grading/test_rubric_grader.ts
@tests/grading/grading-engine.ts
@vitest.config.ts
@scripts/sync-check.ts
@backend/AsyncGrading.gs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reducer mirror + shared fixture (D-03 per-signal thresholds, D-07 effectiveVerdict re-export)</name>
  <files>tests/analytics/analytics-reducers.ts, tests/analytics/fixtures/attempts_20.json</files>
  <read_first>
    - tests/grading/rubric-grader.ts — the mirror shape to copy (pure functions, exports named constants, re-exports effectiveVerdict, zero vitest imports; see AsyncGrading.gs:31 confirming the mirror is byte-equivalent semantically).
    - backend/AsyncGrading.gs lines 38-45 — effectiveVerdict signature (override || verdict resolver) reused verbatim; lines 127-160 — computeAggregatesForAttempt A2 denominator policy (graded = correct + incorrect, ungraded excluded), the path this parity-tests against (D-07).
    - .planning/phases/11-recruiter-analytics-dashboard/11-RESEARCH.md § Aggregation Recipes (all five reducer sketches) and § Data-Shape Reference (Attempts idx 0..14, Responses idx 0..4, GradingTranscripts idx 0..8) — exact column indices the mirror consumes.
    - .planning/phases/11-recruiter-analytics-dashboard/11-PATTERNS.md § 4 (effectiveVerdict hard constraint), § 5 (A2 denominator hard constraint block), § 9 (test mirror pattern + vitest exclusion).
    - .planning/phases/11-recruiter-analytics-dashboard/11-CONTEXT.md <decisions> D-03 (per-signal low-N thresholds — LOCKED, the key replan delta) and D-07 (A2 parity non-optional).
  </read_first>
  <behavior>
    - Mirror exports named pure functions: buildScoreTrend(attemptsByStatus), buildQuestionStats(responses, transcripts), buildViolationCorrelation(attemptsByStatus), buildDiscriminationIndex(attemptsByStatus, responses, transcripts), buildBiasSignals(attemptsByStatus).
    - Mirror re-exports effectiveVerdict from tests/grading/rubric-grader.ts via import-then-export — MUST NOT define a local function effectiveVerdict.
    - D-03 per-signal thresholds exported as named constants and enforced inside each reducer: MIN_TREND_ATTEMPTS=5 AND MIN_TREND_DAYS=3 (scoreTrend); MIN_QUESTION_SAMPLES=5 (questionStats per-row); MIN_CORR_VIOLATIONS=10 OR MIN_CORR_TOTAL=20 (violationCorrelation); MIN_BIAS_ATTEMPTS=15 (biasSignals); MIN_QUESTION_SAMPLES=5 AND MIN_DISCRIM_READY=8 (discriminationIndex — >=8 ready attempts to form top/bottom quartiles, NOT the old MIN_DISCRIM_QUARTILE_N=3).
    - Every reducer returns a top-level object containing enough_data:boolean, threshold:number, samples:number, plus a thresholdNote:string for any signal whose gate is multi-dimensional (D-03 contract). Per-signal payload: scoreTrend also returns points sorted by date; questionStats also returns rows with per-row enough_data/sampleSize/passRate; violationCorrelation also returns r/samples/violationsCount/points/interpretation; discriminationIndex also returns rows with passTop/passBottom/delta/sampleTop/sampleBottom; biasSignals also returns tierDistribution.
    - Fixture attempts_20.json: 20 attempts across >=4 calendar days; mixed statuses (submitted/graded/emailed + pending_grading + grading_failed); >=5 attempts with UngradedCount > 0; >=3 transcript rows with a non-empty OverrideVerdict; >=1 questionId with <5 samples AND >=1 with >=5 samples; >=1 attempt with ViolationCount > 0 and >=1 with 0; one curated question where top-quartile scorers passed low and bottom-quartile passed high (anti-discriminating) for the discrimination index flag test.
  </behavior>
  <action>
    Create tests/analytics/analytics-reducers.ts modeled on tests/grading/rubric-grader.ts (pure TypeScript, zero vitest imports, so it is importable from anywhere). Structure it in this order:

    (a) Type aliases for the three sheet row layouts so parity with the GAS reducers is trivial: an Attempts row is a 15-tuple, a Responses row is a 5-tuple, a GradingTranscripts row is a 9-tuple, plus a Verdict union of the three literals.
    (b) Export the D-03 per-signal threshold constants by name: MIN_QUESTION_SAMPLES (5), MIN_TREND_ATTEMPTS (5), MIN_TREND_DAYS (3), MIN_CORR_VIOLATIONS (10), MIN_CORR_TOTAL (20), MIN_BIAS_ATTEMPTS (15), MIN_DISCRIM_READY (8). These numeric values MUST be identical to the constants Wave 1's backend/Analytics.gs uses so parity holds.
    (c) Import then re-export effectiveVerdict from ../grading/rubric-grader (the sole resolver — D-07 hard constraint, 11-PATTERNS § 4). Do not declare a local effectiveVerdict.
    (d) Five reducers, each O(N) — build hash maps keyed by attemptId/questionId in a single pass then join by key. No nested cross-sheet loops (Pitfall 1 / 6-minute cap invariant). Each reducer invokes the re-exported effectiveVerdict to resolve verdicts, passing object rows with the capitalized property names the resolver expects: a transcript row shaped { Verdict: row[3], OverrideVerdict: row[6] } and a responses row shaped { IsCorrect: row[3] } (matching AsyncGrading.gs:38-45 and :120-124). Denominator for any pass-rate is graded = correct + incorrect — ungraded is counted separately, never merged into the denominator (A2 policy, 11-PATTERNS § 5). Every emitted rate is accompanied by its sampleSize; never a naked percentage.
    (e) D-03 enough_data logic per reducer: scoreTrend enough_data is (scoredReady >= MIN_TREND_ATTEMPTS) AND (distinctDays >= MIN_TREND_DAYS) with threshold=5 samples=scoredReady thresholdNote describing the >=3 days clause; questionStats enough_data at top-level is (total graded responses across questions >= 5) with threshold=5 samples=that total, plus per-row enough_data per question = (row.sampleSize >= MIN_QUESTION_SAMPLES); violationCorrelation enough_data is (violationsCount >= MIN_CORR_VIOLATIONS) OR (samples >= MIN_CORR_TOTAL) with threshold=10 samples=readyCount and also returning violationsCount as its own field, thresholdNote describing the >=20-ready OR clause; biasSignals enough_data is (samples >= MIN_BIAS_ATTEMPTS) threshold=15 samples=readyCount; discriminationIndex enough_data is (readyCount >= MIN_DISCRIM_READY) threshold=8 samples=readyCount, with per-row gating on MIN_QUESTION_SAMPLES and quartile splits computed as top/bottom 25% of ready attempts.

    Create tests/analytics/fixtures/attempts_20.json with three top-level keys — attempts, responses, transcripts — each an array of row-arrays matching the sheet indexing (header rows already stripped, matching what handleAdminAnalytics receives after .slice(1)). Include the mix described in the behavior block so the Wave 0 specs can exercise both under-threshold and over-threshold paths on the same fixture.

    Keep analytics-reducers.ts under 260 lines. Do not import vitest. Do not depend on simple-statistics or mathjs (D-11 — Pearson is hand-rolled in the backend mirror only; the test mirror reuses the same eight-line two-pass reduce, no statistics dependency).
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment" && npx tsc --noEmit tests/analytics/analytics-reducers.ts && grep -c "import { effectiveVerdict }" tests/analytics/analytics-reducers.ts | grep -q '^1$' && ! grep -qE "function effectiveVerdict" tests/analytics/analytics-reducers.ts && node -e "const d=require('./tests/analytics/fixtures/attempts_20.json'); if(!Array.isArray(d.attempts)||d.attempts.length!==20)process.exit(1); if(!Array.isArray(d.responses)||d.responses.length<100)process.exit(1); if(!Array.isArray(d.transcripts)||d.transcripts.length<20)process.exit(1); console.log('fixture ok')"</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit on the mirror exits 0 — zero type errors.
    - Exactly one import { effectiveVerdict } from ../grading/rubric-grader exists; grep -c returns 1.
    - No local function effectiveVerdict definition exists anywhere in the mirror (grep -E "function effectiveVerdict" returns 0 matches).
    - All seven D-03 threshold constants are present by name with the LOCKED numeric values: MIN_QUESTION_SAMPLES=5, MIN_TREND_ATTEMPTS=5, MIN_TREND_DAYS=3, MIN_CORR_VIOLATIONS=10, MIN_CORR_TOTAL=20, MIN_BIAS_ATTEMPTS=15, MIN_DISCRIM_READY=8 (grep each name returns 1, and MIN_DISCRIM_QUARTILE_N returns 0 — the old wrong constant must be gone).
    - Each reducer's return object contains the keys enough_data, threshold, samples (grep each reducer body confirms the three property names appear in its return).
    - grep -c "simple-statistics\|mathjs" tests/analytics/analytics-reducers.ts returns 0 (D-11 — no statistics dep).
    - Fixture parses as JSON; attempts.length === 20, responses.length >= 100, transcripts.length >= 20; at least one transcript row has a non-empty OverrideVerdict at index 6 (node assert on the fixture).
    - Mirror file is under 260 lines.
  </acceptance_criteria>
  <done>Five-reducer mirror and the 20-attempt fixture exist; effectiveVerdict is re-exported not re-implemented; the seven D-03 per-signal constants are in place with the old MIN_DISCRIM_QUARTILE_N removed; the mirror imports no vitest and no statistics lib.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Spec set 1 — score-trend, question-stats parity, violation-correlation (D-03 gates, D-07 parity, D-11 no-stats-dep)</name>
  <files>tests/analytics/test_score_trend.ts, tests/analytics/test_question_stats.ts, tests/analytics/test_correlation.ts</files>
  <read_first>
    - tests/analytics/analytics-reducers.ts (the Task 1 mirror under test) and tests/analytics/fixtures/attempts_20.json (the fixture).
    - tests/grading/test_rubric_grader.ts (describe/it structure to copy; confirm the JSON-import idiom it uses for fixtures so the new specs match).
    - tests/grading/grading-engine.ts (existing per-attempt aggregation mirror — the parity assertion imports the per-attempt overall-pass-rate helper here; confirm the exported symbol name before writing the parity case).
    - backend/AsyncGrading.gs lines 127-160 (the A2 denominator policy the parity assertion must reproduce — graded = correct + incorrect, overallPercentage = round(100 * correctCount / totalGraded)).
    - .planning/phases/11-recruiter-analytics-dashboard/11-VALIDATION.md Per-Task Verification Map rows for ADMIN-06, ADMIN-07, ADMIN-08 and Wave 0 Requirements (per-signal threshold assertion obligations).
    - .planning/phases/11-recruiter-analytics-dashboard/11-CONTEXT.md D-03 (per-signal thresholds), D-07 (parity), D-11 (no statistics dep).
  </read_first>
  <behavior>
    - test_score_trend.ts (ADMIN-06): with the fixture's healthy slice returns day-bucketed points sorted by date, each carrying date/avgScore/n; avgScore is rounded; enough_data is true when >=5 scored-ready attempts span >=3 distinct calendar days; enough_data is false when only 4 attempts or when 6 attempts span only 2 days (asserts BOTH the >=5 AND >=3-days clauses of D-03, not a bare count); attempts with empty EndTime or non-finite OverallScore are skipped (Pitfall 6, 7).
    - test_question_stats.ts (ADMIN-07, Pitfall 2): passRate === null and enough_data === false for a questionId whose graded sampleSize is below MIN_QUESTION_SAMPLES (5); ungraded answers inflate per-qId .ungraded but are excluded from sampleSize and from the pass-rate denominator; the SPECIFIC parity assertion runs one fixture attempt through both the existing per-attempt aggregation path (imported from tests/grading/grading-engine.ts) and buildQuestionStats, and asserts the weighted-average per-question pass-rate equals the per-attempt overall percentage within plus-or-minus 1 (rounding tolerance) — theAsyncGrading.gs:127,144 A2 anchor (D-07).
    - test_correlation.ts (ADMIN-08): Pearson r on a hand-computed 12-point subset is within 0.001 of an inline numeric reference value declared in the test (D-11 — NO import of simple-statistics or mathjs; reference values are literals in the spec); enough_data is false when violationsCount < 10 AND readyCount < 20 (asserts the D-03 OR-gate — both under-threshold variants produce false); enough_data is true when violationsCount >= 10 (even if readyCount < 20) AND when readyCount >= 20 (even if violationsCount < 10) — the OR is exercised on both sides, NOT a uniform MIN of 5; interpretation label matches weak (|r|<0.2) / moderate (<0.5) / strong (>=0.5).
  </behavior>
  <action>
    Create three spec files modeled on tests/grading/test_rubric_grader.ts. Each starts with importing describe, it, expect from vitest; importing the relevant reducer(s) and the D-03 constants from ./analytics-reducers; loading the fixture via the same JSON-import idiom the rubric-grader spec uses (re-confirm that idiom in the read_first file rather than assuming).

    test_score_trend.ts: a describe block with the healthy-slice case plus a describe.each over two under-threshold variants (count-only-below and days-only-below) so the D-03 dual-clause gate is asserted on both sides. Assert each point's avgScore is rounded and that no point is emitted for an attempt with empty EndTime.

    test_question_stats.ts: a describe block for the below-MIN per-qId case and a dedicated describe block titled to name Pitfall 2 and the A2 parity assertion it contains. The parity assertion imports the per-attempt aggregate helper from ../grading/grading-engine.ts (confirm the exact exported name) and compares its overallPercentage to the sampleSize-weighted average of buildQuestionStats pass-rates for one fixture attempt — assert equality within plus-or-minus 1. The parity block's grep-able title must contain the literal "A2 parity" so the coverage gate can find it.

    test_correlation.ts: a describe block whose it cases declare an inline numeric reference for r (hand-computed literal, not imported), assert the reducer matches within 0.001, then a table of four rows asserting enough_data true via either OR-branch and enough_data false only when BOTH clauses fail — explicitly asserting the >=10-with-violations OR >=20-total gate rather than a uniform 5.

    Keep each spec under 160 lines. Use named imports only; never import simple-statistics or mathjs anywhere (D-11).
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment" && npx vitest run tests/analytics/test_score_trend.ts tests/analytics/test_question_stats.ts tests/analytics/test_correlation.ts -x --reporter=verbose && grep -c "A2 parity" tests/analytics/test_question_stats.ts | grep -qE '^[1-9]' && ! grep -q "simple-statistics\|mathjs" tests/analytics/test_correlation.ts tests/analytics/test_question_stats.ts tests/analytics/test_score_trend.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run on the three file paths exits 0 with every it case passing.
    - test_score_trend.ts contains assertions for BOTH under-count and under-days variants (grep for the days clause keyword, e.g. "distinct" or "days", returns >=1 in the under-threshold assertions).
    - test_question_stats.ts contains the parity describe block whose title includes "A2 parity" (grep -c "A2 parity" >= 1) and which references the per-attempt aggregate helper from ../grading/grading-engine.ts.
    - test_correlation.ts asserts enough_data false require BOTH (violationsCount < 10 AND readyCount < 20) and true on EITHER branch — grep finds both MIN_CORR_VIOLATIONS and MIN_CORR_TOTAL referenced in the spec (the OR-gate, not a uniform 5).
    - grep -c "simple-statistics\|mathjs" across the three new spec files returns 0 (D-11 — no statistics dep).
    - Each spec is under 160 lines.
  </acceptance_criteria>
  <done>The three reducer specs are green; the Pitfall-2 A2 parity case reproduces AsyncGrading.gs:127,144; the correlation spec asserts the D-03 >=10-OR->=20 gate on both branches; no statistics library is imported anywhere in the new specs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Spec set 2 + infra — discrimination (>=8 ready quartile), ungraded-caveat, auth stub, vitest exclude, sync-check sections 16-19</name>
  <files>tests/analytics/test_discrimination.ts, tests/analytics/test_ungraded_caveat.ts, tests/analytics/test_analytics_auth.ts, vitest.config.ts, scripts/sync-check.ts</files>
  <read_first>
    - tests/analytics/analytics-reducers.ts (Task 1 mirror under test) and tests/analytics/fixtures/attempts_20.json.
    - tests/grading/test_rubric_grader.ts (spec shape, and the exact tsconfig/vitest JSON-import idiom to reuse).
    - vitest.config.ts (the current exclude array — add exactly one entry for analytics-reducers.ts after the existing rubric-grader.ts entry; do not touch any other config field).
    - scripts/sync-check.ts (numbered-section convention in sections 1-15, the CODE_GS/ASYNC_GS load near the top at lines ~20-30, the has() and check() helpers, and the failing-on-first-error incremental-failureCounter semantics — copy that convention verbatim for sections 16-19).
    - .planning/phases/11-recruiter-analytics-dashboard/11-VALIDATION.md Per-Task Verification Map rows for ADMIN-09, data-quality, and auth + Wave 0 Requirements.
    - .planning/phases/11-recruiter-analytics-dashboard/11-CONTEXT.md D-03 (discrimination >=8 ready + >=5 graded), D-12 (sync-check must gate meta.aggregationMs).
  </read_first>
  <behavior>
    - test_discrimination.ts (ADMIN-09): the curated anti-discriminating fixture question is flagged (its qId appears in the bottom-N rows by smallest delta); a question with fewer than MIN_QUESTION_SAMPLES graded is excluded from rows; enough_data is false until readyCount reaches MIN_DISCRIM_READY (8) — assert false at 7 ready and true at 8 ready (D-03 quartile threshold, NOT a uniform 5); each returned row carries passTop, passBottom, delta, sampleTop, sampleBottom.
    - test_ungraded_caveat.ts (data-quality): buildQuestionStats on the fixture yields at least one qId row with ungraded > 0 and that ungraded count is excluded from sampleSize; confirms ungraded is surfaced and not zero-defaulted (D-09 invariant, V8 data-protection).
    - test_analytics_auth.ts: a spec that documents the Wave-1 auth contract — the mirror has no handler to call, so this file holds a test.todo for handleAdminAnalytics to reject a missing or wrong token returning { success:false, error:"Unauthorized" }, plus a passing assertion that effectiveVerdict is the re-exported resolver (proxies the same function identity). This keeps ADMIN-08-adjacent auth coverage discoverable per VALIDATION.md without faking the GAS handler.
    - vitest.config.ts: analytics-reducers.ts added to exclude so vitest never collects it as a spec.
    - scripts/sync-check.ts: four new numbered sections 16-19 wired to backend/Code.gs and backend/Analytics.gs; until Wave 1 lands they emit labeled FAIL lines pointing at the missing file/handler (the enforcement signal), and they never crash sync-check by throwing.
  </behavior>
  <action>
    Create tests/analytics/test_discrimination.ts: describe block asserting the curated anti-discriminating qId is in the flagged rows, that a sub-MIN graded question is excluded, and a table asserting enough_data false at readyCount 7 vs true at 8 (MIN_DISCRIM_READY threshold). Reference MIN_DISCRIM_READY explicitly so the threshold is assertable, not a uniform 5.

    Create tests/analytics/test_ungraded_caveat.ts: describe block asserting the fixture yields at least one row with ungraded > 0, that row's sampleSize equals correct+incorrect (ungraded excluded from denominator), and the top-level questionStats surfaces the ungraded total (not zero). This is the V8 / D-09 data-quality invariant.

    Create tests/analytics/test_analytics_auth.ts: import effectiveVerdict from ./analytics-reducers and assert a function-identity invariant (the exported name === the imported name), plus a test.todo stating the Wave-1 handler returns { success:false, error:"Unauthorized" } when checkAdminAuth fails — discoverability for the VALIDATION.md auth row without faking the GAS handler.

    Edit vitest.config.ts: append one string literal to the exclude array that matches analytics-reducers.ts (same glob style as the existing 'tests/**/rubric-grader.ts' entry) immediately after that entry. Touch no other field.

    Edit scripts/sync-check.ts: near the top, defensively load backend/Analytics.gs into a new ANALYTICS_GS variable (try/catch on readFileSync so absence does not throw — sections 18-19 will then cleanly FAIL until Wave 1 lands). Before the existing Summary block, add four numbered sections in the established comment convention: section 16 asserting the adminAnalytics action is registered in doGet via regex on CODE_GS; section 17 asserting function handleAdminAnalytics contains a checkAdminAuth(token) call within the first ~300 characters of its body; section 18 asserting the Analytics reducers call effectiveVerdict (regex window over ANALYTICS_GS around buildQuestionStats through the call); section 19 asserting meta.aggregationMs is instrumented (D-12). Reuse the existing has() helper and the incremental failureCounter semantics — do not throw on a failing section; increment failures and continue. Do not modify sections 1-15.
  </action>
  <verify>
    <automated>cd "C:/Users/anoop/OneDrive/Desktop/FS-gamified-assessment" && npx vitest run tests/analytics/test_discrimination.ts tests/analytics/test_ungraded_caveat.ts tests/analytics/test_analytics_auth.ts -x --reporter=default && npx vitest run --reporter=default 2>&1 | grep -q "analytics-reducers" && echo "MIRROR-EXCLUDE-VIOLATION" && false || npx tsx scripts/sync-check.ts ; echo "sync_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run on the two real specs (discrimination, ungraded_caveat) plus the auth stub exits 0.
    - test_discrimination.ts references MIN_DISCRIM_READY (=8) and asserts enough_data false at 7 ready and true at 8 ready — NOT a uniform 5 (grep for MIN_DISCRIM_READY returns >=1; grep for the digits asserting the 7-vs-8 boundary is present).
    - test_ungraded_caveat.ts asserts at least one qId row with ungraded > 0 and that ungraded is excluded from sampleSize.
    - vitest.config.ts excludes analytics-reducers.ts: npx vitest run (whole suite) does NOT collect analytics-reducers.ts as a spec (a sentinel: if the mirror were collected, its lack of vitest imports would surface a load error or a "No test found" warning against it — confirm the mirror is absent from the collected-file list).
    - scripts/sync-check.ts runs to completion without throwing (exit code may be 1); sections 1-15 still PASS; sections 16-19 each emit a labeled PASS or FAIL line (never silent/absent). Section 18 reports FAIL referencing the not-yet-created backend/Analytics.gs until Wave 1 lands.
    - scripts/sync-check.ts file stays under 320 lines.
  </acceptance_criteria>
  <done>Discrimination spec asserts the >=8 ready quartile threshold (D-03 + the key replan delta); the ungraded-caveat spec proves ungraded is surfaced not zero-defaulted (D-09/V8); vitest excludes the mirror; sync-check has four Wave-1 enforcement gates that fail loudly until Wave 1 ships.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline -> repo | sync-check.ts is the drift gate; if it silently passes on missing files, drift lands undetected |
| test runtime -> mirror module | Vitest imports the mirror; if the mirror ever imports vitest, spec-loading side effects contaminate other tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | Analytics reducer resolver | high | mitigate | Mirror re-exports effectiveVerdict from tests/grading/rubric-grader.ts; sync-check section 18 blocks any re-implementation once Wave 1 lands (D-07). |
| T-11-02 | Information Disclosure | Fixture data | low | accept | Fixture uses synthetic names/emails only — do not copy real Attempts rows into fixtures. |
| T-11-03 | Denial of Service | Slow reducer under large N | medium | mitigate | Reducers enforce O(N) hash-map joins (per D-implicit / Pitfall 1). D-12 instrumentation gates the live measurement in Wave 3. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies introduced in Wave 0. Fixture is pure JSON; Pearson parity uses hand-computed inline reference numbers instead of installing simple-statistics (D-11). |
</threat_model>

<verification>
- npx vitest run tests/analytics/ — every Wave-0 spec passes (score_trend, question_stats parity, correlation, discrimination, ungraded_caveat, auth stub).
- npx vitest run (full suite) — green; analytics-reducers.ts NOT collected as a spec.
- npx tsx scripts/sync-check.ts — runs cleanly; sections 16-19 emit labeled FAIL lines pointing at the missing backend/Analytics.gs and missing handleAdminAnalytics (the enforcement signal for Wave 1, not a Wave-0 defect); sections 1-15 unchanged.
- wc -l tests/analytics/*.ts scripts/sync-check.ts — every file under 500 (project rule).
</verification>

<success_criteria>
- The analytics mirror is testable on Node with zero GAS dependencies; the seven D-03 per-signal threshold constants are in place with the old MIN_DISCRIM_QUARTILE_N gone.
- The Pitfall-2 A2 denominator policy is provable — any drift between the per-attempt and per-question reducers fails test_question_stats.ts (D-07).
- test_correlation.ts and test_discrimination.ts assert the D-03 >=10->=20 OR-gate and the >=8 ready quartile threshold respectively — NOT a uniform 5 (the key delta from the pre-replan plans).
- sync-check has four new drift gates ready to enforce Wave 1's contract.
- Zero backend or frontend code exists yet — Wave 1 starts with tests in place.
</success_criteria>

<output>
Create `.planning/phases/11-recruiter-analytics-dashboard/11-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **New file:** `tests/analytics/analytics-reducers.ts` — pure JS mirror of the five analytics reducers landing in Wave 1; re-exports `effectiveVerdict` from `tests/grading/rubric-grader.ts`; exports the seven D-03 per-signal threshold constants (MIN_QUESTION_SAMPLES=5, MIN_TREND_ATTEMPTS=5, MIN_TREND_DAYS=3, MIN_CORR_VIOLATIONS=10, MIN_CORR_TOTAL=20, MIN_BIAS_ATTEMPTS=15, MIN_DISCRIM_READY=8); every reducer returns `{ enough_data, threshold, samples, thresholdNote, ...payload }` aligned to its signal's D-03 threshold.
- **New file:** `tests/analytics/fixtures/attempts_20.json` — shared 20-attempt fixture (mixed statuses, ungraded/override mix, low-N and healthy-N slices, one curated anti-discriminating question).
- **New files:** `tests/analytics/test_score_trend.ts`, `test_question_stats.ts` (Pitfall-2 A2 parity), `test_correlation.ts` (>=10/>=20 OR-gate), `test_discrimination.ts` (>=8 ready quartile), `test_ungraded_caveat.ts` (V8 ungraded-surfaced), `test_analytics_auth.ts` (auth-contract stub).
- **Modified:** `vitest.config.ts` — one entry added to `exclude` so the mirror is not collected as a spec.
- **Modified:** `scripts/sync-check.ts` — four new numbered sections (16-19) enforcing endpoint action registration, auth gate, effectiveVerdict reuse, and `meta.aggregationMs` instrumentation (D-12).