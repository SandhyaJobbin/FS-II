---
phase: 01-content-ingestion-question-bank
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - ingestion/parsers/english.py
  - tests/ingestion/test_english_e2e.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03]

must_haves:
  truths:
    - "English doc yields exactly 105 items: grammar 30, sentence_correction 30, macro 10, reading 25 (5 passages × 5), closure 10"
    - "50 open-ended items ingest faithfully: sentence_correction and macro items have response_type open_text, empty options, and non-empty model_answer; closure items have response_type hybrid (3-option MCQ part + model_answer notes part)"
    - "Macro template placeholders like {{ticket.requester.first_name}} survive verbatim in stored text"
    - "Reading passages are modeled as cases (scenario = passage body) so the 5 questions per passage stay grouped — case-level grouping exists wherever the source groups items"
    - "Every English item carries category (bank=english) plus a section grouping tag; non-case items never carry a null grouping (INGEST-02 empty edge)"
    - "Reading select-all items (multiple trailing ✅) parse as mcq_multi with ≥2 correct options"
  artifacts:
    - ingestion/parsers/english.py (full: Parts 1/2/3/6/7)
    - tests/ingestion/test_english_e2e.py
  key_links:
    - "run.py auto-discovers the expanded english parser with zero edits to run.py (registry contract from 01-02)"
    - "normalize.py item model already supports open_text/hybrid — no schema or normalize changes in this plan"
  prohibitions:
    - "Never drop the 50 open-ended items or fabricate MCQ options for them — ingest faithfully with response_type + model_answer (01-RESEARCH.md Pitfall 4)"
  assumptions:
    - "A-OQ3 (open-ended grading policy): open_text/hybrid items are ingested with model_answer in server-only custody; the serve-and-grade policy (fuzzy-match / exclude / MCQ-ify) is explicitly DEFERRED to the user before Phase 2/3 — ingestion takes no position (01-RESEARCH.md OQ-3 recommendation). FLAG for user confirmation at /gsd-verify-work."
---

<objective>
Expansion: extend the English parser from the grammar tracer slice to the full document — Sentence Correction (30 open_text), Macro Editing (10 open_text with template placeholders), Reading Comprehension (5 passages × 5, trailing-✅, select-all), Case Closure (10 hybrid). 105 items total.

Purpose: INGEST-01's count reconciliation (105) and INGEST-02's section grouping for the whole English bank; proves the item model handles every response_type the phase produces.
Output: full English bank parsed and e2e-tested against the real docx.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md
@.planning/phases/01-content-ingestion-question-bank/01-PATTERNS.md
@.planning/phases/01-content-ingestion-question-bank/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand english.py to Parts 2/3/6/7 (open_text, reading cases, hybrid)</name>
  <files>ingestion/parsers/english.py</files>
  <read_first>
    - ingestion/parsers/english.py (current state — grammar walker from the tracer; extend, do not rewrite)
    - ingestion/normalize.py (Item model contract: response_type, case fields, model_answer)
    - ingestion/parsers/markers.py (trailing_check extractor — reuse, do not duplicate)
    - .planning/phases/01-content-ingestion-question-bank/01-RESEARCH.md (English anatomy: Part 2 shape `Qn. ...\nModel Answer: ...` one paragraph newline-separated; Part 3 blocks Qn/Customer Scenario/Existing Macro/Model Answer; Part 6 `Passage N – Title` + items `1.`–`5.` + options `a)` trailing ✅ (38 ✅ over 25 Q); Part 7 `Qn – Title` + 3-option status MCQ + Case Closure Notes with Model Answer)
  </read_first>
  <action>
    Part 2 Sentence Correction (30): heading `Part 2: Sentence Correction`; each item is ONE paragraph containing `Qn. <faulty sentence>` and `Model Answer: <corrected>` separated by an embedded newline — split_lines, then build Item(response_type="open_text", stem=faulty sentence, options=[], model_answer=corrected, section="sentence_correction").
    Part 3 Macro Editing (10): heading `PART 3: MACRO EDITING & PERSONALIZATION`; blocks of `Qn` / `Customer Scenario` / `Existing Macro` / `Model Answer` — compose stem from scenario + existing macro, model_answer from the Model Answer block; response_type="open_text", section="macro". Template placeholders (e.g. `{{ticket.requester.first_name}}`) preserved byte-verbatim in both stem and model_answer.
    Part 6 Reading (25): heading `PART 6: READING COMPREHENSION`; each `Passage N – Title` opens a case-like group: create case_id `eng-reading-passage-{N}`, case scenario = Customer Message + passage body; the following `1.`–`5.` numbered items (NOT `Qn.`) with `a)` options use `trailing_check` — exactly-1 ✅ → mcq_single, multiple ✅ → mcq_multi; section="reading", case_id set on all 5.
    Part 7 Case Closure (10): heading `PART 7: CASE CLOSURE NOTES`; `Qn – Title` + Scenario + Case Status with 3 options `a) Open b) Pending c) Solved` via trailing_check (exactly 1 ✅) plus open-ended `Case Closure Notes` with `Candidate Response:` blank and `Model Answer` — Item(response_type="hybrid", options=3 status options, model_answer=notes model answer, section="closure", case_id=`eng-closure-{N}`, case scenario=Scenario text).
    Part-number tolerance: headings matched by regex on text; NEVER assert contiguous part numbers (Parts 4–5 are missing — revision residue).
    Keep the file under 500 lines (CLAUDE.md) — factor per-part walkers into small functions sharing a block-iteration cursor.
  </action>
  <verify>
    <automated>$env:PYTHONIOENCODING="utf-8"; python ingestion/run.py --banks english; python -c "import json; d=json.load(open('content/questions.json',encoding='utf-8')); e=[i for i in d['items'] if i['bank']=='english']; print(len(e), {s: sum(1 for i in e if i['section']==s) for s in ['grammar','sentence_correction','macro','reading','closure']})"</automated>
  </verify>
  <acceptance_criteria>
    - run.py exits 0; printed count is 105 with per-section dict {grammar: 30, sentence_correction: 30, macro: 10, reading: 25, closure: 10}
    - All sentence_correction + macro items: response_type open_text, options == [], model_answer non-empty
    - All closure items: response_type hybrid, exactly 3 options with exactly 1 is_correct, model_answer non-empty
    - Reading items: 5 distinct case_ids, 5 items each; select-all items have ≥2 correct options
    - At least one macro item's text contains the literal substring `{{ticket.requester.first_name}}`
  </acceptance_criteria>
  <done>English bank fully parsed: 105 items across 5 sections with correct response_types, verbatim placeholders, reading/closure grouped as cases; run.py emits them without any run.py edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: English e2e tests against the real docx</name>
  <files>tests/ingestion/test_english_e2e.py</files>
  <read_first>
    - tests/conftest.py (fixture builders; malformed corpus conventions)
    - ingestion/parsers/english.py, ingestion/validate.py (code under test)
    - .planning/phases/01-content-ingestion-question-bank/01-VALIDATION.md (sampling: pytest tests/ingestion -x -q per task)
  </read_first>
  <behavior>
    - Full English parse → 105 items; per-section counts 30/30/10/25/10
    - Grammar golden anchor still holds (Q1 correct = b) — regression guard
    - Sentence correction item: stem ≠ model_answer, model_answer non-empty, options empty
    - Macro item model_answer contains `{{ticket.requester.first_name}}` verbatim
    - Reading: 5 passages × 5 items sharing case_id per passage; ≥1 select-all item with ≥2 correct
    - Closure: hybrid shape (3 options / 1 correct / model_answer)
    - Every item has bank + section tag (no null grouping)
    - Malformed fixture: a docx where a sentence-correction item lacks `Model Answer:` → run exits non-zero naming the item
    - Marker census: reading section yields exactly 38 ✅ across its 25 items
  </behavior>
  <action>
    Create tests/ingestion/test_english_e2e.py using the REAL `Fraud support/FS Question Bank_English Proficiency V2.docx` (path from manifest.SOURCE_DIR). Parse via the english module directly for shape assertions; use subprocess run of run.py for the malformed-fixture exit-code test (build the malformed docx by copying the real one and deleting one `Model Answer:` paragraph — construct via python-docx in the test using tmp_path). All I/O encoding="utf-8". Add the malformed no-model-answer builder to tests/conftest.py if reuse is cleaner (keep conftest under 500 lines).
  </action>
  <verify>
    <automated>pytest tests/ingestion/test_english_e2e.py -q; pytest tests/ingestion -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ingestion/test_english_e2e.py -q` exits 0 with all behavior cases above as named tests
    - Full suite `pytest tests/ingestion -x -q` still green (no tracer regressions)
    - Malformed test asserts exit code ≠ 0 AND the missing item's identity appears in stderr
  </acceptance_criteria>
  <done>105-item English parse proven against the real docx with golden anchors, response-type shapes, placeholder fidelity, case grouping, census, and one loud-failure malformed case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| English docx parts 2/3/6/7 → parser | Untrusted authored content with heterogeneous shapes (open text, placeholders, select-all) |
| model_answer fields → artifacts/DB | Server-only key-equivalent data at rest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | Open-ended items silently dropped or force-fit into the MCQ model (50 of 385 items) | high | mitigate | Explicit response_type open_text/hybrid in item model; per-section count reconciliation (30/30/10/25/10) makes a drop a loud failure; e2e shape tests |
| T-01-09 | Info Disclosure | model_answer leaking into logs or non-server contexts | medium | mitigate | model_answer stored like keys (server-only custody); never printed by run.py; artifacts committed per A-OQ4 (same exposure as keyed docx) |
| T-01-10 | Tampering | Select-all reading items parsed as single-answer (key loss) | high | mitigate | trailing_check counts ALL ✅ per question; census assertion (38 ✅ / 25 items); mcq_multi invariant ≥1 correct |
| T-01-SC | Tampering | npm/pip installs | high | mitigate | No new installs in this plan; 01-RESEARCH.md audit covers the full stack |
</threat_model>

<verification>
- `python ingestion/run.py --banks english` exits 0 with 105 items
- `pytest tests/ingestion -x -q` green including test_english_e2e.py
- Reading ✅ census == 38; macro placeholder verbatim; closure hybrid shape
</verification>

<success_criteria>
English bank (105 items) fully ingested with all four response_types exercised, open-ended content preserved faithfully, and grading policy explicitly deferred (A-OQ3) rather than silently chosen.
</success_criteria>

## Artifacts this phase produces (this plan)

- Extended module: `ingestion/parsers/english.py` — per-part walkers for Part 2 (sentence_correction), Part 3 (macro), Part 6 (reading, case_id `eng-reading-passage-{N}`), Part 7 (closure hybrid, case_id `eng-closure-{N}`)
- Test file: `tests/ingestion/test_english_e2e.py` (real-doc e2e + malformed no-model-answer fixture)
- Data shapes exercised: response_type `open_text` and `hybrid`; case-grouped reading passages; model_answer custody
- Emitted artifact: content/questions.json with 105 English items (regenerated; final full-bank emit in 01-06)

<output>
Create `.planning/phases/01-content-ingestion-question-bank/01-03-SUMMARY.md` when done
</output>
