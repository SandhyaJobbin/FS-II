# Phase 3, Plan 01 — Backend Grading Engine Hardening

## Goal

Close all 5 grading gaps in `backend/Code.gs` against the Phase 3 success criteria.

## Changes

### backend/Code.gs

#### handleSubmitAnswers (lines 280–530 after edit)

1. **Fix 1 — Narrative from `difficulty_tier === 'complex'`**: Replace `q.section === 'critical' || q.level === 'L2/L3'` heuristic with `q.difficulty_tier === 'complex'`. Add `complexCorrect` and `complexTotal` counters per bank.
2. **Fix 2 — 5-branch narrative pool**: Generate narrative from globalComplexFailed ratio (6 branches total including overall-proxy fallback).
3. **Fix 3 — Field names**: Return `recommendationTier` / `narrativeInsight` (not `recommendation` / `insight`).
4. **Fix 4 — Atomic batch write**: Replace 8 individual `setValue` with 3 `setValues` range calls. Batch response rows into single `setValues` block.

#### handleGetAttemptReport (lines 427–461 after edit)

5. **Fix 5 — Field alignment**: Return `recommendationTier` / `narrativeInsight` to match `handleSubmitAnswers` contract.

## Status: COMPLETE
