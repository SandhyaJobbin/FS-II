---
phase: 09
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/async/queue-logic.ts
  - tests/async/email-content.ts
  - tests/async/test_queue.ts
  - tests/async/test_email.ts
  - vitest.config.ts
autonomous: true
requirements: [ASYNC-03, ASYNC-04, ASYNC-05]
must_haves:
  truths:
    - "The Stage/retry state machine (queued -> graded -> done/permanently_failed) and the 5-row batch-selection logic are covered by fast, dependency-free unit tests (ASYNC-02/05)"
    - "Candidate email content always includes overall score, 3 trait scores, narrative insight, and recommendation tier, and never includes violation data; recruiter email content always includes a violation/integrity summary with tier highlighted (ASYNC-03/04)"
    - "parseRecruiterEmails correctly handles comma-separated, empty, and whitespace-heavy RECRUITER_EMAILS input (D-06/D-07)"
  artifacts:
    - "tests/async/queue-logic.ts, tests/async/email-content.ts -- new pure mirrors with zero SpreadsheetApp/MailApp/LockService/PropertiesService dependencies"
    - "tests/async/test_queue.ts, tests/async/test_email.ts -- new vitest suites"
    - "vitest.config.ts -- exclude array extended so the two new mirror files are importable helpers, not run as test suites themselves"
  key_links:
    - "queue-logic.ts and email-content.ts encode the same Stage/retry/email-content rules that backend/AsyncGrading.gs (plan 09-02) implements -- plan 09-05's sync-check.ts drift-checks the two against each other after both exist"
    - "vitest.config.ts's include (`tests/**/*.ts`) already picks up files under tests/async/ -- only the exclude array needs the 2 new mirror filenames, mirroring how grading-engine.ts/admin-auth.ts are already excluded"
---

<objective>
Fill the 4 Wave-0 test-infrastructure gaps identified in 09-VALIDATION.md: pure, dependency-free TypeScript mirrors of the async queue's stage/retry/batch-selection logic and the candidate/recruiter email-content builders, each with a vitest suite, following the exact GAS-to-Node mirror convention established in Phase 08 (`tests/grading/grading-engine.ts`, `tests/admin/admin-auth.ts`).

Purpose: `backend/AsyncGrading.gs` (plan 09-02) cannot be executed or unit-tested locally -- Apps Script has no local runner. This plan is what makes ASYNC-02/03/04/05's behavioral contracts (D-01 through D-12) actually verifiable in under 2 seconds via `npm test`, and gives plan 09-05's `sync-check.ts` something to drift-check the real `.gs` file against.
Output: `tests/async/queue-logic.ts`, `tests/async/email-content.ts`, `tests/async/test_queue.ts`, `tests/async/test_email.ts`, and an updated `vitest.config.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-async-grading-report-delivery-pipeline/09-CONTEXT.md
@.planning/phases/09-async-grading-report-delivery-pipeline/09-RESEARCH.md
@.planning/phases/09-async-grading-report-delivery-pipeline/09-PATTERNS.md
@.planning/phases/09-async-grading-report-delivery-pipeline/09-VALIDATION.md
@tests/grading/grading-engine.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure mirror of the queue stage/retry/batch-selection logic</name>
  <files>tests/async/queue-logic.ts, tests/async/test_queue.ts</files>
  <read_first>
    tests/grading/grading-engine.ts (full file, for the header-comment convention and the "pure mirror, zero GAS globals" style). 09-PATTERNS.md's `tests/async/queue-logic.ts` section (exact header-comment template to copy, substituting "Phase 3" for "Phase 9"). 09-CONTEXT.md D-09 (5-minute cadence), D-10 (batch cap 5), D-11 (retry cap 3).
  </read_first>
  <behavior>
    - Test: `selectEligibleRows(rows)` returns only rows whose `stage` is `"queued"` or `"graded"`, excluding `"done"` and `"permanently_failed"`, and caps the result at 5 items even when more than 5 rows qualify
    - Test: `isPermanentlyFailed(attemptsCount)` returns `false` for 0, 1, 2 and `true` for 3 and above (D-11's retry cap)
    - Test: `nextStageAfterGradingSuccess()` returns `"graded"`; `nextStageAfterCandidateEmailSent()` returns `"done"`
    - Test: `nextStageAfterFailure(attemptsCount)` returns `"permanently_failed"` once the incremented count reaches 3, otherwise returns the unchanged current stage (queue stays eligible for retry)
  </behavior>
  <action>
    Create `tests/async/queue-logic.ts` as a pure TypeScript module with zero imports from `SpreadsheetApp`/`LockService`/`MailApp`/`PropertiesService` -- only plain functions and types operating on plain objects/arrays, matching the "pure mirror" convention from `tests/grading/grading-engine.ts`. Open with the same header-comment style as `grading-engine.ts` (phase-labeled purpose comment), adapted to reference Phase 9's async queue instead of Phase 3's grading. Export `selectEligibleRows`, `isPermanentlyFailed`, `nextStageAfterGradingSuccess`, `nextStageAfterCandidateEmailSent`, and `nextStageAfterFailure`, implementing exactly the behavior listed above -- these names and their logic must match what `backend/AsyncGrading.gs`'s `readEligiblePendingRows`/`recordQueueItemFailure`/`processQueueItem` do (plan 09-02), since plan 09-05 will regex-check both files for the same literals (batch cap `5`, retry cap `3`, cadence `5`). Create `tests/async/test_queue.ts` importing from `./queue-logic` and asserting each behavior above using vitest `describe`/`it`/`expect`, following the existing `test_grading.ts` structure and naming convention (`test_<domain>.ts`).
  </action>
  <verify>
    <automated>npx vitest run tests/async/test_queue.ts</automated>
  </verify>
  <acceptance_criteria>
    `tests/async/queue-logic.ts` has zero references to any GAS global (`SpreadsheetApp`, `LockService`, `MailApp`, `PropertiesService`, `ScriptApp`). `tests/async/test_queue.ts` passes and covers all 4 behaviors listed above, including the batch-cap-of-5 and retry-cap-of-3 edge cases.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure mirror of candidate/recruiter email content builders</name>
  <files>tests/async/email-content.ts, tests/async/test_email.ts</files>
  <read_first>
    tests/grading/grading-engine.ts (header-comment convention, reused for this file too). 09-PATTERNS.md's `tests/async/email-content.ts` section. 09-CONTEXT.md D-01 (candidate content: overall score, 3 trait scores, narrative, tier), D-02 (recruiter content: violation/integrity summary, tier highlighted), D-04 (candidate email never includes violation data), D-06/D-07 (recruiter recipient parsing and single comma-joined send).
  </read_first>
  <behavior>
    - Test: `buildCandidateEmailContent(report)` output string contains the overall score, all 3 trait scores (language/research/critical), the narrative insight text, and the recommendation tier -- and contains no violation-count or integrity-related substring anywhere in the output (D-04)
    - Test: `buildRecruiterEmailContent(report, integritySummary)` output string contains everything the candidate version contains, plus a violation/integrity summary section with the tier visibly highlighted (D-02)
    - Test: `parseRecruiterEmails("a@x.com, b@y.com")` returns `["a@x.com", "b@y.com"]`
    - Test: `parseRecruiterEmails("")` returns `[]`
    - Test: `parseRecruiterEmails("  ,  ,c@z.com  ,  ")` returns `["c@z.com"]` (whitespace-heavy and empty-segment tolerant, D-06/D-07)
  </behavior>
  <action>
    Create `tests/async/email-content.ts` as a pure TypeScript module (no `MailApp`/`PropertiesService` dependency -- `parseRecruiterEmails` and the two content builders accept plain string/object parameters instead of reading Script Properties directly) exporting `buildCandidateEmailContent(report)`, `buildRecruiterEmailContent(report, integritySummary)`, and `parseRecruiterEmails(raw)`, implementing exactly the behavior above. Use the same header-comment convention as Task 1's `queue-logic.ts`. Create `tests/async/test_email.ts` importing from `./email-content` and asserting each behavior above with vitest `describe`/`it`/`expect`, explicitly asserting the D-04 negative case (no violation-related substring in the candidate output) using a targeted string-search assertion, not a loose snapshot.
  </action>
  <verify>
    <automated>npx vitest run tests/async/test_email.ts</automated>
  </verify>
  <acceptance_criteria>
    `tests/async/test_email.ts` passes and explicitly asserts: candidate content includes all 4 required fields and excludes violation data; recruiter content includes the violation/integrity summary with tier highlighted; `parseRecruiterEmails` handles the comma-separated, empty, and whitespace-heavy cases correctly.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire the new mirror files into vitest.config.ts and confirm zero regressions</name>
  <files>vitest.config.ts</files>
  <read_first>
    vitest.config.ts (current `exclude` array -- confirm the existing entries for `grading-engine.ts`/`admin-auth.ts` and match their exact array-entry format).
  </read_first>
  <action>
    Add `tests/async/queue-logic.ts` and `tests/async/email-content.ts` to `vitest.config.ts`'s existing `exclude` array, in the same format/style already used for `tests/grading/grading-engine.ts` and `tests/admin/admin-auth.ts` -- these two files are plain helper modules imported by test files, not test suites themselves, and must not be picked up and run directly by vitest's `include: ['tests/**/*.ts']` glob. Do not touch `include` or any other config key. After the edit, run the full suite to confirm the new async tests integrate cleanly alongside the existing grading/admin/assembly/integrity suites with zero regressions.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    `vitest.config.ts`'s `exclude` array contains both new mirror file paths. `npm test` reports all existing tests plus the new `test_queue.ts`/`test_email.ts` suites passing, with zero failures and zero files run twice (mirror files are not separately executed as suites).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds only test infrastructure -- no new runtime trust boundary is introduced. The mirrors run exclusively in the local/CI Node test process, never in the Apps Script production environment.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-09-11-partial | Tampering (code drift) | `tests/async/queue-logic.ts`, `tests/async/email-content.ts` vs `backend/AsyncGrading.gs` | medium | mitigate | These mirrors encode the same literals (retry cap 3, batch cap 5, cadence 5 min) and content rules as `backend/AsyncGrading.gs`; plan 09-05 adds automated drift detection (`scripts/sync-check.ts`) comparing the two -- this plan alone only reduces risk by making the contract explicit and testable, full mitigation lands in 09-05 |

No package-manager installs in this plan (0 new dependencies) -- Package Legitimacy Gate not applicable.
</threat_model>

<verification>
`npx vitest run tests/async/test_queue.ts tests/async/test_email.ts` after Tasks 1-2 (~1-2 seconds per 09-VALIDATION.md's estimated runtime). `npm test` after Task 3 to confirm the full suite (existing 90 tests + new async tests) is green with the `vitest.config.ts` exclude update in place.
</verification>

<success_criteria>
- `tests/async/queue-logic.ts` and `tests/async/email-content.ts` exist with zero GAS-global dependencies
- `tests/async/test_queue.ts` and `tests/async/test_email.ts` pass, covering ASYNC-02/03/04/05's behavioral contracts
- `vitest.config.ts` excludes both new mirror files from direct execution
- `npm test` passes with zero regressions
</success_criteria>

## Artifacts this phase produces

- `tests/async/queue-logic.ts` (new) -- pure mirror: `selectEligibleRows`, `isPermanentlyFailed`, `nextStageAfterGradingSuccess`, `nextStageAfterCandidateEmailSent`, `nextStageAfterFailure`
- `tests/async/email-content.ts` (new) -- pure mirror: `buildCandidateEmailContent`, `buildRecruiterEmailContent`, `parseRecruiterEmails`
- `tests/async/test_queue.ts`, `tests/async/test_email.ts` (new) -- vitest suites
- `vitest.config.ts` (modified) -- `exclude` array extended with the 2 new mirror files

<output>
Create `.planning/phases/09-async-grading-report-delivery-pipeline/09-03-SUMMARY.md` when done
</output>
