---
phase: 13-accessibility-f-05-remaining-polish
plan: 05
type: execute
wave: 1
depends_on:
  - 13-01
files_modified:
  - assessment-app/src/components/TestScreen.tsx
  - assessment-app/src/components/test-screen/ScenarioBlock.tsx
  - assessment-app/src/components/test-screen/CaseFilePanel.tsx
  - assessment-app/src/components/test-screen/OptionsList.tsx
  - assessment-app/src/components/test-screen/QuestionHeader.tsx
autonomous: false
requirements:
  - UX-03
  - UX-04
  - UX-05
  - UX-06
tags:
  - ux
  - testscreen
  - reading
  - critical-thinking

must_haves:
  truths:
    - EP Part 6 (reading section) renders the scenario as an email-letterhead simulacrum (From/To/Subject header + serif body) inside ScenarioBlock, per 13-RESEARCH.md Pattern 7 (UX-03).
    - A "Re-review the email" button on reading items scrolls-and-focuses the scenario block via a ref, per 13-RESEARCH.md Pattern 7.
    - The reading-branch render is code-path-separated from the critical-thinking-branch render, per 13-RESEARCH.md Pitfall 2 (they no longer share the single ternary at TestScreen line 1017).
    - Critical Thinking case file panel renders for every CT question via CaseFilePanel — when `tabs` is empty, the existing "Case file not available for this question" message renders inside CaseFilePanel so the layout stays stable, per 13-RESEARCH.md Pattern 8 (UX-04).
    - QuestionHeader shows "Question {zonePosition} of {zoneQuestionCount}" where zonePosition is derived from the current question's bank against `questions.filter(q => zoneOf(q) === currentZone)`, per 13-RESEARCH.md Pattern 5 (UX-05).
    - OptionsList renders a visible "Select all that apply" pill above the options when `question.response_type === 'mcq_multi'`, per 13-RESEARCH.md Pattern 6 (UX-06 relates to AoD collapsible cards — verify-only, no structural change).
    - AoD collapsible-cards behavior (999.1 item 6, UX-06 verify-only): confirm existing SectionShell collapse + max-h-[360px] scroll works cleanly with realistic long content — no code change if verified; document any needed follow-up in SUMMARY, not this plan.
    - No answer-submission, timer, or zone-transition state machinery is modified — additions are JSX + one useRef only.
  artifacts:
    - Modified ScenarioBlock.tsx with reading-branch email-letterhead simulacrum + re-review focus.
    - Modified CaseFilePanel.tsx with always-rendered CT case file (fallback message when tabs empty).
    - Modified OptionsList.tsx with mcq_multi "Select all that apply" pill.
    - Modified QuestionHeader.tsx with zone-wise numbering.
    - Modified TestScreen.tsx: `zonePosition`/`zoneQuestionCount` computed and passed to QuestionHeader; reading and CT render branches split (no shared ternary).
    - Human sign-off on all 4 flows.
  key_links:
    - 13-01 must have completed (5 sub-components exist).
    - 13-RESEARCH.md § Patterns 5, 6, 7, 8 — exact JSX shapes to apply.
    - 13-RESEARCH.md § Pitfall 2 — "do not merge CT and reading branches" is the load-bearing constraint.
---

<objective>
Wave 1: apply the UI/UX changes from 999.1 items 5 (EP Part 6 email block + re-review), 7 (CT case file always available), plus zone-wise numbering and mcq_multi "Select all that apply" cue. Verify AoD collapsible behavior. All work goes into the 5 test-screen sub-components extracted by 13-01, so TestScreen.tsx grows minimally.

Purpose: These are the remaining 999.1 items the user explicitly locked (D-01 workstream 2). Splitting the reading + CT code paths is a prerequisite (Pitfall 2) — the shared ternary at TestScreen line 1017 makes it impossible to change item 5 without breaking item 7.
Output: 5 modified files, human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-accessibility-f-05-remaining-polish/CONTEXT.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-RESEARCH.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-PATTERNS.md
@assessment-app/src/components/TestScreen.tsx
@assessment-app/src/components/case-dashboard/DashboardCards.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Split reading vs CT scenario render branch in TestScreen + ScenarioBlock + CaseFilePanel</name>
  <files>assessment-app/src/components/TestScreen.tsx, assessment-app/src/components/test-screen/ScenarioBlock.tsx, assessment-app/src/components/test-screen/CaseFilePanel.tsx</files>
  <read_first>
    - assessment-app/src/components/TestScreen.tsx — locate the shared scenario/case render branch (per 13-RESEARCH.md, this is around line 1017 pre-refactor; post-13-01 refactor, it lives inside the invocation site that mounts ScenarioBlock and/or CaseFilePanel).
    - assessment-app/src/components/test-screen/ScenarioBlock.tsx (created by 13-01 — currently renders both reading and CT scenario in the same code path).
    - assessment-app/src/components/test-screen/CaseFilePanel.tsx (created by 13-01 — wraps CaseDashboard).
    - assessment-app/src/components/CaseDashboard.tsx (for prop shape reference — do NOT modify).
    - 13-RESEARCH.md § Patterns 7 and 8 (exact JSX shapes).
    - 13-RESEARCH.md § Pitfall 2 (why the split is load-bearing).
  </read_first>
  <action>
    In TestScreen.tsx: replace any remaining shared-branch conditional (`(section === 'reading' || bank === 'critical') ? scenarioDiv : <CaseDashboard/>` or its post-13-01 equivalent) with TWO independent conditional blocks:

      ```
      {currentQuestion.section === 'reading' && (
        <ScenarioBlock kind="reading" question={currentQuestion} scenarioRef={scenarioRef} />
      )}
      {currentQuestion.bank === 'critical' && (
        <CaseFilePanel question={currentQuestion} />
      )}
      ```

    Add `const scenarioRef = useRef<HTMLDivElement>(null);` near the top of TestScreen's function body. Pass it into ScenarioBlock so the "Re-review" button can focus back into it.

    In `ScenarioBlock.tsx`:
    - Accept a `kind: 'reading' | 'critical' | 'other'` prop plus `question` and (for reading) `scenarioRef`.
    - When `kind === 'reading'` and `question.tabs?.[0]` exists, render the email-letterhead simulacrum per 13-RESEARCH.md Pattern 7:
      ```
      <article ref={scenarioRef} tabIndex={-1} aria-label="Customer email scenario" className="border border-slate-200 rounded-lg bg-white shadow-sm">
        <header className="border-b border-slate-200 px-6 py-3 text-xs text-slate-600 space-y-0.5">
          <div><span className="font-semibold">From:</span> customer@example.com</div>
          <div><span className="font-semibold">To:</span> support@fraud-support.com</div>
          <div><span className="font-semibold">Subject:</span> {question.case_title || 'Customer Inquiry'}</div>
        </header>
        <div className="px-6 py-5 text-sm text-slate-800 leading-relaxed whitespace-pre-wrap font-serif">
          {question.tabs[0].content}
        </div>
      </article>
      ```
    - When `kind === 'critical'`, render nothing (CaseFilePanel handles CT; the ternary is now split).

    In `CaseFilePanel.tsx`:
    - Accept a `question` prop.
    - When `question.tabs && question.tabs.length > 0`, render `<CaseDashboard ...>` with the existing prop passthrough.
    - When empty, render the fallback per 13-RESEARCH.md Pattern 8:
      ```
      <div role="note" className="border border-amber-300 bg-amber-50 rounded-lg p-4 text-sm text-amber-900">
        Case file not available for this question. Answer based on the question text alone.
      </div>
      ```
    - Always wrap in `<aside aria-label="Case file" className="animate-fade-in w-full sticky top-6">`.
  </action>
  <verify>
    <automated>cd assessment-app && npx tsc --noEmit 2>&1 | tail -10 ; echo "---"; grep -c "kind=\"reading\"" src/components/TestScreen.tsx ; echo "---"; grep -c "<CaseFilePanel" src/components/TestScreen.tsx ; echo "---"; grep -c "scenarioRef" src/components/test-screen/ScenarioBlock.tsx ; echo "---"; grep -c "Case file not available" src/components/test-screen/CaseFilePanel.tsx ; echo "---BUILD---"; npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - No single ternary in TestScreen conflates `section === 'reading'` and `bank === 'critical'`.
    - ScenarioBlock reading branch renders From/To/Subject header + serif body.
    - CaseFilePanel always renders for CT questions (fallback when tabs empty).
    - `scenarioRef` is created in TestScreen and passed into ScenarioBlock.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <done>Reading and CT render paths are cleanly separated; each has its own JSX shape.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add "Re-review the email" button on reading items in OptionsList (or new sibling) with scenarioRef focus</name>
  <files>assessment-app/src/components/TestScreen.tsx, assessment-app/src/components/test-screen/OptionsList.tsx</files>
  <read_first>
    - assessment-app/src/components/test-screen/OptionsList.tsx (created by 13-01).
    - 13-RESEARCH.md § Pattern 7 last block — the exact button JSX + focus behavior.
  </read_first>
  <action>
    Extend `OptionsList.tsx` to accept an optional `onRereview?: () => void` callback and an optional `showRereview?: boolean` flag. When `showRereview` is true, render a button below the options:

    ```
    <button
      type="button"
      onClick={onRereview}
      className="self-start inline-flex items-center gap-2 text-xs font-semibold text-cyan-800 hover:underline"
    >
      <svg aria-hidden="true" focusable="false" className="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
        <path d="M1 4v6h6"/>
        <path d="M3.51 15a9 9 0 1 0 2.13-9.36L1 10"/>
      </svg>
      Re-review the email
    </button>
    ```

    In `TestScreen.tsx`: define `const focusScenario = () => { scenarioRef.current?.scrollIntoView({ behavior: 'smooth', block: 'start' }); scenarioRef.current?.focus(); };`. Pass `showRereview={currentQuestion.section === 'reading'}` and `onRereview={focusScenario}` to `<OptionsList />`.
  </action>
  <verify>
    <automated>cd assessment-app && npx tsc --noEmit 2>&1 | tail -10 ; echo "---"; grep -c "Re-review the email" src/components/test-screen/OptionsList.tsx ; echo "---"; grep -c "focusScenario" src/components/TestScreen.tsx ; echo "---"; grep -c "showRereview" src/components/TestScreen.tsx ; echo "---BUILD---"; npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - OptionsList renders the "Re-review the email" button conditionally on `showRereview` prop.
    - TestScreen defines `focusScenario` and passes it as `onRereview` for reading items only.
    - Button click smoothly scrolls to and focuses the ScenarioBlock article via the ref.
    - tsc + build clean.
  </acceptance_criteria>
  <done>Reading candidates can jump back to the customer email at any time during questions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Zone-wise question numbering in QuestionHeader ("Question 3 of 12")</name>
  <files>assessment-app/src/components/TestScreen.tsx, assessment-app/src/components/test-screen/QuestionHeader.tsx</files>
  <read_first>
    - assessment-app/src/components/test-screen/QuestionHeader.tsx (created by 13-01).
    - assessment-app/src/components/TestScreen.tsx — locate current global "Q1 of 105"-style rendering (per 13-RESEARCH.md, this was line 981 pre-refactor; post-13-01, it lives inside QuestionHeader).
    - 13-RESEARCH.md § Pattern 5 (exact zonePosition derivation).
  </read_first>
  <action>
    In `TestScreen.tsx`: compute zone position derived values before rendering:

    ```
    const zoneOf = (q: Question): 'english' | 'attention' | 'critical' => q.bank;
    const currentZoneKey = zoneOf(currentQuestion);
    const zoneQuestions = useMemo(() => questions.filter(q => zoneOf(q) === currentZoneKey), [questions, currentZoneKey]);
    const zonePosition = zoneQuestions.findIndex(q => q.id === currentQuestion.id) + 1;
    const zoneQuestionCount = zoneQuestions.length;
    ```

    Pass `zonePosition` and `zoneQuestionCount` into `<QuestionHeader ... />` as props.

    In `QuestionHeader.tsx`: replace the current global counter render with:

    ```
    <span className="text-[11px] font-semibold whitespace-nowrap">
      Question {zonePosition} of {zoneQuestionCount}
    </span>
    ```

    Keep the existing zone progress bar (zonesDoneCount/totalZones) elsewhere in QuestionHeader unchanged — the change is scoped to the per-question counter label.
  </action>
  <verify>
    <automated>cd assessment-app && npx tsc --noEmit 2>&1 | tail -10 ; echo "---"; grep -c "zonePosition" src/components/TestScreen.tsx ; echo "---"; grep -c "zoneQuestionCount" src/components/TestScreen.tsx ; echo "---"; grep -c "Question {zonePosition} of {zoneQuestionCount}" src/components/test-screen/QuestionHeader.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TestScreen computes `zonePosition` and `zoneQuestionCount` via a memoized filter of `questions` by current bank.
    - QuestionHeader renders "Question {zonePosition} of {zoneQuestionCount}" in place of the previous global counter.
    - No component still renders a global "of 105"-style label.
    - tsc + build clean.
  </acceptance_criteria>
  <done>Candidates see zone-relative progress instead of overall question count.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: mcq_multi "Select all that apply" pill in OptionsList + AoD collapsible verify</name>
  <files>assessment-app/src/components/test-screen/OptionsList.tsx</files>
  <read_first>
    - assessment-app/src/components/test-screen/OptionsList.tsx (created by 13-01, extended in Task 2 of this plan).
    - assessment-app/src/components/case-dashboard/DashboardCards.tsx SectionShell (lines 33-72) — verify collapse + max-h-[360px] scroll behavior on realistic long-content input.
    - 13-RESEARCH.md § Pattern 6 (exact multi-select pill JSX).
  </read_first>
  <action>
    Step A — mcq_multi cue in OptionsList (UX-05):
    At the top of the options render, before the `<ul>` of options, conditionally render:

    ```
    {question.response_type === 'mcq_multi' && (
      <div role="note" aria-live="polite" className="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-cyan-50 border border-cyan-200 text-cyan-800 text-xs font-semibold mb-2">
        <svg aria-hidden="true" focusable="false" className="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
          <path d="M9 11l3 3L22 4"/>
          <path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/>
        </svg>
        Select all that apply
      </div>
    )}
    ```

    Step B — AoD collapsible verify (UX-06, verify-only):
    Do NOT modify DashboardCards.tsx code. In this task's SUMMARY, document the outcome of a manual test: load a fixture Attention-to-Detail case with intentionally long tab content (~1500 chars) and confirm the SectionShell collapse toggle works, `max-h-[360px] overflow-y-auto` scrolls cleanly, and collapsed content does not visually clip. If the test reveals a real defect, file a NEW plan (do not silently expand this plan's scope).
  </action>
  <verify>
    <automated>cd assessment-app && grep -c "Select all that apply" src/components/test-screen/OptionsList.tsx ; echo "---"; grep -c "response_type === 'mcq_multi'" src/components/test-screen/OptionsList.tsx ; echo "---BUILD---"; npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - mcq_multi pill renders above options when response_type is mcq_multi.
    - Pill uses light-theme cyan tokens matching the palette from plan 13-04.
    - DashboardCards.tsx is unchanged.
    - AoD collapsible manual test documented in the SUMMARY (pass/fail + evidence).
  </acceptance_criteria>
  <done>Multi-select intent is visible to candidates; AoD collapsible verified or defect filed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: Human sign-off on 4 UX flows</name>
  <files>assessment-app/src/components/TestScreen.tsx, assessment-app/src/components/test-screen/ScenarioBlock.tsx, assessment-app/src/components/test-screen/CaseFilePanel.tsx, assessment-app/src/components/test-screen/OptionsList.tsx, assessment-app/src/components/test-screen/QuestionHeader.tsx</files>
  <action>Human visual verification of the 4 UX changes shipped in Tasks 1-4 (email-letterhead reading block + re-review, CT case-always, zone-wise numbering, mcq_multi cue) plus AoD collapsible verify. See how-to-verify for the 7-step walk-through.</action>
  <verify>
    <human-check>Human walks the 7-step how-to-verify flow and types the resume-signal.</human-check>
  </verify>
  <done>Resume-signal received (`approved` or a describe-issues re-work note).</done>
  <what-built>Four UX changes shipped: EP Part 6 renders as an email-letterhead simulacrum with a "Re-review the email" button; CT questions always show a case file (fallback message when no tabs); question counter is zone-relative ("Question 3 of 12"); mcq_multi questions carry a visible "Select all that apply" pill. AoD collapsible cards were manually verified.</what-built>
  <how-to-verify>
    1. Start the dev server: `cd assessment-app && npm run dev`.
    2. Complete a real attempt and drive to an English Proficiency Part 6 (reading) item:
       - Scenario renders as From/To/Subject header + serif body block (not a card).
       - "Re-review the email" button visible below the options.
       - Clicking it scrolls up and puts focus on the email article; the article's tabIndex=-1 accepts focus without appearing in the natural tab order.
    3. Drive to a Critical Thinking case with tabs:
       - CaseFilePanel renders on the right column with the full case dashboard.
    4. Drive to a Critical Thinking question with NO tabs (fixture may be needed if none exists in current data):
       - CaseFilePanel renders the amber "Case file not available for this question" fallback in the right column.
    5. Watch the question counter across zones:
       - English zone shows "Question X of {english count}".
       - Attention zone resets to "Question 1 of {attention count}" at the zone boundary.
       - Critical zone resets similarly.
    6. Drive to an mcq_multi question:
       - Cyan pill "Select all that apply" is visible above the options.
    7. Drive to an Attention-to-Detail case with a long tab content:
       - SectionShell collapse toggle works, scroll behavior is clean, no visible clipping. If broken, note the defect and file a new plan (do not lower this checkpoint's threshold).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 items check out, or describe issues for a re-work.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UX render changes → answer submission flow | Additions are JSX + one useRef; no submission or timer logic touched — but any accidental restructure of OptionsList or QuestionHeader could break click handlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-11 | Tampering | Focus management on re-review button traps users or disorients screen readers | medium | mitigate | tabIndex={-1} + explicit aria-label on the scenario article; scrollIntoView is `smooth` (respects reduced-motion via CSS media query added in 13-04). |
| T-13-12 | Denial of Service | Zone-position filter miscalculates for hybrid questions and displays 0/0 | high | mitigate | Task 3 uses `q.bank` as the zone key — every Question has a `bank` per assessment-app/src/types/index.ts. useMemo dependencies are correct. Task 5 sign-off item 5 explicitly verifies the counter across zone transitions. |
| T-13-13 | Elevation of Privilege | mcq_multi pill is presentational only — no scoring or submission implications | low | accept | Purely visual cue; no data flows through it. |
</threat_model>

<verification>
- Task 1 automated: branches split, ref wired, build clean.
- Task 2 automated: button renders, focusScenario passed, build clean.
- Task 3 automated: zonePosition + zoneQuestionCount computed and rendered.
- Task 4 automated: mcq_multi pill renders conditionally.
- Task 5 human sign-off across 4 flows + AoD verify.
</verification>

<success_criteria>
- UX-03 satisfied: EP Part 6 scenario is an email letterhead + re-review button works.
- UX-04 satisfied: CT case file panel always renders (with fallback for empty tabs).
- UX-05 satisfied: zone-wise question numbering displayed.
- UX-06 satisfied: mcq_multi "Select all that apply" pill visible; AoD collapsible cards verified (verify-only per assumption A4).
</success_criteria>

<output>
Create `.planning/phases/13-accessibility-f-05-remaining-polish/13-05-SUMMARY.md` when done. Include the AoD collapsible manual-test outcome and the Task 5 sign-off resume-signal.
</output>

## Artifacts this phase produces

- **Modified:** `assessment-app/src/components/test-screen/ScenarioBlock.tsx` — reading branch renders email-letterhead simulacrum.
- **Modified:** `assessment-app/src/components/test-screen/CaseFilePanel.tsx` — always renders for CT (fallback when tabs empty).
- **Modified:** `assessment-app/src/components/test-screen/OptionsList.tsx` — mcq_multi "Select all that apply" pill + "Re-review the email" button on reading items.
- **Modified:** `assessment-app/src/components/test-screen/QuestionHeader.tsx` — zone-wise question numbering.
- **Modified:** `assessment-app/src/components/TestScreen.tsx` — split reading vs CT branches; add scenarioRef, focusScenario, and zonePosition/zoneQuestionCount memos.
- **No behavior change to submission, timer, or scoring. No new dependencies.**