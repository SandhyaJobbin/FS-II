---
phase: 09
plan: 05
type: execute
wave: 2
depends_on: ["09-02", "09-03"]
files_modified:
  - scripts/sync-check.ts
  - package.json
autonomous: true
requirements: [ASYNC-02, ASYNC-05]
must_haves:
  truths:
    - "sync-check.ts fails loudly if backend/AsyncGrading.gs's retry cap (3), batch cap (5), or trigger cadence (5-minute) literals diverge from the tested tests/async/queue-logic.ts mirror -- the same drift-detection guarantee Phase 08 established for grading-engine.ts/admin-auth.ts"
    - "A single npm script runs both new async test files without needing to remember the exact vitest invocation"
  artifacts:
    - "scripts/sync-check.ts (modified) -- 2nd readFileSync pair (backend/AsyncGrading.gs + tests/async/queue-logic.ts) plus 3 new has()/check() divergence assertions"
    - "package.json (modified) -- test:async script added"
  key_links:
    - "This plan can only run after both backend/AsyncGrading.gs (plan 09-02) and tests/async/queue-logic.ts (plan 09-03) exist -- it reads both files by path and will error if either is missing, hence Wave 2 depends_on [09-02, 09-03]"
---

<objective>
Extend `scripts/sync-check.ts` with a second file-pair comparison (`backend/AsyncGrading.gs` vs `tests/async/queue-logic.ts`) so the same drift-detection CI gate Phase 08 built for the grading/admin-auth mirrors also covers Phase 9's async queue logic, and add a `test:async` npm script mirroring the existing `test:grading`/`test:assembly` convention.

Purpose: Without this, `backend/AsyncGrading.gs`'s retry cap, batch cap, and trigger cadence could silently diverge from the tested `queue-logic.ts` mirror in a future edit, and nobody would notice until a live grading run misbehaved -- exactly the class of bug Phase 08's CI sync-check was built to catch (ASYNC-05's "failures are tracked, not silently dropped" extends to the drift-detection layer itself).
Output: `scripts/sync-check.ts` with a 2nd comparison block, and `package.json` with a `test:async` script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-async-grading-report-delivery-pipeline/09-CONTEXT.md
@.planning/phases/09-async-grading-report-delivery-pipeline/09-RESEARCH.md
@.planning/phases/09-async-grading-report-delivery-pipeline/09-PATTERNS.md
@scripts/sync-check.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend sync-check.ts with the AsyncGrading.gs / queue-logic.ts comparison</name>
  <files>scripts/sync-check.ts</files>
  <read_first>
    scripts/sync-check.ts (full file -- the existing `readFileSync` pair for `backend/Code.gs`/`tests/grading/grading-engine.ts`, and the `has(label, text, pattern)`/`check(label, mirrorVal, gsVal)` tolerant-regex helper functions). 09-PATTERNS.md's `scripts/sync-check.ts` section for the exact call-pattern to replicate. 09-CONTEXT.md D-09 (5-minute cadence), D-10 (batch cap 5), D-11 (retry cap 3).
  </read_first>
  <action>
    In `scripts/sync-check.ts`, add a second `readFileSync` pair reading `backend/AsyncGrading.gs` and `tests/async/queue-logic.ts` (following the exact same pattern as the existing `Code.gs`/`grading-engine.ts` pair -- same encoding, same variable-naming style). Using the existing `has()`/`check()` helpers (not a new mechanism), add 3 divergence assertions: (1) the retry-cap literal `3` appears in both `recordQueueItemFailure`'s (or equivalent) permanent-failure condition in `AsyncGrading.gs` and `isPermanentlyFailed`'s threshold in `queue-logic.ts`; (2) the batch-cap literal `5` appears in both `processGradingQueue`'s `.slice(0, 5)` in `AsyncGrading.gs` and `selectEligibleRows`'s cap in `queue-logic.ts`; (3) the trigger-cadence literal `everyMinutes(5)` appears in `AsyncGrading.gs`'s `installGradingTrigger`. Use tolerant regex matching (e.g. `\bslice\(\s*0\s*,\s*5\s*\)` style, matching the existing helpers' tolerance for whitespace/quote-style variation) rather than exact-string matching, consistent with how the existing `Code.gs` checks tolerate suffixed variable names and quote-style differences. Each new check must print a clear failure label (e.g. `"AsyncGrading retry cap"`, `"AsyncGrading batch cap"`, `"AsyncGrading trigger cadence"`) so a future CI failure is immediately actionable.
  </action>
  <verify>
    <automated>npx tsx scripts/sync-check.ts</automated>
  </verify>
  <acceptance_criteria>
    `sync-check.ts` reads both `backend/AsyncGrading.gs` and `tests/async/queue-logic.ts` and runs 3 new labeled checks (retry cap, batch cap, trigger cadence) alongside the existing grading/admin-auth checks. Running the script reports all checks passing (matching the existing "N/N checks passed" style output) with zero drift detected between the real `.gs` file and its tested mirror.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add test:async npm script and run the full suite</name>
  <files>package.json</files>
  <read_first>
    package.json (root) -- the existing `test:grading`/`test:assembly` script entries, to match their exact naming and vitest-invocation style.
  </read_first>
  <action>
    Add `"test:async": "vitest run tests/async/test_queue.ts tests/async/test_email.ts"` to the root `package.json`'s `scripts` object, placed alongside the existing `test:grading`/`test:assembly` entries and matching their exact style (same vitest `run` invocation shape, same relative path convention). Do not modify the `test` script itself (the full-suite entry) -- it already picks up `tests/**/*.ts` via `vitest.config.ts`'s `include`, so the new async test files are already covered by a plain `npm test`. After the edit, run the full suite once to confirm the addition of Phase 9's mirrors, sync-check extension, and this new script cause zero regressions across the existing grading/admin/assembly/integrity suites plus the new async suite.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    `package.json`'s `scripts` object contains `test:async` matching the `test:grading`/`test:assembly` naming convention. `npm run test:async` runs only the 2 async test files. `npm test` (full suite) passes with zero failures, covering the pre-existing 90 tests plus the new async tests.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds only CI/dev-tooling code -- no new runtime trust boundary. `sync-check.ts` runs in CI/local dev only, never in the Apps Script production environment or against untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-09-11 | Tampering (code drift) | `scripts/sync-check.ts` | medium | mitigate | The 2nd file-pair comparison closes the gap left open by plan 09-03 -- retry cap, batch cap, and trigger cadence literals in `backend/AsyncGrading.gs` are now automatically compared against `tests/async/queue-logic.ts` on every CI run (per Phase 08's existing GitHub Actions workflow), so a future edit that changes one without the other fails CI rather than shipping silently |

No package-manager installs in this plan (0 new dependencies) -- Package Legitimacy Gate not applicable.
</threat_model>

<verification>
`npx tsx scripts/sync-check.ts` after Task 1 to confirm the new checks pass against the real files produced by plans 09-02/09-03. `npm test` after Task 2 for the full-suite regression pass (~1-2 seconds per 09-VALIDATION.md).
</verification>

<success_criteria>
- `sync-check.ts` detects drift between `backend/AsyncGrading.gs` and `tests/async/queue-logic.ts` on retry cap, batch cap, and trigger cadence
- `npm run test:async` and `npm test` both pass with zero regressions
- Phase 08's existing CI workflow (already gated on `sync-check.ts`) now also covers Phase 9's async logic with no further workflow-file changes needed
</success_criteria>

## Artifacts this phase produces

- `scripts/sync-check.ts` (modified) -- 2nd `readFileSync` pair + 3 new labeled divergence checks (retry cap, batch cap, trigger cadence)
- `package.json` (modified) -- `test:async` script added

<output>
Create `.planning/phases/09-async-grading-report-delivery-pipeline/09-05-SUMMARY.md` when done
</output>
