---
phase: 13-accessibility-f-05-remaining-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - assessment-app/src/components/TestScreen.tsx
  - assessment-app/src/components/test-screen/QuestionHeader.tsx
  - assessment-app/src/components/test-screen/ScenarioBlock.tsx
  - assessment-app/src/components/test-screen/OptionsList.tsx
  - assessment-app/src/components/test-screen/ZoneOverlays.tsx
  - assessment-app/src/components/test-screen/CaseFilePanel.tsx
  - assessment-app/src/data/zone-config.ts
autonomous: true
requirements:
  - A11Y-01
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05
tags:
  - refactor
  - testscreen
  - extract-first

must_haves:
  truths:
    - TestScreen.tsx behavior after refactor is byte-identical to before for all 8 zone flows (english/attention/critical + Part 4 macro pre-populate + Part 6 reading + CT case + zone overlays + timer).
    - TestScreen.tsx line count drops below 500 (CLAUDE.md cap), unblocking downstream plans 13-04 and 13-05 to grow it again without exceeding cap.
    - No behavior change to answer submission, timer, zone transitions, or ZONE_CONFIG runtime shape (extract is data-move + component-split only, per D-02).
    - All extracted sub-components live under assessment-app/src/components/test-screen/ and share the same 'use client' boundary as the parent (Next.js constraint per assessment-app/AGENTS.md).
    - ZONE_CONFIG data moved to assessment-app/src/data/zone-config.ts with identical shape (icons kept as React.ReactNode).
  artifacts:
    - assessment-app/src/components/test-screen/QuestionHeader.tsx (new, presentational)
    - assessment-app/src/components/test-screen/ScenarioBlock.tsx (new, presentational)
    - assessment-app/src/components/test-screen/OptionsList.tsx (new, presentational)
    - assessment-app/src/components/test-screen/ZoneOverlays.tsx (new, presentational)
    - assessment-app/src/components/test-screen/CaseFilePanel.tsx (new, presentational)
    - assessment-app/src/data/zone-config.ts (new, data-only)
    - assessment-app/src/components/TestScreen.tsx (reduced, orchestrator-only)
  key_links:
    - assessment-app/src/components/case-dashboard/DashboardCards.tsx SectionShell (lines 33-72) — the exact sub-component prop-forwarding pattern to mirror per 13-PATTERNS.md.
    - assessment-app/AGENTS.md — "This is NOT the Next.js you know" — all extracted components stay inside the parent's 'use client' boundary (no new server components).
    - CLAUDE.md "Keep files under 500 lines" — this plan brings TestScreen.tsx into compliance BEFORE plans 13-04/13-05 add material.
---

<objective>
Wave 0 foundation: extract-first refactor of TestScreen.tsx (1178 lines → <500 lines) with zero behavior change, per locked decision D-02. Split into 5 presentational sub-components (QuestionHeader, ScenarioBlock, OptionsList, ZoneOverlays, CaseFilePanel) plus one data extract (zone-config.ts). Downstream plans 13-04 and 13-05 depend on these extracted components existing before they can safely add material — otherwise TestScreen would grow past 1300 lines and blow the 500-line cap.

Purpose: The CLAUDE.md 500-line file cap is a hard constraint; TestScreen already violates it at 1178 lines. If we add light-theme migration (13-04) and EP Part 6 + CT case + zone numbering + multi-select cue (13-05) directly to TestScreen without extracting first, the file grows past 1400 lines and future edits become progressively harder. Do it once, safely, with no behavior change.
Output: 6 new files + 1 shrunk file. Zero test failures. Zero behavior change verified against pre-refactor snapshot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-accessibility-f-05-remaining-polish/CONTEXT.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-RESEARCH.md
@.planning/phases/13-accessibility-f-05-remaining-polish/13-PATTERNS.md
@assessment-app/AGENTS.md
@assessment-app/src/components/TestScreen.tsx
@assessment-app/src/components/case-dashboard/DashboardCards.tsx
@assessment-app/src/components/CaseDashboard.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract ZONE_CONFIG to assessment-app/src/data/zone-config.ts</name>
  <files>assessment-app/src/data/zone-config.ts, assessment-app/src/components/TestScreen.tsx</files>
  <read_first>
    - assessment-app/src/components/TestScreen.tsx line 1 (verify 'use client' directive) and the entire ZONE_CONFIG object literal (currently around line 38 through the end of that literal — read the file first to locate exact line range).
    - assessment-app/src/components/case-dashboard/DashboardCards.tsx lines 1-5 (precedent for a data-carrying module that imports React because it holds JSX icon nodes).
    - assessment-app/AGENTS.md — confirm you have read the Next.js note before adding a new file under src/.
  </read_first>
  <action>
    Create `assessment-app/src/data/zone-config.ts`. Export the `ZoneConfig` type (interface for a single zone: keys are `label`, `bank`, `section`, `title`, `whatToDo`, `example` — infer exact shape from the current object literal). Export the `ZONE_CONFIG` constant with identical structure and identical values to the current in-file literal. Because zone entries carry JSX icon nodes, the module must import React and be `.tsx` if any icon uses JSX literals; otherwise `.ts` is fine — inspect the current literal and choose accordingly. If .tsx, name the file `zone-config.tsx` and update the frontmatter path accordingly. In TestScreen.tsx, replace the inline `ZONE_CONFIG` declaration with `import { ZONE_CONFIG, type ZoneConfig } from '../data/zone-config';` (adjust extension). Do not change any consumer code. Do not add, remove, or rename any zone entry, any field within an entry, or any icon.
  </action>
  <verify>
    <automated>cd assessment-app && npx tsc --noEmit 2>&1 | grep -E "(zone-config|ZONE_CONFIG|TestScreen)" | head -20 ; echo "---"; grep -c "ZONE_CONFIG" src/components/TestScreen.tsx ; echo "---"; test -f src/data/zone-config.ts -o -f src/data/zone-config.tsx && echo "extract-file-exists"</automated>
  </verify>
  <acceptance_criteria>
    - `assessment-app/src/data/zone-config.ts` OR `assessment-app/src/data/zone-config.tsx` exists and exports `ZONE_CONFIG` + `ZoneConfig` type.
    - TestScreen.tsx no longer contains the ZONE_CONFIG literal body; it contains exactly one `import` line referencing the new data module.
    - `npx tsc --noEmit` in assessment-app/ produces no new errors related to zone-config or TestScreen.
    - Diff of TestScreen.tsx shows only: (a) the ZONE_CONFIG literal removed, (b) one added import statement, (c) the local `ZoneConfig` type declaration removed (now imported). No JSX / handler / state changes.
  </acceptance_criteria>
  <done>ZONE_CONFIG lives in its own module; TestScreen consumes it via import; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract 5 presentational sub-components to assessment-app/src/components/test-screen/</name>
  <files>assessment-app/src/components/test-screen/QuestionHeader.tsx, assessment-app/src/components/test-screen/ScenarioBlock.tsx, assessment-app/src/components/test-screen/OptionsList.tsx, assessment-app/src/components/test-screen/ZoneOverlays.tsx, assessment-app/src/components/test-screen/CaseFilePanel.tsx, assessment-app/src/components/TestScreen.tsx</files>
  <read_first>
    - assessment-app/src/components/TestScreen.tsx in full (already loaded via context) — locate the following JSX regions before extraction:
      - QuestionHeader region: question header + countdown timer + zone-progress area (around lines 970-1010 per 13-RESEARCH.md).
      - ZoneOverlays region: the two-phase complete/brief zone-transition overlay (around lines 780-870).
      - ScenarioBlock region: the scenario left-panel div (around lines 1017-1035) — currently shared between `section === 'reading'` and `bank === 'critical'`. Keep the current branching intact in this task; splitting is a plan 13-05 concern.
      - OptionsList region: the MCQ / mcq_multi / hybrid options render block.
      - CaseFilePanel region: the CT `<CaseDashboard>` wrapper block (do not modify CaseDashboard itself).
    - assessment-app/src/components/case-dashboard/DashboardCards.tsx lines 33-72 (SectionShell pattern — the exact prop-forwarding shape to mirror per 13-PATTERNS.md).
  </read_first>
  <action>
    Create the 5 new files under `assessment-app/src/components/test-screen/`. Each file exports a single default React function component using the SectionShell prop-forwarding shape from DashboardCards.tsx: inline object-literal prop types, children pass-through where composition is needed, and `useState` co-located only if the state is purely local to the sub-component (do NOT lift shared state; it stays in TestScreen).

    Sub-component prop contracts (derive exact prop shape from the source JSX region — the receiving JSX must be a pure move):
    - QuestionHeader: props include the current question metadata slot, elapsed / remaining time, and zone progress indicators. Preserve the exact classNames and DOM structure of the source region.
    - ScenarioBlock: props include the scenario data (tabs[0] or equivalent) and the current section/bank. In THIS plan, keep the shared reading+CT branch behavior identical — do NOT split them yet (13-05 will).
    - OptionsList: props include the current question, current answer value, onChange handler, and `isMulti` flag. Preserve the current isMulti-derived click semantics exactly.
    - ZoneOverlays: props include the current overlay phase ('complete' | 'brief' | null), zone metadata, and dismiss/advance callbacks. Preserve the two-phase animation timing exactly.
    - CaseFilePanel: props include tabs, tables, caseTitle, caseId. Internally just wrap the existing `<CaseDashboard .../>` render.

    Add `'use client';` at the top of each file (parent TestScreen is already a client component, per assessment-app/AGENTS.md guidance: keep the child inside the same client boundary).

    In TestScreen.tsx, replace the moved JSX regions with the equivalent sub-component invocations (`<QuestionHeader ... />`, etc.), passing the exact same values that the previously-inline JSX consumed. Do NOT change the parent's state shape, useEffect list, or any handler. Do NOT touch answer submission, timer, or zone transition state machines. The refactor is a mechanical move.
  </action>
  <verify>
    <automated>cd assessment-app && npx tsc --noEmit 2>&1 | tail -30 ; echo "---LINE-COUNT---"; wc -l src/components/TestScreen.tsx src/components/test-screen/*.tsx ; echo "---BUILD---"; npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 5 new files exist under assessment-app/src/components/test-screen/, each with `'use client'` on line 1.
    - `wc -l assessment-app/src/components/TestScreen.tsx` reports fewer than 500 lines.
    - `npx tsc --noEmit` produces zero new errors.
    - `npm run build` from assessment-app/ completes successfully.
    - TestScreen.tsx diff shows: (a) 5 JSX region replacements with sub-component invocations, (b) 5 corresponding imports added. No changes to useState, useEffect, useRef, useMemo, handler signatures, or the answer/timer/zone state machinery.
  </acceptance_criteria>
  <done>TestScreen.tsx is under 500 lines and composed of orchestration + sub-component invocations; all 5 sub-components render as they did before; build clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Behavior-parity smoke check (manual) + Playwright happy-path snapshot capture</name>
  <files>tests/e2e/testscreen-refactor-snapshot.md</files>
  <read_first>
    - assessment-app/src/components/TestScreen.tsx (post-refactor) — confirm all sub-component invocations receive the props the source JSX consumed.
    - .playwright-mcp/ (already present per repo tree, so Playwright infra is at least partially wired).
  </read_first>
  <action>
    Because this is a mechanical extract-first refactor with zero-behavior-change intent, the primary parity guarantee comes from tsc + build passing (Task 2 automated). This task documents a manual smoke checklist in `tests/e2e/testscreen-refactor-snapshot.md` covering the 5 flows most at risk from a component split:

    1. Start assessment → answer 3 English Grammar MCQs → answer advances currentIdx correctly, timer resets per question.
    2. Reach an English Proficiency Part 4 (Macro Editing) item → textarea is pre-populated with the extracted "Existing Macro:" text (source: TestScreen lines 560-571 preserved by ScenarioBlock/OptionsList extraction).
    3. Reach an English Proficiency Part 6 (Reading) item → scenario renders in the left panel (unchanged in this plan; 13-05 will replace it with an email block).
    4. Reach a Critical Thinking case → the case scenario renders in the left panel and `<CaseDashboard>` renders in the right panel via `<CaseFilePanel>` invocation.
    5. Complete a zone → the two-phase 'complete' → 'brief' overlay sequence fires via `<ZoneOverlays>` (verify animation ordering).

    Do NOT modify any behavior to make the smoke checklist pass; if any item fails, the refactor moved something incorrectly — fix the extraction, do not adapt the checklist.

    (Automated verify below runs the existing vitest suite to catch any drift picked up by unit tests, and confirms the snapshot doc exists.)
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -30 ; echo "---"; test -f tests/e2e/testscreen-refactor-snapshot.md && echo "snapshot-doc-exists"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/testscreen-refactor-snapshot.md` exists and lists the 5 smoke-check flows above with expected observations.
    - `npm test` passes with the same test count as before this plan (no regressions).
    - No production code changed in this task (documentation only).
  </acceptance_criteria>
  <done>Manual smoke-check checklist is documented; existing test suite passes; no code drift introduced by the refactor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Refactor blast radius → downstream plans | This plan touches 1 large file (TestScreen.tsx) and creates 6 new files; downstream plans 13-04 and 13-05 depend on the extracted surface being stable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | TestScreen behavior drift during mechanical extract | high | mitigate | Task 2 acceptance criteria explicitly forbids any change to useState/useEffect/handler signatures; Task 3 provides a 5-flow smoke checklist covering the highest-risk zone transitions. |
| T-13-02 | Denial of Service | File-cap violation blocking future edits | medium | mitigate | Task 2 acceptance requires wc -l < 500 on TestScreen.tsx post-refactor; without this, plans 13-04 and 13-05 cannot proceed cleanly. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies in this plan; no install tasks. |
</threat_model>

<verification>
- Task 1 automated: tsc clean, extract file exists, TestScreen has import instead of literal.
- Task 2 automated: tsc clean, build clean, TestScreen line count < 500.
- Task 3 automated: existing vitest suite passes; snapshot doc exists.
- Task 3 manual: 5-flow smoke check performed by developer before commit (per D-02 acceptance).
</verification>

<success_criteria>
- TestScreen.tsx reduced from 1178 lines to < 500 lines (CLAUDE.md cap satisfied).
- 6 new files created (5 components + 1 data extract) under stable paths that 13-04 and 13-05 can safely import from.
- Zero behavior change: tsc + build clean, existing test suite passes, 5-flow smoke checklist documented for developer confirmation.
- Downstream plans 13-04 and 13-05 unblocked.
</success_criteria>

<output>
Create `.planning/phases/13-accessibility-f-05-remaining-polish/13-01-SUMMARY.md` when done. Include the pre/post `wc -l TestScreen.tsx` count and confirm all 5 smoke flows ran green.
</output>

## Artifacts this phase produces

- **New:** `assessment-app/src/data/zone-config.ts` (or `.tsx`) — extracted ZONE_CONFIG constant + `ZoneConfig` type.
- **New:** `assessment-app/src/components/test-screen/QuestionHeader.tsx` — presentational sub-component for the question header + timer + zone-progress row.
- **New:** `assessment-app/src/components/test-screen/ScenarioBlock.tsx` — presentational sub-component for the scenario panel (unchanged shared reading+CT branch — 13-05 will split it).
- **New:** `assessment-app/src/components/test-screen/OptionsList.tsx` — presentational sub-component for MCQ/multi/hybrid options render.
- **New:** `assessment-app/src/components/test-screen/ZoneOverlays.tsx` — presentational sub-component for the two-phase complete/brief zone-transition overlay.
- **New:** `assessment-app/src/components/test-screen/CaseFilePanel.tsx` — presentational wrapper around the existing `<CaseDashboard>` for CT case files.
- **New:** `tests/e2e/testscreen-refactor-snapshot.md` — 5-flow manual smoke checklist for the extract-first refactor.
- **Modified:** `assessment-app/src/components/TestScreen.tsx` — reduced from 1178 to < 500 lines; now orchestrator + sub-component invocations.
- **No behavior change. No new dependencies. No new state.**
